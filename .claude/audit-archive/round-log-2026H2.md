# Round Log 아카이브 (2026-07-19 ~ 2026-08-03)

`.claude/audit-progress.md`에는 최근 5건만 둔다. 그 이전은 여기, 2026-06-06 이전은
`round-log-2026H1.md`.

이 로그는 **쓰기 전용**이다 — audit 스킬 Step 4가 추가하지만 어느 단계도 읽지 않는다.
그런데 Step 1이 progress 파일을 통째로 Read하므로, 쌓아두면 매 라운드 컨텍스트를 먹는다.
영역별 처리 이력은 progress의 Covered 인덱스가 이미 갖고 있다.

- 2026-07-19: HospitalUI (신규 병원 치료 UI, 이번 세션 추가) — clean, P0/P1 0건. audit_candidates score 36(프레임할당9/싱글턴9)이 **둘 다 거짓양성**: `new GUIStyle`은 stylesReady로 1회 캐시, `new Rect/Color`는 struct(스택 할당), 싱글턴 역참조는 렌더 경로 전부 삼항 가드 + `UITheme.Instance`는 CreateInstance 폴백이라 null 불가. 이 프로젝트 반복 버그 7종(모달 ESC 무력화·이벤트 짝·결제 원자성·stale 아이템·미캐싱 조회) **전부 회피** — TrainingUI 패턴을 정확히 따름. **P2:1 보류**(OnDisable은 InsectUpdated 해지하나 OnEnable 재구독 없음 — Hospital GO가 SetActive 토글 안 돼 현재 미발현). 이 세션 빌드 블로커 2건 동반 수정: `InsectDatabase.FindById`→`GetById`(컴파일 CS1061), 신규 테스트 4개 `#if UNITY_EDITOR` 가드(IL2CPP nunit 링크 실패). 검증: error CS 0건, APK 112MB 빌드 성공. **P2 후속**(7265b37): OnEnable에서 InsectUpdated 재구독 추가.
- 2026-07-19: GuidedTutorialController (가이드형 튜토리얼 오버레이, 이번 세션 신규) — clean, P0/P1 0건. score 15 "프레임 할당 5"는 거짓양성(`new Rect`×5·`new Color`×2는 struct 스택, `new GUIStyle`×2는 stylesInit 1회 캐시). 항목 1~7 전부 통과: Find는 Start 1회, null 가드 완비, **구독은 Start↔OnDestroy 페어링**이라 SetActive 토글 소실버그에 애초 해당 안 됨(HospitalUI와 다른 정당한 설계 — 씬 상주 오버레이). Dictionary 기반이라 스텝 범위초과 불가, OnQuestCompleted가 항상 ExitGuided로 clear해 영구정지·완료후잔존 없음. 코루틴 없음, 비모달(IsAnyOpen로 겹침 회피). 검증: error CS 0건.
- 2026-07-19: StoryDirector (스토리 트리거 디렉터, score 2) — P1:1 처리 + P2:1 보류. **P1**: OnInsectUpdated가 InsectUpdated(포획+XP+치료+진화 전부 발화)를 CaptureInsect 트리거로 매핑 → 이번 세션 치료경제가 발화지점(HealInsect/CurePoison/SetAfterBattle)을 늘려 pond/swamp/mountain에서 곤충 **치료만 해도** 빈-param 스토리 비트(ch2_water/ch4_bond/ch5_thesis)가 오발화. PlayerInsectCollection에 포획 전용 InsectCaptured 이벤트(AddInsectInternal에서만 발화) 신설, StoryDirector가 그걸 구독(1884971). 싱글턴 참조 2는 거짓양성(CloudSaveManager.Instance 동일라인 null 가드). Update/OnGUI/코루틴 없음, 이벤트 Start↔OnDestroy 페어링. **P2 보류**: Immediate 인트로 ch1_intro의 렌더러(NpcDialogueUI) 구독이 buildWorld+try 안이라 빌더 예외 시 구독 누락→헤드리스 CompleteBeat로 대사 미표시+보상 소모+oneShot 영구소실(buildWorld 기본 true라 정상 플레이 무해). 검증: ci_check 통과, error CS 0건. **P0 동반**(사용자 신고, 커밋 8cb7da8): 부팅 canary가 MaxLearnedSkills(6)를 오검사해 "핵심 시스템 초기화 실패"로 게임 중단 → MaxEquipSlots(4) 정정.
- 2026-07-22: GameConstants — P0:0, P1:2 처리. MaxLearnedSkills=6과 불일치하던 테스트를 6/7번째 기준으로 갱신하고, 누락된 story_progress.json을 SaveScope MigrationVersion 4의 마이그레이션·삭제 파일 목록에 추가. 회귀 테스트 3건 추가, 전체 Unity PlayMode 73/73 통과. 자체 발견 회귀 0건.
- 2026-07-22: PlayerProgressUIController — P0:0, P1:1 처리. Bootstrap이 텍스트 참조를 늦게 연결해 저장 레벨·XP·사탕이 기본값으로 남던 문제를 Refresh 호출과 회귀 테스트로 수정. 자체 발견 회귀 0건.
- 2026-07-23: InsectElement (score 0 표면 점검) — clean, P0/P1 0건. 현재 enum 전체가 상성 switch에 포함되고, `None`·중복 보조 타입·이중 상성 결과를 안전하게 처리함. 미등록/향후 enum 값은 중립값으로 폴백해 배열·인덱스·null 위험이 없으며, 1v1·레이드 호출부도 누락 Data를 None으로 대체함. 파일 수정 없음.
- 2026-07-24: InsectExpansionDefinitions — clean, P0/P1 0건. 신규 ID 64개의 내부·기존 ID 비중복, 전 항목 리전/서브에리어 풀 배정, 기존 insectId 저장 호환성을 확인. 파일 수정 및 자체 발견 회귀 0건.
- 2026-07-27: InsectLoreService — clean, P0/P1 0건. DB/Lore ID 128/128 일치, 중복·누락 없음. Bootstrap AutoWire 순서와 정적 캐시 초기화·조회 경로를 확인. 파일 수정 및 자체 발견 회귀 0건.
- 2026-07-31: ItemRarityPalette (score 0 표면 점검) — clean, P0/P1 0건. `ItemRarity` 5값이 6개 switch 전부에 4 case + default(Common)로 정합. **`Assets/Resources/ItemRarityPalette.asset`이 존재하지 않아 `PlaySceneBootstrap:3938`의 `Resources.Load`가 항상 null을 반환**하지만, 소비자 3곳이 모두 안전: `ItemRarityTuningUIController.Start`는 `palette == null`이면 즉시 return(`UpdateLabels`의 무가드 역참조에 도달 불가), `ItemInventoryGridItem`의 색·펄스·그라디언트 3경로는 전부 null 가드 + 하드코딩 폴백. 파일 수정 0건. **P2 보류**: 튜닝 패널(슬라이더 5 + 라벨 5 + 패널 1 + 컨트롤러)이 매 세션 생성되나 palette가 null이라 영구 무동작이고 `PlaySceneBootstrap:3967`에서 항상 `SetActive(false)` — 개발용 잔재. **P2 후속 처리(사용자 선택: 애셋 생성)**: `Assets/Editor/ItemRarityPaletteBuilder.cs` 신설로 `Assets/Resources/ItemRarityPalette.asset`을 재현 가능하게 생성(색·펄스·두께·글로우·속도는 SO 필드 초기값, 인스펙터 없이는 비는 Gradient 5개만 등급색 기반으로 채움). `CreateInventoryItemPrefab`(`PlaySceneBootstrap:4441`)에 `rarityPalette` 주입 2줄 추가 — `Resources`에 UI 프리팹이 없어 `TryBuildUIFromPrefab`이 항상 false를 반환하므로 절차 생성 경로가 실제 경로다. 검증: error CS 0, ci_check 통과, PlayMode 168/168.
- 2026-07-31: PlayUIRefs (score 0 표면 점검) — **P1:1 처리**. 순수 참조 홀더라 프레임 할당·이벤트·싱글턴은 애초에 없다. 실질 점검은 **3자 계약 대조**: 필드 90개 ↔ `PlayUIPrefabGenerator` 대입 77개 ↔ `WireFromRefs` 읽기 88개. 읽는데 안 채우는 13개 중 **12개는 `Text`/`TMP_Text` 이중화로 정상** — `WireFromRefs`가 양쪽을 각각 리플렉션 주입하고 생성기는 TMP만 만들므로 non-TMP가 null인 건 설계대로다. **남은 1개 `itemRarityPalette`가 진짜 누락**: TMP 짝이 없는 유일한 참조인데 생성기가 채우지 않아, 프리팹 경로로 뜨면 직전 라운드에 만든 `Resources/ItemRarityPalette.asset`이 무시되고 하드코딩 폴백으로 되돌아간다(절차 생성 경로는 Bootstrap이 주입 중이라 현재는 무해). `PlayUIPrefabGenerator`에 `Resources.Load` 1줄 추가로 두 경로를 일치시켰다. 컨벤션상 `public` 필드 90개는 `unity-csharp.md` 위반이지만 인스펙터 배선 홀더의 정당한 예외라 보고만 하고 수정하지 않았다(audit는 컨벤션 자동 수정 금지). 큐 소진 → `audit_candidates.py`로 12건 재생성.
- 2026-07-31: NpcVisualBuilder (score 0 표면 점검) — clean, P0/P1 0건. **암묵 계약 2개가 핵심**이라 파일 밖 상대편까지 대조: (1) `NpcWalkAnimator`가 `root.Find`로 캐시하는 8개 노드명(ArmL/ArmR/LegLPivot/LegRPivot/Body/HeadPivot/NetHandle/NetRing)이 `Build`의 생성명과 전부 일치하고 **모두 root 직계 자식** — `Find`는 직계만 보므로 이 조건이 깨지면 애니메이션이 조용히 죽는데 충족됨. `BuildLeg`의 `$"Leg{side}Pivot"` 보간까지 확인. NetHandle/NetRing은 아이만 생성되고 애니메이터가 null 가드. (2) 머티리얼 생명주기 — 스폰 3경로(`SpawnVillager`/`SpawnKid`/`SpawnStoryNpc`)가 각각 VillagerNpc/CatcherKidNpc/**VillagerNpc 재사용**이라 셋 다 `OnDestroy`의 `CleanupMaterials`로 커버(스토리 NPC 누락 없음). **`OnDisable`이 아니라 `OnDestroy`에 건 것이 정확** — `NpcManager`엔 `Destroy` 호출이 아예 없고 수 캡·컬링을 `SetActive` 토글로만 처리하므로, OnDisable이었다면 NPC가 꺼질 때마다 머티리얼이 파괴돼 재활성화 시 마젠타가 됐을 것. `CleanupMaterials`의 자식 렌더러 전수 스캔은 현재 안전(`CatcherKidNpc`가 `targetInsect`를 참조만 하고 `SetParent`하지 않아 풀 곤충의 sharedMaterial이 딸려 들어갈 경로 없음). `PlayerVisualBuilder.SafeDestroyMat`의 "사용 중이면 스킵"을 안 따른 것도 정당 — 플레이어는 영구 객체라 의상 재빌드 때 캐시를 버리지만 NPC판은 파괴 시점 전용. 팔레트 하드코딩 인덱스 15개 전부 범위 내, `hairStyle` 0~2가 `BuildHair` switch(case 1/2 + default)와 정합. OnGUI/Update·이벤트·싱글턴 없음, 세이브 무관(seed 기반 결정적 재생성). 파일 수정 및 자체 발견 회귀 0건.
- 2026-07-31: NpcDialogueDatabase (score 0 표면 점검) — clean, P0/P1 0건. **리전 키 정합이 핵심**: `RegionLines`의 7개 키(meadow/pond/forest/swamp/mountain/garden/ruins)가 `RegionManager`의 진행 체인 리전 ID와 정확히 일치 — 누락 리전이 조용히 폴백 대사만 말하는 품질 저하 없음. `StableHash`는 FNV-1a(string.GetHashCode의 런타임 변동 회피, 결정적 시드 유지), `Mod`는 음수·length 0 가드 완비라 `h >> 4`/`h >> 8` 산술 시프트의 음수 결과도 안전. `GetLines`의 `new[]` 3원소 할당은 `NpcDialogueUI.ShowDialogue` 진입 시 1회(프레임 할당 아님), `GetVillagerName`은 NPC 생성 시 1회. 파일 수정 및 자체 발견 회귀 0건.
- 2026-08-03: OpeningSceneController (score 48, 큐 1순위) — **P1:2 + P2:1 처리**. 채점 근거 "프레임 할당 16"은 대부분 거짓양성(`new Rect`/`new Color`는 struct, `new GUIStyle`×6과 `new RectOffset`은 `EnsureStyles` 1회 가드 안). **진짜는 `FitFontSize`의 `new GUIContent`** — GUIContent는 class라 GC 할당이고, 문구 3개가 전부 `const`인데도 매번 새로 만들어 `style.CalcSize`에 넘겼다. OnGUI는 프레임당 Layout/Repaint/이벤트로 여러 번 도므로 타이틀 구간(6.2~10초) 내내 프레임당 6~12개 할당 + 텍스트 실측 6~12회. `static readonly GUIContent` 3개로 승격하고 fit 결과를 `(minSide, availableWidth)` 키로 메모이즈해 화면 크기가 바뀔 때만 재측정하도록 했다. **P1-2**: `OnDestroy`가 `StopOpeningAudio` 없이 `Resources.UnloadAsset(openingTheme)`을 호출 — replay가 완주/스킵으로 끝나면 `OnSequenceCompleted`가 먼저 멈추지만 **중단(Destroyed) 경로**에선 AudioSource가 클립을 문 채로 언로드된다(coordinator의 `OnDisable`/`OnDestroy` → `CleanupImmediately`). 언로드 앞에 `StopOpeningAudio()` 추가. **P2 처리**: `UpdateAudioVolume`이 매 프레임 `PlayerPrefs.GetFloat`을 치던 것을 재생 시작 1회로 캐시(페이드 반영은 매 프레임 유지) — cold start엔 설정 UI가 없고 replay 중엔 PlayScene UI 루트가 비활성이라 재생 중 변경 경로가 없다. **거짓양성/무해 확인**: `activeInputHandler: 0`이라 레거시 `Input` 정상, 오프닝 이미지 6장·테마 wav 전부 존재, OpeningScene에 자체 AudioListener가 있어 replay 중 `gameplayListener` 비활성화에도 무음 아님, `MasterVolume` 키는 계정 스코핑 없이 AudioManager/SettingsPanel과 동일하고 `AudioListener.volume`을 건드리는 코드가 없어 이중 적용 없음, `OpeningReplayCoordinator` AutoWire는 `PlaySceneBootstrap:349`에 배선됨, `ApplyKenBurns`의 UV는 항상 coverUv 안에 갇힘. 검증: error CS 0, ci_check 통과, PlayMode 198/198(OpeningSequenceTests 17건 포함). 변경이 전부 OnGUI 렌더/생명주기라 `rules/testing.md`상 신규 테스트 대상 아님.
- 2026-08-03: UISurface (score 3, 이번 세션 UI 개편으로 신설된 공용 서피스) — **P1:1 처리 + P2:3 보고**. 채점 근거 "싱글턴 참조 3"은 거짓양성(`UITheme.Instance`는 `Resources.Load` 실패 시 `CreateInstance` 폴백이라 null 불가). **진짜 결함은 스타일 캐시의 키 만드는 방식**: `ChipStyle`/`StatLabelStyle` 등 6개가 `UIHelper.CachedStyle("surf_chip_" + fontSize, () => new GUIStyle(…){ fontSize = fontSize })` 형태였다. 이 프로젝트의 다른 7개 호출부(`CharacterOutfitUI`)는 전부 **상수 키 + 무캡처 람다**라 컴파일러가 델리게이트를 정적 재사용해 공짜지만, 여기는 키를 폰트 크기로 결합하고 람다가 `fontSize`를 캡처해 **캐시 적중이어도 호출마다 문자열 1 + 클로저 1 + 델리게이트 1 = 3개를 할당**한다. `Dictionary<int, GUIStyle>` 하나 + `(kind << 16 | size)` 키로 통합해 적중 경로를 무할당으로 만들고, 6개 스타일의 정렬·굵기·색을 `SurfaceText` enum switch로 1:1 이관(원본과 대조 확인). 폰트 크기는 Rect 높이 파생이라 안전 영역이 극단적으로 좁으면 0·음수가 들어올 수 있어 `Mathf.Clamp(1, 400)` 추가. **현재는 잠복 결함**이다 — `Chip`/`StatRow`/`Header` 호출부가 아직 0이라 실행 경로에 없지만, `rules/ui-layout.md`가 이 셋을 칩·스탯행·헤더의 표준 API로 명시하고 있어 첫 채택 화면이 그대로 프레임 할당을 물려받는다. **거짓양성/무해 확인**: 실제 hot path인 `Rounded`/`Card`/`Button`은 적중 시 `MakeKey`(struct) + long 딕셔너리 조회뿐이라 무할당이고, 호출부 46곳의 색 인자를 전수 확인한 결과 전부 토큰·등급색·상수 Lerp 파생이라 캐시 키가 유한하다(애니메이션 색 없음 → 리베이크 폭주 없음). `MakeKey`는 색 32비트를 8비트 왼쪽 시프트 후 반경을 OR해 충돌 없음. 둥근 알파 생성식은 반경 3/8/12 전부에서 `size = radius*4`가 딱 맞아 중앙 불투명·모서리 투명이 정확. **P2 보고(미처리)**: ①상한 256 초과 시 `Clear()`만 하고 텍스처를 파괴하지 않아 사이클마다 ~2MB 고아 발생(`GetRoundedStyle` 외부 호출이 0이라 파괴해도 안전하지만, 상한에 닿지 않는 현재는 투기적 변경) ②`Rounded`가 `GUI.color`를 흰색으로 덮어 호출부의 앰비언트 페이드를 무시(현재 `AnimatePanelOpen` 사용처가 `CharacterOutfitUI` 하나뿐이고 그건 UISurface 미사용이라 미발현) ③`HospitalUI:151`의 8px 액센트 바가 `ui-layout.md`의 "높이 ~8px 이하는 각진 채로" 규칙과 어긋남. 검증: error CS 0, ci_check 통과, PlayMode 198/198. 변경이 OnGUI 스타일 캐시라 `rules/testing.md`상 신규 테스트 대상 아님.
- 2026-08-03: RaidRoundResolver (score 0 표면 점검) — **clean P0/P1, P2:1 보고**. 순수 정적 리졸버라 OnGUI/Update·이벤트·싱글턴·AutoWire·세이브가 애초에 없고, 328줄이라 분해 대상도 아니다. 실질 점검은 **1v1(`InsectBattleController`)과의 스킬 효과 계약 대조**. **P2(미처리, 사용자 판단 대기)**: `BuffAttack`/`DebuffAttack`/`DefenseBuff`가 레이드에서 **영구 누적**된다. 1v1은 `AddEffect(…, skill.effectDurationTurns, …)`로 만료되는 효과 목록을 두고 `RecalculateBonuses()`가 매 턴 `AttackBonus = 효과합 + 의상 + 아이템`으로 **덮어쓰지만**, 레이드엔 그 목록도 재계산도 없어 `+=`한 값이 전투 끝까지 남고 쓸 때마다 쌓인다 — `effectDurationTurns`는 Battle 폴더 전체에서 레이드 경로가 단 한 번도 읽지 않는다(PoisonDot 총량 계산에만 쓰임). 영향이 큰 쪽은 디버프다: `break`(DebuffAttack, effectValue 0.3, 쿨다운 3)를 **세 번** 쓰면 `Mathf.Clamp(1f + AttackBonus, 0.3f, 3f)`의 하한에 닿아 보스 공격력이 남은 라운드 내내 30%에 고정된다. 구조적 원인은 `RaidBattleController:111-113`이 레이드 시작 시 같은 필드에 의상·아이템 보너스를 넣어둬서 **장비 기준값과 전투 버프가 한 필드에 섞여 있다**는 것 — 버프만 되돌릴 수 없다. 고치려면 지속턴 상태와 장비/버프 분리가 필요해 밸런스 결정이 따르므로(agent-coordination.md상 game-designer 영역) 자동 수정하지 않고 **divergence를 코드 주석으로 명시**만 했다. **거짓양성 4건**: ①`damageBySlot` stale 우려 — `RaidRoundResult` 생성자가 라운드마다 `new int[teamSize]`로 새로 잡아 무해 ②보스 시그니처가 비-Damage면 회복도 피해로 처리될 우려 — 생성부(`PlaySceneBootstrap:3340`)가 항상 `SkillEffectType.Damage`라 도달 불가 ③`case Stun: return result;`가 무동작 — 실제 기절은 `RaidBattleController:191-192`가 적용하므로 정상 ④PoisonDot·Stun이 레이드에서 명중 판정을 안 함 — 저명중 스킬은 `storm`(0.9)·`nova`(0.85) 둘뿐이고 둘 다 `Damage` 타입이라 현재 도달 불가. `SetBossDamage`(`RaidRoundModels`)는 호출부 0인 dead code로, 다음 라운드 대상 파일이라 그쪽에서 다룬다. 검증: error CS 0, ci_check 통과, PlayMode 198/198.
- 2026-08-03: RaidRoundModels (score 0 표면 점검) — **P1:1 처리**. 순수 데이터 모델이라 checklist 7항목은 해당 없음. 실질 점검은 **모델↔컨트롤러 집계 계약**. **P1**: `SetBossDamage(slot, damage)`가 `BossDamageBySlot[slot]` 기록과 `TotalDamageToTeam` 가산을 **한 번에** 하는데, 실제 경로는 `RaidRoundResolver.ResolveBossIntent`가 배열에 직접 쓰고 `RaidBattleController:271`이 배열을 훑어 따로 합산한다. 호출부가 0이라 지금은 무해하지만, 이름만 보면 이게 정석 API라 나중에 누가 쓰는 순간 팀 피해가 두 배로 잡힌다 — 삭제하고 `BossDamageBySlot`에 "이런 세터를 만들지 말 것"과 그 이유를 주석으로 남겼다(리졸버가 결과 모델을 모르게 두는 의도적 분리라 순수 배열로 테스트된다). **거짓양성 2건**: ①`AddTeamAction`의 `UniteSlotDamages[slot] = ` 대입이 누적(`+=`)이 아니라 덮어쓰기지만, `ResolveUniteCommand`가 슬롯당 1회만 호출하고 일반 라운드는 `isUnite:false`라 배열이 null이므로 이중 기여 경로가 없다 ②`TotalDamageToTeam`의 `internal set`을 컨트롤러가 `+=`로 쓰지만 `ResolveBossResponse`가 `IsAwaitingBossResponse` + roundStage로 재진입을 막아 이중 가산 불가. 검증: error CS 0, PlayMode 219/219.
- 2026-08-03: CaptureChanceCalculator (score 0 표면 점검) — **P1:1 처리**. 순수 정적 계산기라 checklist 7항목은 해당 없음. 실질 점검은 **같은 규칙을 구현한 두 계산기의 대조** — 미니게임 경로(`CaptureChanceCalculator`)와 전투 경로(`BattleCaptureChanceCalculator`). 기본값·floor·ceiling 차이는 의도된 설계지만(미니게임 0.60/등급별 floor, 전투 0.90/0.10~0.95), **레벨 차 상한만 전투 쪽에만 있었다**(`MaximumLevelDelta = 5`, 전투 테스트가 `GetLevelModifier(99,1)`/`(1,99)`로 고정까지 해둠). 미니게임 쪽은 무제한이라 `diff × 0.02`가 그대로 더해졌다. **실제로 도달하는 조합이다**: `InsectSpawner`가 메인 필드를 리전 요구레벨과 무관하게 **항상 Lv.1부터** 지수 분포로 스폰하므로, Lv.40 플레이어가 Lv.2 전설을 만나면 +38×0.02 = **+0.76**이 붙어 등급 페널티(0.32)와 난이도 페널티(0.40)를 통째로 상쇄하고 1.0에 닿는다 — 주 포획 경로에서 등급·난이도 설계가 고레벨 구간에 무력화됐다. 전투 경로와 같은 `MaximumLevelDelta = 5`로 clamp. **기존 테스트 무영향 확인**: 최대 델타를 쓰던 `Calculate_CoreBelowFloor_PositiveBonusesAreNotSwallowed`(1 vs 50)는 상한 적용 후에도 코어가 여전히 floor 아래라 결과가 동일하고, 나머지는 전부 ±5 안이다. 회귀 테스트 3건 추가(양방향 clamp / 두 경로 상한 일치 / Lv.40 vs Lv.2 전설이 1.0에 닿지 않음). `rules/balance.md`의 포획 기준점에 상한과 그 이유 기록. 검증: error CS 0, PlayMode 222/222.
- 2026-08-03: QuestRewardFormatter (score 0 표면 점검) — **P1:2 처리**. 순수 함수 모음이라 checklist 7항목은 해당 없음. 이 파일의 존재 이유가 "표시 조건 = 지급 조건"이므로 **`TutorialQuestManager.GrantRewards`와 1:1 대조**가 실질 점검이었다. 포함 조건 4개(candy>0 / exp>0 / itemId∧count>0 / insectId)는 **정확히 일치**했다. **P1-1**: 그런데 곤충 보상의 **레벨이 표시에서 통째로 빠졌다** — `GrantRewards`는 `Mathf.Max(1, quest.rewardInsectLevel)`로 실제 레벨을 적용하는데 Label엔 종 이름만 담겼다. 퀘스트 29개를 파싱해 확인하니 `q_approach`가 `rhinoceros_beetle`을 **Lv.6**으로 주는데 완료 배너엔 "장수풍뎅이"로만 떴다(옛 아이템 누락 버그와 같은 형태의 축소판). `Lv.N` 접미사를 붙이되 1 이하는 생략(지급도 1이라 동치). **P1-2**: `HasAny`가 4개 포함 조건을 **그대로 복사**해 갖고 있었다 — 조건을 한 곳에 모으려고 만든 파일 안에서 조건이 두 벌이던 셈이라, 한쪽만 고치면 "받았는데 안 보이거나 / 보이는데 안 주는" 어긋남이 그대로 재발한다. `HasCandy`/`HasExp`/`HasItem`/`HasInsect` 술어로 뽑아 `Collect`와 `HasAny`가 공유하게 했다(무캡처 static이라 할당 없음). **거짓양성/무해 확인**: `Format`의 문자열 결합은 호출부 2곳이 모두 1회성이라 프레임 할당이 아니다(`OnQuestCompleted`는 이벤트, `GetRewardText`는 questId 캐시 — 반복 서브퀘스트도 보상 정의는 불변이라 캐시 유효). `formatBuffer` 정적 재사용은 resolver가 재진입하지 않는 한 안전(현 resolver는 DB 조회만). 데이터 검사: 29개 퀘스트 전부 `rewardItemId`↔`rewardItemCount` 짝이 정합(한쪽만 채워 조용히 증발하는 건 없음). 회귀 테스트 3건 추가(레벨 표시 / 1 이하 생략 / HasAny↔Collect 일치). 검증: error CS 0, ci_check 통과, PlayMode 225/225.
- 2026-08-03: DexBrowseLayout (score 0 표면 점검) — **P1:1 처리 + P2:1 보고**. 순수 계산 헬퍼라 checklist 7항목은 해당 없음. 계산부 자체(`WrapIndex` 감김·무선택 진입, `GetGridColumns`의 `floor((w+gap)/(card+gap))`, 높이 2종)는 **전부 정확**했고 `DexScreenUI:591`의 `cardWidth = (contentWidth - (columns-1)*gap)/columns`가 그 부등식의 정확한 역이라 정합했다. **문제는 호출부였다** — `DexScreenUI`의 ↑↓ 키가 `SelectRelative(±1)`을 부른다. 도감이 **좌측 1열 리스트**였을 땐 ±1이 곧 한 행이었지만, 이번 세션에 전체 폭 그리드로 개편하면서 ±1이 '옆 칸'이 됐다. 6열 기준으로 ↓를 **여섯 번** 눌러야 한 줄 내려가고, 그동안 `ScrollToSelected`가 `selectedIndex / lastListColumns`로 행을 계산하므로 스크롤은 제자리였다 — 키보드 이동이 화면과 어긋난 상태. 행 단위 상수(`lastListColumns`, 이미 필드로 존재하고 `ScrollToSelected`가 쓰던 값)를 delta로 넘기도록 수정하고 ←→는 ±1 유지(그리드에서 좌우는 옆 칸이 맞다). 회귀 테스트 2건 추가(행 이동이 열을 보존하는지 / 마지막 행에서 감길 때 범위 이탈이 없는지). **거짓양성 확인**: `WrapIndex`가 도는 목록이 `database.insects` 전체인데 그리드도 필터 없이 같은 목록을 그리므로 불일치 없음(`DexSortMode`/`DexFilterMode` PlayerPrefs는 uGUI 경로인 `DexListUIController` 소유라 IMGUI `DexScreenUI`와 무관). 키 처리는 `currentTab == 0` 가드 안이라 보유·아이템 탭에서 도감 선택이 움직이지 않음. **P2 보고**: `FormatElementLabel`이 호출부 0(UI는 `DrawElementBadges`로 배지 2개를 따로 그린다) — `ShouldShowSecondary`를 공유해 동작은 정확하므로 함정은 아니고 단순 미사용. 또 보유 탭(`DexScreenUI:1116`)이 `GetGridColumns` 대신 `floor(panelW/410f)`로 열 수를 따로 구한다 — 자체 정합(넘침 없음)이지만 같은 질문에 대한 두 번째 답이다. 검증: error CS 0, ci_check 통과, PlayMode 254/254.
- 2026-08-03: OpeningReplayCoordinator (score 0 표면 점검) — **P0:1 + P1:2 처리**. 코디네이터 자신은 방어가 촘촘했다(timeScale 복원이 정상·실패·OnDisable·OnDestroy 4경로 전부 커버, `BeginCleanup(immediate)` 이중 가드, `ModalUIRegistry` 짝, `loadOperation.completed`가 static이라 누수 없음, `CanReplay`가 `DependenciesReady` 뒤에 역참조). **문제는 이 파일이 무엇을 촉발하는가였다** — `ApplyReplayBlock`의 `playUiRoot.SetActive(false)`가 UI 루트 아래 **41개** 컴포넌트의 OnDisable을 발화시킨다. **P0**: `BattleScreenUI.OnEnable`이 `// 구독은 AutoWire에서만 수행`이라는 주석만 든 **빈 메서드**인데 OnDisable은 `BattleUpdated`/`BattleEnded`/`PlayerFainted`를 해지한다. AutoWire는 Bootstrap 1회뿐이라, **오프닝을 한 번 다시보면 배틀 화면이 영구히 열리지 않는다**(`OnBattleUpdated`가 `Phase.Intro`를 세우는 유일한 지점). 설정에서 누구나 닿는 경로다. `RaidBattleUI`도 같은 형태(P1), `RegionMapUI`는 OnEnable 자체가 없어 `RaidBossSpawned` 마커 소실(P1). 셋 다 구독을 `Subscribe___()`로 빼 AutoWire와 OnEnable이 공유하게 했다(`-=` 뒤 `+=`라 중복 없음). **재발 방지**: 같은 계열이 `HospitalUI`(2026-07-19)까지 네 번째라 `subscription_lint.py`를 신설해 `ci_check`의 `REPO_CHECKS`에 등록했다 — Bootstrap의 `EnsureComponent<T>("UI/...")`로 대상을 뽑고 OnDisable의 `-=`가 OnEnable(및 그 안에서 부르는 헬퍼 1겹)의 `+=`로 되살아나는지 대조한다. 주석을 제거하고 분석해 주석 처리된 구독을 오인하지 않는다. **동반 발견**: `ApplyReplayBlock`의 줄 순서가 load-bearing이었다 — `BattleScreenUI`/`RaidBattleUI`의 OnDisable에 `if (Time.timeScale < 0.99f) Time.timeScale = 1f` 슬로우모션 복구가 있어, `Time.timeScale = 0f`를 SetActive보다 **먼저** 두면 그 복구가 즉시 1로 되돌려 오프닝 뒤에서 월드가 계속 돌아간다. 현재 순서는 안전하지만 재배열 금지를 주석으로 못박았다. 검증: error CS 0, ci_check 6검사 전부 통과(신규 포함), PlayMode 254/254.
- 2026-08-03: UISafeLayout (score 0 표면 점검) — **clean P0/P1, P2:1 보고. 파일 수정 0건.** 순수 정적 계산부 + 파사드라 checklist 7항목은 해당 없음. 계산부(`Compute`/`Build`/`ClampSize`/`CenterStart`/`EndStart`)는 `UISafeLayoutTests` 10건이 마진 clamp 3경계·비대칭 인셋 중앙정렬·초과 패널·하단 앵커·옛 HospitalUI 관용구 동치까지 이미 고정하고 있다. 그래서 **실질 점검은 이 파일이 아니라 두 파사드의 사용 계약**이었다. ①**좌표계 계약 34개 파일 전수 대조** — `UISafeLayout.Px.*`를 쓰는 7개(PlaySceneBootstrap/SubAreaWorldBuilder/AccountLinkUI/CapturePopupUI/LoginUI/SaveConflictUI/WorldFieldMultiplayerUI)는 전부 `UIScale.Begin()`이 0회, 가상 파사드를 쓰는 26개는 전부 1회 이상이다. 뒤바뀌면 패널이 스케일 배수만큼 어긋난 자리에 그려지는데 그런 파일이 없다. ②**Begin/End 균형** — 텍스트 카운트로 6건이 불일치로 보였으나 전수 확인 결과 전부 정상: `UISurface`·`KeyGuideHUD`는 **주석 안의 `UIScale.Begin()` 문자열**이 잡힌 것이고(KeyGuideHUD는 오히려 "Begin 전에 return해야 균형이 유지된다"는 주석), `CashShopUI`·`PlayerStatusHUD`·`QuickAccessBarUI`·`WorldInteractionController` 4건은 조기 반환 경로마다 `End(); return;`을 붙인 형태라 모든 경로에서 짝이 맞는다. ③**가로축 하드코딩은 위반이 아니다** — `TutorialQuestUI`·`GuidedTutorialController` 등이 `VirtualScreenWidth - VirtualSafeLeft - VirtualSafeRight`로 직접 중앙정렬하지만 `ui-layout.md`가 "가로 마진은 24px 고정, 세로만 하네스로 옮겼다"고 명시하고 `ui_layout_lint`도 세로만 검사한다. **P2 보고(미처리)**: 공개 API 5멤버 `ContentLeft`/`ContentWidth`/`MarginY`/`ClampWidth`/`Overflows`가 가상·Px 양쪽 모두 **생산·테스트 호출부 0**이다(합 10개). `Overflows`와 `ContentWidth`는 `ui-layout.md`의 API 표에 실려 있어 "쓰라고 적어뒀는데 아무도 안 쓰는" 상태이고, 특히 `UISafeLayout.ContentWidth`(0회)와 `UIScale.ContentWidth(margin)`(3회)는 **같은 질문에 대한 두 번째 답**이다(둘 다 마진 24 기준이라 값은 일치). 함정은 아니라 자동 수정하지 않고 보고만 한다 — 지우려면 문서 표도 함께 고쳐야 한다. **관찰(수정 안 함)**: `Begin`/`End` 사이가 `try/finally`가 아니라, 그 구간에서 예외가 나면 `matrixStack`(static, 씬 로드로도 안 비워짐)에 항목이 남아 이후 모든 UI의 `End()`가 남의 행렬을 복원하게 된다. 현재 예외를 던지는 경로가 확인되지 않아 투기적 변경이라 손대지 않았다.
- 2026-08-03: UIDirectScroll (score 0 표면 점검) — **P1:1 처리**. 클래스 자체는 견고했다: 터치 경로가 `lastTouchFrame`으로 프레임당 1회만 처리하고, `suppressPointerFrame`으로 `Input.simulateMouseWithTouches`가 만드는 합성 마우스 이벤트를 같은 프레임에 차단하며, 손가락 유실·MouseUp 누락(Repaint에서 `Input.GetMouseButton(0)` 확인)까지 복구한다. 호출부 20곳의 `contentHeight`↔`BeginScrollView` viewRect도 전부 정합했다. **문제는 클래스에 z-order 개념이 없다는 것이었다** — IMGUI는 먼저 그린 쪽이 `Handle`도 먼저 부르는데, 이번 세션 도감 개편(좌측 1열 리스트 → 전체 폭 그리드 + 겹치는 중앙 모달)으로 `DrawPokedex`가 그리드를 그린 **뒤** 상세 모달을 겹치게 됐다. 그래서 배경 그리드가 모달 위에서 굴린 휠·드래그를 `Event.Use()`로 가로챈다. 1920×1080에서 실측하면 그리드 뷰포트 y 232~1026, 상세 뷰포트 y 164~988로 **상세 스크롤 영역의 92%가 그리드 안**이라 위쪽 68px 띠에서만 상세가 움직였다 — 상세 하단 콘텐츠(설명·스탯 행)에 사실상 도달 불가. 터치는 더 나쁘다: 터치 경로는 `Event`가 아니라 `Input.GetTouch`를 직접 읽어 **소비 개념이 없어서** 두 인스턴스가 같은 손가락을 동시에 붙잡고 배경과 모달이 함께 움직인다(포트레이트 1080×1920도 동일). `Handle`에 `interactive` 매개변수를 더해 겹친 배경이 입력을 포기하게 하고, `DexScreenUI`가 `!detailModalOpen`을 넘긴다. **clamp는 비활성 구간에서도 유지**했다 — 모달이 열린 사이 배경 내용이 짧아지면 범위 밖 스크롤이 남는다(회귀 테스트가 이 순서를 고정). **전수 확인**: 같은 겹침이 다른 화면엔 없다 — `TrainingUI`는 `Page` enum(전환 시 `Reset()`), `CollectionUI`는 목록/상세 배타 분기, `WorldLobbyUI`는 `LobbyPhase` switch, `CashShopUI`·`SocialPvpUI`는 탭당 인스턴스, `RegionMapUI`는 겹치는 도감 브라우저 아래에 스크롤 영역이 없다. `DexScreenUI`의 나머지 3개도 `currentTab` 배타 + 모달은 탭 0 전용. **검증 사고 1건(자체 발견)**: `-runTests`에 `-quit`를 같이 넘겨 Unity가 테스트 전에 종료했는데 exit 0 + "Exiting batchmode successfully"라 성공처럼 보였고, 갱신되지 않은 이전 `TestResults.xml`의 254/254를 이번 결과로 잘못 읽었다. `-quit` 제거 후 결과 파일을 치우고 재실행해 확정 — `rules/testing.md`에 이 함정과 "mtime도 함께 볼 것"을 기록. 검증: error CS 0, ci_check 6검사 통과, PlayMode **255/255**(신규 1건 포함).
- 2026-08-03: OpeningSequenceState (score 0 표면 점검) — **clean, P0/P1 0건. 파일 수정 없음.** 한 파일에 순수 클래스 3개(`OpeningPlaybackClock`/`OpeningSkipInputGate`/`OpeningSequenceState`) + enum 1개가 들어 있고 MonoBehaviour가 없어 checklist 7항목은 해당 없음. **핵심 점검은 타임라인 상수 ↔ 렌더러가 읽는 파생값의 정합**이었다: 8개 상수(2.4/3.0/5.2/5.8/6.2/8.0/9.2/10.0)에 대해 `Phase`·`CurrentImageIndex`·`NextImageIndex`·`ImageBlend`가 전 구간에서 어긋나지 않는다 — GlowCrossFade(2.4~3.0)에서 current 0·next 1, Horizon(3.0~5.2)에서 next -1, HorizonCrossFade(5.2~5.8)에서 current 1·next 2, Gathering 이후 current 2로 `openingImages[3]` 범위를 벗어나지 않는다. `Complete`는 `completed` 가드 + 핸들러 지역 캡처로 이중 발화·핸들러 재진입 모두 안전하고, `TrySkip`이 `skipFadeStartAlpha`에 현재 페이드를 담아 자연 페이드 중 스킵해도 화면이 도로 밝아지지 않는다. `OpeningPlaybackClock`은 `MaxFrameDelta` clamp(로딩 stall을 재생시간에서 제외)·시각 역행 방어·NaN/Infinity 차단이 완비돼 `Advance`에 NaN이 도달할 경로가 없다(도달하면 `Math.Min(Duration, NaN)`이 NaN이 돼 영구 미완료가 되지만 유일한 호출부가 이 시계다). `OpeningSkipInputGate`는 시작 시 누르고 있던 입력이 스킵으로 오인되지 않도록 중립 프레임을 요구하는 전이가 정확하다. **테스트가 상수를 심볼로 참조**해(`StateAt(OpeningSequenceState.GlowCrossFadeStart)`) 상수 순서를 뒤집으면 실제로 깨진다 — 하드코딩 수치였다면 못 잡았을 회귀를 이미 막고 있다. **관찰(수정 안 함)**: `OpeningVisualPhase`/`Phase`는 생산 코드 호출부가 0이고 테스트 단언 표면으로만 쓰인다 — 렌더러는 이미지 인덱스·알파를 직접 읽는다. 타임라인을 읽기 쉽게 단언하려는 정당한 용도라 dead code로 보지 않았다. 자체 발견 회귀 0건.
- 2026-08-03: UITween (score 0 표면 점검) — **P1:1 처리**. 순수 정적 트윈 유틸이라 checklist 7항목은 해당 없고, 이징 곡선 5종은 전부 0→0·1→1을 만족했다(`EaseOutBounce`의 마지막 구간 `7.5625*(1-2.625/2.75)^2 + 0.984375 = 1.0`까지 손계산 대조). **문제는 시간을 어디서 재는가였다** — `Evaluate`가 호출될 때마다 `h.elapsed += Time.unscaledDeltaTime`을 더하는데, 유일한 소비자가 `CharacterOutfitUI.OnGUI` → `UIHelper.AnimatePanelOpen` 경로다. **OnGUI는 프레임당 여러 번 돈다**(Layout 1 + Repaint 1 + 입력 이벤트마다 1). 그래서 `Create(..., 0.2f)`로 만든 패널 페이드가 최소 2배 빨랐고, 마우스를 움직이면 MouseMove 패스가 늘어 **더** 빨라졌다 — 프레임률·입력량에 따라 속도가 달라지는, 사실상 페이드가 없는 상태. `TweenHandle`에 `lastFrame`을 추가해 프레임당 한 번만 전진하게 했다. `Event.current.type == Repaint`로 거르는 IMGUI 관용구를 쓰지 않은 건, 이 유틸이 OnGUI 밖에서도 동작해야 하고 Repaint 횟수가 보장되지 않기 때문이다(`Create`/`Reset`도 `lastFrame`을 찍어 생성 프레임에 시간이 앞서가지 않게 한다). **방어적 보강**: `duration`이 0이면 `0/0 = NaN`이고 NaN은 `t >= 1f`가 거짓이라 `active`가 영영 안 풀린 채 NaN을 계속 뱉는다 — `Create`는 clamp하지만 `TweenHandle`이 public 필드를 가진 serializable struct라 직접 생성 경로가 열려 있어 `Evaluate`에도 clamp를 뒀다(현재 도달 경로는 없음). **테스트 신설**: `ApplyEase`를 `internal Ease`로 열어 이징 5종의 양 끝점 계약을 `[TestCase]`로 고정했다 — 깨지면 트윈이 시작·끝에서 튀는데 지금까지 어디에도 고정돼 있지 않았다. `EaseOutBack`이 1을 넘는 구간을 갖는다는 것도 함께 고정했다(그래서 `Evaluate`가 `Lerp`가 아니라 `LerpUnclamped`를 쓴다 — 클램프하면 이 곡선이 죽는다). 같은 프레임 반복 호출이 `elapsed`를 한 번만 늘리는지도 검증. **세이브 무관 확인**: `lastFrame` 필드가 늘었지만 `TweenHandle`은 `[SerializeField]`도 JSON 직렬화도 없다(`CharacterOutfitUI`의 private 필드 1개뿐). 검증: error CS 0, ci_check 6검사 통과, PlayMode 265/265(신규 11케이스 포함).
- 2026-08-03: InsectBattleStats (score 0 표면 점검) — **clean, P0/P1 0건. 파일 수정 없음.** 순수 계산 클래스라 checklist 7항목은 대부분 해당 없다. 실질 점검은 **두 보너스의 소비 지점 대조**였다: `DefenseBonus`는 `ApplyDamage` 안에서 `effDef = def * (1 + DefenseBonus)`로 **한 번만** 소비되고, `AttackBonus`는 호출부 3곳(`InsectBattleController:403`, `RaidRoundResolver:215/245/321`)에서 데미지를 만들 때만 곱해진 뒤 `ApplyDamageAndMeasure`가 다시 곱하지 않는다 — 겹치는 경로가 없다. 스택 카운터는 부호 있는 값이라 상한(±3)에 닿아도 **반대 방향 전환은 항상 통과**해 되돌릴 수 있다. `MaxHp`/`Attack`/`Defense`의 `protected set`은 `RaidBattleController:613`의 `RaidBossStats` 상속 때문이라 정당하고, 그 서브클래스가 기반 생성자 이후 스탯을 덮어쓰므로 `ResetHp()`를 부르는 순서도 맞다(기반 생성자가 옛 MaxHp로 CurrentHp를 잡기 때문). `effDef`가 음수가 될 여지는 `DefenseBuff`의 effectValue가 +0.35뿐이라 도달 불가이고, 그래도 `Mathf.Max(1f, effDef)` 가드가 있다. 기절(0 HP) 곤충의 전투 진입은 `InsectBattleController:74/100`이 양 경로 모두 막는다.
- 2026-08-03: InsectSizeCalculator (score 0 표면 점검) — **clean, P0/P1 0건. 파일 수정 없음.** 순수 정적이라 checklist 해당 없음. `RollFromInstanceId`의 FNV-1a가 `hash < 0 ? -(long)hash : hash`로 `Abs(int.MinValue)` 오버플로를 피하는 것, `EffectiveRoll`이 `-1`(구세이브 센티넬)을 instanceId 해시로 결정적 복원하는 것, `ScaleFor`가 범위 밖 롤을 `Clamp01`로 눕히는 것을 확인. 무게가 길이 배율의 **세제곱**이라 "조금 큰데 훨씬 묵직하다"가 성립한다. 라벨 포맷 경계(10g/1000g/100mm)도 대조. `ToString("0.0")`이 culture 의존이지만 표시 전용이고 저장·파싱 경로가 없어 무해(로케일에 맞는 소수점이 오히려 정상).
- 2026-08-03: WeeklyContestSchedule (score 0 표면 점검) — **clean, P0/P1 0건. 파일 수정 없음.** 주차 경계(`604800` 전후), 음수·0 시각의 눕힘, `IsWithinWeek`가 구세이브 `capturedUnix = 0`을 항상 false로 거르는 것, `WeekStartUnix`의 int×long 승격에 오버플로가 없는 것을 확인. `BuildPool`이 `rarity > Uncommon`을 걸러내고 `CompareOrdinal`로 정렬해 **기기·재설치와 무관하게 같은 주에 같은 종**이 나온다(서버 없이 동기를 맞추는 유일한 방법). `TargetFor`는 `Mathf.Abs(weekIndex) % Count`라 음수 인덱스가 불가능하고 빈 풀은 null 폴백.
- 2026-08-03: WeeklyContestManager (score 0 표면 점검) — **clean, P0/P1 0건. 파일 수정 없음.** 이벤트는 `InsectCaptured`(포획 전용)를 구독한다 — `InsectUpdated`였다면 치료·레벨업·진화에도 울려 기록 토스트가 오발화했을 자리이고, StoryDirector가 같은 이유로 이미 옮겨간 이벤트다. **World 루트 소속이라 UI 루트 SetActive 토글과 무관**하므로 `AutoWire`↔`OnDestroy` 페어링이 정확하다(`subscription_lint`의 검사 대상도 아니다). PlayerPrefs 값이 `"주차:등급"` 문자열이고 파싱이 `int.TryParse`뿐이라 **culture 의존이 없다**(소수점 포맷이 끼어들 여지가 없음). `ResolveTarget`이 주차 캐시로 128종 정렬을 조회마다 돌리지 않고, `TutorialQuestUI`가 OnGUI에서 `WeeklyContestTarget`을 읽어도 캐시 적중 경로만 탄다. **관찰(수정 안 함)**: 풀이 비면(`cachedTarget == null`) 매 조회마다 재정렬하는데, `InsectDatabase`에 Common이 반드시 있어 도달하지 않는다.
- 2026-08-03: NpcDuelController (score 1, 큐 1순위) — **P1:1 처리 + 방어 1건**. 채점 근거 "싱글턴 참조 1"은 거짓양성(`TutorialQuestManager.Instance?.NotifyNpcDuelWon()` — null 조건 연산자). **P1: 도주하면 대결 쿨다운이 통째로 우회된다.** `DuelEnded`는 `CheckEnd`의 승리(`InsectBattleController:718`)·전멸패배(`:751`) 두 경로에서만 발화하는데, `TryEscape`(`:229` 부근)는 `battleEnded = true; BattleEnded?.Invoke(false)`만 하고 끝난다. 그래서 도망치면 `OnDuelEnded`가 안 돌아 `kid.MarkDuelFinished`가 호출되지 않고, **같은 아이에게 즉시 재도전**할 수 있다 — `DuelCooldownSeconds` 주석이 "결과와 무관하게 … 연속 파밍 차단"이라고 설계 의도를 못박아 두었는데 정작 한 결과가 빠져 있었다. 도주 성공률이 레벨차에 따라 50~90%라 쉽게 닿는 경로다. `TryEscape`에 `if (duelMode) DuelEnded?.Invoke(false);` 추가(승리/패배와 같은 형태). 부수 효과로 `activeKid`가 도주 후 남아 있던 것도 함께 해소된다. **방어 보강**: `EnsureDuelInsect`가 `Mathf.Abs(StableHash(kid.NpcId))`로 시드를 만드는데 `Abs(int.MinValue)`는 그 자신(음수)이라 `pool[음수]`로 즉사한다 — 같은 저장소의 `InsectSizeCalculator.RollFromInstanceId`와 `NpcDialogueDatabase.Mod`가 이미 쓰는 방어인데 여기만 빠져 있었다(확률은 2^-31이지만 한 줄이고 하우스 패턴이 이미 존재). 순수 함수 `PoolIndexFor(npcId, poolLength, attempt)`로 분리해 long 양수화 + 빈 풀·null ID 폴백을 넣고 테스트 3건으로 고정. **거짓양성/무해 확인**: `AutoWire`의 `DuelEnded` 구독은 `battleController == null` 가드 안이라 이중 구독 불가, `activeRarity`는 `TryStartDuel` 시점 스냅샷이라 대결 중 아이 상태가 바뀌어도 보상 등급이 흔들리지 않는다, `FindPlayerLeader`가 팀·보유 양쪽에서 기절 곤충을 거른다. 검증: error CS 0, ci_check 6검사 통과, PlayMode 268/268(신규 3케이스 포함).
- 2026-08-03: InsectBattleController **재감사** (2026-05-20 최초 감사, 이후 StartDuel·버프 상한·DuelEnded 유입) — **P1:1 처리**. 이 라운드는 `audit_candidates.py`가 "후보 0건"을 낸 상태에서 시작했다 — 스크립트가 `if stem in reviewed: continue`로 **이름 기반 영구 제외**를 하기 때문이다("처리했는데 그 뒤로 바뀐 파일" 개념이 없다). 실측하니 감사 이후 수정된 파일이 **76개**였고, 그중 구조 변경이 가장 큰 이 파일을 1순위로 잡았다. **P1**: `RecalculateBonuses()`가 `AddEffect`(510)·`TickEffects`(558)·`SwapPlayerInsect`(853) 세 곳에서만 불리고 **전투 시작 경로에는 없었다**. `InsectBattleStats` 생성자가 `AttackBonus/DefenseBonus = 0`으로 두므로, `UseSkill`/`UseBasicAttack`의 순서(데미지 계산 → 적 반격 → `TickEffects`)상 **첫 턴에 의상·아이템 전투 보너스가 통째로 빠진다** — 플레이어의 첫 공격은 `GetDamage`의 `1 + AttackBonus`가 1.0이 되고, 적의 첫 반격은 `ApplyDamage`의 `def × (1 + DefenseBonus)`를 못 받는다. 야생 전투가 대개 2~4턴이라 소모품으로 산 효과의 큰 몫이 증발한다. **발현 조건이 결정적 증거였다**: `SeedPersistentState`가 **감염된 곤충일 때만** `AddEffect`를 부르고 그게 `RecalculateBonuses`를 태우므로, 독에 걸린 곤충만 첫 턴부터 보너스를 받았다(건강하면 못 받음). **교차 대조**도 같은 방향 — `SwapPlayerInsect`는 이미 `RecalculateBonuses()`를 부르고(교체 곤충은 정상), 레이드는 `RaidBattleController:111-113`이 시작 시 직접 대입한다(정상). 1v1 시작 경로만 빠져 있었다. `BeginBattleCommon` 끝에 호출 추가(`outfitBonus`/`itemEffects` null 가드가 메서드 안에 이미 있어 AutoWire 전 호출도 안전). **거짓양성/무해 확인**: `duelMode`는 `StartBattle`/`StartDuel` 양쪽이 명시 대입해 이전 대결 상태가 새지 않고, `BeginBattleCommon`이 `enemyEntity` 대입 **전에** 도는 것도 의도대로다(호출부가 뒤에 채운 뒤 이벤트를 울려야 `BattleScreenUI.OnBattleUpdated`의 `GetEnemyEntity()`가 유효하다). `CheckEnd`의 듀얼 분기는 포획 롤·야생 드랍을 정확히 건너뛴다. **테스트**: 호출 순서 문제라 씬 없이 검증 불가 — `rules/testing.md`의 MonoBehaviour 생명주기 제외 대상이고, 배율 공식 자체는 `BuffStackCapTests`가 이미 고정한다. 검증: error CS 0, ci_check 6검사 통과, PlayMode 268/268.
- 2026-08-03: PlaySceneBootstrap **재감사** (2026-05-27 감사 이후 2107줄 변경, 새 재감사 큐의 1순위) — **clean P0/P1, 파일 수정 0건.** 4913줄을 줄줄이 읽는 대신 **계약을 기계적으로 대조**했다(감사 이후 바뀐 부분이 부트스트랩의 배선 계약을 깼는지가 이 파일의 실질 위험이다). ①**EnsureComponent 87경로/102생성** — 서로 다른 타입이 같은 경로를 쓰는 건 `World/CharacterOutfit`(CharacterOutfitManager + OutfitBonusProvider) 하나뿐이고 매니저와 그 보너스 제공자를 한 GameObject에 얹는 정상 패턴이다. ②**AutoWire 오버로드 커버리지** — 생성 타입 88개 중 정의됐는데 그 인자수로 안 불리는 건 2건뿐이고 둘 다 무해: `CharacterOutfitUI.AutoWire(manager)`는 2인자 오버로드(`AutoWire(manager, bonus)`, :466)로 이미 배선되고, `ItemRarityTuningUIController.AutoWire(palette)`는 리플렉션 주입을 쓰는 개발용 잔재다. ③**리플렉션 주입 154건 전수 검증** — `GetType().GetField("x")?.SetValue()`는 필드명이 어긋나도 **컴파일·런타임 모두 조용하다**(PlayUIRefs 라운드가 잡았던 결함 계열). 대상 타입의 실제 필드 목록과 대조해 **미해석 0건** 확인. ④**Resources.Load** — `ItemDatabase`가 애셋 부재로 항상 null이지만 바로 다음 줄 `if (itemDatabase == null) itemDatabase = ItemDatabase.CreateRuntimeDefault();`가 받는다(런타임 팩토리가 실제 단일 출처). ⑤**부팅 canary** — 2026-07-19 P0(`MaxEquipSlots`를 `MaxLearnedSkills`로 오검사해 부팅 차단)가 재발하지 않았고, 단언하는 두 불변식(`MaxEquipSlots == 4`, Leaf→Water 상성 > 1)이 실제로 성립한다. 개별 곤충 데이터 결함은 throw가 아니라 로그+보정이라 한 종의 회귀가 게임 전체를 막지 않는다(그 P0의 교훈이 반영된 형태). ⑥**이번 세션 신규 등록 3종** — `WeeklyContestManager`(:485)·`NpcDuelController`(:565)의 AutoWire 인자가 전부 앞서 만들어지고, `ApplySizeProfile`이 `ScriptableObject.CreateInstance<InsectData>()` **두 경로 모두**(:2595→2610, :3008→3025)에 걸려 `baseSizeMm`을 못 받는 종이 없다(주간 대결 티어가 종 기준값에서 파생되므로 한 경로라도 빠지면 그 종의 임계가 틀어진다). ⑦**구독 시점 대 세이브 로드** — `weeklyContest`가 구독하는 `InsectCaptured`는 `AddInsectInternal`에서만 발화하고 `LoadAndIndex`(Awake)는 그 경로를 타지 않아, 세이브 로드가 보유 곤충 수만큼 포획 이벤트를 쏘는 오발화가 없다. **거짓양성 확인**: score 228의 `inst=158`(싱글턴 참조)은 전부 가드 안이다 — 유일한 무가드 후보였던 `AudioManager.Instance.PlayBGMForRegion`(:413)도 **앞줄**에 `if (region != null && AudioManager.Instance != null)`가 있다(한 줄 단위 스캔의 한계). **검사하지 않은 것**: 절차 생성 지오메트리·좌표값과 4913줄의 라인 단위 로직은 이 라운드 범위 밖이다 — 계약과 델타를 봤다. 코드 변경이 없어 테스트는 재실행하지 않았다(직전 검증 상태 유지: error CS 0, ci_check 통과, PlayMode 268/268).
- 2026-08-04: RegionMapUI **재감사** (2026-05-27 감사 이후 2048줄 변경, 새 재감사 큐 2순위) — **P1:1 처리**. 이 파일은 **score 41로 낮은데 변경량은 2위**였다 — 옛 점수 정렬이었다면 한참 아래였을 자리이고, 새 큐가 아니었다면 안 열렸을 라운드다. **P1**: `DrawDexItem`이 곤충 하나당 매 OnGUI 패스에 문자열을 새로 만든다 — 포획됨이면 `$"{data.rarity}  |  CP {CalculateBasePreview(...)}"`(보간 + enum 박싱 + **전투력 재계산**), 미포획이면 `new string('?', displayName.Length)`. 리전당 곤충이 20여 마리이고 OnGUI는 프레임당 Layout+Repaint+입력마다 돌며, IMGUI 스크롤뷰는 가상화가 없어 화면 밖 항목도 전부 실행된다. 둘 다 **종 데이터에서만 파생돼 세션 내내 불변**이므로 `insectId`(정보줄)와 이름 길이('???')로 캐시했다. **회귀 아님을 확인**: 처음엔 "2026-05-27 캐시 라운드(P1:28)가 7월 전면 재설계(70adc8e)로 되돌아갔다"고 의심했으나, 재설계 **직전** 버전(`70adc8e^`)을 꺼내 보니 같은 자리에 같은 코드가 있었다 — 그 라운드는 GUIStyle 28개를 다뤘고 문자열 할당은 원래 범위 밖이었다. 선재 결함이다. **거짓양성으로 판정(보고 안 함)**: `Label()` 헬퍼가 `wordWrap = false`를 전역으로 걸어(지도 핀 라벨은 1줄이 맞다) **지역 설명**까지 한 줄로 그린다. 방금 고친 글씨 잘림과 같은 계열로 보였으나 실측하면 설명 최대 38자 ≈ 912px에 박스가 972px(세로 모바일)이라 여유 7%로 들어간다 — 지금 잘리지 않으므로 보고하지 않는다. 다만 `UIHelper.LabelFit`은 `CalcHeight` 기반이라 **`wordWrap=false`의 가로 넘침을 못 잡는다**(폭 기준 축소는 `CalcSize().x`가 필요) — 설명이 한 줄만 길어지면 조용히 잘리고 헬퍼도 막지 못한다. 별건 판단 대상. **나머지 checklist**: `OnEnable`/`OnDisable`이 `RaidBossSpawned`를 해지-후-구독으로 짝 맞추고(이번 세션 P0 수정분), `EnsureAssets`가 `ready` 가드로 GUIStyle 26개+disc 텍스처를 1회만 만들며, `GetPlayer()`는 최초 1회만 Find한다. 렌더 경로에 남은 힙 할당은 패스당 1~2개(`$"도감 {caught} / {total}"` 등 항목별이 아닌 것)뿐이라 두었다. 검증: error CS 0, ci_check 6검사 통과, PlayMode 268/268.
- 2026-08-06: DexScreenUI **재감사** (2026-05-20 감사 이후 1797줄 변경, 재감사 큐 1순위) — **P0:1, P1:2 처리 / P2:3 보고.** 이 라운드의 결함은 전부 **3D 썸네일 도입(726795a)과 그리드 개편의 접점**에서 나왔다. 개별 코드는 각자 맞는데 **전제가 호출부에서 깨진** 형태다. **P0: 도감을 열어둔 내내 프레임마다 곤충 모델 1개를 통째로 만들었다 부순다.** `DrawDexGrid`(:623)가 스크롤 위치와 무관하게 128종을 전부 순회하고 타일마다 `InsectVisual.Draw` → `GetThumbnail`을 부르는데, 캐시 상한은 `ThumbCacheMax = 24`이고 **적중할 때마다 `Touch(key)`로 LRU를 갱신**한다. 128개가 매 패스 같은 순서로 LRU를 훑으므로 24칸은 절대 안정되지 않고, 미적중분이 큐에 쌓이면 `Update`가 프레임당 1개를 렌더한 뒤 `EvictOverflow()`가 **다음 프레임에 곧바로 다시 요청될 항목**을 버린다. 미적중 1건의 비용이 `RenderInsectTo`(`new GameObject` + `BuildForBattle` + `new RenderTexture(192,192,16,ARGB32){AA=2}` + 머티리얼 Destroy 루프 + `Destroy`)다. 부수효과로 대부분 타일이 2D 폴백과 3D를 오가며 깜빡인다. **결정적 근거는 렌더러 자신의 주석**이었다 — "프레임당 1개만 렌더한다 … 도감을 여는 순간 20종을 한 프레임에 처리하면 확실히 튄다". 상한 24는 **한 뷰포트 분량**(6열×~3.5행)에 맞춘 값인데 호출부가 컬링을 안 해 그 전제가 깨져 있었다. IMGUI 스크롤뷰엔 가상화가 없으므로 컬링은 호출부 책임이다. `DexBrowseLayout.GetVisibleRowRange`/`GetVisibleItemRange`(순수, overscan 1행)를 신설해 도감 그리드와 보유 탭 두 곳에 적용. **P1: `GetInsectData`가 `database.insects.Find(람다)`**(`PlayerInsectCollection.cs:445`) — `insectId`를 캡처하므로 **호출마다** display class + `Predicate<InsectData>`가 할당되고 128종 선형 탐색이다. 보유 탭이 카드마다 부르고 OnGUI는 패스가 여럿이라 60마리 보유 시 패스당 120개 할당 + 7,680회 문자열 비교. 바로 옆 `InsectModelPreviewRenderer.cs:110`이 같은 함정을 for 루프로 **일부러 피했다**고 주석까지 달아둔 자리인데 이 호출부만 놓쳤다. id→data 사전으로 교체(모든 호출부가 함께 이득). **P1: `OwnsShiny`**(:1509) 주석이 "선택 곤충 1종에 대해서만 호출(저빈도)"라고 단언하고 그 믿음 위에 `GetAllOwned()`(새 List) + 전수 스캔을 하는데, 호출부 `DrawDetail`은 상세 모달이 열려 있는 동안 **매 OnGUI 패스** 돈다. 결과는 라벨 한 줄에만 쓰인다 — 선택 변경 시에만 세도록 캐시(`SelectIndex`가 무효화). **P2 3건은 보고만**(사용자 결정 대기): ①`:683`/`:1178`의 `insectColor`가 계산만 되고 `InsectVisual.Draw`에 전달되지 않아(그 API엔 색 인자가 없고 `alpha`만 있다) **"발견만 됨" 타일이 포획 완료와 똑같은 풀컬러**로 그려진다 ②`:700`/`:1186`이 `displayName`을 `TextClipping.Clip` 스타일 고정 박스에 그려 6열(≈206px)에서 말줄임 없이 잘린다 — 같은 파일이 상세·아이템에선 `LabelFit`을 쓰는데 이 둘만 누락 ③`$"NO. {index+1:D3}"`(:668) 등이 타일마다·패스마다 문자열을 만든다. **Explore 판정 정확**(거짓양성 0건) — 체크 2·3·4는 실제로 clean이었다(이 파일엔 `+=` 구독이 없고, `UITheme.Instance`는 자기치유형, `TutorialQuestManager.Instance`는 가드됨, `InitDexStyles`의 `new GUIStyle` 31개는 `dexStylesInitialized` 가드 안). 검증: error CS 0, ci_check 통과, PlayMode **346/346**(컬링 순수부 신규 8케이스 포함). **P2 3건 후속 완료(사용자 승인)**: ①"발견만 됨" 타일을 알파 0.42 실루엣으로 구분 — **색이 아니라 알파를 쓴 이유**는 `InsectVisual`의 두 경로 중 알파만 양쪽이 지원하기 때문이다(2D 폴백 `CapturePopupUI.DrawTypedInsectPortrait`는 색을 id·등급에서 스스로 뽑고 alpha만 받는다. 색 인자를 새로 뚫었다면 3D 썸네일에만 먹고 폴백에선 무시돼 두 경로가 갈렸을 것). `DrawOwnedCard`에도 같은 계열의 죽은 지역 변수 `ic`가 있어 함께 제거 — 보유 곤충은 전부 포획 완료라 감쇠 자체가 없다 ②그리드 타일·보유 카드 이름을 `UIHelper.LabelFit`으로 전환 ③`NO. NNN`을 `DexBrowseLayout.NumberLabel`로 옮겨 미리 굽고 테스트 4건으로 고정(1-based 오프셋·캐시 재사용·128 초과 성장·음수 인덱스). 도감 순수 계산 헬퍼가 이미 그 자리라 사유 필드로 두지 않고 그쪽에 뒀다.
- 2026-08-06: RaidBattleUI **재감사** (2026-05-27 감사 이후 1609줄 변경, 재감사 큐 1순위) — **P1:3 처리 / P2:2 보고.** 세 결함이 전부 **한 커밋(5f0776f)의 라운드 파이프라인 교체**가 남긴 것이다 — `BossTelegraph`/`TryCompleteRoundPresentation` 도입, `UniteRushMinDuration` 신설, `BeginTurnAnnounce` 호출부 5곳 삭제가 모두 그 커밋이고(`git log -S`로 확인), 새 구조는 맞는데 **옛 구조가 들고 있던 전제들이 같이 안 옮겨졌다.** **P1: 합체공격 연출의 클라이맥스가 통째로 안 보인다.** 종료 조건이 `uniteAnimTimer > 2.5f`에서 `phaseTimer >= UniteRushMinDuration(1.15f)`로 옮겨졌는데 `DrawUniteAttackAnimation`의 타임라인은 2.5s 기준 그대로다. `animationReady`는 게이트가 못 된다 — 아레나 코루틴이 rush 0.42 + return 0.28 = **0.70s**에 끝나 `teamAnimationComplete`가 먼저 서고, 2D 폴백은 `teamAnimationComplete = arena == null || !arena.IsActive`로 **즉시 true**라 두 경로 모두 정확히 상한에서 이탈한다. 그래서 t가 1.15를 못 넘고, 5인 팀 기준 멤버별 착탄 시각이 0.75/0.95/**1.15/1.35/1.55**라 **3명분의 히트 플래시와 데미지 숫자**가 사라지며, 최종 폭발(t>1.5)·텍스트 쉐이크(1.5~2.0)·`TOTAL -N`(t>1.8)은 **어떤 경우에도 렌더되지 않는다**. 이 오버레이는 3D 아레나 위에도 그려지므로 두 모드 공통이다. 상한을 회귀 전 값 2.5f로 되돌리고, 두 숫자가 함께 움직여야 하는 이유를 상수 옆에 못박았다. **P1: 모바일 탭 래치.** `wantMouseClick`은 `OnGUI`의 MouseDown이 세우는데 소거가 `SelectInsect`/`SelectSkill` 분기 **안**에만 있었다. Intro·PlayerAttack·BossTelegraph·BossAttack·UniteAttack·Result에서 화면을 탭하면 true로 남았다가 다음 라운드가 SelectInsect로 들어오는 첫 프레임에 **지난 라운드의 `insectBtnRects`로** 소비돼, 선택 패널을 보기도 전에 곤충이 골라지고 SelectSkill로 건너뛴다. 결과 화면(5s) 탭은 `EndRaid`가 rect를 안 지워 **다음 레이드까지** 넘어갔다. 유일하게 중간에서 비워주던 자리가 하필 아래 P1로 죽은 `TurnAnnounce` 블록이었다 — 즉 이 결함은 그 삭제의 부산물이다. `Update` 말미 무조건 소거로 `Input.GetMouseButtonDown`과 같은 한 프레임 수명을 주고, `EndRaid`에도 버튼 캐시 4개 초기화를 추가(Explore P2 중 1건 동반 해소). **P1: 턴 배너 기계장치가 사장된 채 살아 있는 코드처럼 남았다.** 2654b8c(07-19)가 넣은 "팀의 턴 / 보스의 턴" 배너의 호출부 5곳을 5f0776f가 지웠는데 `Phase.TurnAnnounce`·announce* 5필드·`TurnAnnounceDuration`·`BeginTurnAnnounce`/`FinishTurnAnnounce`/`DrawTurnAnnounce`·OnGUI 디스패치·`TriggerBossAttackEffect`가 전부 남았다. **복원이 아니라 삭제로 판정**했다 — `DrawBossTelegraph`가 **같은 `introFightStyle`로 같은 중앙 위치**에 "⚠ 보스 공격 준비!"를 그리고 `DrawBossIntent`가 `CASTING` + 진행바까지 얹어 옛 배너보다 많이 알려주므로 의도된 대체이고, 되살리면 0.72s 텔레그래프 위에 0.9s가 더 붙는다. 1v1(`BattleScreenUI`)의 동명 코드는 호출부가 살아 있어 **건드리지 않았다**. 연쇄로 `BattleArenaController`의 `bossAttackTargetSlot` + `SetBossAttackTargetSlot`도 제거 — **쓰는 쪽 없이 읽히기만 하는 필드**라 초기값 때문에 컴파일 경고도 안 나던 자리다(호출부 0인 `RaidRoundModels.SetBossDamage`를 없앴던 것과 같은 함정). **Explore 판정 1건 과대평가**: `SetBossAttackTargetSlot` 미호출을 "보스 공격 대상 지정 경로가 죽었다"고 보고했으나 **기능 손실은 없다** — 새 경로 `OnRaidBossResponseResolved`가 `arena.PlayRaidBossAttack(..., lastHitSlot, ...)`로 피격 슬롯을 인자로 넘기고 `BattleArenaController:656`이 그 인자를 폴백보다 **먼저** 처리한다. 대상 지정이 필드에서 매개변수로 옮겨간 것뿐이라 결함이 아니라 정리 대상이었다. **통과 확인**: 구독은 `SubscribeRaidController()`가 해지→재구독을 모아 `OnEnable`·`AutoWire`가 공유하고 `OnDisable`의 5개 `-=`와 정확히 짝(2026-08-03 P0의 재발 없음), `OnDisable`의 `timeScale` 복구는 `OpeningReplayCoordinator.ApplyReplayBlock`이 "UI 먼저 끄고 timeScale 마지막" 순서를 지켜 성립하며 `CanReplay`가 `IsRaidActive`로 레이드 중 재생을 막는다, `new GUIStyle` 57개는 전부 `stylesInitialized` 가드 안, `Update`/`OnGUI`에 조회 없음. **P2 2건은 보고만**(사용자 결정 대기): ①3537줄 단일 클래스에서 `Draw*` 40여 개가 ~2600줄 — partial 분리 시 이번 같은 "페이즈 타이밍 변경 ↔ 드로잉 타임라인 불일치"를 리뷰에서 잡기 쉬워짐 ②`DrawBossIntent`의 `TeamStats[intent.TargetSlot].Data.displayName`이 `.Data` null 가드 없음(상위 조건은 `TeamStats[i] != null`까지만). **보고**: "팀의 턴" 신호는 텔레그래프가 대체하지 못하는 유일한 부분이라 대체 없이 사라졌다 — 선택 패널 자체가 신호 역할을 하지만 배너를 원하면 새 파이프라인의 `OnRaidRoundCompleted`에 다시 걸 수 있다. 검증: error CS 0, ci_check 8검사 통과, PlayMode **346/346**(신규 테스트 0 — 프레임 수명·페이즈 타이밍·사장 코드 제거는 `rules/testing.md`의 MonoBehaviour 생명주기 제외 대상). **P2 2건 + 배너 후속 완료(사용자 승인)**: ①`DrawBossIntent`에 `.Data != null` 가드 추가 ②**모놀리스 partial 분리** — `RaidBattleUI.Draw.cs` 신설로 3537줄이 본체 774줄 + 렌더 2748줄이 됐다(멤버 170개 중 렌더 102개 이동: GUIStyle 캐시 57 + `InitStyles` + `Draw*` 40여 + 순수 라벨·색 헬퍼 4. `EndRaid`·`Update`·`OnGUI`·`AutoWire`·컨트롤러 이벤트 핸들러는 본체 유지). 손으로 옮기지 않고 중괄호 깊이로 멤버 경계를 잡는 스크립트를 썼다 — 문자열 보간의 중괄호에 속지 않도록 리터럴과 줄 주석을 먼저 지우고 세며, 최종 깊이 0을 단언한다 ③**"팀의 턴" 배너 복원** — `Phase.TeamTurnAnnounce` + `OnRaidRoundCompleted` 진입, 0.9s·탭 스킵. **보스 쪽은 여전히 되살리지 않는다**(`DrawBossTelegraph`와 겹친다). Intro→SelectInsect와 SelectSkill의 Escape 복귀는 배너 없이 즉시. **부수 발견 1건**: `verify_coverage.py`의 경로 정규식이 스템 중간의 점을 허용하지 않아 **`RaidBattleUI.Draw.cs`를 agents/*.md에 제대로 적어도 조용히 무시**했다(미할당 0이 나온 건 coordination 표의 basename 경로가 우연히 받아준 덕이고, 공유 표기는 통째로 빠져 있었다). 점을 허용하도록 수정 — 안 고쳤으면 다음 partial 분할 파일이 소리 없이 미할당이 된다. 검증: error CS 0(신규 파일이라 2회 컴파일), ci_check 통과, verify_coverage 196/196 미할당 0, PlayMode **350/350**.
- 2026-08-06: SubAreaWorldBuilder **재감사** (2026-05-27 감사 이후 1352줄 변경, 재감사 큐 1순위) — **P1:1 처리.** 이번에도 Explore 위임 없이 직접 봤다. **P1: 서브에리어에 들어갈 때마다 런타임 머티리얼 41개가 샌다.** `Mat(Color)`가 `new Material(shader)`로 만들고 `Apply`가 `mr.material`에 대입하는데, 파기는 `Destroy(subAreaRoot)` **하나뿐**이라 GameObject 트리만 사라지고 머티리얼은 남았다 — `new Material`은 GameObject가 아니라 별개 오브젝트라 명시적으로 파괴해야 한다. 같은 저장소가 이미 그 이유로 `NpcVisualBuilder.CleanupMaterials`와 `PlayerVisualBuilder.SafeDestroyMat`를 갖고 있는데 여기만 빠져 있었다. **반복 경로라 누적된다** — 25m 이탈 자동 퇴장과 [E] 재진입으로 세션 내내 들락거린다. `runtimeMaterials` 리스트 + `DestroySubAreaWorld()`로 트리와 머티리얼을 함께 파기하고, 파기 지점 2곳(재빌드 전·퇴장)을 그 하나로 통일한 뒤 `OnDestroy`에도 걸었다(씬 전환 시 마지막 빌드분). **거짓양성으로 제외한 것 2건**: ①`OnDisable`이 `SubAreaChanged`를 해지하는데 `OnEnable`이 없다 — 형태는 BattleScreenUI P0와 같지만 이 컴포넌트는 `World/SubAreaWorld`라 `playUiRoot` 토글에 닿지 않고, 저장소 어디에도 World 루트를 SetActive하는 코드가 없다(그래서 `subscription_lint`가 `"UI/"` 루트만 검사하는 것도 설계상 맞다). ②`OnGUI`의 `$"{이름} 들어가기"` 문자열은 패스당 1개라 RegionMapUI 라운드가 세운 기준(항목별이 아닌 패스당 1~2개는 둔다)에 따라 보고하지 않았다. **통과 확인**: `notifyStyleCache`·`entryExitButtonStyleCache` 둘 다 null 가드로 1회 생성, `Update`의 플레이어 조회는 `cachedPlayerTransform` lazy 캐싱, `FindFirstObjectByType<PlayerMovement>`도 1회 캐시, 25m 이탈이 `RequestExit` 단일 경로로 통일돼 상태 누수 없음, `AudioManager.Instance`는 전부 null 가드 안. 검증: error CS 0, ci_check 8검사 통과, PlayMode **384/384**(코드 변경이 순수 생명주기라 신규 테스트 0 — 머티리얼 파괴는 `rules/testing.md`의 MonoBehaviour 생명주기 제외 대상).
- 2026-08-06: CharacterModelPreviewRenderer (큐 1순위, score 16) — **P1:3 처리.** 이번 세션에 내가 쓴 파일이라 더 회의적으로 봤고, 셋 다 **호출부가 OnGUI 그리드 루프라는 사실**에서 나왔다. **P1: 썸네일 캐시 키가 카드마다·패스마다 새 문자열이었다.** `ThumbKey(slot, itemId, lookHash)`가 `(int)slot + "|" + itemId + "|" + lookHash`를 만드는데 호출부는 `CharacterOutfitUI:541`의 카드 루프다 — 보이는 카드 16장 × 패스 3회 × 60fps면 초당 수천 개다(도감·지역맵 라운드가 잡은 것과 같은 형태). `ThumbId` 구조체(`IEquatable`)로 바꿔 조회 할당을 0으로 만들고, `TouchKey`/`EvictKeys`를 제네릭화해 기존 LRU 테스트는 그대로 돌게 뒀다. **외형 해시는 키에서 뺐다** — 외형이 바뀌면 `EnsureMannequin`이 이미 `ReleaseThumbs`로 캐시를 통째로 비우므로 키가 그걸 또 표현할 이유가 없었고, 그 중복이 키를 문자열로 만든 원인이었다. **P1: 렌더하는 프레임마다 PlayerPrefs를 4번 읽었다.** `EnsureMannequin`이 무조건 `AppearanceSpec.FromPlayerPrefs()`를 부르는데, 큰 패널은 각도가 바뀔 때 렌더하므로 **드래그로 돌리는 동안 매 프레임**이고 `SaveScope.PrefsKey` 키 문자열 4개도 함께 났다(OpeningSceneController 라운드가 "매 프레임 PlayerPrefs 조회"를 P1으로 잡았던 그 형태). `appearanceDirty` 플래그로 바꿨다. **부수 발견**: 그 플래그를 세울 자리로 `InvalidatePreview()`가 딱 맞았는데 **호출부가 0이었다** — 이 저장소가 반복해 제거해 온 사장 API 계열(`RaidRoundModels.SetBossDamage`, `DexBrowseLayout.FormatElementLabel`)이다. 제거하는 대신 `CharacterOutfitUI.Toggle`에 배선해 실제 역할을 줬다(외형은 캐릭터 생성 화면에서만 바뀌므로 의상 모달 여닫는 지점이 유일하고 충분한 재확인 시점이다). **P1: 경계 계산이 렌더마다 60여 개짜리 배열을 만들었다.** `TryComputeBounds`가 `GetComponentsInChildren<Renderer>(false)` 배열 반환판을 쓰는데 마네킹 렌더러가 60개쯤이고 이 경로도 드래그 중 매 프레임이다. 리스트 채우기 오버로드 + 재사용 버퍼(`boundsBuffer`)로 교체(static → 인스턴스 메서드). **거짓양성으로 제외 1건**: 퇴출·해제가 `rt.Release()`만 하고 `Destroy(rt)`는 안 한다 — 형제 `InsectModelPreviewRenderer`도 정확히 같아서(:178, :285) 이 저장소의 하우스 패턴이고, GPU 메모리는 Release가 푼다. 바꾸려면 두 파일을 함께 봐야 하므로 이 라운드 범위 밖. **통과 확인**: `Camera.Render`는 `Update`에서만(OnGUI 중 호출 금지 계약 유지), 리그 레이어 29 · 원점 -5200으로 곤충 프리뷰(30 / -5000)와 분리 유지, `CharacterOutfitManager.Instance` null 가드 있음, 마네킹은 외형 바뀔 때만 재빌드(풀링 유지), `OnDestroy`가 RT 전량 해제. 검증: error CS 0, ci_check 8검사 통과, PlayMode **386/386**(ThumbId 신규 5 − 옛 ThumbKey 3).
- 2026-08-06: OutfitShapeLibrary (큐 1순위, **score 0**) — **P1:2 처리(그중 1건은 교차 수정).** score 0은 "OnGUI/Update 표면이 없다"는 뜻일 뿐이라 스킬 경고대로 clean으로 보지 않고 열었고, **둘 다 이 파일이 아니라 호출부의 빈도에서 나왔다.** **P1: `Apply`가 슬롯마다 `SpawnPrefix + slot`으로 문자열을 만든다.** enum을 문자열로 바꾸는 연결이라 할당이 나고, `Apply`는 슬롯 5개 × (컨테이너 조회 + 필요 시 생성) 두 자리에서 불린다. 슬롯별 이름을 정적 배열에 미리 구워 `SpawnContainerName(slot)`으로 꺼낸다(테스트 2건으로 리터럴 연결과의 일치·인스턴스 동일성 고정 — 새 슬롯이 늘었을 때 조용히 어긋나면 컨테이너를 못 찾아 파츠가 매번 새로 생긴다). **P1(교차): 프리뷰가 각도만 바뀐 프레임에도 옷을 통째로 다시 입혔다.** `CharacterModelPreviewRenderer.Update`의 조건이 `로드아웃 해시 != 렌더된 것 || 각도 != 렌더된 것`인데 그 안에서 `ApplyLoadout`을 무조건 불렀다 — **드래그로 돌리는 동안 각도가 매 프레임 바뀌므로 매 프레임** `ApplyToCharacter`(8슬롯 색 적용 + 5슬롯 레시피 재배치, 그때마다 `FindDeep`이 마네킹 계층을 훑는다)가 돌았다. 마네킹이 **지금 입고 있는** 조합을 `appliedLoadoutHash`로 따로 들고, 그것과 다를 때만 다시 입힌다 (RT에 찍힌 `shownLoadoutHash`와는 다른 개념이다 — 썸네일 경로가 중립 조합을 입히면 여기서 갈라지고 다음 큰 패널 프레임이 자연히 되돌린다). **직전 라운드에서 내가 놓친 것을 이번에 잡았다** — CharacterModelPreviewRenderer 라운드는 그 파일 안의 할당만 봤고 `ApplyLoadout`이 부르는 **바깥 비용**은 세지 않았다. **통과 확인**: `ApplySpawned`가 `index < container.childCount`로 파츠를 **재사용**해 드래그 중 생성/파괴 왕복이 없고, `TrimContainer`가 남는 파츠의 `sharedMaterial`까지 파괴하며, spawn 파츠 머티리얼이 자기 전용이라 `sharedMaterial` 직접 수정이 안전하다(주석이 그 이유를 못박고 있다). 레시피 테이블은 순수 정적 데이터라 구독·싱글턴·세이브 항목은 해당 없음. 검증: error CS 0, ci_check 8검사 통과, verify_coverage 197/197 미할당 0, PlayMode **388/388**(신규 2).
- 2026-08-06: RaidSupportPlanner (큐 1순위, score 0) — **P1:2 처리.** 한 시간 전에 내가 쓴 파일이고, **둘 다 같은 라운드 안에서 뒤에 온 단계가 앞 단계를 낡게 만든 자리**였다. **먼저 확인한 것(P0 후보 → 무해)**: Stage 1이 기절·독에도 `Lands()`(명중 굴림)를 붙였는데, `InsectSkill.accuracy`의 선언 기본값이 0이면 `RollHit`의 하한 clamp(0.3) 때문에 **게임의 모든 스킬이 30% 명중**이 될 수 있었다. 확인 결과 선언이 `accuracy = 1f`이고 Bootstrap의 `CreateSkill`이 건드리지 않아 그대로 1 — 무해. **P1: 버프가 팀 전체로 바뀐 걸 플래너가 모른다.** Stage 4가 `BuffAttack`/`DefenseBuff`를 시전자 전액·아군 절반으로 넓혔는데, 플래너는 여전히 `attacker.AttackStacks >= MaxBuffStacks`만 보고 0점을 냈다 — **시전자가 상한이면 아군 4명에게 여유가 있어도 스킬을 버렸고**, 반대로 값도 시전자 몫만 세어 과소평가했다. `BuffRoom(team, caster, defense)`으로 실제로 붙을 대상의 가중 합(시전자 1.0 + 여유 있는 아군 × 전달비율)을 세고, 0일 때만(=전원 상한) 거른다. 점수도 그 값에 비례시켰다. **P1: 보스 연속기절 면역을 플래너가 모른다.** Stage 1이 `bossStunImmuneRounds`를 넣어 2라운드 연속 기절을 막았는데, 플래너는 면역 라운드에도 기절을 최고점으로 매겨 **기절기를 든 팀원들이 나란히 저항당할 시도**를 했다 — 버프를 스택 상한에서 거르는 가드와 정확히 같은 계열인데 기절만 빠져 있었다. `bossStunImmune` 인자(기본 false)를 받아 0점 처리하고 컨트롤러가 `bossStunImmuneRounds > 0`을 넘긴다. **통과 확인**: 라운드당 4회 호출이라 할당은 애초에 쟁점이 아니고(호출 빈도가 초 단위), 난수 미사용·동점 최저 인덱스라 결정론 유지, `Heal`은 Stage 4에서 이미 리졸버와 같은 대상(`LowestHpAlly`)을 보도록 맞춰 뒀다, `DebuffAttack`은 보스 단일 유지라 팀 확장과 무관. 순수 정적이라 구독·싱글턴·세이브·Bootstrap 항목은 해당 없음. 검증: error CS 0, ci_check 8검사 통과, PlayMode **391/391**(신규 3 — 시전자만 상한일 때 여전히 쓰는지, 전원 상한이면 거르는지, 면역이면 기절 대신 다른 걸 고르는지).
- 2026-08-06: CharacterOutfitData (큐 1순위, score 0) — **P1:2 처리.** 데이터 파일 자체는 순수 모델이라 결함이 없었고, **둘 다 이 파일이 노출한 API를 UI가 어디서 부르느냐**에서 나왔다. **P1: `GetPrimaryBonusText()`가 카드마다·OnGUI 패스마다 문자열을 만든다.** `$"포획 +{x*100:0}%"` 형태라 float 포맷까지 도는데 호출부(`CharacterOutfitUI:607`)가 카드 루프 안이다. **바로 윗줄 주석이 "카드 12개 × 30FPS = 360회/초 new GUIStyle 회귀 차단"이라고 적혀 있다** — GUIStyle은 막아뒀으면서 같은 빈도의 문자열은 다음 줄에 그대로 남아 있었다. 카탈로그가 세션 내내 불변이라 itemId로 memoize(`BonusTextFor`)하면 무효화도 필요 없다. **P1: 세트 진행도 별 표시가 덧붙이기 루프였다.** `stars += ...`를 `total`번 돌려 세트마다·패스마다 문자열이 제곱으로 났다(`CharacterOutfitUI:421-423`). 조합 수가 적어(채운 수, 전체 수) 정적 캐시로 구웠다. **거짓양성 1건 — 내 예전 계획서의 오판을 정정한다**: 의상 계획서(S1~S4)가 "`bonusProvider.GetActiveSets()`가 카드 루프 안 — O(카드×세트) 할당/프레임"이라고 적어 두고 개선 후보로 남겼는데, 실제 구현은 `public ActiveSetInfo[] GetActiveSets() => cachedActiveSets;`로 **캐시 필드 반환이라 할당이 0**이다. 중첩 루프는 문자열 비교만 돈다. 계획서의 그 항목은 근거 없이 적힌 것이었다. **통과 확인**: `OutfitLoadout.SlotCount = 8`이 enum과 어긋나면 프리뷰·입어보기가 새 슬롯을 조용히 무시하는데, 주석이 주장하는 `OutfitLoadoutTests`가 실재하고(`CharacterPreviewTests:145`) `Enum.GetValues(...).Length`와 고정한다 — 주석과 현실이 일치. `Hash()`는 8슬롯 다항식이라 실사용 아이템 95개 규모에서 충돌이 사실상 불가능하고, null과 ""가 같은 해시로 접히지만 `GetThumbnail`이 빈 id를 먼저 거르고 `CopyFrom`은 null만 쓰므로 도달 경로가 없다. `OutfitStatBonus`는 순수 struct라 구독·싱글턴·세이브 항목 해당 없음(세이브는 PlayerPrefs 2키가 단일 출처이고 이 타입은 안 들어간다). 검증: error CS 0, ci_check 8검사 통과, PlayMode **391/391**(순수 캐시 교체라 신규 테스트 0 — 동작이 바뀌지 않았고 기존 테스트가 계약을 이미 덮는다).
- 2026-08-06: TutorialQuestUI **재감사** (2026-05-27 감사 이후 1224줄 변경, 재감사 큐 1순위) — **P1:1 처리.** 이 파일은 2026-05-27에 캐시 라운드를 두 번(P0:1 + P1:8) 받았는데, 그 뒤 Phase A/B(강제 가이드 인트로, 스토리/서브 분류, 반복 상승 서브 퀘스트)로 상세 패널이 새로 들어오면서 **새 코드에만** 같은 결함이 다시 났다. **P1: `DrawDetailPanel`이 패스마다 `List<TutorialQuest>` 2개를 만들고 31개를 다시 가른다.** 스토리/서브 분리는 `TutorialQuestManager.Initialize()`가 코드로 만드는 고정 배열의 `category`에서만 파생돼 세션 내내 불변인데, 상세 패널이 열려 있는 동안 IMGUI 패스마다(프레임당 Layout·Repaint·입력) 재계산됐다. `EnsureQuestPartition`으로 캐시하고 **무효화 키는 원본 배열의 참조 자체**로 잡았다 — 매니저가 다시 초기화되면 배열 인스턴스가 바뀌므로 자동으로 다시 갈린다(`PlayerInsectCollection`의 `insectDataIndexSourceCount`와 같은 형태이되 참조 동일성이라 더 강하다). **거짓양성으로 제외 3건**: `:475`의 `ccur + "/" + ctgt`, `:588`의 같은 형태, `:459`의 `"★ " + act.title` — 전부 **활성 퀘스트 하나에 대한 패스당 1개**라 RegionMapUI 라운드가 세운 기준(항목별이 아닌 패스당 1~2개는 둔다)에 해당한다. **통과 확인**: `GetAllQuests()`는 배열을 그대로 반환해 할당이 없고(호출부가 매 패스 부르므로 중요하다), `InitQuestPanelStyles`가 `questPanelStylesReady` 가드로 GUIStyle 22개를 1회만 만들며, `DrawQuestRow`(행마다 호출)에는 할당이 하나도 없다 — 2026-05-27 캐시 라운드가 거기까지는 지켜냈다. `OnEnable`/`OnDisable`이 `QuestActivated`/`QuestProgressUpdated`/`QuestCompleted` 3쌍을 정확히 맞추고 `AutoWire`도 재구독 경로를 갖는다. `rewardChipBuffer`는 이미 재사용 버퍼다. 검증: error CS 0, ci_check 8검사 통과, PlayMode **396/396**(순수 캐시 교체라 동작 불변, 신규 테스트 0).
- 2026-08-06: TrainingUI **재감사** (2026-05-21 감사 이후 1007줄 변경, 재감사 큐 1순위) — **P0:1, P1:1 처리.** 2026-05-21에 캐시 라운드를 크게 받은 파일인데(P0:1 + P1:34), 그 뒤 **726795a가 3D 곤충 썸네일을 도입하면서** 도감과 똑같은 결함이 여기에도 생겼다. **P0: 보유 곤충 목록이 뷰포트 컬링 없이 전 개체를 돈다.** `DrawInsectSelect`가 `owned.Count` 전체를 순회하며 개체마다 `InsectVisual.Draw`(3D 썸네일 요청)를 부르는데, `InsectModelPreviewRenderer`의 캐시 상한은 **한 뷰포트 분량(24칸)**이고 적중할 때마다 LRU를 갱신한다. 60마리를 매 패스 같은 순서로 훑으면 24칸이 절대 안정되지 않아, 렌더러가 프레임마다 `InsectEntity`를 통째로 만들었다 부수고 RenderTexture를 create/Release 한다. **도감(DexScreenUI)에서 이번 세션 P0로 잡은 것과 완전히 같은 구조**인데, 그때 도감만 고치고 같은 썸네일을 쓰는 다른 목록은 보지 않았다 — 726795a가 건드린 화면이 9곳이므로 나머지도 같은 위험을 진다. 도감이 쓰는 순수 계산 `DexBrowseLayout.GetVisibleRowRange`(overscan 1행)를 그대로 공유했다 (UI→Dex 참조는 `InsectVisual`·`PlayerStatusHUD` 등 4파일이 이미 쓰는 방향이라 계층 위반이 아니다). **P1: 목록 한 줄의 정보 문구가 개체마다·패스마다 문자열 2개.** `"  |  " + SizeLabel(SizeMm(...))` 접미와 `$"Lv.n | 스킬 a/b | 장착 c/d{크기}"` 본문을 매번 만들고 크기 계산까지 돌았다(60마리면 패스당 120개). 값이 레벨·습득/장착 수·크기에서만 파생되고 그 변화가 전부 `InsectUpdated`로 오므로, **이미 있던 보유 목록 캐시와 같은 신호로** 비우는 `ownedInfoCache`를 붙였다(`HandleInsectUpdated`가 둘을 함께 무효화). **통과 확인**: GUIStyle 40여 개는 `trainDetailStylesReady`/`insectSelectStylesReady` 가드 안이라 1회 생성이고 (score 246의 대부분이 이 줄들이다 — 채점은 가드를 못 본다), `GetCachedOwned`가 `GetAllOwned` 매 프레임 호출을 이미 막고 있으며, `OnEnable`/`OnDisable`이 `InsectUpdated` 구독을 짝 맞춘다. **남긴 것**: 스킬 카드·장착 슬롯·교체 화면의 `"DMG {power}"` 류 문자열이 4곳에 같은 3분기로 중복돼 있고 항목마다 패스마다 생성되지만, 그 목록들은 최대 4~6개라 60마리 목록과 규모가 다르다 — 다음 라운드 후보로 남긴다. 검증: error CS 0, ci_check 8검사 통과, PlayMode **396/396**(컬링 순수부는 도감 라운드가 이미 8케이스로 고정, 문자열 캐시는 동작 불변이라 신규 테스트 0).
- 2026-08-06: BattleScreenUI **재감사** (2026-05-27 감사 이후 1586줄 변경, 재감사 큐 1순위) — **P1:2 처리 / P2:1 보고.** 이번 라운드는 Explore에 위임하지 않고 직접 봤다 — 직전 RaidBattleUI 라운드의 결함 3건이 전부 형제 파일과 같은 계열이라 그쪽부터 대조하는 게 빨랐고, 실제로 **첫 번째 P1이 같은 결함의 1v1판**이었다. **P1: 인트로 중 탭 한 번에 도주가 실행된다.** `wantMouseClick`은 OnGUI(MouseDown)가 세우는데 소거는 SwapSelect/PlayerTurn/TurnAnnounce 분기 **안**에서만 해서 Intro·PlayerAttack·EnemyAttack·Result에선 true로 남는다. 레이드보다 나쁜 건 두 가지 때문이다 — ①`Update`에서 **Intro 종료 전이와 PlayerTurn 처리가 같은 패스**에 있어 (`phase = Phase.PlayerTurn` 직후 30줄 아래에서 `phase == Phase.PlayerTurn` 블록이 돈다) 인트로 중 탭이 곧바로 그 프레임에 소비된다 ②`EndBattle`이 `skillBtnCount = 0`만 지우고 **`basicAtkRect`/`escapeRect`는 남긴다.** 히트 테스트가 `escapeRect.width > 0`라 지난 전투의 Rect가 그대로 유효하고, 도주 버튼은 화면상 같은 자리라 **2번째 전투부터 인트로 중 그 근처를 탭하면 턴을 보기도 전에 도주가 나간다**(스킬은 `skillBtnCount`가 0이라 무해). `Update` 말미 무조건 소거로 `Input.GetMouseButtonDown`과 같은 한 프레임 수명을 주고, `EndBattle`에 basicAtk/escape/swap Rect 초기화를 추가. **P1: 신규 스킬 4종이 1v1에서만 회색이다.** `GetSkillColor`가 `SkillEffectType` 7종 중 Damage/BuffAttack/DebuffAttack 3종만 처리하고 `default: Color.gray`로 떨어뜨린다 — d3d90cf가 Heal/PoisonDot/Stun/DefenseBuff를 추가하면서 **같은 파일의 라벨 switch 2개(`SkillTypeLabel`/`SkillPowerLabel`)는 7종을 전부 채웠는데 색 switch만 놓쳤다.** 레이드 쪽 `GetSkillColor`는 7종을 갖고 있어 같은 스킬이 화면마다 다른 색이었다 — 레이드 팔레트에 맞춰 4종 추가(enum↔switch 1:1 회복). **통과 확인**: 구독은 `SubscribeBattleController()`를 `AutoWire`·`OnEnable`이 공유하고 `OnDisable`의 3개 `-=`와 짝 (2026-08-03 P0의 재발 없음), `FindFirstObjectByType` 2곳 전부 캐시 뒤, `new GUIStyle`은 `stylesInitialized` 가드 안, `pendingSwap`/`pendingResult`는 전투 시작·Enter* 양쪽에서 소거돼 새지 않는다, `Time.timeScale`은 단일 아비터 + `EndBattle`의 try-finally 이중 복구. **연출 게이트는 이번엔 깨져 있지 않다** — 레이드의 P1(유나이트 상한 1.15s vs 2.5s 타임라인)과 대칭인 자리를 확인했는데, `PhaseAnimDone`의 2D 경로가 `phaseTimer > 0.8f`이고 `DrawAttackAnimation`도 `phaseTimer / 0.8f`로 정규화해 정확히 맞는다(3D 경로는 0.5~2s 사이 `IsPlayingSkill` 게이트, 2D 오버레이는 Clamp01로 유지). **거짓양성 제외**: `DrawBattleOverlay`의 `$"BATTLE - Turn {n}"`은 패스당 1개라 RegionMapUI 라운드가 세운 기준(항목별이 아닌 패스당 1~2개는 둔다)에 따라 보고하지 않았다. **P2 1건 보고**: `DrawSkillPanel`(:1921)이 매 패스 `battleController.GetPlayerCooldowns()`를 부르는데 그쪽이 **방어적 복사본**(`new int[len]` + `Array.Copy`)을 만든다. PlayerTurn은 플레이어가 생각하는 동안 수 초간 지속되는 페이즈라 그 내내 프레임당 여러 번 할당된다. 고치려면 `OnBattleUpdated`에서만 갱신하는 캐시가 필요한데, 쿨다운이 `BattleUpdated` 없이 감소하는 경로가 있으면 패널이 낡은 값을 보이므로 모놀리스 깊은 변경이다(`rules/audit-flow.md`: 표면 회귀만 자동 처리). 검증: error CS 0, ci_check 8검사 통과, PlayMode **350/350**(신규 테스트 0 — 프레임 수명·enum switch 보강은 `rules/testing.md`의 MonoBehaviour 생명주기 제외 대상).
- 2026-08-06: LoginUI **재감사** (2026-05-20 감사 이후 968줄 변경) — **clean P0/P1, 파일 수정 0건.** **score 183은 거짓양성**이었다 — `InitStyles` 안의 `new GUIStyle` 40여 줄인데 그 메서드는 `stylesInitialized` 가드로 1회만 돈다(채점기는 가드를 못 본다. TrainingUI의 score 246도 같은 형태였다). **P1 후보였다가 무해로 판정 2건**: ①`Update`가 매 프레임 `CheckCharacterCreation()`을 부르고 그 안에 `PlayerPrefs.GetInt` + **`System.IO.File.Exists`(디스크 I/O)**가 있다 — 매 프레임이면 심각하지만, 세 분기가 전부 `OnGameReady()`(→`phase = Done`) 또는 `phase = CharacterCreate`로 끝나고 호출 조건이 `phase == Login && IsLoggedIn`이라 **한 프레임만** 돈다. ②`"v" + Application.version + " · …"`이 패스마다 연결되지만 항목별이 아닌 패스당 1개라 RegionMapUI 기준에 해당한다. **통과 확인**: 텍스처 생성기 3종(`MakeGradientTex`/`MakeRadialTex`/`MakePanelTex`)이 **전부** `generatedTextures`에 등록하고 `OnDestroy`가 일괄 파기한다(2026-05-20 P1:2 수정이 유지되고 있다 — WorldLobbyUI의 MakeTex 누수 P0:7과 같은 계열인데 여기선 세 경로 모두 커버). 정리를 `OnDisable`이 아니라 `OnDestroy`에 건 것도 정확하다 — 텍스처는 1회 생성 후 재사용이고 `stylesInitialized`가 재생성을 막으므로, 비활성마다 지우면 다시 켤 때 빈 화면이 된다. `OnEnable`/`OnDisable`이 Auth 이벤트 3쌍을 정확히 맞추고, `AuthManager.Instance`는 참조 지점 전부에 null 가드, `TutorialQuestManager.Instance?.`는 null 조건 연산자. 코드 변경이 없어 테스트는 재실행하지 않았다(직전 검증 상태 유지: error CS 0, ci_check 통과, PlayMode 396/396).
- 2026-08-06: RaidBattleController **재감사** (오늘 레이드 5단계로 924줄 변경 — 큐 순서 대신 사용자 요청으로 앞당김) — **P1:1 처리 + 방어 1건.** 오늘 내가 가장 많이 고친 파일을 스스로 감사했고, **찾은 결함은 계획서에 적어 놓고 구현하지 않은 것**이었다. **P1: 회복 수치가 시전자 슬롯에 뜬다.** Stage 4가 회복 대상을 "생존 아군 중 최저 HP"로 바꿔 시전자와 대상이 갈렸는데, Stage 3이 만든 `BuildSlotContributions`는 여전히 `slotContribText[action.SourceSlot]`에만 적었다 — 즉 **멀쩡한 힐러 위에 `+40`이 뜨고, 정작 HP 바가 오른 곤충 위엔 아무것도 안 나온다.** 계획서 Stage 4에 "대상은 `RaidActionResult.TargetSlot`에 기록 → Stage 3 UI가 그 슬롯 위에 `+N`을 띄운다"라고 적어 두고 리졸버 쪽만 하고 UI 쪽을 빠뜨렸다(모델엔 `TargetSlot`이 제대로 들어가 있었고 UI가 안 읽었다 — `Healing`/`Capped`가 원래 그랬던 것과 똑같은 형태의 재발이다). 회복 전용 배열 `slotHealText`를 **받은 슬롯으로 색인**해 본문보다 한 단 위에 민트색으로 띄운다 — 배열을 나눈 이유는 회복받은 슬롯도 자기 행동(피해)이 있어 한 배열이면 서로 덮어쓰기 때문이다. **방어 보강**: `ResolveTeamCommand`의 서포트 루프가 `TeamSkills[i]`를 가드 없이 인덱싱했다. 바로 위 `CanUseSkill`은 `TeamSkills != null && i < Length`로 가드하는데 Stage 2에서 추가한 경로만 빠져 대칭이 깨져 있었다. **현재는 도달 불가**(유일한 호출부 `CaptureChoiceUI`가 항상 `MaxSlots` 길이를 채우고 `count < MaxSlots`면 시작을 거부한다)라 P1로 올리지 않고 한 줄 가드만 맞췄다 — `NpcDuelController` 라운드의 `Abs(int.MinValue)` 방어와 같은 판단. **통과 확인**: 기절 면역 타이밍을 라운드 단위로 되짚어 정확함을 확인(N라운드 적중 → 스킵 시 2 세팅 → `CompleteRoundPresentation`에서 1 → N+1 차단 → 0 → N+2 허용), `TeamCooldowns[i]`는 스킬이 없는 슬롯도 길이 0 배열로 항상 할당되고, 팀 전체 버프가 `StartRaid`의 의상·아이템 보너스와 같은 필드를 쓰지만 스택 카운터가 대상별이라 상한이 정상 동작하며, `BossEnraged`·`bossStunImmuneRounds`·`TeamStance` 전부 `StartRaid`에서 초기화된다. 검증: error CS 0, ci_check 8검사 통과, PlayMode **396/396**(회복 표시는 OnGUI라 `rules/testing.md` 제외 대상).
- 2026-08-06: BattleArenaController **재감사** (2026-05-27 감사 이후 933줄 변경, 재감사 큐 1순위) — **P1:1 처리.** **P1: 스킬 연출이 만든 런타임 머티리얼을 아무도 지우지 않는다.** `CreateSafeMaterial`(단일 생성 헬퍼, 호출 31곳)이 `new Material(shader)`로 만들고 각 이펙트 오브젝트의 `.material`에 대입하는데, 코루틴은 오브젝트만 `Destroy`하고 머티리얼은 남긴다. `CleanupArena`도 `arenaRoot`만 지운다. **스킬을 쓸 때마다** 링·스파크·구름·기둥이 새로 나므로 전투가 길수록 계속 쌓였다(SubAreaWorldBuilder 라운드와 같은 계열이되, 그쪽은 서브에리어 진입당이고 여기는 연출당이라 빈도가 훨씬 높다). 근거는 저장소 안에 있다 — `OutfitShapeLibrary.TrimContainer`가 GameObject보다 **머티리얼을 먼저** 파괴하고, `PlayerVisualBuilder.SafeDestroyMat`·`NpcVisualBuilder.CleanupMaterials`가 같은 이유로 존재한다. `CreateSafeMaterial`/`CreateTransparentMaterial`을 인스턴스 메서드로 바꿔 `runtimeMaterials`에 등록하고 `CleanupArena`에서 일괄 파기 — **호출 31곳을 건드리지 않고** 단일 생성 지점만 고쳐 누수를 전투 1회 수명으로 가뒀다. **거짓양성으로 제외 1건**: 페이드 코루틴들(`:1157`·`:1307`·`:1484`)이 매 프레임 `GetComponent<MeshRenderer>().material` **게터**를 부른다. 게터는 인스턴스가 없을 때만 만들고 그 뒤로는 같은 것을 돌려주는데, 여기선 생성 시 이미 `.material = mat`으로 대입해 인스턴스가 있으므로 **프레임마다 새로 생기지 않는다**. 다만 `GetComponent` 자체는 매 프레임 반복이라 루프 밖 캐싱 여지가 있다 — 이펙트가 5~8개 규모라 이번엔 두었다. **통과 확인**: `CleanupArena`가 `StopAllCoroutines`를 **먼저** 불러 파괴된 GameObject 접근을 막고 `playingSkill`을 강제 리셋한다(2026-05-27 라운드가 잡은 고착 회귀의 방어가 살아 있다), `OnGUI`의 GUIStyle 2개는 null 가드로 1회 생성, `rendererCache`가 렌더러 조회를 캐싱한다. 검증: error CS 0, ci_check 8검사 통과, PlayMode **396/396**(머티리얼 파괴는 `rules/testing.md`의 MonoBehaviour 생명주기 제외 대상이라 신규 테스트 0).
- 2026-08-06: **3D 썸네일 목록 컬링 일괄 스윕** (큐 순서 대신 사용자 요청으로 횡단 점검) — **P0:4 처리.** 직전 TrainingUI 라운드에서 "726795a가 건드린 화면이 9곳인데 도감·훈련 2곳만 컬링됐다"는 걸 발견해, 큐 순서(LoginUI)를 미루고 `InsectVisual.Draw` 호출부를 **전수(11곳) 기계적으로** 훑었다 — 각 호출의 위 45줄에서 루프·BeginScrollView·컬링 흔적을 찾는 스크립트로 판정했다. **컬링 없이 전 항목을 돌던 4곳**: `BattleTeamUI:324`(곤충 선택기), `CollectionUI:381`(보유 목록), `HospitalUI:248`(치료 대상 목록), `RegionMapUI:668`(리전 도감). 전부 `DexBrowseLayout.GetVisibleRowRange`로 통일했다 (UI→Dex는 `InsectVisual`·`PlayerStatusHUD` 등이 이미 쓰는 방향). **해당 없음으로 판정한 5곳**(전부 근거 확인): `DexScreenUI:703`은 이미 컬링된 그리드 루프가 부르는 타일 내부, `BattleTeamUI:207`·`CaptureChoiceUI:504`는 고정 5슬롯, `CaptureChoiceUI:186`·`CollectionUI:468`은 단일 개체. **재발 방지**: 원인은 개별 화면의 부주의가 아니라 **썸네일 도입 커밋 하나가 6개 화면에 동시에** 같은 결함을 심은 것이므로, 단일 진입점 `InsectVisual`의 클래스 주석에 "목록에서 부를 때는 반드시 뷰포트 컬링을 먼저 하라"를 이유·계산 함수·이력과 함께 못박았다. 새 목록을 만드는 사람이 반드시 읽는 자리다. **이 스윕이 왜 필요했나**: 도감 라운드(P0)에서 결함을 정확히 진단하고도 **같은 썸네일을 쓰는 다른 화면을 보지 않아** 5개 화면이 그대로 남아 있었다. 라운드 단위 감사가 "한 커밋이 여러 화면에 심은 결함"에 약하다는 사례다. 검증: error CS 0, ci_check 8검사 통과, PlayMode **396/396**(컬링 순수부는 도감 라운드의 8케이스가 이미 고정).
- 2026-08-06: UIShapes (큐 1순위, score 0) — **P1:1 처리.** 147줄짜리 도형 헬퍼인데 **계산 방향이 뒤집힌 진짜 그림 버그**가 있었다. **P1: `Part()`의 중간 roundness 혼합이 반대로 동작한다.** disc 위에 덮는 안쪽 사각형의 인셋을 `(1f - r)`로 잡아서, r이 클수록(둥글어야 할수록) 덮는 사각형이 커져 **각지게** 보이고 r이 작을수록 조각으로 줄어 **둥글게** 보였다 — 정확히 반대다. 경계로 확인하면 명백하다: r=0.02면 disc가 거의 다 드러나 타원, r=0.98이면 사각형이 98%를 덮어 네모다. **사장 코드가 아니라 살아 있는 경로였다** — 호출 277개 중 275개는 기본값 1f(순수 타원 분기)라 무사했지만, 2D 의상 카드 폴백의 `CharacterPortraitRenderer.RecipeRoundness`가 **Cube에 0.12f, 세운 원통에 0.8f**를 실제로 넘긴다. 즉 큐브 파츠가 타원으로, 원통 파츠가 사각형으로 그려지고 있었다. 그 폴백은 3D 썸네일이 구워지기 전 몇 프레임 동안 보이는 그림이라 품질이 중요하다고 파일 주석이 못박은 자리이기도 하다. 인셋을 roundness에 **비례**시키고(`BlendInsetRatio`), 그리기와 분리된 순수 수식으로 떼어 테스트 5건으로 방향·연속성을 고정했다 (경계 0/1, 단조 증가, clamp, 양쪽 순수 분기 임계에서의 연속). **통과 확인**: `Disc` 텍스처는 정적 지연 생성 후 재사용이고 파괴돼도 Unity의 `!= null` 오버라이드가 받아 다시 굽는다, `Capsule`이 `GUI.matrix`를 저장·복원하며 회전, `new Rect`/`new Color`는 struct라 GC 없음. MonoBehaviour가 아니라 구독·싱글턴·Bootstrap·세이브 항목은 해당 없음. 검증: error CS 0, ci_check 8검사 통과, PlayMode **396/396**(신규 5). **큐 재생성**: Uncovered가 0이 돼 `audit_candidates.py`로 새로 뽑았다 — 이제 전부 **재감사 후보**이고, 이번 세션에 대폭 바뀐 `RaidBattleController`(레이드 5단계)가 큐에 들어왔다.
- 2026-08-07: PlayerVisualBuilder **재감사** (2026-05-20 감사 이후 1170줄 변경, 큐 1순위) — **P1:1 처리 + 트랩 1건 제거.** **P1: 얼굴·머리 장식 머티리얼이 정리 목록에 없다.** `OnDestroy`가 `SafeDestroyMat(ref hatMat)` 식으로 **슬롯 필드 11개를 손으로 나열**하는데, `BuildFace`(눈·동공·하이라이트·눈썹·코·입 + 여성 홍조·속눈썹)와 `BuildUpHair`(리본)가 만드는 6~9개는 **지역 변수**라 그 목록에 오를 수가 없었다. 실플레이어는 영구 객체라 영향이 실질적 0이지만(파일 주석이 그렇게 적어둔 근거가 여기까지만 맞았다), **마네킹은 파괴가 정상 수명**이다 — `CharacterModelPreviewRenderer.EnsureMannequin`이 외형 해시가 바뀔 때마다 `Destroy(mannequin)` 후 다시 짓는다. 즉 외형을 바꿀 때마다 6~9개씩 샜다. 하필 `SafeDestroyMat`의 `previewMode` 분기 주석이 "여기서 강제로 지우지 않으면 마네킹 하나당 머티리얼 11개가 영구히 샌다"라고 **정확히 이 문제를 알아채고도 11개(=필드 개수)만 세었다** — 실제 생성량은 17~20개다. 생성 지점이 `MakeMaterial` 하나뿐이므로 static을 인스턴스 메서드로 바꿔 `runtimeMaterials`에 등록하고 `OnDestroy`가 목록을 일괄 처리하게 했다 — **호출 19곳을 건드리지 않았다**(2026-08-06 `BattleArenaController` 라운드가 31곳에 쓴 것과 같은 형태). 부수 효과로 `GetComponentsInChildren<MeshRenderer>` 조회가 필드마다 11번 돌던 것이 1번으로 줄었다. **트랩 제거**: `BuildFace`가 코에 쓰려고 **필드와 같은 이름의 지역 변수** `Material skinMat`을 같은 색으로 하나 더 만들고 있었다 — 피부색을 한 곳에서 바꾸려 하면 코만 옛 색으로 남는다. 필드를 쓰도록 접고 머티리얼 하나를 없앴다. **거짓양성으로 제외 1건**: 그 지역 `skinMat` 때문에 "코 색이 안 따라간다"를 P1로 올릴 뻔했으나, `CharacterOutfitManager.ApplyToCharacter`가 실제로 칠하는 노드는 Cap/CapBrim/Shirt/Body/Arm/Leg/Boot/Backpack/Net 14개뿐이고 **피부 노드는 아무도 recolor하지 않는다**(skinColor는 outer_none일 때 팔 색으로만 쓰인다). 현재는 시각 결함이 아니라 잠재 함정이다. **통과 확인**: `Awake`↔`BuildForPreview`의 `builtOnce` 상호 배제가 비활성 AddComponent 계약과 정확히 맞고(활성 상태로 붙이면 PlayerPrefs 외형으로 먼저 지어진다는 주석대로), `OnEnable`↔`OnDisable`의 `OutfitChanged` 짝이 `subscribedToOutfit` 가드로 중복 없이 성립하며(마네킹은 `previewMode`로 구독 자체를 건너뛴다 — 구독하면 핸들러가 `GameObject.Find("Player")`로 실플레이어까지 갱신하는 조용한 버그가 된다), `CharacterOutfitManager.Instance`는 참조 3곳 전부 null 가드, `MakeMaterial`의 셰이더 4단 폴백과 진단 로그 1회 래치도 정상. 검증: error CS 0, ci_check 8검사 통과, PlayMode **405/405**(머티리얼 파괴는 `rules/testing.md`의 MonoBehaviour 생명주기 제외 대상이라 신규 테스트 0).
- 2026-08-07: CharacterPortraitRenderer **재감사** (2026-05-27 감사 이후 1119줄 변경, 큐 1순위) — **P1:2 처리.** **P1-①: `DrawWithOutfit`가 주변 `GUI.color`를 오염시킨 채 반환한다.** 내부 `Draw`는 마지막 줄에서 `GUI.color = Color.white`로 스스로 되돌리는데, `DrawWithOutfit`은 그 **뒤에** `DrawArmsAsSkin`·`DrawBackpackWithSlot`·`DrawOutfitAccessories` 셋을 더 붙이고 그 셋은 전부 `DrawCol`(= `GUI.color = col` 후 그리기)로 끝난다. IMGUI의 `GUI.color`는 이후 모든 그리기에 곱해지므로, **호출부가 다음에 그리는 것이 마지막 파츠 색으로 물든다.** 실제 피해는 `CashShopUI:269-275`다 — `DrawWithOutfit` 바로 아래 "보유 재화"·"💎 {gems}"·"🪙 {coins}" 라벨 3개가 색을 따로 세팅하지 않아 그대로 곱해진다. 하필 `DrawOutfitAccessories`의 마지막 분기가 악세서리 색이고 **기본 악세서리 색이 `(0.1, 0.1, 0.1)`**이라, 그 계열을 장착하면 재화 라벨이 거의 검정으로 깔려 안 보이는 수준까지 간다. 도구만 낀 상태면 망 색(0.95 크림)이라 미묘하고, 장비 조합에 따라 증상이 오락가락해 재현이 어려운 형태다. 같은 파일의 `DrawItemPreview`는 `prevCol`을 저장·복원하고 `Draw`도 스스로 되돌린다 — **저자가 이 위험을 알고 두 곳엔 방어를 했는데 공개 진입점 하나만 빠졌다.** `CashShopUI:254`가 `DrawWithOutfit` **직전에** `GUI.color = Color.white`를 두고 있는 것도 같은 오해의 흔적이다(복원이 필요한 쪽은 호출 뒤다). 공개 진입점이 스스로 되돌리게 고쳐 호출부가 이 사정을 몰라도 되게 했다. **P1-②: `DrawOutfitAccessories`가 `OutfitCache`를 우회한다.** 이 파일의 캐시 블록은 주석에 *"매 OnGUI 호출 시 PlayerPrefs 5회 + GetEquipped 8회를 60FPS×13회/초=780회/초 차단"*이라고 목적을 못박아 뒀는데, 정작 `DrawOutfitAccessories:509`가 `PlayerPrefs.GetInt(SaveScope.PrefsKey("InsectGame.Character.Gender"))`를 **매 패스 직접** 읽고 있었다 — 값은 이미 `cache.gender`에 있다. `PrefsKey`→`AuthManager.ScopedKey`는 `baseKey + "." + uid` **문자열을 매번 새로 만들므로** 네이티브 조회에 더해 프레임 할당까지 붙는다(OnGUI는 Layout·Repaint·입력마다 패스가 돌아 초당 60회보다 많다). 유일한 호출부 `DrawWithOutfit`이 이미 `gender`를 지역에 들고 있어 매개변수로 넘겼다. 이제 이 파일의 `PlayerPrefs` 접근은 `RefreshCache` 5줄뿐이다. **거짓양성으로 제외 2건**: ①`SortRecipeByDepth`가 static `recipeOrder[32]`를 공유해 "동시 호출 시 뒤섞임"으로 보이지만 Unity는 단일 스레드고 정렬→그리기가 `DrawRecipePreview` 안에서 동기적으로 끝난다. 32칸 상한의 조용한 절단도 확인했으나 **실제 레시피 45개의 파츠 수는 최대 2개**라 도달 불가. ②`EnsureSubscribed`가 매니저 파괴 시 `-=`를 건너뛰는 것처럼 보이지만, 파괴된 MonoBehaviour는 Unity의 `!= null` 오버라이드가 false를 주고 그 이벤트도 함께 사라지므로 결과가 같다(새 매니저가 오면 재구독 + 캐시 무효화도 정상). 검증: error CS 0, ci_check 8검사 통과, PlayMode **405/405**(둘 다 OnGUI 경로라 `rules/testing.md`의 IMGUI 제외 대상 — 신규 테스트 0).
- 2026-08-07: RegionTerrainBuilder **재감사** (2026-05-21 감사 이후 1147줄 변경, 큐 마지막 항목) — **P1:1 처리 + 큐 재생성.** **P1: 월드 지형지물이 실행마다 다른 자리에 선다.** 이 빌더는 `Random.Range`를 **87번** 부르는데 프로젝트 어디에도 `Random.InitState`가 없다 — Unity 전역 난수는 실행마다 다른 상태에서 시작하므로 나무 25그루·통나무·바위·죽은나무·버섯·생울타리·아치·폐허 벽/기둥의 배치가 **매 실행 새로 뽑힌다.** 장식이면 넘어갈 일이지만, `Prim()` 55곳 중 **9곳이 collider를 남긴다**(나무 trunk·통나무·숲 바위·죽은나무·산 바위·생울타리·아치 기둥·폐허 벽·폐허 기둥). 그 9종은 `PlayerMovement.IsBlockedPosition`의 OverlapSphere가 막는 실제 장애물이라, **지나갈 수 있는 길 자체가 실행마다 바뀐다** — 어제 걷던 경로가 오늘 막히고, 그런 버그는 재시작하면 사라져 재현조차 되지 않는다(`CameraFollower.ResolveObstruction`의 차폐 판정에도 같은 콜라이더가 걸린다). 고정 시드(`TerrainLayoutSeed`)로 배치 구간만 가두되 **`Random.state`를 저장·복원**했다 — 전역 난수를 고정한 채 두면 스폰·IV·포획 판정까지 결정론이 되어 훨씬 나쁜 문제가 되고, 빌드 중 예외가 나도 반드시 되돌아가도록 `finally`에 뒀다. 세션 안의 다양성은 그대로고 세션 사이의 안정성만 생긴다. **거짓양성으로 제외 3건**: ①`Mat()`이 `new Material`을 64번 만들고 정리 코드가 없어 누수로 보이지만, 지형은 부팅 시 1회 짓고 씬 수명 내내 존재하는 영구 객체다(마네킹·연출처럼 반복 생성·파괴되는 경로가 아니다). ②그 64개가 루프 안에서 만들어지면 오브젝트마다 머티리얼이 하나씩 생겨 배칭이 깨지는데, **실측 결과 루프 안 호출은 0건**이고 `BuildFenceArc`처럼 루프 밖에서 한 번 만들어 60개 기둥이 공유한다 — 이미 올바른 형태다. ③`Prim()`이 `SetParent`를 하지 않아 씬 루트가 지저분해지지만 `BuildAllRegions`의 호출부는 `PlaySceneBootstrap` 1곳·1회뿐이라 중복 생성이나 정리 누락이 생기지 않는다. **통과 확인**: 랜덤 소품이 본 마을 부지를 피하는 `RandomSpotAvoiding`이 meadow의 랜덤 배치 2곳 모두에 걸려 있고(상수는 `VillageBuilder`가 단일 출처), 나무 배치 반경이 최대 0.65R이라 1.0R 근처의 fence gateway(통로)를 막지 않으며, `centerPosition.y`가 7리전 모두 0이라 과거의 `c.y` 이중 합산 계열 회귀는 재발 여지가 없다. MonoBehaviour이지만 생명주기 콜백·이벤트 구독·싱글턴 참조가 하나도 없어 검사 항목 1~4·7은 해당 없음. **큐 재생성**: 이 항목으로 Uncovered가 0이 돼 `audit_candidates.py`로 15건을 새로 뽑았다(1순위는 오늘 2961줄이 바뀐 `RaidBattleUI`). 검증: error CS 0, ci_check 8검사 통과, PlayMode **405/405**(지형 생성은 씬 의존이라 `rules/testing.md`의 MonoBehaviour 생명주기 제외 대상 — 신규 테스트 0).
- 2026-08-07: RaidBattleUI **재감사** (큐 1순위, score 33, "2026-08-06 감사 이후 2961줄 변경") — **clean P0/P1, 파일 수정 0건.** **후보 점수의 근거가 통째로 허수였다** — git numstat이 센 2961줄은 대부분 `RaidBattleUI.Draw.cs`로의 **이동**이고, 실제 추가는 116줄(그중 상당수가 주석, 5줄은 오늘 내가 넣은 카메라 호출)이다. 채점기는 "무엇이 왜 바뀌었나"를 모르고 줄 수만 세므로 **파일 분할이 곧 재감사 최우선순위로 둔갑한다** — 스킬 문서가 "점수는 열어볼 순서이지 무엇이 문제인지가 아니다"라고 적어둔 그대로다. **사장 코드 계열 기계 점검(0건)**: 이 파일의 과거 결함 3건이 전부 "5f0776f 파이프라인 교체가 남긴 죽은 배선"(`TriggerBossAttackEffect`·`FinishTurnAnnounce`·`SetBossDamage`)이었으므로, 두 파일의 주석을 걷어낸 뒤 private 필드 전량의 쓰기/읽기 횟수를 세어 불균형을 찾았다. 걸린 3건(`TeamRushMinDuration`·`UniteRushMinDuration`·`BossImpactMinDuration`)은 전부 `const`라 선언이 쓰기로 잡힌 것이고 각각 `Update`에서 정확히 한 번 읽힌다 — 진짜 사장 필드는 없다. **입력 표면 잔존 4종 전수 추적(전부 안전)**: `insectBtnRects`·`raidSkillRects`·`stanceRects`·`uniteBtnRect`는 OnGUI가 채우고 Update가 읽는데 Update가 프레임에서 **먼저** 돌아 한 프레임 낡은 값을 본다. ①슬롯 선택 → SelectSkill 전이는 `else if` 체인이라 같은 프레임에 스킬 분기가 이어 돌지 않고, ②`wantMouseClick`이 Update 말미에 매 프레임 소거돼(이전 라운드가 고친 자리) 배너를 넘긴 탭이 슬롯 선택으로 새지 않으며, ③`stanceRects`는 그리는 곳이 `DrawInsectSelector` 하나뿐이고 히트 테스트도 SelectInsect에서만 하며, ④`uniteBtnVisible`은 `DrawUniteGaugeBar`가 Intro/Result에서 **세팅 전에 early-return**해 낡은 값이 남을 수 있으나, `EndRaid`가 false로 비우고 클릭 경로가 `CanUniteAttack`(IsActive + roundStage Ready + 게이지 만충 + 생존 2 이상)을 함께 요구해 도달 불가다 — 잠재 형태이지 현재 결함이 아니라 P1로 올리지 않았다. **통과 확인**: `OnEnable`↔`OnDisable`이 컨트롤러 이벤트 5개를 정확히 짝짓고(`SubscribeRaidController`를 `AutoWire`와 공유, `-=` 뒤 `+=`라 중복 없음 — 오프닝 다시보기가 UI 루트를 토글해도 되살아난다), `AudioManager`·`TutorialQuestManager` 싱글턴은 참조 전부 null 가드, `cachedRegionMgr`가 `FindFirstObjectByType`을 레이드 승리당 1회로 묶고, `EndRaid`가 미초기화 상태로 남기는 필드들(`actionTimer`·`bossShake`·`resultShown`·`displayBossHp`)은 5초 결과 화면에서 자연히 소진되거나 `OnRaidUpdated`의 시작 블록이 다시 채운다. **문서가 못박은 교차 불변식도 확인**: 이 파일 `OnDisable`의 `timeScale` 복구가 오프닝 다시보기를 깨뜨리지 않으려면 `ApplyReplayBlock`이 `SetActive(false)`를 `Time.timeScale = 0f`보다 **먼저** 불러야 하는데(`rules/ui-layout.md`), 현재 코드가 그 순서이고 "반드시 마지막" 주석까지 살아 있다. 코드 변경이 없어 테스트는 재실행하지 않았다(직전 검증 상태 유지: error CS 0, ci_check 통과, PlayMode 405/405).
- 2026-08-07: CollectionUI **재감사** (2026-05-21 감사 이후 913줄 변경, 큐 1순위, score 226) — **P1:1 처리 + 사장 코드 2건 제거.** **P1: 보유 목록이 행마다·매 패스마다 문자열 4개를 새로 만든다.** `DrawInsectItem`이 ①`data.rarity.ToString()`(enum → **박싱** + 문자열) ②`"  |  " + SizeLabel(SizeMm(...))`(계산 + 할당 + 연결) ③`$"Lv.{level} | {rarity} | IV: {n}%{size}"` ④`$"HP:{n} ATK:{n} DEF:{n}"`를 매번 만든다. 네 값 모두 **(종·개체·레벨) 파생이라 불변**인데 OnGUI는 Layout·Repaint·입력마다 패스가 도므로 프레임당 여러 번 반복된다. 이 세션의 3D 썸네일 컬링 스윕이 같은 루프(`CollectionUI:381`)를 이미 손봤지만 **컬링은 "몇 행을 그리나"를 줄일 뿐 "행마다 무엇을 새로 만드나"는 그대로**라, 보이는 행 × 패스 수만큼 계속 나오고 있었다. 고치는 방법은 저장소 안에 이미 두 번 있다 — `RegionMapUI` 재감사(2026-08-04)가 "항목마다 매 패스 `$"등급 | CP n"` 보간+enum 박싱"을, `TrainingUI` 재감사(2026-08-06)가 "개체마다 정보 문자열 2개를 매 패스 생성"을 같은 방식으로 고쳤다. **CollectionUI만 남아 있었다.** 무효화 배선을 새로 만들 필요도 없었다 — `ownedCacheDirty`가 이미 `InsectUpdated` 구독으로 돌아가고, `TryLevelUpWithCandy`가 `data.level++` 직후 그 이벤트를 쏘므로(PlayerInsectCollection:389) 레벨이 바뀌면 문자열도 함께 다시 굽힌다. `cachedOwned`를 다시 만드는 바로 그 자리에 `BuildRowTextCache()`를 붙이고 행 그리기는 구운 문자열을 받기만 한다. **사장 코드 2건**: 빈 `Update()`(본문이 없어도 Unity가 매 프레임 managed→native 호출을 한다)와 빈 `DrawToggleButton()`(`OnGUI` 첫 줄에서 `isOpen` 검사보다 **앞에** 불려 화면이 닫혀 있을 때도 매 패스 돌았다) 제거 — 퀵바 진입점은 `QuickAccessBarUI`가 맡는다. **거짓양성으로 제외 1건**: score 226의 실체는 `InitDetailStyles`의 GUIStyle 30여 개 + `InitItemStyles` 5개인데 둘 다 `detailStylesReady`/`itemStylesReady` 가드로 1회만 돈다(LoginUI 183·TrainingUI 246과 같은 형태 — 채점기는 가드를 못 본다). **통과 확인**: `OnEnable`↔`OnDisable`의 `InsectUpdated` 짝이 성립하고(`-=` 뒤 `+=`), `ModalUIRegistry` 등록/해제가 `Toggle`·`CloseModal`·`OnDisable` 세 경로 모두에 있으며, `TutorialQuestManager.Instance`는 null 가드, 상세 패널의 스크롤 뷰포트가 `Mathf.Max(1f, ...)`로 음수를 막고, 설명 라벨이 `UIHelper.LabelFit`을 써 고정 84px 상자에서 잘리지 않는다. 상세 패널·학습기술 목록도 매 패스 문자열을 만들지만 **한 번에 개체 하나 · 5행 안팎**이라 목록과 규모가 다르고, 이번 라운드는 목록만 다뤘다. 검증: error CS 0, ci_check 8검사 통과, PlayMode **405/405**(OnGUI 경로라 `rules/testing.md`의 IMGUI 제외 대상 — 신규 테스트 0).
- 2026-08-07: WorldChannelManager **재감사** (2026-05-27 감사 이후 893줄 변경, 큐 1순위, score 29) — **P1:1 처리.** **P1: 온라인 필드의 내 레벨이 항상 Lv.1이다.** `joinWorld`·`syncWorld` 페이로드가 `level = PlayerPrefs.GetInt("player_level", 1)`로 채워지는데, 그 키는 **옛 저장소**다 — 전체 저장소를 뒤져보니 쓰는 곳이 `CloudSaveManager.ApplySaveData:467` **단 하나**(클라우드 복원 시 1회)이고, 실제 레벨은 `PlayerProgressController` → `player_progress.json`에 산다(`save-system.md`). 레벨업은 그 JSON에만 남고 이 키를 갱신하지 않으므로 **신규 설치·로컬 플레이는 영원히 1**, 클라우드에서 복원한 계정은 **복원 시점 값으로 굳는다.** 이 값은 서버로 올라가 다른 사람 화면에 그대로 뜬다 — `WorldFieldMultiplayerUI`의 근처 탐험가 배너(:163)·플레이어 라벨(:328)·친구 목록(:510)과 `WorldLobbyUI`의 로비 목록(:439), **4곳**이다. **이 결함은 이미 한 번 진단된 것이다** — `CloudSaveManager` 상단 주석이 *"옛은 PlayerPrefs(player_level/player_candies/player_coins)를 읽어 JSON 파일에 저장하는 실제 시스템과 어긋나 레벨/XP/캔디/코인이 클라우드에 전혀 동기화되지 않았음(항상 기본값 0/1 업로드)"*라고 적어두고 그 파일은 `progressController != null ? progressController.Level : PlayerPrefs...` 형태로 고쳤는데, **`WorldChannelManager`만 그 정정에서 빠졌다.** 같은 폴백 식을 쓰는 `LocalPlayerLevel` 프로퍼티로 교체했다(진행도 컨트롤러 우선, 없으면 옛 키). 조회는 `CacheLocalPlayer`와 같은 지연 캐시라 비용 프로필이 같고, 호출 지점도 Update가 아니라 join/sync 코루틴(최대 1초에 1회)이다. **거짓양성으로 제외 2건**: ①`AuthManager.Instance.IdToken`·`.DisplayName`을 null 가드 없이 역참조하지만 모든 진입점이 `CanStartRequest()`/`Update()`에서 `IsFirebaseReady()`(=`Instance != null && IsLoggedIn && IdToken 비어있지 않음`)를 먼저 통과한다. ②`CacheLocalPlayer`가 `PlayerMovement`를 못 찾으면 매 sync마다 `FindFirstObjectByType`을 다시 부르지만 1초에 1회이고 파괴된 참조를 되찾는 데 필요한 형태다(Unity의 fake-null). **통과 확인**: `SendRequest`에 `timeout = 12`가 있어 half-open 연결이 `IsBusy`/`syncInFlight`를 영구 true로 고정하는 소프트락을 막고, 코루틴 5종이 전부 `try/finally`로 플래그를 복구하며(이벤트 핸들러 예외에도), sync 응답이 돌아왔을 때 `CurrentWorld == null`이면 stale 응답으로 월드를 되살리지 않고(`나가기 눌렀는데 재입장` 차단), `RespondInviteRoutine`이 `JoinWorldRoutine` 호출 **전에** 자기 `IsBusy`를 먼저 푸는 순서도 정확하다. 401 재시도가 `allowRetry=false`로 한 번만 돌아 무한 재귀가 없다. 검증: error CS 0, ci_check 8검사 통과, PlayMode **405/405**(네트워크 경로라 `rules/testing.md`의 외부 서비스 제외 대상 — 신규 테스트 0).
- 2026-08-07: CapturePopupUI **재감사** (2026-05-27 감사 이후 657줄 변경, 큐 1순위, score 76) — **P1:1을 5개 파일에서 처리.** `"beetle"` ⊃ `"bee"`인데 bee 분기가 stag/rhinoceros/hercules/longhorn보다 앞이라 **딱정벌레 31종이 전 화면에서 벌로 그려졌다**(포획 팝업·도감·1v1·레이드 2곳). `InsectEntity.BuildModel`만 가드를 갖고 있었다 — **같은 규칙이 5곳에 복제된 구조 자체가 결함의 원인**이라, 고치는 김에 `InsectPortraitRoutingTests`로 소스에서 조건식을 읽어 가드 누락을 고정했다. 거짓양성 1건 제외(`ResolveWings`의 bee 분기는 모델이 아니라 날갯짓 파라미터라 무해). 검증: error CS 0, ci_check 8검사 통과.
- 2026-08-07: CloudSaveManager **재감사** (2026-05-20 감사 이후 669줄 변경, 큐 1순위, score 35) — **P1:1 수정.** 이번 라운드의 결함은 **같은 세션에서 방금 만든 것**이다: 2막 Phase 5의 간부 보스전이 격파 기록을 새 PlayerPrefs 키(`DefeatedLedgerBosses`)에 쓰면서 클라우드 5지점을 배선하지 않아, 기기를 바꾸면 같은 간부와 다시 싸우고 승리 보상도 다시 받았다. `defeatedGuardians`와 동형으로 붙이고 `NpcDuelController`를 `ICloudReloadable`로 만들었다(PlayerPrefs만 갈아끼우면 인메모리 캐시가 낡는다). **audit이 자기 세션의 산출물을 잡은 첫 사례** — 큐가 마침 CloudSaveManager를 가리킨 건 우연이지만, 신규 세이브 키를 만들 때 이 파일을 함께 열어보라는 규칙이 `save-system.md`에 이미 있었다. 검증: error CS 0, ci_check 8검사 통과.
- 2026-08-07: CharacterOutfitManager **재감사** (2026-05-20 감사 이후 671줄 변경, 큐 1순위, score 9) — P1:1 수정, P2:1 보고. **점수 9로 큐 최하위권인데 진짜 P1이 나왔다** — 스킬 문서의 "점수는 열어볼 순서이지 무엇이 문제인지가 아니다"가 그대로 맞았다. 수정: `GetItemsForSlot`의 OnGUI 매 패스 LINQ 할당(95벌 순회 + 배열 신규) → `Initialize`에서 슬롯별 1회 캐시. 보고만: `unlockCondition` 미평가로 조건부 의상 4벌 영구 잠금(해금 시점 결정이 게임 디자인 영역). 거짓양성 2건 제외(`Instance` 가드는 같은 식 앞 절에 있음 / `OnDestroy`에서 `Instance`를 안 비워도 Unity fake-null이 스스로 낫는다).
- 2026-08-07: TutorialQuestManager **재감사** (2026-05-27 감사 이후 681줄 변경, 큐 1순위, score 38) — **clean P0/P1, 파일 수정 0건.** 문서가 실패 양식을 이미 아는 드문 경우라(`rules/quest-system.md`의 q_team 영구정지 사고 + `quest_lint.py` 6검사) **린터가 보는 것과 보지 않는 것을 나눠** 접근했다. 린터 쪽은 7 PASS / 0 FAIL. 린터가 안 보는 3가지를 직접 대조해 전부 성립: ①퀘스트 PlayerPrefs 5키가 클라우드 4지점 + DTO에 모두 있고 `QuestUnseen`은 문서대로 의도된 로컬 전용 ②`Subscribe`/`Unsubscribe`의 `+=`/`-=`가 6/6으로 맞고 **q_team을 정지시켰던 `TeamChanged += OnTeamChanged`가 살아 있다**(그때 넣은 방어가 681줄 변경을 견뎠다) ③`ResetForNewAccount`가 `TutorialHidden`을 스코프·비스코프 양쪽 삭제. 거짓양성 2건 제외(`Instance` 가드는 같은 if의 앞 절에 있음 / `Update`는 3중 조기이탈 + 캐시된 Transform). 코드 변경 0건이라 테스트 미재실행.
- 2026-08-07: ProceduralAudioGenerator **재감사** (2026-05-21 감사 이후 696줄 변경, 큐 1순위, score 0) — **clean P0/P1, 파일 수정 0건.** static 생성기라 검사 1~7이 대부분 해당 없어 **8번(데이터 매칭)에 라운드를 걸었고**, 요청(`AudioManager`)↔생성(`ProceduralAudioGenerator`) 문자열을 양방향 전수 대조했다 — 요청 37개 전부 처리(누락 0), 생성기 전용 24개도 계산 문자열 경로 2종(`"skill_" + ElementToString`, `PlayAmbient(environmentType)`)으로 도달한다. **거짓양성 3건 제외, 그중 둘은 내 검사 도구의 오경보라 확인 후 철회했다** — 환경음 대조 스크립트가 메서드 범위 추출에 실패해 케이스 0개를 읽고 "11개 리전 전부 무음"이라는 오경보를 냈다(파싱 실패를 결과 0으로 읽으면 안 된다는 `verify_coverage`의 교훈과 같은 형태). 보고만 한 관찰 1건: 정적 캐시에 상한이 없으나 클래스의 명시된 설계이고 실측 없이 수치를 단정할 수 없어 P1로 올리지 않았다. 코드 변경 0건이라 테스트 미재실행.
- 2026-08-07: CashShopUI **재감사** (2026-05-21 감사 이후 766줄 변경, 큐 1순위, score 203) — **clean P0/P1. P2:1 보고 후 사용자 승인으로 처리.** score 203은 거짓양성(`new GUIStyle` 29개가 전부 `stylesReady` 가드 뒤). 실결제 경로를 먼저 봤고 전부 정상 — 탭 3곳이 `CashShopManager.Instance == null` 가드로 시작하고, `PurchaseWithGems`가 사전검증→잔액확인→차감→실패시환불 순으로 원자적이다. **P2(승인 후 처리): 카드 라벨 리치텍스트를 OnGUI 패스마다 재생성**(아이템 탭 기준 패스당 36개). `CardText` 구조체 + `cardTextCache`로 품목당 1회만 굽되, **실결제 가격은 캐시에서 뺐다** — IAP 준비 후 스토어 현지화가로 바뀌므로 굳히면 틀린 가격이 남는다. 레이아웃 무효화는 `EnsureCardTextLayout(mobile)`로 회전 시 `<size>` 태그를 갱신. 검증: error CS 0, ci_check 8검사 통과, PlayMode 405/405.
- 2026-08-07: InsectEntity **재감사** (2026-05-21 감사 이후 883줄 변경, 큐 1순위, score 8) — **P1:2 처리.** **P1-①: `AnimateWings`가 매 프레임 `transform.Find`를 두 번 한다.** `Update`가 무조건 부르는데 본문 첫 줄이 `transform.Find("WingL")` + `transform.Find("WingR")`이고, 이어서 종별 날갯짓 파라미터를 정하려고 `insectId.Contains`를 최대 10회 다시 돌린다. 둘 다 **빌드당 한 번이면 끝나는 값**이다. 가장 나쁜 건 **날개가 없는 종**이다 — 실패하는 Find가 자식 전체를 훑어 제일 비싸다. `ResolveWings()`로 노드와 파라미터를 함께 확정하고 리셋 블록 두 곳에 배선했다(못 찾아도 `wingsResolved`를 맨 먼저 세워 재시도하지 않는다). **P1-②: `Update`의 NameLabel 조회도 같은 형태** — 배틀 모델엔 그 노드가 아예 없어 전투 내내 실패하는 Find가 돌았다. `nameLabelResolved` 센티넬로 1회만. **거짓양성 2건 제외**: `AnimateShinySparkle`은 없으면 만들어 캐시하므로 2프레임째부터 조회 없음, `ClearChildren`의 배열 할당은 스폰/디스폰 1회 경로. 검증: error CS 0, ci_check 8검사 통과, PlayMode 405/405.
- 2026-08-07: AuthManager **재감사** (2026-05-20 감사 이후 630줄 변경, 큐 1순위, score 3) — **인증 코어 clean, 드리프트 1건 처리.** 토큰 로깅 0건 / `ClearAuth` 완전 / `Logout`의 플러시-후-무효화 순서 / `DeleteAccount`의 PII 부활 차단 — 급소는 전부 정상이었다. 처리한 건 `ApplyMasterPrivileges`의 **하드코딩 리전 목록**으로, 2막 6지역을 얹으며 해금 6·격파 7건이 누락돼 마스터 계정이 2막에서만 진행을 못 건너뛰었다. `RegionDefinitions`에서 파생하도록 바꾸고 테스트로 고정. **증상은 P2급인데 처리한 이유는 고치는 방법이 단일 출처 파생이라 위험이 0이고, 두면 리전 추가마다 같은 자리가 또 낡기 때문이다.** 검증: error CS 0, ci_check 8검사 통과.
- 2026-08-07: CharacterOutfitUI **재감사** (2026-05-27 감사 이후 595줄 변경, 큐 1순위, score 155) — **P1:1 처리.** 잠긴 의상 카드에 `unlockCondition` 원문 토큰이 그대로 그려져 한국어 화면에 `region_garden`·`level_15`가 노출됐다(조건부 4벌 전부). `DescribeUnlockCondition`으로 문장화하되 지역명은 `RegionDefinitions`에서 파생하고, 그리는 쪽은 `LabelFit`으로 바꿨다(고정 26px + wordWrap이라 한국어로 길어지면 잘리는 자리였다). `OutfitUnlockConditionTextTests` 5검사로 고정. **직전 CharacterOutfitManager 라운드의 P2와 겹치지만 다른 것을 고쳤다** — 그쪽은 해금 **판정**이 없다는 것이고 게임 디자인 결정이 섞여 그대로 남았다, 여기서 고친 건 **표시**뿐이다. 거짓양성 2건 제외(score 155의 GUIStyle 12개는 `stylesInitialized` 가드 뒤 — 이번 큐에서만 다섯 번째 같은 형태 / 호버 툴팁 문자열은 한 번에 카드 하나라 CollectionUI 라운드가 이미 제외한 규모). 검증: error CS 0, ci_check 8검사 통과, PlayMode 443/443.
- 2026-08-07: InsectSpawner **재감사** (2026-05-22 감사 이후 518줄 변경, 큐 1순위, score 26) — **P1:1 처리. 스포너 본체는 clean이었고 결함은 그것을 먹이는 부트스트랩에 있었다.** `GetRegionLevelRange`가 ver1 7지역만 아는 switch + `default: 5`라, 2막 6지역의 필드 스폰 상한이 47/51/55/59/63/67에 묶여 있었다(의도 48/52/56/60/64/70). 6 case를 바이블 대역대로 넣고 **default에 LogWarning**을 붙여 다음 사람이 알아채게 했다. **거짓양성 3건 제외 — 전부 내가 세운 가설이었고 코드를 읽어 철회했다**(먼 리전 낭비 스폰은 55m 거리 게이트가 이미 막고, 스폰포인트 밀도는 반경 비례라 리전 수와 무관하며, 리전 필터 할당은 5초 스로틀 뒤에만 돈다). 이 라운드의 교훈은 **"의심스러운 구조를 발견해도 방어 코드를 먼저 찾아라"**다 — 세 번 다 이미 방어가 있었고 주석이 이유까지 적어 뒀다. 검증: error CS 0, ci_check 8검사 통과, PlayMode 444/444.
- 2026-08-07: PlayerMovement **재감사** (2026-05-27 감사 이후 515줄 변경, 큐 1순위, score 23) — **P1:1 처리. 걷는 내내 매 프레임 `Collider[]`를 2개씩 할당했다.** `IsBlockedPosition`이 할당형 `Physics.OverlapSphere`를 쓰는데 Update가 이동 중 두 번 부른다(다음 위치 차단 + 끼임 감지) — 초당 120개 배열이 GC로 간다. `OverlapSphereNonAlloc` + 고정 버퍼(16)로 바꾸고 같은 형태의 `IsClearAt`도 같은 버퍼를 공유하게 했다(둘은 호출이 겹치지 않는다 — 끼임 판정 후에야 복구가 돈다). 버퍼가 차도 오판정 없음: 가득 찼다 = 이미 막을 것을 찾았다는 뜻. **거짓양성 2건 제외**: 월드 경계를 320→520으로 넓힌 게 이동 제한과 어긋날까 봤으나 이 파일엔 좌표 클램프가 없고 물리 벽이 담당한다 / 리전 순회가 7→13이 됐지만 `ContainsPoint`는 곱셈 두 번이라 무시할 수준. 검증: error CS 0, ci_check 8검사 통과, PlayMode 444/444.
- 2026-08-07: BattleTeamUI **재감사** (2026-05-21 감사 이후 487줄 변경, 큐 1순위, score 102) — **P1:2 처리. 이 라운드로 큐가 0이 되어 재생성했다**(상위 15, 신규 파일 3개 포함). ①화면 전체가 한국어인데 이 화면만 영어였다(`"BATTLE TEAM"`·`"Select Insect for Slot {n}"`·`"< Back"`) — 퀵바와 HUD가 이 화면을 "배틀팀"이라 부르므로 같은 말로 맞췄다. 화살표는 ASCII `<` 유지(전각으로 바꿨다가 폰트 아틀라스 확실성 때문에 되돌렸다). ②빈 `Update() { }` 제거 — 본문이 없어도 매 프레임 managed→native 호출이 든다(같은 날 CollectionUI 라운드가 지운 것과 동일). **거짓양성 3건 제외**: score 102의 GUIStyle 11개는 `teamStylesInit` 가드 뒤로 **이번 큐에서만 여섯 번째 같은 형태** / 피커 목록은 이미 `GetVisibleRowRange`로 컬링됨 / 슬롯 문자열은 5개 고정이라 기존 라운드가 명시적으로 제외한 규모. 검증: error CS 0, ci_check 8검사 통과, PlayMode 444/444.
- 2026-08-08: StoryJournalUI **신규 감사** (큐 1순위, score 41, 한 번도 감사된 적 없는 2막 신규 파일) — **P1:1 처리. 탭 한 줄이 프레임마다 스토리 전체를 훑었다.** `DrawChapterTabs`가 챕터 15개마다 그 챕터 비트를 세는데 `HasSeen`이 `List<string>.Contains`라, 다 본 세이브에서 **패스당 5천 회가 넘는 문자열 비교** + 보간 문자열 15개가 들었다(OnGUI는 프레임당 2패스 이상). 열람 상태는 저널을 열어 둔 동안 불변이므로(다시 읽기가 seen을 마킹하지 않는 게 이 화면의 설계다) `SeenCount`를 키로 라벨과 `done==0`을 캐시했다. **자체 회귀 1건 즉시 되돌림** — 배경색 판정을 캐시된 라벨의 문자열 파싱으로 되읽게 썼다가 매 패스 문자열 연산을 도로 넣는 꼴이라 `bool[]` 동반 캐시로 고쳤다. **거짓양성 3건 제외**: score 41의 실체는 struct(`new Rect`/`new Color`)와 `stylesReady` 가드 뒤 GUIStyle 7개로 **이번 큐에서만 일곱 번째 같은 형태** / `UITheme.Instance`는 정적 캐시라 첫 호출만 Resources.Load / 비트 목록의 컬링 부재는 도감 P0와 형태가 같아 보이나 3D 썸네일 요청이 없어 LRU 스래싱이 없고 최대 8행이라 뷰포트에 거의 다 들어온다. 검증: error CS 0, ci_check 8검사 통과, PlayMode 459/459.
- 2026-08-08: InsectExpansion2Definitions **신규 감사** (큐 1순위, score 0, 2막 신규 파일) — **데이터는 clean, 결함은 "짝"을 아무도 검사하지 않는다는 것.** 54종의 ID 유일성·1막 비충돌·레어도 밴드·등급분포·풀배정·BuildModel 라우팅 전수 정상(파일 수정 0건). 시드 주석이 habitat ↔ InferPrimaryType zone 폴백을 **짝**이라 못박았는데 기존 테스트는 시드 쪽만 자기 하드코딩 목록과 봤다 — 부트스트랩에서 분기가 사라지면 통과하고 그 리전 종의 속성이 전부 조용히 Bug가 된다. Epic/Legendary 12종의 전용기 switch도 같은 형태로 무검사였다. 둘 다 소스 대조 테스트로 고정하고 **오타·삭제를 주입해 검출력을 실측**했다. **거짓양성 2건 철회 — 둘 다 내 가설이었다**: zone 폴백 누락(grep -A 45 범위가 짧았다, 실제로는 6개 전부 존재) / IsAntInsectId가 mantis를 잡을 것(StartsWith("ant_")||"_ant_"||"antlion"이라 안 걸림). 이번 큐에서 세 번째 같은 교훈 — **의심스러운 구조를 보면 방어 코드를 먼저 찾을 것.** 검증: error CS 0, ci_check 8검사 통과, PlayMode 461/461.
- 2026-08-08: NpcBossDuels **신규 감사** (큐 1순위, score 0, 2막 신규 파일) — **표는 clean, 결함은 주석이 약속한 검증이 실제로는 없다는 것.** "아이템 ID는 ItemDatabase에 실재해야 하며 NpcBossDuelTests가 정합을 고정한다"고 적혀 있으나 그 테스트는 **비어 있지 않은지만** 본다(곤충은 실재성을 본다). 오타를 물면 조용히 실패해 **간부를 이겼는데 보상만 안 들어온다**. 아이템 ID가 네 소스의 합집합이라 그 레지스트리를 이미 모으는 quest_lint.py에 검사 4b를 붙이고, 추출 0건이면 ExtractorBroken을 던지게 해 파서가 낡았을 때 조용히 죽지 않게 했다. **오타 주입으로 검출력 실측**(net_gold → net_goldd 검출, 현재 3건은 전부 실재). 주석도 "고정하는 곳이 둘로 나뉜다"로 정정했다 — 약속과 실제가 어긋난 게 본질이라 문서를 두면 다음 사람이 또 믿는다. 통과 확인: 상대 곤충 3종 실재, storyNpcId 3개가 story.json NpcTalk 트리거와 일치, ledger_ink 제외(아군화), 호출부 3곳 배선. 검증: error CS 0, ci_check 8검사 통과, PlayMode 461/461.
- 2026-08-08: OutfitShapeLibrary **재감사** (2026-08-06 감사 이후 1053줄 변경, 큐 1순위, score 0) — **P1:1 처리. 직전 PlayerVisualBuilder 라운드가 고친 머티리얼 누수의 "나머지 절반"이었다.** 그 라운드는 `MakeMaterial` 생성 지점에 등록을 걸어 17~20개를 덮었는데, spawn 의상 파츠는 생성 지점이 `OutfitShapeLibrary.CreatePartMaterial`로 따로 있어 그 목록에 들어가지 않는다. 이쪽 정리는 `TrimContainer`뿐이고 그건 갈아입을 때 **남는** 파츠만 지우므로, **마지막까지 입고 있던 파츠**가 루트 파괴 시 샌다 — 마네킹은 파괴가 정상 수명이라(외형 해시가 바뀔 때마다 재생성) 커스터마이즈 토글 한 번마다 **최대 22개**(hat 7 / acc 6 / outer 5 / bag 4)가 누수됐다. `DestroySpawnedMaterials`를 신설해 `OnDestroy`가 자기 목록을 비운 직후 부르게 하고, bind 노드 이중 파기(캐릭터가 분홍색이 된다)를 막는 `OP_` 접두 계약을 `BindNames_NeverStartWithSpawnPrefix`로 고정했다. **거짓양성 2건 철회 — 둘 다 내 가설이었고 코드를 읽어 접었다**: hideNodes 복원 부재(→ 복원은 `ApplyPartColor`가 하고 `ApplyToCharacter`가 ApplyPartColor → ApplyShapeRecipe 순서로 부른다, 주석이 그 계약을 명시해 뒀다) / `TryGet`의 부분문자열 오매칭(→ Tool만 Contains 사슬이고 나머지 슬롯은 `ExactRecipes.TryGetValue` 정확 매칭). 검증: error CS 0, ci_check 8검사 통과, PlayMode 462/462.
- 2026-08-08: CharacterModelPreviewRenderer **재감사** (2026-08-06 감사 이후 476줄 변경, 큐 1순위, score 19) — **P1:2 처리.** ①`RenderTexture.Release()`는 GPU 리소스만 놓고 **객체는 남긴다**. 세 정리 지점(24장 LRU 축출 · 외형 변경 시 전체 무효화 · `OnDestroy`)이 전부 `Release()`만 해서 참조 끊긴 껍데기가 쌓였다 — 외형 토글 한 번마다 최대 24장. `DisposeRT`로 `Release()` + `Destroy()`를 묶었고, **도감 3D 썸네일 `InsectModelPreviewRenderer`의 세 지점도 같은 형태라 함께 고쳤다**(이번 세션 네 번째 "형제 파일 복제" 결함). ②마네킹이 씬 루트에 있어 컴포넌트가 죽어도 고아로 남았다 — `Destroy(mannequin)`이 외형 변경 경로에만 있고 수명 종료 경로엔 없었다. `OnDestroy`에 추가하니 직전 라운드가 붙인 `DestroySpawnedMaterials`까지 연쇄로 도달한다. 검증: error CS 0, ci_check 8검사 통과, PlayMode 462/462.
- 2026-08-08: PlayerStatusHUD **재감사** (2026-05-21 감사 이후 451줄 변경, 큐 1순위, score 98) — **P0:1, P1:2 처리.** ①**"보유" 숫자가 세션 내내 고정.** `AutoWire`가 `insectCollection`을 대입만 하고 구독을 안 걸었다 — Bootstrap이 `EnsureComponent` → `AutoWire` 순서라 `OnEnable`은 참조가 null인 상태로 지나가고, 그러면 `ownedCountCacheDirty`를 되살릴 경로가 사라진다. 바로 아래 `regionManager`가 같은 이유로 이미 지연 구독을 하고 있는데 여기만 빠졌다. `subscription_lint`는 해지↔재구독 짝만 봐서 "애초에 구독된 적 없음"은 못 잡는다. ②**펼침 패널 아래 깔린 버튼이 여전히 클릭을 받았다** — 패널(ContentTop~+540)이 미니맵·퀘스트 칩·목표 행을 통째로 덮는데 IMGUI는 겹침으로 입력을 막지 않는다. 오늘 Phase A가 그 자리에 목표 행 버튼을 놓아 실입력 결함이 됐고, `expanded` 기본값이 true라 데스크톱은 첫 프레임부터 해당된다. `IsExpanded` + `MinimapUI.LeftStackOccluded`로 좌측 스택이 스스로 빠지게 했다. ③**서브에리어 배너만 y가 70/74/110으로 박혀** 노치 기기에서 상태바 뒤로 숨었다 — 하네스를 도입한 바로 그 플랫폼에서 실패하는 자리. `ContentTop + 70`으로 옮겨 상단 중앙 관례(ContentTop → +30 → **+70** → +150)를 지켰다. **거짓양성 제외**: struct 할당, `stylesInitialized` 가드 뒤 GUIStyle(이번 큐 여덟 번째 같은 형태), `subscribedSubArea` 플래그가 이중구독·누수를 이미 방어. 검증: error CS 0, ci_check 8검사 통과, PlayMode 505/505.
- 2026-08-08: GachaBoxManager **재감사** (2026-05-20 감사 이후 291줄 변경, 큐 1순위, score 8) — **P1:1 + P2:1 처리.** 확률 공시의 구멍은 표기가 아니라 **풀 배치**였다. `data_lint`가 전용 풀·임계 분포·UI 파생은 지키는데 **`normalPool` 티어 배치는 무검사**였다 — 이 파일 주석이 "티어 ID는 DB 실제 rarity와 일치해야 한다"고 못박아 뒀는데도. `GetDbRarity`는 표시만 보정하고 풀 선택은 안 바꾸므로 어긋나면 브론즈가 55%로 상위 곤충을 흘린다(공시 ≠ 실제 분포). 검사 10번을 붙였고 **현재 64종 전부 정합**이라 회귀 방지가 목적이다. **검출력은 등급 변조·DB 누락을 각각 주입해 실측**했다. P2로 도감 조회 lazy 캐시를 맞췄다(`database`만 캐시가 있었다). **P2 보고**: 전용 곤충 확률 50/30/20%가 공시 문자열에 없다 — 법적 판단이 섞여 결정 위임. 검증: error CS 0, ci_check 8검사 통과, PlayMode 526/526.
- 2026-08-08: CameraFollower **재감사** (2026-05-21 감사 이후 287줄 변경, 큐 1순위, score 0) — **P0:1, P1:2 처리. P0는 같은 세션에서 두 시간 전에 내가 만든 것이었다.** Phase D의 `CutsceneDirector.Play`에 재진입 가드가 없어, 재생 중 재호출 시 "되돌릴 상태"가 **컷신 1번이 스스로 만든 값**(battleMode/frozen=true)을 읽고 `Stop()`이 복구를 둘 다 건너뛴다 — 조작이 영영 안 돌아오고 카메라도 배틀 모드에 갇힌다. 클래스 주석이 스스로 경고해 둔 바로 그 상태다. `if (playing) Stop();` 한 줄로 막았다. 도달 경로는 지금 데이터로는 안 닿지만(컷신 2비트가 둘 다 SubAreaEnter) **위험이 비대칭**이라 처리했다 — 방어는 한 줄, 증상은 영구 먹통. **P1-1**: `restoreBattleMode`일 때 `battlePos`가 컷신 마지막 컷 값으로 남아 전투 카메라가 그 구도로 굳었다 → `GetBattleFraming` 신설로 원복. **P1-2(컷신과 무관한 기존 결함)**: 쉐이크가 역대 최댓값을 붙들고 매 호출마다 되감아, 강한 흔들림 뒤의 약한 호출이 **강한 것을 처음부터 재생**했다. 레이드처럼 타격이 연달아 오면 영영 잦아들지 않는다. 비교 대상을 "지금 남아 있는 세기"로 바꾸고 순수 함수 `ResolveShake`로 분리해 7건으로 고정. **거짓양성 1건 — 내가 위임 전에 세운 가설이었다**: 매 프레임 `EnterBattleModeFramed`가 복귀 지점(`normalPos`/`normalRot`)을 덮을 줄 알았으나 그 둘은 **저장소 어디서도 읽히지 않는 죽은 필드**다(복귀는 `battleTransition` 감쇠로 산 위치를 향한다). 검증: error CS 0, ci_check 8검사 통과, PlayMode 533/533.
- 2026-08-08: WorldLobbyUI **재감사** (2026-05-27은 텍스처 누수만 처리, 그 뒤 267줄 변경, 큐 1순위, score 94) — **P0:1, P1:2 처리. P0: 월드 선택 화면 뒤에서 캐릭터가 걸어 다녔다.** `IModalUI` 미구현 + `ShowLobby()` 정상 경로에 `SetFrozen(true)` 누락이라 WASD(frozen 게이트)와 클릭 이동·포획(`IsAnyOpen()` 게이트)이 전부 살아 있었다. **계약 불일치가 증거** — 같은 phase로 들어가는 `OnWorldLeft`는 "로비로 돌아오면 이동 잠금" 주석과 함께 이미 잠그고 있었고 최초 진입만 빠졌다. Bootstrap 주석이 그 자리를 뺀 이유(옛 영구 frozen 버그)를 적어 뒀기에 **되돌리기 전에 퇴출 경로를 전수 확인**했다: WorldSelect를 빠져나가는 길 셋이 전부 해제하고, 유일한 예외 `HideLobby()`는 호출부 0인 죽은 메서드라 거기에도 해제를 넣어 재발을 막았다. phase 대입 10곳을 `SetPhase()` 단일 통로로 모아 등록/해제 누락을 구조적으로 없앴다. `CloseModal`은 InWorld 오버레이만 닫는다 — 월드 선택은 필수 단계라 ESC 하나로 온라인을 포기시키지 않는다(Explore 권고와 다르게 판단). **P1-1**: `CanStartRequest`가 `IsBusy`면 **이벤트조차 안 쏘고** 반환하는데 호출부가 phase를 먼저 Joining으로 바꿔, 5초 주기 목록 새로고침과 겹친 클릭이 증발하고 화면만 "접속 중"에 갇혔다 → `BeginJoinRequest()`로 `IsBusy` 선확인 + 15초 타임아웃 폴백. **P1-2**: 에러 표시가 월드 선택 패널에만 있어 나가기 실패 시 화면에 아무 변화가 없었다("눌러도 아무 일 없는 버튼") → InWorld에도 에러 라벨 + 요청 중 비활성화. 거짓양성: `stylesInitialized` 가드 뒤 GUIStyle(이번 큐 아홉 번째 같은 형태). 검증: error CS 0, ci_check 8검사 통과, PlayMode 533/533.
- 2026-08-08: AudioManager **재감사** (2026-05-21 이후 263줄 + 오늘 변경분, 큐 1순위, score 13) — **P1:2 처리. 둘 다 "오늘 내가 넣은 기능"과 부트스트랩 사이에서 났다.** ①**게임 시작 시 리전 곡이 안 나왔다** — `DelayedAudioStart`가 2프레임 뒤 무조건 `PlayBGM(Explore)`를 부르는데 그 사이 frame 0에 `RegionChanged(meadow)`가 이미 리전 곡을 걸어 놨다. 조기 반환 가드는 "같은 곡이면 무시"라 다른 곡인 이 호출이 통과해 **리전 곡 13개가 시작할 때마다 범용 곡에 덮였다**(리전을 옮기거나 첫 전투가 끝나야 제 곡이 나오는 역전). 오늘 넣은 `RestoreExploreBGM()`이 부트스트랩보다 정확해 그걸로 교체. ②**보스 BGM에 긴장 램프가 안 걸렸다** — Update 피치 조건이 `Battle || RaidBattle`을 직접 적어 둔 형태라 보스 2종이 else로 떨어져 피치가 1.0으로 되돌려졌다. **enum 주석이 등록을 "3지점"이라 못박아 둔 것이 화근** — 이 분기가 목록에 없는 4번째 지점이었다. `IsCombatBgm`으로 모으고 주석을 4지점으로 정정, 테스트 추가 후 **누락을 주입해 검출력 실측**. **내가 의심으로 지목했던 2건은 Explore가 clean 확인**: 보스전 복귀는 승/패/도주/fallback이 전부 `BattleEnded`로 수렴 / 서브에리어는 BGM을 안 건드리고 `subAreaSticky`가 위치 판정을 스킵해 `lastExploreBgm`이 리전 기준으로 정확. **P2 보고**: `SettingsPanel`이 어느 씬에도 없어 인게임에서 볼륨 조절 수단이 없다(선재 문제, UI 신설 필요). 검증: error CS 0, ci_check 8검사 통과, PlayMode 534/534.
- 2026-08-08: PlayerInsectCollection **재감사** (2026-05-21 이후 256줄 변경, 큐 1순위, score 24) — **P1:1, P2:2 처리. 세이브의 심장부라 되돌리기가 가장 어려운 영역.** ①`GetAllOwned()`를 캐시 없이 매 OnGUI 패스에서 부르는 호출부 2곳이 남아 있었다(나머지 5곳은 이미 캐시, 같은 파일 `OwnsShiny`도 같은 함정을 고쳐 놓은 상태였다). **공유 캐시 대신 재사용 버퍼 `OwnedView`를 따로 뒀다** — 캐시면 무효화 지점 하나만 빠뜨려도 "안 보이는 곤충"이 되고, 기존 호출부 다섯은 반환값을 보관하므로 공유 버퍼를 주면 그쪽이 깨진다. ②**P2: 문서가 보증한다고 적은 `EnsureHp` 보정이 부트에서 한 번도 실행되지 않았다** — `AddComponent`가 부르는 `Awake`→`LoadAndIndex`가 `AutoWire`보다 먼저 돌아 `database`가 null이고, "다음 로드에 미룸" 분기가 전부 스킵되는데 그 다음 로드도 순서가 같아 영영 오지 않는다. 증상이 없는 이유까지 확인했다(음수를 풀피로 보는 `GetEffectiveHp` + `IsFainted`가 `==0`)지만, 가드 없는 소비자가 하나만 생기면 터지고 고치는 비용이 기존 루프에 세 줄이라 처리했다. ③**P2: `ReloadFromDisk`가 이벤트를 안 쏘면서 근거 주석이 낡았다** — UI 넷이 이제 목록을 캐시하고 `InsectUpdated`로만 무효화하는데, 클라우드 복원이 `saveData`를 통째로 바꾸면 열려 있던 UI가 **고아 데이터**를 들고 남아 거기서 레벨업·치료를 하면 **재화만 빠지고 변경이 사라진다**. 한 줄 발화 + 주석 갱신. **테스트는 붙이지 않았다** — `Awake`가 실제 세이브를 읽고 조건부로 덮어쓰므로 하네스가 사용자 세이브를 건드릴 위험이 이득보다 크다. 검증: error CS 0, ci_check 8검사 통과, PlayMode 534/534.
- 2026-08-08: RaidSupportPlanner **재감사** (2026-08-06 감사 다음 날 252줄 변경 — 파일 크기와 맞먹는 양, 큐 1순위, score 0) — **P1:2 처리. 직전 감사가 고친 2건은 재발하지 않았고, 문제는 이 파일이 순차 턴 커밋에서 한 줄도 안 바뀌었다는 것이었다** (컨트롤러 391줄·리졸버 11줄이 바뀌는 동안 플래너만 옛 전제로 남았다). ①**이번 라운드에 이미 명중한 기절을 모른다** — 기절은 팀 전체에서 한 번만 판정되는데 플래너는 "지난 라운드 면역"만 받는다. 순차 턴에서 슬롯 0이 맞히면 나머지 4명의 기절은 확정 무의미인데 Guard 가중치가 1.4로 최고라 그대로 골라져 **라운드당 최대 4턴이 날아갔다**. 호출부 한 줄(`|| roundStunLanded`). ②**전체공격이 꺼지면서 방어 버프가 5명 중 4명에게 영구히 죽었다** — `RaidBossUsesAreaAttack = false`라 `incomingArea`가 상시 false이고 게이트가 "지목된 한 슬롯"으로 축소됐다. 레이드 버프는 만료가 없어 지목과 무관하게 값이 있는데 옆의 `BuffAttack`만 `×2f` 다라운드 가치를 받는 비대칭이었다. 게이트를 없애되 밸런스 변화를 최소화(미지목 `×0.5`). **테스트가 초록인 채 통과하던 이유도 고쳤다** — 두 테스트가 AOE 예고로만 검증해 프로덕션이 안 타는 분기를 고정하고 있었다. 단일 대상으로 바꾸는 과정에서 **내 기대값이 틀린 것도 하나 잡았다**(단일 대상 기절 가치는 0.4배라 Assault로는 데미지가 이기는 게 정상). **검출력 실측**: 옛 게이트 주입 시 스탠스 테스트가 정확히 실패. 검증: error CS 0, ci_check 8검사 통과, PlayMode 534/534.
- 2026-08-08: CashShopManager **재감사** (2026-05-22 감사 이후 239줄 변경, 큐 1순위, score 14) — **P1:1 처리. 돈이 오간 직후 클라우드에 확정하지 않았다.** 릴리스의 유일한 지급 경로인 `ApplyVerifiedGemBalance`에 `SaveToCloud()`가 없고, 정작 **에디터 전용** `GrantGemPackage`에만 있었다 — 결제 직후 종료하면 120초 자동저장을 못 만나고 다음 실행의 클라우드 복원이 결제 전 잔액을 덮는다("결제했는데 보석이 없다"). 두 경로를 대칭으로 맞췄다. 결제 코어 자체는 정상이었다: 클라이언트가 rewardCount를 더하지 않아 중복 지급이 없고, 프로덕션 모듈 부재 시 무료 지급 대신 거부하며, 보석 결제가 사전검증→차감→실패 시 환불로 원자적이다. **P2 보고 2건**(보석 *소비*의 즉시 저장 범위는 아키텍처 결정 / 사전검증·지급의 FindFirstObjectByType 중복 조회). **거짓양성 1건 철회**(가챠 `isOpening` 가드는 동기 실행이라 도달 불가). 검증: error CS 0, ci_check 통과, PlayMode 534/534.
- 2026-08-08: CashShopManager **재감사** (2026-05-22 이후 239줄 변경, 큐 1순위, score 14) — **P1:2 처리 + P1:1 보고만. 재화를 다루는 파일이라 결함이 곧 손실이다.** ①**젬 소비가 클라우드에 확정되지 않아 복제가 가능했다** — 서버 IAP 지급이 Firestore `gems`(클라이언트 PATCH로만 갱신)를 읽어 더하는데, `PurchaseWithGems`가 `SaveToCloud()`를 안 불러서 자동저장 전에 결제하면 **차감 전 잔액**에 보너스가 붙는다. 지급 경로 둘은 이미 저장하고 **소비 경로만 빠진 비대칭**이었다. ②**재검증이 로컬 잔액을 뒤집었다** — 이미 청구된 토큰은 서버가 `newlyGranted: false` + 마지막 PATCH 시점 잔액을 주는데 그걸 절대 세팅해서, 그 사이 번 젬은 증발하고 쓴 젬은 되살아났다. `newlyGranted`는 **DTO에 이미 있었고 호출부가 안 넘기고 있었다**. 같은 메서드의 `Max(0, verifiedBalance)`도 함께 고쳤다 — 신규 문서(gems 필드 부재)면 **결제 직후 잔액이 통째로 0**이 됐다(CloudSaveManager가 같은 필드에 `-1` 센티넬을 두는 것과 정반대). ③**보고만**: 레거시 젬 마이그레이션이 로그인 전 전역 지갑에 병합한 뒤 PlayerPrefs 키를 지운다. 키 삭제만 막으면 다음 AutoWire에서 또 병합돼 **젬 복제**가 되고, 시점을 옮기려면 AutoWire가 부트 1회인 구조를 바꿔야 한다 — 재화가 걸려 자동 처리하지 않았다. **결함 아님**: 클라우드 5지점 전부 배선 / 구매 원자성(사전 검증 + 차분 환불, 이중 환불 없음) / 중복 구매 가드 2종. 검증: error CS 0, ci_check 8검사 통과, PlayMode 534/534.
- 2026-08-08: RaidRoundResolver **재감사** (2026-08-03 이후 234줄 변경, 큐 1순위, score 0) — **P1:1, P2:1 처리. 리졸버 자신은 순차 턴 개편과 정합했고 결함은 그것을 부르는 컨트롤러의 합체 경로에 있었다** (직전 라운드가 "주변이 바뀌었는데 이 파일만 안 바뀜"이었다면 이번은 그 반대다). ①**합체를 라운드 중간에 쓰면 이미 행동한 슬롯이 한 라운드에 두 번 때린다** — 개편 전엔 합체가 팀 턴을 대체했지만 지금은 게이지가 슬롯마다 차올라 팀 턴 도중에 100을 넘고 `CanUniteAttack`은 `ActiveSlot`을 안 본다. **컨트롤러 주석이 반대쪽 절반만 막고 있었다**("아직 차례가 안 온 팀원"은 처리, 이미 행동한 쪽은 미처리). `HasActedThisRound(i)` 한 줄 + 합체 이전 피해 이월(새 결과 객체로 갈아끼우며 버려져 라운드 집계·UI 기여도가 과소 표시됐다). ②**P2: `allowAreaAttack` 기본값이 `true`** — 인자를 빠뜨린 새 호출부가 조용히 AOE를 부활시키고 그 경로는 rageMultiplier를 무시해 곧 전멸이다. 필수 인자로 전환(컴파일 오류 0 = 모든 호출부가 이미 넘기고 있었다). **검출력 실측**: 두 수정을 되돌리니 신규 테스트 2건이 각자의 결함을 정확히 검출. **정합 확인**: 리졸버는 라운드 상태를 안 들고 전부 호출 시점 라이브 읽기 / 기절은 컨트롤러가 라운드당 1회 소비 / `LowestHpAlly`가 죽은 슬롯을 거르고 플래너와 같은 함수를 쓴다 / 버프 스택은 대상별 `TryStack*`으로 상한에서 정확히 멈춘다. 검증: error CS 0, ci_check 8검사 통과, PlayMode 536/536.
- 2026-08-08: RegionManager **재감사** (2026-05-20 감사 이후 207줄 변경, **큐 2순위 — 1순위 `RaidRoundResolver`는 다른 세션이 미커밋으로 편집 중이라 건너뛰었다**) — **P1:1 처리. 진행 사슬이 세 곳에 적혀 있는데 둘만 대조하고 있었다.** 런타임 switch(③)가 빠져도 데이터(①)↔테스트 사본(②)만 맞으면 전부 통과했다 — Next 누락은 **진행 영구 차단**, Prev 누락은 수문장이 맵 한복판에 스폰. 소스에서 메서드 본문을 잘라 양방향 대조하는 테스트 3건을 추가했다. **자체 발견 1건**: 파일 전체 검색으로 짰다가 두 switch가 서로의 역방향이라 종착지 검사가 거짓 실패 — 중괄호 균형으로 본문만 자르도록 고쳤다. **검출력은 간선 삭제·주입 3종으로 실측**. 거짓양성 1건 철회(PlayerPrefs 키는 이미 `SaveScope.PrefsKey` 스코프). 검증: error CS 0, PlayMode 539/539.
- 2026-08-08: 컷신 3파일 **신규 감사** (CutsceneData·CutsceneLibrary + 인접 CutsceneDirector, 큐 1·2순위) — **clean P0/P1, 파일 수정 0건.** 이 세션이 다른 영역에서 반복 지적한 "짝 규칙 무검사"가 여기엔 이미 적용돼 있다: `story_lint` 검사 9번이 JSON↔Library cutsceneId를 **양방향**(실재성 + switch 미배선)으로 보고, 테스트 15건이 타임라인 경계를 `EveryInstantIsCoveredExactlyOnce`까지 덮으며, 디렉터는 재진입 가드 + 모든 종료 경로를 `Stop` 하나로 모으고 `OnDisable`도 그걸 부른다(배틀 카메라는 **구도까지** 기억해 복원). **거짓양성 2건 철회**: TotalDuration↔TryGetShot의 0 보정 불일치(실제 duration이 전부 양수고 종료 판정은 후자만 쓴다) / UI 루트 토글로 영구 frozen(World 루트라 대상 아님 + OnDisable이 복구). 검증: 코드 변경이 없어 기존 PlayMode 539/539 유효, ci_check 통과.
- 2026-08-08: RegionManager 재감사 **후속** (다른 세션이 984e3cb로 같은 라운드를 먼저 커밋 — 겹치지 않고 짝을 이룬다) — **P0:1, P1:1 처리.** 그쪽은 진행 사슬의 세 번째 사본을 테스트로 묶었고, 이쪽은 **좌표의 네 번째 사본**을 찾았다. ①**실물 수문장이 지도 마커와 다른 자리에 서 있었다** — `PlaySceneBootstrap.GetGuardianWorldPosition`이 `RegionManager.GetGuardianPosition`의 낡은 사본으로, 같은 공식을 쓰면서 이전 리전 switch만 ver1 5개에서 멈춰 있었다. ruins와 2막 6리전은 원점 기준이 되어 최대 105m 어긋났다(dunes는 연못 안, canopy는 유적 안). ruins는 2막의 유일한 문이라 **지도가 가리키는 곳에 아무것도 없으면 조용히 진행이 막힌다**. 사본 삭제 + RegionManager 직접 조회로 단일 출처화. ②**2막 서브에리어 4종에 조명 프로필이 없었다 — 내가 Phase C에서 만든 누락이다.** 지오메트리만 넣고 `SubAreaEnvironment.GetProfileForType`을 빠뜨려 전부 야외 주광으로 떨어졌다(밀폐 공간에 햇빛). **검출력 실측**: 4종 제거 시 정확히 그 넷을 지목. 좌표 사본 재발도 소스 검사로 고정. 검증: error CS 0, ci_check 8검사 통과, PlayMode 541/541.
- 2026-08-08: RaidBattleController **재감사** (순차 행동 전환 후속, 590줄 변경) — P1:2 + 문서결함:1 처리. 두 P1 모두 **"짝 규칙을 한쪽 입구만 지킨다"** 형태다: ①`CanUniteAttack`이 `AliveCount()`를 봐서 실제 참가자가 1마리인데도 열려 게이지 100을 1마리분에 태웠다(→`RemainingActors`), ②`CanUseSkill`에 `HasActedThisRound` 검사가 없어 `RaidMemberActionResolved` 창에서 한 곤충이 두 번 행동할 수 있었다(형제 입구는 막고 있었다). ③`RaidBossHpMultiplier=8.5`는 산출 근거가 무효화됐으나 **값은 유지** — 사용자가 요청한 난이도 하향을 되돌리지 않기 위해. 주석에 현재 화력(3.4→5.0유닛)과 판단 근거 기록. P2 5건은 사용자 결정 위임(합체 시 이전 액션 소실·연출 하이라이트 1슬롯 밀림·keyLabels 프레임 할당·미호출 연출 헬퍼 2개). 검증: 컴파일 0 error, ci_check 통과, PlayMode 555/555(+3 신규).
- 2026-08-08: VillageBuilder **재감사** (2026-07-17 이후 479줄 증분) — P1:2 처리 + 회귀 방지 테스트 1건 신설. 둘 다 **"주석이 약속한 것을 아무도 검산하지 않는다"**: ①병원 주석이 "빈 각도 315°"라 적었으나 집5가 320°에 있어 중심 거리 1.48m로 **집이 병원 안에 박혀 있었다**(둘 다 콜라이더 유지 → 통행 차단, 주민이 벽에 붙어 스폰). 352°(이등분선)로 이동. ②`BuildIceShelter` 얼음 블록 9개만 접선 정렬 관례(`-(a+90)`)에서 +90이 빠져 바람막이 호가 **방사형 스파이크**가 됐다 — 같은 파일의 울타리·육각벽 2곳은 지키고 있었다. `VillageBuildings_DoNotOverlapEachOther`가 실제 벽 크기로 쌍마다 겹침을 검산한다(각도를 눈으로 세는 방식이 이미 틀렸다). **거짓양성 1건 철회**: EditMode `Destroy` 로그로 테스트가 무력화된다는 지적 — 이 저장소는 PlayMode 러너를 쓰고 해당 픽스처 11건이 통과 중이다. 검증: 컴파일 0 error, ci_check 통과, PlayMode 561/561(+6 신규).
- 2026-08-08: BattleArenaController **재감사** (아레나 가시성 56줄 증분) — P1:2 처리 + UI 파일 1개 신설. 증분은 깨끗했고 결함은 **3D 컴포넌트가 화면 문구까지 직접 그리던 `OnGUI`**였다: ①`UIScale` 밖 픽셀 좌표라 스케일이 1이 아닌 기기(1.333)에서 전투 문구만 25% 작게 찍혔다 — 1920×1080에서만 우연히 맞아 Game View로는 안 보인다. `BattleEffectTextOverlay`(UI)로 옮겼다. 아레나가 `UIScale`을 직접 부르면 UI→Battle과 맞물려 순환이라 방향을 지켰고, 호출부 0이던 `GetActiveEffectTexts()`가 원래 그 이음매였다. ②고정 600×50 상자에 40px 글자 + 데이터 문자열 (`{displayName}!`)이라 세로·가로 양쪽으로 잘렸다 → 58×760 + `LabelFit`. `text_fit_lint`는 Rect 높이가 리터럴이 아니라 못 잡는 자리다. P2 3건 보고(1v1 카메라의 벽 안쪽 여부를 아무도 검산 안 함 — 레이드만 테스트로 고정돼 있다 / `new MeshRenderer[0]` 반복 할당 / 레이드 카메라 주석의 "6도"는 실제 11도). 검증: 컴파일 0 error, PlayMode 572/572. (`story_lint`는 다른 세션이 편집 중이라 KeyError로 실패 — 무관)
- 2026-08-08: **12개 영역 일괄 감사** (사용자 요청) — Explore 12개 병렬 + 전건 자체 검증. **P0:1, P1:10 처리**, 거짓양성 1건 철회, P2 다수 위임. P0은 **전멸 패배에서 `DuelEnded`가 영영 발화하지 않던 것** — `PlayerFainted`가 구독자 유무만 보고 종료를 UI에 넘기는데 UI는 벤치가 비면 컨트롤러에 되돌려 알리지 않았다. NPC 대결 쿨다운이 통째로 우회되고 `activeBossId`가 남아 **아이 대결 승리가 간부 격파로 영구 오기록**됐다(클라우드 동기라 복구 불가). 나머지 P1: 서브에리어 신규 4방 경계벽 전무(낙하) + 얼음기둥이 입구 차단 + 접선 정렬 위반, SocialPvpUI `isOpen` 누출(ESC 무효·핫키 오작동), 훈련이 `InsectUpdated` 미발화, 병원 null 가드 비대칭, 레이드 버프 오라가 엉뚱한 곤충 위에 + 단일 공격에 "TEAM RUSH ×3", 회복 버튼 라벨 잘림, `Label` default 흡수. **이 라운드의 반복 주제도 "형제 입구는 안 지킨다"였다** — 병원 4경로 중 1개만 가드, `CashShopUI`만 `isOpen` 정리, 레벨업만 `InsectUpdated` 발화, 도주만 `DuelEnded` 보강. 거짓양성 철회: 포획 UGUI 패널은 "첫 포획 뒤 사라지는" 게 아니라 **처음부터 한 번도 안 뜨는 죽은 패널**이라 켜는 것이 수정이 아니다. 검증: 컴파일 0 error, ci_check 7/7, PlayMode 576/576(+1 신규).
- 2026-08-17: StoryBeat (Story/StoryBeat.cs, 큐 1순위, score 0 표면 점검) — **P1:1 처리, P2:1 위임.** 데이터 모델 자체는 깨끗했다(JsonUtility 관례 준수, 신규 필드 전부 null 기본값, 소비자 4곳 모두 null 가드). 결함은 **소비자 쪽 enum↔switch 드리프트**였다: `StoryJournalUI.HintFor`가 트리거 타입을 **문자열 리터럴 사본**으로 switch해서, 뒤늦게 추가된 `GuardianDefeat`·`DexProgress`가 `default`로 흘러 **82비트 중 9건(11%)이 저널에서 "조건 미상"만 표시**했다 — 미열람 비트의 힌트가 저널의 유일한 존재 이유인데 수문장 6건·도감 3건이 통째로 무용했다. case 라벨을 `StoryDirector.Trigger*` const로 바꿔 사본을 없애고(같은 이유로 `StoryObjectiveResolver.KindOf`가 이미 상수를 쓴다 — 그 파일 주석이 이 함정을 정확히 예고하고 있었다) 두 케이스를 채웠다. **P2 위임**: `StoryBeat.oneShot`이 완전한 사문 — 선언(`= true`)만 있고 읽는 코드가 0이라, `oneShot: false`로 저작해도 `seenBeatIds`가 무조건 1회로 막는다(데이터가 동작을 거짓말한다). **P2도 같은 라운드에서 처리**(사용자 승인): `oneShot` 필드와 JSON 키 82개를 지우고, "모든 비트는 seenBeatIds가 전역으로 1회 보장"을 클래스 주석에 못박았다. 반복 구현은 배제했다 — 앰비언트 비트(talk_elder·talk_rival·talk_scholar)조차 캔디 5개를 주므로 그대로 반복시키면 **말 걸기 무한 파밍**이 된다(정리가 아니라 보상 재설계가 먼저다). 필드만 지우면 JsonUtility가 모르는 키를 계속 조용히 무시하므로 **검사 14(Story.json 키 ↔ StoryBeat 필드 실재성)**를 신설했다 — 되살아난 `oneShot`·대소문자 오타(`stageEnterID`)·중첩 객체 오타(`lines[].speeker`)·`trigger` 내부 오타를 주입해 정확히 그 4건만 지목하는 것을 실측했다. 검증: 컴파일 error CS 0, ci_check 8검사 통과, PlayMode 614/614.
- 2026-08-17: BattleScreenUI **재감사** (큐 1순위, 3769줄 score 372, 미커밋 증분 158줄) — **P1:1 처리.** 증분(명암비 재조정·LabelFit·`GUI.color` 곱셈 누락 수정)과 신규 `BattleEffectTextOverlay`는 clean이었고, 결함은 **직전 라운드 P0 수정의 부작용**이었다: `EnterResult` 첫 줄에 들어간 `ConcludeDefeatWithoutSwap`이 `BattleEnded`를 발화 → `OnBattleEnded`가 (phase가 아직 Result가 아니라) `EnterResult`를 **재진입** → 결과 진입이 같은 프레임에 2회 돌고 **패배 SFX·BGM이 겹쳐 재생**됐다. `ConcludeDefeatWithoutSwap`이 멱등해 무한 재귀는 없었던 탓에 조용했다. `phase = Phase.Result`를 Conclude **앞**으로 옮기고 조기 반환을 더했다 — 순서가 가드의 전부다(상태가 뒤에 있으면 재진입이 가드를 통과한다). **거짓양성 2건 철회**: 크리티컬 그림자의 `GUI.color` 검정 곱셈은 의도된 연출(다음 줄에서 복구), score 372의 프레임 할당은 `InitStyles` 단일 가드 + 콤보 스타일 2건 null 가드로 전부 방어됨. 도주 경로는 `battleEnded`를 먼저 세워 오기록 없음을 확인. 검증: 컴파일 error CS 0, ci_check 8검사 통과, PlayMode 614/614.
- 2026-08-17: StoryDirector **재감사** (큐 1순위, 536줄 score 2, 2026-07-19 이후 커밋 5건) — **P1:1 처리.** `EvaluateTriggers`가 `StoryService.AllBeats()`를 훑어 **첫 일치**를 발화했는데 그건 `Dictionary.Values`라 순서가 비결정적이다. 같은 트리거에 동시에 자격을 갖는 비트를 데이터에서 세어 보니 **5쌍**이었고, 가장 아픈 것이 마을 어르신 대화다 — 1막 개막 `ch1_intro`(프롤로그 컷신 + 은빛 그물)와 앰비언트 `talk_elder`(캔디 5)가 함께 걸려, **처음 하는 사람이 개막 대신 잡담을 볼 수 있었고 어느 쪽이 뜰지는 실행마다 달랐다.** 더 나쁜 건 HUD 목표(`StoryObjectiveResolver`)는 *결정적으로* 골라 놓고 실제 발화만 임의였다는 점 — 안내를 따라갔는데 다른 대사가 나온다. 순위 규칙을 `CompareBeatPriority`(스파인→챕터→order→beatId) 하나로 모아 두 경로가 같은 답을 내게 했고, `SelectObjectiveBeat`의 4단 비교 사본도 그 함수로 대체했다. 수정 후 5쌍 전부 상식적 승자(개막·낮은 order)로 고정됨을 데이터로 확인. 회귀 테스트 6건 신설(스파인 우선·ch10>ch2 숫자 비교·본편>곁이야기·beatId 타이브레이크·null·개막 vs 잡담 + 입력 순서 무관). 검증: 컴파일 error CS 0, ci_check 8검사 통과, PlayMode 620/620(+6 신규).
- 2026-08-17: **애니메이션 흐름 전수 점검**(사용자 요청) + OpeningSceneController 재감사 — **P1:2 처리, 관측 1건 위임.** 표시되는 움직이는 주체 12종의 구동 경로를 하나씩 따라가 **`PlayerMovement`에서 2건**을 찾았다: ①**플레이어에게 idle 애니메이션이 아예 없었다** — `AnimateWalk(false)`가 swing·bob·headTilt를 전부 0으로 떨어뜨려 멈추면 완전 정지. NPC는 조각상 방지용 호흡(`NpcWalkAnimator`)을 갖는데 플레이어만 없어, 숨 쉬는 주민 옆에 마네킹처럼 섰다(대화 중 카메라가 둘을 나란히 잡아 특히 티가 난다). ②**frozen이면 애니메이션이 통째로 멈췄다** — `Update`가 `if (frozen) return`이라 `AnimateWalk`가 안 불렸고, 대화·컷신·연출이 전부 frozen이므로 **매 스토리 비트마다 걷던 자세로 굳었다**. `catchSwingTimer`도 안 줄어 스윙 중 모달이 열리면 팔이 든 채 남았다. 같은 파형·진폭(1.5rad/s, 팔 ±1.2°, 몸통 ±1.8cm, 고개 ±4.5°)을 NPC에서 이식하고 frozen 분기에서도 애니를 돌린다. **도구 각도를 팔과 분리(`toolDeg`)한 것이 급소** — 도구 base 재캐싱이 idle 프레임마다 도는데 idle 회전을 섞으면 base로 누적돼 도구가 드리프트한다(잡기 스윙 중 재캐싱을 막아 둔 것과 같은 함정). 나머지 10종(필드/배틀/레이드 곤충·주민·잡기 아이·컷신 카메라·연출·썸네일·2D 초상·오프닝)은 구동 정상 확인. **위임**: 패널 페이드 트윈(`UIHelper.AnimatePanelOpen`+`UITween`)이 테스트까지 있으면서 모달 23개 중 1개만 쓴다 — 22개 확산은 IMGUI 알파가 `GUI.color` 곱셈이라 글자까지 물들이는 부류(직전 라운드 BattleScreenUI 결함과 동형)라 눈으로 못 보고 하기엔 위험해 보고만 한다. OpeningSceneController 재감사는 clean(P0/P1 0건). 검증: 컴파일 error CS 0, ci_check 8검사 통과, PlayMode 620/620.
- 2026-08-17: 패널 페이드 확산(사용자 승인, 추천안대로 1화면 한정) + UIHelper **재감사** — **P1:2 처리, 자체 발견 1건.** 대화창에 페이드를 넣으려다 **공유 렌더러가 페이드를 깨고 있는 것**을 먼저 찾았다: `UISurface`는 주변 `GUI.color`를 저장→곱셈→복구하는데 `CharacterPortraitRenderer`는 파츠 색을 **절대값으로 덮고** 끝에 `Color.white`로 되돌렸다 — 그래서 유일하게 페이드를 쓰던 `CharacterOutfitUI`조차 **초상 이후 그려지는 것 전부가 알파 1로 되돌아가** 페이드에서 빠져 있었다(주석에 '주변 색 복원' 교훈이 이미 있었지만 복원 대상이 흰색이라 절반만 맞았다). 공개 진입점 4개가 진입 시 `GUI.color`를 스냅샷하고 `DrawCol`이 거기에 곱하도록 고쳤다 — `LoginUI`·`CashShopUI`의 초상도 함께 정상화된다. 그 위에 `NpcDialogueUI` 페이드인을 넣었다(페이드아웃은 배제 — `CloseModal`이 `lines`/`currentBeat`을 그 자리에서 비우고 `CompleteBeat`으로 보상까지 지급해, 내용을 살리려면 비트 완료를 미뤄야 하는데 그건 영구 정지를 겪은 자리다). **구현 중 자체 발견**: `AnimatePanelOpen`이 조기 반환 뒤에 있어 닫힘 전이가 전달되지 않고 `wasOpen`이 true로 굳어 **두 번째 열림부터 페이드가 사라졌다** → `CloseModal`에서 리셋. UIHelper 재감사 **P1:1** — `FitKey`가 스타일을 키에 안 담아 서로 다른 스타일이 축소 결과를 공유했다(wordWrap이 다르면 가로 검사 여부까지 갈린다). 참조 동일성 추가 + 테스트 5건. 텍스처 캐시·DrawTinted·DrawBorder 등 나머지는 전부 주변색 곱셈/복구 정상. 검증: 컴파일 error CS 0, ci_check 8검사 통과, PlayMode 625/625(+5 신규).
- 2026-08-17: 라이브 캡처 하네스 구축(사용자 승인) + CollectionUI **재감사** — **도구 1건 신설, 감사 clean.** 배치모드에서 실제 게임 화면을 렌더해 PNG로 남기는 `Assets/Editor/LiveSceneCapture.cs`를 만들었다 — 3D 변경을 코드만 보고 판단하던 것을 눈으로 확인할 수 있게 됐다. 실측으로 확인한 함정 셋: ①`[UnityTest]`는 asmdef 부재로 `UnityEngine.TestTools` 참조가 없어 못 쓴다 ②`ScreenCapture.CaptureScreenshot`은 배치모드에 게임뷰가 없어 **조용히 실패**한다(카메라→RenderTexture→ReadPixels로 우회, 디바이스는 Direct3D11로 존재) ③플레이모드 진입의 도메인 리로드는 `SessionState`+`[InitializeOnLoadMethod]`로 넘긴다. **게임 카메라를 뺏지 않는다** — 전용 카메라를 `CopyFrom`으로 만들어 쓰고 파괴한다(Camera.main을 옮기면 CameraFollower와 다툰다). 첫 성과로 **플레이어 idle 호흡이 실제로 도는 것을 픽셀 차분으로 확인**했다(2초 간격 3장, 차이가 플레이어 영역에만 몰림 — 코드로는 증명 불가하던 항목). **한계**: IMGUI는 카메라를 거치지 않아 안 잡힌다(상점·대화창·배틀 UI). 에디터를 열어두면 락파일로 도구가 못 돈다. `rules/testing.md`에 사용법·한계 절 신설, visual-dev에 등록. CollectionUI 재감사는 **clean** — 정렬 증분의 불변식(정렬 순서↔행 문자열 캐시 인덱스 결합)이 무효화 2지점에서 모두 지켜지고 TeamChanged도 구독돼 있다. **거짓양성 2건 철회**: `Compare`의 null 미가드는 `Sort`가 상류에서 거르므로 안전(오히려 하류 null 가드가 죽은 분기), `GUI.backgroundColor` 5건은 버튼 배경 틴트라 글자에 곱해지지 않는다. 검증: 컴파일 error CS 0, ci_check 8검사 통과, PlayMode 625/625, 캡처 도구 3회 실행 exit 0.
- 2026-08-17: 어르신 조우 접근 **라이브 검증**(사용자 요청) + PlayerMovement **재감사** — **P1:1 처리, P1:1·P2:1 위임.** 새 캡처 하네스로 접근 연출을 실제로 찍었다: 11m에서 시작해 어르신이 걸어와 **2.10m에서 멈추고 손을 흔든 뒤 더 쫓아오지 않는다**(설계값 `ArriveRadiusFor(3m)=2.1`과 소수점까지 일치). 첫 실행은 접근이 아예 안 떠서 버그를 의심했으나 **오진이었다** — 계정 세이브에 `ch1_intro`·`talk_elder`가 이미 열람돼 있어 목표가 정확히 그 둘을 건너뛴 것이었다(정상 동작). 진행 파일을 백업·비움·복원해 재현했고 복원은 내용까지 대조 확인했다. **관측**: 플레이어 자동 주행과 NPC 접근이 서로 상대의 옛 위치를 향해 움직여 t=2s에 거리가 5.35→7.26m로 벌어졌다가 4s에 수렴한다(스쳐 지나갔다 되돌아옴 — 처리 방향 3안 위임). PlayerMovement 재감사 **P1:1 처리** — frozen 안내가 대화·컷신 중에도 떠서 "ESC로 이동 잠금 해제"라는 틀린 안내를 스토리 내내 노출했다(ESC는 그때 모달을 닫는 키다) → 모달 가드 추가. **위임 2건**: ①그 문구와 리전 차단 문구가 `UIScale` 밖 픽셀 좌표(BattleArenaController가 직전에 같은 이유로 UI로 옮겨졌다) — 제대로 고치려면 UI 계층 이전이 필요하고 Core→UI는 금지라 단독 처리 보류 ②`hasReceivedInput`이 제거된 배너의 잔재로 CS0414 경고를 매 빌드 발생시킨다. 부수 확인: `ui_layout_lint`가 Rect 인라인 `Screen.height - N`을 못 잡는다(전 저장소 실제 3건, 나머지 7건은 전체 화면 채우기라 정당 — 좁히지 않고 패턴만 넣으면 거짓양성이 된다). 검증: 컴파일 error CS 0, ci_check 8검사 통과, PlayMode 625/625.
- 2026-08-17: DexScreenUI **재감사** — **P1:5 처리 + 저장소 전수 스윕 신설.** 증분(23줄, 잘림 2곳 수정)은 clean이었는데 그 커밋이 남긴 한 줄이 단서였다: "둘 다 리터럴 문구라 text_fit_lint의 대상이 아니었다". 그 린터는 **길이를 데이터가 정하는 자리만** 보므로 `GUI.Label(rect, "고정 문구", style)`은 아무도 안 본다. 스타일 변수의 fontSize와 Rect 높이를 소스에서 뽑아 대조하는 스캐너를 만들어 세어 보니 **82건**(fontSize×1.35 기준 미달), 그중 **36건이 10% 이상 부족**하고 13개 파일에 걸쳐 있었다 — 최악은 `28pt를 28px 상자에`(35% 부족, 글립 상자는 언제나 폰트 크기보다 크므로 논란의 여지가 없다). DexScreenUI에서 **이웃 레이아웃을 검산할 수 있는 5곳**만 `LineH()`로 고쳤다: 보유 아이템 헤더(headerH 74 안이라 자유), 곤충 친구 헤더(여백이 8px뿐이라 리스트 뷰포트 y·높이도 함께 밀었다), 타일 3줄(타일 182 안 여유 확인). → 77건(31건)으로 감소. **나머지는 손대지 않았다** — 상자를 키우면 아래가 밀리므로 자리마다 검산이 필요하고 IMGUI는 배치모드 캡처가 안 돼 눈으로 확인할 수 없다. 스캐너를 `.claude/scripts/literal_fit_scan.py`로 남기되 **ci_check에는 아직 넣지 않았다**(켜면 즉시 실패). 전용 정리 라운드로 줄인 뒤 올린다. 검증: 컴파일 error CS 0, ci_check 8검사 통과, PlayMode 628/628.
- 2026-08-17: 잘린 리터럴 문구 **일괄 정리**(사용자 요청) + CharacterOutfitUI **재감사** — **31건 처리, 검사기 1개 신설, 감사 clean.** 직전 라운드가 센 심각 31건(10%↑ 부족, 10파일)을 전부 `UIHelper.LabelFit`으로 바꿔 0건으로 만들었다. **상자를 키우지 않은 것이 요점** — `rules/ui-layout.md`가 "상자는 그대로 두고 글자를 줄인다"를 기본으로 정해 뒀고, IMGUI는 배치모드 캡처가 안 돼 결과를 눈으로 확인할 수 없으니 레이아웃을 안 건드리는 쪽이 맞다. `literal_fit_lint.py`를 만들어 `ci_check`에 등록(FAIL 기준 10%↑ — 전부를 기준 삼으면 text_fit_lint가 247→31로 좁혀졌던 실수를 되풀이한다). **검출력 실측에서 검사기 자체의 결함을 잡았다**: 정규식이 `new GUIStyle(...)`만 보고 `new GUIStyle { ... }`(괄호 없는 초기화)를 놓쳤다 — 그대로 올렸으면 아무것도 못 잡는 검사기가 됐다. 보강 후 두 형태 모두 검출(주입 시 exit 1, 복구 시 0). 경미 46건은 남겼다(폰트 패딩에 묻힐 수 있고 LabelFit은 보간 문자열마다 캐시 항목을 만든다). CharacterOutfitUI 재감사는 **clean** — 좌우 배치 증분은 `Mathf.Max` 가드·고정 가상 캔버스로 안전하고, 구독이 0개라 OnEnable이 불필요하며, `new GUIStyle` 9건은 전부 `CachedStyle` 팩토리다(거짓양성). 이번 세션의 `CharacterPortraitRenderer` 주변색 수정이 이 화면에서 온전히 도는 것도 확인했다. 검증: 컴파일 error CS 0 / warning CS 0, ci_check **9검사** 통과, PlayMode 628/628.
- 2026-08-17: CashShopUI **재감사 2회차** (큐 1순위, 953→1000줄 score 206) — **P1:2 처리, Explore 부분 오류 1건 정정.** 이 파일은 이미 세 번 감사됐어서(가챠 P0:11 / 보석·아이템 상점 P0:16 / 08-07 재감사) 이번엔 **08-08 이후 증분**, 즉 이번 세션에 내가 넣은 상자 아이콘·가챠 초상에 초점을 뒀다. 그 초상이 결함이었다 — `CapturePopupUI.DrawTypedInsectPortrait`를 **직접** 불렀는데 그건 파사드가 아니라 **파사드 아래의 2D 폴백 계층**이다. 진짜 이음매는 `InsectVisual.Draw`(3D 썸네일 있으면 그것, 없으면 안에서 같은 2D로 내려감)이고 도감·보유곤충·팀편성·병원·훈련·포획선택·지역맵 9곳이 그걸 쓴다. 결과적으로 **돈을 쓴 직후 화면만 게임 전체에서 홀로 2D**였다. DB 조회를 `cachedWallet`과 같은 lazy 캐시로 두고 파사드 경유로 바꿨다 — 단, 조회가 null이면 파사드는 **아무것도 안 그리므로**(빈 사각형 회귀가 바로 이번 세션에 고친 결함이다) 그때만 2D를 직접 부르는 분기를 남겼다. 둘째로 Phase 2 플래시가 `new Rect(0,0,850,620)` 하드코딩인데 감싸는 Area는 1200x820(모바일 1200x1560)이라, 1.5~2.0초 구간에 좌상단만 덮는 **각진 흰 판**이 떴다(모바일에선 높이의 40%) → Area 크기를 인자로 받게 했다. Explore는 이걸 "기존 코드"라며 P2로 냈지만 **나이가 심각도를 정하지 않는다** — 눈에 보이는 렌더링 결함이라 P1로 올려 함께 고쳤다. 부수 1건: 썸네일은 첫 요청이 null이라 결과가 열리는 프레임에 2D→3D로 튀고 하필 그 프레임에 `InsectEntity` 생성/파괴까지 겹친다 — 연출의 절정에서 한 번 끊기므로 상자 흔들림 1.5초 구간으로 예열을 옮겼다. **정정**: Explore의 "shiny 플래그도 빠진다"는 틀렸다 — `GachaResult`에 shiny 필드 자체가 없고 지급 시점 `AddCapturedInsect`가 굴린다(늘 false가 맞다). **정상 확인**: `DrawChest` 계수는 전부 w/h의 양수 배수 + 진입 가드 이중, roundness는 0/1만 써 중간값 뭉갬을 피했고, `BeginArea`는 여전히 OnGUI 1회(08-08의 3탭 공백 회귀 미재발), 구독 짝 대칭, 신규 코드에 프레임 할당 없음. 검증: 컴파일 error CS 0 / warning CS 0, ci_check **9검사** 통과, PlayMode 628/628.
- 2026-08-17: **큐 잔여 5건 일괄**(사용자 요청) — PlayerItemInventory / CaptureController / AccountSettingsUI / DexController / DexBrowseLayout — **P0:1, P1:3 처리, 검사기 1개 신설, 밸런스 1건 현행 유지 결정.** 대상이 안 겹쳐 탐색 5건을 병렬로 돌리고 수정은 순차 처리했다. **가장 큰 건은 DexController 라운드의 P0**인데, 정작 그 파일은 clean이었다 — 마지막 증분이 `CapturedSpeciesCount`를 **스토리 진행의 판정값으로 승격**시키면서, 원래 있던 도감 등록 누락이 "표시가 좀 빈다"에서 "전 플레이어의 진행이 한 종 늦게 열린다"로 격상된 것이다. 튜토리얼 보상 곤충(첫 파트너)이 소유·출전까지 하는데 도감엔 영원히 미발견이라 100% 완주도 불가능했고, `dexController`가 선언·대입만 되고 **한 번도 읽히지 않는 죽은 필드**인 게 결정적 증거였다. `StoryDirector`도 같은 형태(현재는 전 비트 `rewardInsectId:""`라 휴면이지만, 곤충 보상 비트를 저작하는 순간 자기가 준 보상이 자기 트리거를 못 밀어올린다). **같은 누락이 두 곳에서 동시에 나왔으므로 `dex_grant_lint.py`를 신설**했다 — 지급 6경로가 각자 도감 등록을 따로 불러야 하는데 빠져도 예외·경고가 없고 곤충은 멀쩡히 손에 들어와서, 도감을 세어보기 전엔 아무도 모른다. 검출력 실측: 두 수정을 되돌리자 exit 1 + 두 곳 정확 지목, 복구 후 exit 0. CaptureController에서 **P1:2** — `CaptureResolved`가 `AddCapturedInsect`보다 먼저 발화해 팝업이 **같은 종의 직전 개체**를 표시했고(첫 포획이면 조회가 비어 이전 팝업 값이 잔류, 초기화 코드가 없었다) → 지급 뒤로 이동 + 방어적 초기화. `NotifyCapture`가 연출 컴포넌트에만 있어 앞선 구독자 예외 하나로 포획 퀘스트가 영구 유실될 수 있었다 → 컨트롤러 성공 분기로 이동. **P1:1은 고치지 않고 사용자 판단을 받았다** — 07-17에 `CaptureController`+`GameplayTuningProfile` **두 파일을 같은 값으로** 내린 포획 너프(base .6→.35 / 난이도 .4→.45 / 퍼펙트 .15→.1)를 08-03 리팩터가 통째로 원복했고 같은 커밋이 그 잘못된 쌍을 고정하는 테스트까지 심었다. 원복이 사고인 건 분명하지만(08-03 커밋 메시지는 레벨 차 상한만 언급한다) **그냥 되돌리면 더 나빠진다** — 같은 08-03 커밋이 도입한 레어도 floor(.30/.22/.14/.08/.04)가 base 0.60 전제로 잡혀 있어서, 0.35로 내리면 난이도 0.5 동레벨 기준 **전 등급이 floor에 닿아**(Common `.35-.135=.215 < .30`) 난이도·레벨·base 항이 전부 죽고 포획률이 등급별 상수가 된다. 너프의 원래 의도(더 어렵게)조차 이루지 못한다. → **현행 유지로 결정**하고 경위를 4곳(`CaptureChanceTuning` 상단 주석 / `GetRarityFloor` 요약 / `rules/balance.md` 신설 절 / 해당 테스트 주석)에 남겼다. 특히 그 테스트는 **일치만 검사할 뿐 값이 옳은지는 보지 않는다**는 점을 적어 뒀다 — 그게 원복을 가린 장치였다. 나머지 3라운드는 clean. 검증: 컴파일 error CS 0 / warning CS 0, ci_check **10검사** 통과, PlayMode 628/628.
- 2026-08-17: **큐 15건 전수**(사용자 요청) — **P0:2, P1:9 처리, P2·설계 5건 위임.** 탐색 12건을 병렬로 돌렸다(작은 순수부 4개는 묶음). 큐 앞 6건이 **이번 세션에 내가 새로 쓴 코드의 첫 감사**였고 거기서 절반이 나왔다 — 자기가 쓴 걸 자기가 다시 보는 것보다 독립적으로 보게 하는 편이 낫다는 증거다. **P0 둘**: ①`QuickAccessBarUI`에 `FieldHudInput.RegisterBlockingRect`가 **한 줄도 없었다** — 탭한 프레임엔 모달이 아직 안 열려 `IsAnyOpen()`이 false, IMGUI라 `pointerOverUI`도 false여서 버튼 아래 월드 지점이 클릭-이동 목표로 잡혔다. **메뉴를 열 때마다 매번**, 모달을 닫는 순간 캐릭터가 걸어간다(모바일은 키 입력 자동 해제도 없어 더 나쁘다). ②`VillagerNpc.WarpTo`가 레이 첫 히트를 무조건 지면으로 채택했다 — 자기 주석이 "`SampleGround`는 지붕 오인을 막으려 ±0.75m를 거부한다"고 적어 두고 그 가드를 대체 없이 버린 것이다. 건물 몸통은 콜라이더를 남기므로 상판 y=2.8~3.8m가 잡히고, 이후 클램프가 **지상 복귀를 영구 거부**해 자가 회복이 불가능하다. 라온의 등장 워프 좌표가 실제로 그 입력을 만든다. **P1 아홉** 중 무게가 큰 것: `PlayerMovement`의 frozen 분기가 `return`해 `blockedMsgTimer`가 안 줄어 차단 문구가 **모달 위에 영구 고착**(바로 옆 `catchSwingTimer`는 이미 같은 이유로 줄이고 있었다), `StoryStageDirector`의 복귀 래치가 8초 타임아웃과 만나 **스토리 NPC를 들판에 영구 유기**(진입 데드존을 `engaged`로 고쳤던 것의 거울상) + `SnapToFinalPose`가 `stepIndex`부터 훑어 **정상 완주 시 한 번도 안 돌아** 걷다 만 귀가가 보정되지 않음, 레벨업 UI가 **"사탕 0"을 표시하면서 실제로는 차감**(`InsectLevelCurve` 에셋이 프로젝트에 0개라 표시 경로만 늘 null), `WeeklyContestClaimed` 클라우드 미동기(기기 교체 후 **주간 보상 재수령**)와 `SaveScope` 5키 누락(계정 삭제 후 같은 uid로 재로그인하면 진행 부활). **콜백 계약은 clean**임을 11경로로 확인했다 — 캠페인 영구 정지가 이 하네스의 최대 위험이었는데 `onPreludeDone`이 모든 경로에서 정확히 1회다. **위임**: `NpcManager`의 AI 예산이 인구 49명과 앨리어싱해 주민 갱신 주기가 배증(프레임 예산 튜닝), `OutfitSetData`의 세트 보너스 표시가 **최소 수치를 광고**하고 0개 착용 세트는 목록에서 빠짐(젬 전용 세트가 구매 전엔 존재조차 안 보임), 퇴장 연출의 9초 조작 잠금(안 기다리게 하면 워프로 바뀌어 연출이 사라진다 — 잠금과 진행의 분리가 필요한 설계 판단). 검증: 컴파일 error CS 0 / warning CS 0, ci_check 10검사 통과, PlayMode 628/628.
- 2026-08-17: **큐 12건 전수 — 큐 소진**(사용자 요청) — **P0:2, P1:8 처리, 위임 7건, clean 6영역.** 탐색 12건 병렬. 이 라운드로 `audit_candidates.py`가 후보 0건을 반환했다 — 저장소의 모든 `.cs`가 최소 1회 감사를 거쳤다. **P0 둘**: ①`WorldFieldMultiplayerUI`에 `RegisterBlockingRect`가 0건이라 비모달 버튼 6개 위의 탭이 월드 클릭-이동으로 샜다("3:3 대전"을 누르면 도전과 동시에 캐릭터가 상대 뒤로 걸어간다). **직전 라운드의 `QuickAccessBarUI`와 같은 결함이 다른 파일에서 또 나왔다** — 3파일 중 2곳이 빠져 있었으므로 `rules/ui-layout.md`에 절을 신설했다(검사기는 두지 않았다: "필드 위 비모달 버튼"을 정적으로 판정하려면 모달 여부·그리기 조건을 따라가야 해 오탐이 크고, 전체화면 모달은 등록하면 **안 되는데** 그 구분이 소스 패턴에 안 드러난다). ②`PlayerItemInventoryGridUIController.BuildGrid`가 `contentRoot`의 모든 자식을 파괴하는데 부트스트랩이 템플릿을 바로 그 자식으로 만든다 → 첫 갱신에 템플릿이 죽고 두 번째부터 null 가드로 조기 반환, **인벤토리 그리드가 옛 수량으로 영구 동결**(아이템을 써도 개수가 그대로). 덤으로 셀이 템플릿의 `SetActive(false)`를 상속해 보이지도 않았다. **P1 여덟** 중 무게가 큰 것: `NpcDialogueUI` 프렐류드 게이트 2건 — 예외 미봉쇄와 **인터페이스 타입 `!= null`이 Unity 파괴 검사를 하지 않는 것**(제공자는 `World/`, 대화창은 `UI/`라 한쪽만 죽을 수 있고 그때 `SetFrozen(true)`만 남고 타임아웃이 영영 안 돈다) — 둘 다 캠페인 영구 정지 계열이다. 젬 싱크 4곳 중 3곳이 클라우드 확정을 빠뜨려 **결제 시 쓴 젬이 되돌아오는 복제**가 가능했고(`SpendGems` 초크포인트 한 곳으로 덮음), `TrainingUI`의 새 캐시가 **이 화면 자신의 장착·해제로는 안 비워졌으며**(`ForceSave`는 이벤트를 안 쏜다), 「지워진 개체」 연출이 3D 모델 밖으로 전파되지 않아 포획 선택창이 감춘 이름을 바로 알려주고 있었다. **위임 7건**: `BuildForBattle`의 `erasedOverride`가 **호출부 0개**(전투 진입 시 원색 복귀, 다지점 플러밍) / 초대 팝업과 친구 패널이 겹쳐 IMGUI 선그리기 우선순위 탓에 "거절"이 "초대"로 눌림 / 대상지정 아이템의 `hospital` 미배선 무음 소멸 / 서브에리어 `erasedChance` 0% / 오프닝 최단 내레이션 창 알파 미고정(테스트 보강) 등. 검증: 컴파일 error CS 0 / warning CS 0, ci_check 10검사 통과, PlayMode 628/628.
