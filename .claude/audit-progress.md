# Audit 진척

이 파일은 `/audit` 슬래시 커맨드가 자동으로 읽고 갱신합니다. 수동 편집도 가능하지만 구조 유지 필수.

- **Covered**: 누적 P0/P1 처리 완료 시스템
- **Uncovered**: 다음 audit 후보 (우선순위순 — 위에서 아래로)
- **Round Log**: 일자별 처리 요약

## Covered (P0/P1 처리 완료)

- [x] CharacterPortraitRenderer (P0:2, P1:2, 2026-05-20) — DrawWithOutfit 캐싱, DrawItemPreview 추가, footHalfGap 0.18→0.28, Proportions 헬퍼 통일. **사용자 명시 요청**: 캐릭터 미리보기 도구가 magnify만 표시되던 회귀 → DrawCharacterTool 9종 분기 추가 (gun/wand/lasso/shuriken/sword/web_shooter/magnify/camera/기본). ApplyToolShape ↔ DrawToolPreview ↔ DrawCharacterTool 3곳 분기 순서 1:1 정합.
- [x] PlayerVisualBuilder (P1:5, 2026-05-20) — Body Cube화, 어깨 ±0.29, 신발 ±0.19, SafeDestroyMat 가드, 도구 손 좌표
- [x] CloudSaveManager (P1:2, 2026-05-20) — 401 토큰 갱신 재시도 (Load/Save), pendingSave 회귀 수정
- [x] PlaySceneBootstrap (P1:1, 2026-05-20) — CriticalBootstrapException fail-fast + OnGUI 사용자 알림, GachaBox.AutoWire(database), RegionDefinitions 분리
- [x] CharacterOutfitUI (P1:2, 2026-05-20) — 카드 미리보기 DrawItemPreview 호출, bigTitle/bigBonus CachedStyle
- [x] CharacterOutfitManager (P1:2, 2026-05-20) — ApplyToolShape 손 좌표 재배치, magnify/camera 분리, **도구별 mesh 교체** (Cube/Cylinder/Sphere)로 막대기 회귀 차단
- [x] PlayerMovement (P1:1, 2026-05-20) — OutfitChanged 구독 + InvalidateToolBase, 도구 swing 동기
- [x] CashShopManager (P1:1, 2026-05-20) — wallet 단일 소스, 1회 PlayerPrefs 마이그레이션
- [x] CaptureMinigameController (P0:1, P1:1, 2026-05-20) — InitMinigameStyles 6 캐시 필드, OnDisable 안전망
- [x] GachaBoxManager (P1:1, 2026-05-20) — ValidateInsectId + DB 캐시
- [x] TutorialQuestManager (P1:1, P0:4, 2026-05-20) — CompleteQuest 후 CloudSave 즉시 동기. **튜토리얼 진행 회귀 P0:4건** — NotifyTeamSet/NotifyItemUsed/NotifyTraining/NotifySkillEquipped 호출 누락으로 q_equip/q_item/q_training/q_team이 영구 정지 + 후속 13개 퀘스트 연쇄 정지. 4곳에 Notify 호출 추가 (BattleTeamManager.TeamChanged 구독, ItemEffectManager.ActivateItem, TrainingManager.TrainSkill, TrainingUI EquipSkill 사용자 액션).
- [x] BattleScreenUI (P2:1 DrawCombo + P1:2 P2:1 추가, 2026-05-20) — DrawCombo ComboCol, DrawSwapSelect SwapHeaderBase + alpha 갱신, DisableCanvasBattleUI + CheckGuardianDefeat FindFirstObjectByType 캐싱
- [x] SubAreaWorldBuilder (P0:8 + UI:1, 2026-05-20) — FindSafeSpawnPosition spiral 탐색, BuildCave/BuildTemple 입구 끼임 차단. 진입/퇴장 토스트 알림(3초) + F2 수동 Exit + 우측 하단 안내. RegionManager.ForceExitSubArea(). **사용자 명시 요청 2차**: 9개 환경 boundary wall halfSize를 floor 안쪽으로 조정 (Cave 16→14, DeepForest 18→16, Underwater 16→14, FogSwamp 16→14, MountainPeak 14→11, Temple 14→13, Reeds 14→11, GenericArea 12→9), BuildGreenhouse CreateGlassWall NoCollider 제거(collider 보존), Y 안전망(Y<-3 → 자동 RequestExit + 알림) + cachedPlayerTransform lazy 캐싱.
- [x] AuthManager (P1:2, 2026-05-20) — TryRefreshTokenForRetry public + refreshInProgress 가드. **사용자 명시 요청**: ValidateCredentials 헬퍼 + Register/Login 진입 시 호출 (빈 이메일/짧은 비밀번호 차단, Firebase API 호출 절약). 인증 흐름 전반 검증: TryAutoLogin 단일 호출, idTokenAcquiredAt 갱신 모든 경로 OK, AuthFailed→LoginUI 복귀, 회원가입→CharacterCreate 흐름 정상.
- [x] LoginUI (P1:2, 2026-05-20) — AuthFailed 구독 + 외형 변경 후 InvalidateCache. **사용자 명시 요청**: OnGUI 매 프레임 17개 new GUIStyle 변형 → base 스타일 fontSize 동적 갱신 패턴으로 교체 (BattleScreenUI/DexScreenUI 패턴). OnGUI 본문 잔존 new GUIStyle = 0.
- [x] RaidBattleUI (P0:2, P2:1, 2026-05-20) — EndRaid 재진입 누수(타이머 4개 + 팀 캐시 3개 초기화), GameObject.Find Player → playerMovement.transform 캐싱
- [x] RaidBattleController (P1:1, P0:1, 2026-05-20) — UseUniteAttack 데미지 표시 정합성, CheckEnd 패배 시 BossEntity.Despawn 누락 + OnRaidVictory의 Despawn 가드 분리
- [x] InsectBattleController (P0:2, 2026-05-20) — 패배 + 도주 성공 시 enemyEntity.Despawn 누락 — 둘 다 추가. 옛은 전투 후 곤충 필드 잔존, 중첩 발동 가능
- [x] CaptureController (P0:1, 2026-05-20) — 미니게임 캡처 실패 시 50% Despawn → 100% Despawn. 사용자 명시: "미니게임 끝나면 사라져야"
- [x] CaptureFeedbackController (clean, 2026-05-20) — Explore 점검 완료. P0/P1 회귀 없음. CaptureResolved += -= 짝 정상, AudioManager/TutorialQuestManager.Instance null 가드 모두 있음, OnGUI 메서드 없음(TextMeshPro 사용), 타이머 흐름 순차적
- [x] BattleArenaController (P1:1, 2026-05-20) — OnDisable ↔ CleanupArena 중복 정리 → OnDisable이 CleanupArena 위임으로 단일화. CleanupArena가 model 필드 null화까지 포함 (DRY + 옛 OnDisable 누락 보강). Explore P0:2 거짓양성 (Rect는 struct stack 할당, SetupNormalBattle 첫 줄 CleanupArena 호출됨)
- [x] DexController (clean, 2026-05-20) — Explore P0/P1 모두 거짓양성. SaveAndNotify는 RegisterEncounter/RegisterCapture 끝에 무조건 호출됨 (라인 51, 70), encounter/capture는 별개 dictionary라 race 없음, 0.1초 debounce 시나리오는 정상 게임 흐름에서 발생 불가. DexListUIController는 별도 시스템 (DexController audit 범위 외).
- [x] DexDetailUIController (clean, 2026-05-20) — Canvas 기반 UI (TMP_Text/Image), OnGUI 없음. 이벤트 구독 없음 (직접 Show/Hide 호출), AutoWire 안전, null 가드 견고 (라인 35-44). Resources.Load는 Show 1회 호출이라 P0 아님. 부수 발견: DexScreenUI에 OnGUI new GUIStyle 다수 → Uncovered 1순위로 분리.
- [x] DexScreenUI (P0:31, 2026-05-20) — 매 OnGUI 31개 new GUIStyle + 수십 개 new Color 회귀. InitDexStyles 패턴 + 31개 캐시 필드 + 매 프레임 핫패스 Color 38개 static readonly. OnGUI 본문 잔존 new GUIStyle = 0. 매 프레임 GC 압박 105~150 객체 → 0 객체. DrawTopBar(6) + DrawPokedex loop(6) + DrawDetail(12) + DrawOwnedCard loop(5) + DrawItems/Row(4) + DrawNoSelection(1) + DrawCentered(1) 모두 캐시 사용.
- [x] DexListUIController (clean, 2026-05-20) — Explore P0/P1 모두 거짓양성. SortList의 dexController null 가드는 BuildList:60 early return으로 보장됨. AutoWire 이벤트 짝은 모든 시나리오(null/동일/새 인스턴스)에서 정상. BuildList 재생성은 사용자 액션 시만 호출 (매 프레임 아님).

## Uncovered (우선순위순)

가장 위가 다음 `/audit`에서 자동 선택됩니다.

**2026-05-21 — 원 18개 Uncovered 모두 Covered 완료. 추가 발굴 항목 신규 등록:**

- [x] SaveService 글로벌 atomic write 라운드 (P1:8, 2026-05-21) — 신규 AtomicFileWriter (Core/AtomicFileWriter.cs, 38줄) — tmp 파일 쓰기 + File.Replace로 OS 레벨 atomic rename. 8곳 일괄 교체: PlayerProgressSaveService/PlayerCandyInventory/PlayerCurrencyWallet/PlayerItemInventory/PlayerInsectCollection/BattleTeamManager/DexSaveService/CloudSaveManager.SaveLocalFile. 게임 종료 직전 크래시/SIGKILL 시 부분 쓰기로 인한 세이브 손상 차단. CashShopManager는 PlayerPrefs 사용으로 글로벌 라운드 제외. DexSaveService에 Save(null) 가드도 일관성 추가.
- [x] CollectionUI 핫스팟 (P0:1, P1:3, 2026-05-21) — (a) P0: DrawInsectItem 핫스팟 5개 GUIStyle + 4개 Color 캐시화 — owned.Count × 5 = 최대 100+/프레임 → 0. InitItemStyles + itemNameStyle/itemInfoStyle/itemGradeStyle/itemStatMiniStyle/itemViewStyle + 6 static readonly Color. nameStyle/gradeStyle textColor 동적 갱신(BattleScreenUI 패턴). (b) P1: GetAllOwned 캐싱 — cachedOwned + ownedCacheDirty 플래그, HandleInsectUpdated에서 invalidate. line 116(DrawInsectList) + line 320(DrawStats) 매 프레임 List<PlayerInsectData> 할당 제거. (c) P1: OnEnable/OnDisable/AutoWire에 InsectUpdated -=/+= 짝 추가. 옛은 OnDisable이 ModalUIRegistry.Unregister만 호출, 이벤트 해제 누락. AutoWire가 OnEnable 후 호출되는 경우 isActiveAndEnabled 가드로 구독. (d) P1: DrawCenteredLabel의 new Color 2곳 static readonly. **잔여**: DrawPanel(P1:5)/DrawDetailPanel(P1:10)/DrawStats(P1:5)/DrawLevelUpSection(P1:10)/DrawStatBar(P1:5) — 호출 빈도 1회/프레임으로 P1, 별도 라운드 권고.
- [x] TrainingUI 핫스팟 (P0:1, P1:3, 2026-05-21) — (a) P0: DrawInsectSelect loop 4 GUIStyle + 4 Color 캐시 — owned.Count × 4 = 최대 80+/프레임 → 0. InitInsectSelectStyles + insectSelectSub/Name/Info/BtnStyle + 4 static readonly Color. NameStyle textColor 동적 갱신. (b) P1: GetAllOwned 캐싱 — cachedOwned + ownedCacheDirty + HandleInsectUpdated invalidate (CollectionUI 패턴). (c) P1: OnEnable/OnDisable/AutoWire에 InsectUpdated -=/+= 짝 추가. 옛은 OnDisable이 ModalUIRegistry.Unregister만 호출, 이벤트 해제 누락. AutoWire가 OnEnable 후 호출되는 경우 isActiveAndEnabled 가드. (d) **잔여**: DrawPanel/DrawMethodSelect/DrawSkillLearn/DrawSkillEquip/DrawSkillReplace — 약 40+ new GUIStyle/Color, 호출 빈도 1회/프레임으로 P1, 별도 라운드 권고.
- [x] CollectionUI 잔여 영역 (P1:27, 2026-05-21) — DrawPanel/DrawDetailPanel/DrawStats/DrawLevelUpSection/DrawStatBar/DrawCenteredLabel 27개 new GUIStyle 일괄 캐시화. InitDetailStyles + 25 캐시 필드(panelTitle/Close/TabActive/TabInactive, detailBack/Name/Rarity/GradeDisp/GradePerc/Desc/Hint, statsLabel/Value/CandyVal, luLvLabel/Num/XpLabel/XpVal/MaxLv/Btn/CandyInfo/Msg, barLabel/Iv/Total/IvLabel, centeredLabel) + 25 static readonly Color. 5개 메서드 모두 textColor 동적 갱신 패턴(detailName/Rarity/GradeDisp/GradePerc + luCandyInfo/Msg + barIv + centeredLabel). DrawStats에 GetCachedOwned 사용 추가(매 프레임 GetAllOwned 회피). 동적 rarityCol scaled/alpha scaled new Color는 struct stack 거짓양성. OnGUI 본문 잔존 new GUIStyle = 0.
- [x] TrainingUI 잔여 영역 (P1:34, 2026-05-21) — DrawPanel/DrawBackButton/DrawMethodSelect/DrawSkillLearn/DrawSkillEquip/DrawSkillReplace/DrawSkillCard/DrawFeedback 34 new GUIStyle 일괄 캐시화. InitTrainDetailStyles + 33 캐시 필드 + 38 static readonly Color. 8 메서드 textColor 동적 갱신 패턴(methodName/CardName/Cost + learnHeader + equipName/SlotName/LearnedName + replaceNew/OldName + cardSkillName + feedback). themeColor scaled new Color (line 246)는 struct stack 거짓양성, feedback alpha 동적 new Color도 거짓양성. OnGUI 본문 잔존 new GUIStyle = 0.
- [x] PlayerStatusHUD (P0:1, P1:2, 2026-05-21) — (a) P0: OnGUI 본문 new Color 16곳 → 16 static readonly Color 일괄 추출 (PanelBg/AccentBlue/Divider, LvBadgeBgDark/Accent, XpBarBg/FillDark/Light, StatCandyPink/CoinGold/GemBlue/TeamOrange/OwnedGreen/DiscoveredBlue/CapturedGold, RegionDefault). DrawResourceSection 4 + DrawCollectionSection 3 + DrawLevelSection 5 + DrawPanel/DrawRegionSection 3+1. (b) P1: GetAllOwned 캐싱 — cachedOwnedCount + ownedCountCacheDirty + HandleInsectUpdated invalidate (CollectionUI/TrainingUI 패턴). (c) P1: OnEnable/OnDisable에 InsectUpdated 구독/해제 짝 추가 (subscribedInsects 플래그). 옛은 매 프레임 DrawCollectionSection이 GetAllOwned() 호출하여 새 List 할당. shine alpha 동적/regionCol scaled/accent scaled/alertCol alpha 동적 new Color는 struct stack 거짓양성. new Rect 좌표 동적도 거짓양성.
- [x] TrainingManager (P1:1, 2026-05-21) — TrainSkill의 SpendCandy 호출을 LearnSkill/ReplaceSkill 성공 후로 이동 + 실패 시 early return. 옛은 ReplaceSkill 실패(replaceSkillId가 learnedSkillIds에 없는 경우 등) 시 candy 차감 후 학습 안 됨 → candy 손실. 정상 흐름(TrainingUI.DrawSkillReplace의 learned 리스트에서 선택)에선 안전하나 방어 부재. LearnSkill은 line 64(HasLearnedSkill) + line 65(ContainsKey) + line 75(IsSkillsFull) 사전 가드로 실패 불가(거짓양성)이나 일관성 차원에서 동일 패턴 적용. CanTrain vs SpendCandy race / 자동 장착 NotifySkillEquipped 미발화 / TrainingCompleted vs NotifyTraining 순서 모두 거짓양성.
- [x] CashShopManager 잔여 영역 (P1:3, 2026-05-22) — (a) PurchaseWithGems line 119 stale `gems` 캐시 → `Gems` 프로퍼티(wallet 우선) 사용 — 외부 wallet 변경 후 잘못된 잔액 검사 차단. (b) line 137 `gemsBefore = gems` → `Gems` — 환불 액수 stale 방지. (c) AddGems(0) early return — 무의미한 GemsChanged 발화 + 구독자 무의미한 갱신 차단. 단일 스레드 race는 모두 거짓양성, FindFirstObjectByType 매 호출은 결제 빈도 낮음으로 P2, shopItems 데이터 검증은 inline 정의로 수동 책임 P2.
- [x] InsectSpawner Spawn/Despawn 다중 호출 검증 (clean, 2026-05-22) — 진짜 P0/P1 회귀 없음. 직전 라운드 InsectEntity.despawnedThisCycle 플래그 동작과 일관. DespawnAllActiveInsects snapshot + Clear 패턴 안전(콜백 Remove no-op), DespawnFarInsects 역순 for + Remove 안전(i-- 인덱스 보존), 풀 재사용 시 Initialize의 cache/플래그 리셋으로 안전, OnSubAreaChanged 빠른 Exit는 currentSubArea != subArea 가드(line 698), DespawnEntity 외부 직접 호출 없음, activeInsects 중복 추가 불가, Unity 단일 스레드로 모든 race 거짓양성. SpawnSubAreaInsects maxActiveTotal 검사 부재는 DespawnAll 직후 호출이라 P2 수용. Covered 이동만.
- [x] RegionMapUI 깊은 점검 (clean P0, P1 보류, 2026-05-27) — Explore P0:2(이벤트 누락/null 가드) 모두 거짓양성. 라인 143 `regionManager == null || regionManager.Regions == null` early return 가드 있음. CurrentRegion stale은 OnGUI 매 프레임 regionManager.CurrentRegion 직접 조회로 정합. 30+ new GUIStyle 매 OnGUI는 진짜 P0이나 변경 폭 크고 RegionMapUI는 모달 토글 시만 진입(매 프레임 아님) → P1 별도 라운드 권고.
- [x] TutorialQuestUI DrawQuestPanel 캐시 (P0:1, 2026-05-27) — DrawQuestPanel은 OnGUI에서 매 프레임 호출되어 핫스팟. 5 GUIStyle(doneStyle/title/desc/prog/hint) 매 프레임 new → InitQuestPanelStyles + 5 캐시 필드 + 3 static readonly Color (DoneTextCol/QuestTitleCol/QuestHintBaseCol). hintStyle은 alpha 동적이라 textColor만 갱신 (BattleScreenUI 패턴). AutoWire 이벤트 중복 구독은 라인 499-504 `-=` + `+=` idempotent로 거짓양성. 잔존 8곳 new GUIStyle은 일시 표시(DrawCompletionNotification 3초 / DrawNewQuestNotification 2초 / DrawDetailPanel 수동 토글)이라 별도 라운드.
- [x] WorldChannelManager 깊은 점검 (clean P0/P1, 2026-05-27) — Explore P1:3 모두 거짓양성/P2 강등. BestEffortLeaveWorld dispose 누락은 OnApplicationQuit 직후 OS 프로세스 정리. LeaveWorld race는 leaveInProgress 플래그 미적용이나 LeaveWorld 호출 빈도 매우 낮음(사용자 의도 액션 1회) + isUpdatingPresence 이전 처리로 PATCH 일반 race 차단됨 → P2 수용. 401 자동 갱신은 WorldChannel 영역 외(이전 AuthManager TryRefreshTokenForRetry 처리는 CloudSaveManager 한정 의도된 범위).
- [x] **RegionTerrainBuilder c.y 이중 합산 버그 — 진짜 원인** (P0:21, 2026-05-27) — 사용자 명시 "고대유적/숲 layer 층이 위에 떠있어 캐릭터/곤충 가려짐" 보고. WorldTerrainBuilder.ApplyElevation(라인 22-39)이 region center.y를 forest:4 / ruins:8 / mountain:12 / pond:-3 / swamp:-2로 변경한 후, BuildXxxTerrain의 `c + new Vector3(x, c.y + offset, z)` 패턴이 c.y를 또 더해 결과 Y = **2 × c.y + offset**. ruins(c.y=8) 환경 모두 Y=16m로 솟아 캐릭터(Y=8.2 머리) 위 8m 떠있던 게 "층이 위에" 인상의 진짜 원인. 21곳 일괄 수정: `, c.y, ` → `, 0f,`, `, c.y + ` → `,`, `, c.y - ` → `, -`. Pond/Swamp/Mountain/Garden 동일 버그 함께 수정. Forest/Ruins 환경 축소 이전 라운드들(P1:4 + P0:11 누적)은 효과 거의 없었음 — 진짜 원인은 이 이중 합산 버그.
- [x] Forest/Ruins 환경 추가 축소 — "layer 층 전체 위에" (P1:2, 2026-05-27) — 사용자 추가 보고 "layer 층 전체가 위에 떠있어 캐릭터/곤충이 가려짐". 이전 절반 축소(treeH 1.2~2.5, wh 0.5~1.5, ph 1~2)로 부족 → 추가 강제 축소. (a) Forest treeH 0.8~1.5(낮은 관목), leafS 1~1.8 → 잎사귀 Y 1~2.5m로 캐릭터 머리(2.2) 이하/근처. (b) Ruins wh 0.3~0.8(잔해), ph 0.7~1.3(캐릭터 어깨 아래). 25 트리/8 기둥이 평면 layer로 모이지 않음.
- [x] 메인 월드 Forest/Ruins region 환경 높이 (P1:2, 2026-05-27) — 사용자 명시 "고대유적과 숲이 메인 월드에서 하늘에 있는 것처럼 보임" 보고. RegionTerrainBuilder.BuildForestTerrain/BuildRuinsTerrain (이전 SubArea 변경은 사용자 의도 영역 외였음, 영향 없으나 무관). (a) **Forest**: treeH 3~6→1.2~2.5(절반 축소), leafS 2~4→1.5~2.5. 트리 trunk Y 0~6m→0~2.5m, 잎사귀 Y 3.6~7.2m→1.8~3.5m로 캐릭터 머리(2.2) 부근. 잎사귀 ShadowsOnly 제거 — 옛 ShadowsOnly가 leaf mesh 안 보이고 그림자만 공중에 떠 보이는 "환경이 하늘에 떠있어" 인상의 진짜 원인이었음. 정상 렌더링 + leafS 축소로 카메라 시야 차단 최소화. (b) **Ruins**: wh 1~3→0.5~1.5, ph 2~4→1~2. 벽 Y 0~3m→0~1.5m, 기둥 standing Y 0~4m→0~2m로 캐릭터와 비슷한 평면감.
- [x] SubArea 덮개 가시성 (P1:2, 2026-05-27) — 사용자 명시 "고대유적/숲이 하늘처럼 덮여서 캐릭터 안 보임" 보고. (a) DeepForest 잎사귀 ShadowsOnly 적용 — 그늘 인상 유지하며 카메라(Y=10)와 캐릭터(Y=1.04) 사이 시야선 차단 해제 (Cave 천장 동일 패턴). (b) CameraFollower SubAreaOffset (0,16,-12)→(0,10,-14) 측면화 — 부감 53도→36도로 줄이고 z 거리 -12→-14로 늘려 캐릭터 옆에서 보이도록. 환경 위 평면에서 캐릭터 가려지던 회귀 차단. Temple 기둥은 양옆 배치라 측면 시야로 충분히 보임 (ShadowsOnly 미적용).
- [x] 어깨 추가 + SubArea 부유/환경 높이 후속 (P1:4, 2026-05-27) — 사용자 명시 후속 보고. (a) **어깨 추가 하향**: ArmL/R Y 1.55→1.40 (캡슐 범위 1.15~1.65m, Body 중간 위치). HandL/R Y 0.98→0.95. (b) **SubArea 부유 해결**: SubAreaOrigin Y 0→0.5로 한 줄 변경 → 8개 모든 환경(subAreaRoot 자식)이 world Y=0.5로 일괄 올라가서 캐릭터/곤충(Y=0.5 텔레포트→Y=1.0)과 같은 평면감. (c) **DeepForest 높이 축소**: 트리 기둥 Y 3→1.5 scale 3→1.5, 잎사귀 Y 6.5→3 scale (3.5,2.5,3.5)→(2.5,1.5,2.5). (d) **Temple 기둥 축소**: Y 3→1.8 scale 3→1.8. 카메라 SubAreaOffset (0,16,-12) 유지 — 환경이 낮아져 차폐 가능성 추가 감소. 다른 6개 환경(Cave/Underwater/FogSwamp/MountainPeak/Reeds/Greenhouse/GenericArea) 높이 축소는 사용자 추가 보고 시 별도 라운드.
- [x] 캐릭터 시각 버그 3종 (P1:3+구조 변경, 2026-05-27) — 사용자 명시 요청. (a) **어깨**: ArmL/R Y 1.62→1.55 + scale Y 0.55→0.50 + HandL/R Y 1.0→0.98. 캡슐 상단 1.895→1.80m로 Body 상단(1.79)과 일치, 머리(2.20) 영역 침투 차단. (b) **발 swing**: BootL/R을 Player 직접 자식 → LegLPivot/LegRPivot 빈 컨테이너 자식으로 구조 변경 (Capsule scale 비균등이라 Pivot scale=1 유지 필수). PlayerMovement cachedLegL/R → cachedLegPivotL/R, AnimateWalk 회전 대상 Pivot으로 교체 → Leg+Boot 둘 다 자동 전파. CharacterOutfitManager.ApplyPartColor는 FindDeep 재귀 검색이라 호환. (c) **SubArea 차폐**: SubAreaWorldBuilder에 GetSubAreaEnvLayer(Layer 31 fallback) + SetLayerRecursively 헬퍼 추가, EnterSubArea의 8개 환경 빌드 끝에 1회 호출(개별 빌드 메서드 미수정). CameraFollower.ResolveObstruction hit loop에 layer 제외 1줄. CameraFollower.SetSubAreaMode(bool) 추가 (NormalOffset (0,12,-8) ↔ SubAreaOffset (0,16,-12)). SubAreaWorldBuilder EnterSubArea/ExitSubArea에서 SetSubAreaMode(true/false) 호출 (ResetBaseline 내장).
- [x] TutorialQuestUI 잔존 캐시 (P1:8, 2026-05-27) — DrawCompletionNotification 3 + DrawNewQuestNotification 1 + DrawDetailPanel 4 (header/close/row/status) GUIStyle 매 호출 new → InitNotifStyles + InitDetailStyles + 8 캐시 필드 + 9 static readonly Color. 알림 4 textColor alpha 동적, row/status textColor 분기 동적(완료/활성/잠금/대기). 17 quest×2(row+status)=34 GUIStyle/프레임 차단. OnGUI 본문 잔존 new GUIStyle=0.
- [x] CapturePopupUI 캐시 (P1:12, 2026-05-27) — DrawSuccessPopup 8 + DrawFailPopup 2 + DrawMiniIVBar 2 GUIStyle 매 OnGUI(popupTimer>0 5초) new → InitPopupStyles + 12 캐시 필드 + 7 static readonly Color (SubGrayBase/GradeTitleGrayBase/RewardCandyBase/RewardExpBase/FailMsgBase/FailSubBase/IvLblBase). 12 textColor alpha+rarityCol+gc 동적 갱신. OnGUI 본문 잔존 new GUIStyle=0.
- [x] RegionMapUI 캐시 + OnDisable 정리 (P1:28+1, 2026-05-27) — (a) DrawMap 5 + DrawErrorMessage 1 + DrawMiniMap 5 + DrawRegionList 5 + DrawRegionDetail 6 + DrawDexItem 6 + DrawMapTerrain symStyle 1 = 28 GUIStyle 매 OnGUI new → InitMapStyles + 28 캐시 필드 + 3 static readonly Color. DrawMap/DrawRegionDetail 진입에 InitMapStyles 호출. textColor 분기/alpha/themeColor 동적 갱신. (b) OnDisable에 isOpen=false + selectedRegionId=null + ModalUIRegistry.Unregister 추가 (CharacterOutfitUI 동일 P1 패턴, GO SetActive 토글 시 stale 모달 차단). OnGUI 본문 잔존 new GUIStyle=0.
- [x] WorldLobbyUI MakeTex 누수 (P0:7, 2026-05-27) — DrawWorldSelectPanel/DrawWorldRow/DrawJoiningPanel/DrawInWorldPanel의 OnGUI 매 프레임 7곳 `MakeTex(1,1,...)` 새 Texture2D 생성 → UIHelper.GetCachedTex 교체. static readonly Color 10개 추출(BgOverlayCol/RowFull/Almost/OkCol/BarBgCol/FillFull/Almost/OkCol/LineSepCol/MeRowBgCol). 월드 1개당 4 Texture2D(rowTex/barBg/fillTex)/프레임 + 배경 1 = 월드 수×4+1 객체/프레임 누적 누수 차단. InitStyles의 4곳 MakeTex(라인 397/426/429/430)은 1회만 호출이라 유지.
- [x] CharacterOutfitUI infoStyle 캐시 + OnDisable 정리 (P0:1+P1:1, 2026-05-27) — (a) P0: OnGUI 라인 234/239의 infoStyle/infoNameStyle 매 프레임 new GUIStyle 생성 → InitStyles에 infoStyleCache/infoNameStyleCache 캐시 추가 + DrawPanel에서 직접 참조. InfoLabelCol static readonly로 추출. (b) P1: OnDisable이 ModalUIRegistry.Unregister만 호출하고 isOpen=true 잔존 → 같은 GO SetActive 토글 시 isOpen=true이지만 Registry 미등록 → HandleEscape가 모달 무시. isOpen=false 추가.
- [x] CapturePopupUI 점검 (clean P0, P1:1 보류, 2026-05-27) — 알파 계산 라인 117은 의도된 fade-in(animTime/0.3, 0.3초)/fade-out(popupTimer/0.5, 마지막 0.5초) 분기로 P2 수용. OnGUI 매 프레임 new GUIStyle 14개+new Color 다수는 popupTimer>0(약 5초 표시 시간)에만 진입하여 일시적 → P1이나 다른 핫패스 처리 후 별도 라운드 권고. 이벤트 구독/null 가드/UIHelper 사용 모두 정합.
- [x] CharacterPortraitRenderer 깊은 정합성 (clean, 2026-05-27) — Explore P0:2+P1:2 모두 거짓양성. hatId 처리 분기는 hat_none 시 customHat=true(라인 332)→Draw 내부 모자 투명 + 라인 501 hat_none 조건 거짓→형태 분기 안 들어감 → 모자 미표시 일관. cutlass(라인 435-441) DrawCharacterTool은 손잡이+칼날 2단계, DrawToolPreview의 차이는 별도 미리보기 컨텍스트(아이템 카드 vs 캐릭터)이라 의도된 시각적 차이. InvalidateCache 트리거는 LoginUI에서 직접 호출(라인 590)로 처리됨. 9종 도구 결합도는 구조적 P2.
- [x] PlaySceneBootstrap 깊은 점검 (clean, 2026-05-27) — Explore P0:1+P1:2 모두 거짓양성. RegionChanged 람다 누수는 RegionManager가 DontDestroyOnLoad 아님(grep 결과 AudioManager/Auth/CloudSave 3개만) → 씬 재로드 시 옛 RegionManager+옛 람다 함께 GC. AuthManager.Instance 라인 401은 != null 가드 있음. playerMovement.AutoWire는 라인 330에서 실제 호출됨(Awake → Build() → Update 라이프사이클상 안전).
- [x] SubAreaWorldBuilder 깊은 점검 (clean, 2026-05-27) — Explore P0:1+P1:6 모두 거짓양성. EnterSubArea race는 Unity Input 단일 프레임 폴링이라 같은 프레임 F2 불가능. ShowMainWorld 복원은 hiddenMainObjects 기반(HideMainWorld가 add한 것만)이라 이름 규칙 벗어난 오브젝트는 add도 안 되어 복원 필요 없음(정합). sticky 회피는 0.01s < 1.5s 쿨다운으로 유지. Destroy 비동기는 Unity 표준 동작. 코루틴 누수는 현재 횃불 애니메이션 없음(future bug). cachedPlayerTransform은 Player Transform 자체라 위치만 변하지 stale 참조 아님. InsectSpawner 부모 mismatch는 SubAreaWorldBuilder 영역 외(별도 라운드 후보).
- [x] TutorialQuestManager 깊은 점검 (clean, 2026-05-27) — Explore P0:1+P1:3 모두 거짓양성. NotifyAction 비활성 무시는 prerequisite 체인 강제 순차 진행으로 비순차 시나리오 발생 불가. CloudSave race는 pendingSave 플래그로 단일 대기 처리. 옛 세이브 activeQuestId 불일치는 LoadProgress가 completedQuests 함께 로드 후 ApplySaveData 적용 + Start의 ActivateNextQuest 흐름이 일관. 마스터 계정 activeQuestId=null은 17 퀘스트 모두 완료 상태에서 의도된 동작(퀘스트 다 끝낸 유저는 UI 표시 안 하는 게 자연스러움). 의도 변경 필요 시 별도 디자인 작업.
- [x] BattleScreenUI 깊은 로직 (clean, 2026-05-27) — Explore P0:2+P1:3 모두 거짓양성. cachedRegionMgr는 라인 313에 `regionMgr == null` 가드 있음. EndBattle 라인 3094-3099 finally 블록에 ExitBattleMode + SetFrozen(false) + Time.timeScale 복구 완비. SwapSelect↔PlayerTurn 키 처리는 phase별 격리(라인 379-384/405-412) + OnGUI Event 라인 726-748 phase 체크 후 set + Update phase 체크 후 consume + 즉시 reset(라인 411-412)으로 안전. InsectBattleUIController.ShowResult는 별개 Canvas UI 시스템, BattleScreenUI(OnGUI)와 무관. lastDamageToEnemy/Player phase 전환 미초기화는 다음 OnBattleUpdated에서 덮어쓰니 P2 수용. nameTagCache.fontSize 동적 할당은 GUIStyle 자체 캐시 + 프로퍼티만 갱신이라 P2.
- [x] RaidBattleUI 깊은 로직 (clean, 2026-05-27) — Explore P0:3+P1:5 모두 거짓양성. EndRaid 라인 2380-2384 displayTeamHp/teamShake/selectedSlot/cameraFollower/playerMovement 모두 정리(이전 라운드 P0:2 처리분과 정합). AutoWire 라인 3005 `raidController != rc` 가드로 동일 rc 재호출 시 중복 구독 차단, null rc 시 라인 3005 false라 raidController=null 미설정 안전. Result→None은 EndRaid에서만 세팅, 라인 309 None 진입은 새 레이드 시작이라 정상. lastDmgToBoss 게이지 누적은 RaidController 턴 단위 관리로 race 없음. SelectInsect KO 차단은 TrySelectInsect 라인 585-586 CurrentHp > 0 체크 있음.
- [x] BattleArenaController 이펙트 (clean, 2026-05-27) — Explore P0:3+P1:3 모두 거짓양성. Material 누수는 Unity가 GameObject Destroy 시 attached Renderer.material 인스턴스 자동 정리. StopAllCoroutines 후 arenaRoot Destroy하면 child GameObjects + 그 Material 인스턴스 일괄 정리. ExitBattleMode/SetFrozen(false)는 호출자(BattleScreenUI EndBattle:3097-3098, RaidBattleUI EndRaid:2383-2384)에서 책임 분리 패턴으로 처리. 다중 SetupNormalBattle 호출은 라인 57/92에서 CleanupArena가 진입 첫 줄이라 안전. 투사체는 라인 623 arenaRoot.SetParent로 자동 정리. 팀 배치 정적 계산은 게임이 runtime 팀 변경 미지원으로 비해당.
- [x] PlayerMovement 모달 차단 + InputAction 흐름 (clean, 2026-05-27) — 559줄, 8 책임 매트릭스 점검 결과 진짜 P0/P1 회귀 0건. 모달 차단: HandleEscape() + IsAnyOpen() 이중 방어(Line 137-143 ESC return + Line 169 마우스 클릭 차단). Frozen ESC 처리는 모달 우선(Line 121-131). 이중 ESC 회귀는 ModalUIRegistry.IsAnyOpen 자동 정리(Line 37-40)로 거짓양성. InputAction 흐름은 Unity 구식 Input.GetKey + OnGUI Event 백업(guiKey*/guiEsc/guiClick) 이중 경로로 F9/F11 OS 인터셉트·Editor·접근성 장치 우회. KeyUp에서 guiKey* 명시 false로 누적 없음(Line 318-330). OutfitChanged 동기는 InvalidateToolBase 즉시 호출로 stale base 1프레임 지연 차단(2026-05-20 P1:1 완료). CameraFollower 점프는 SetTarget baselineValid 리셋(2026-05-21 P1:2 완료). FindFirstObjectByType 호출 0건. cachedArm/Leg/Body/HeadPivot은 transform.Find 1회 후 캐싱(Line 42-46) 정상. outfitBonus null 가드(Line 240) 있음. **Covered 이동만**.

- [x] InsectSpawner (P1:2, 2026-05-20) — OnGUI debugStyleCache lazy 캐싱(매 프레임 new GUIStyle 차단), GetPlayerTransform lazy 캐싱(매 호출 FindWithTag+Find 회피). Explore P0:2 거짓양성 (CleanupDeadEntities는 외부 SetActive(false) 직접 호출 없음 + DespawnFarInsects null entity는 이미 destroy 상태라 RemoveAt만 정상), P1 Sin/Cos는 8초당 8회 = 초당 1회 거짓양성.
- [x] RegionManager (P1:3, 2026-05-20) — IsRegionAccessible 마스터 우회 (AuthManager.IsMasterAccount race 차단), DefeatGuardian 중복 가드 (HashSet idempotent 명시 + SaveUnlockState 중복 PlayerPrefs.Save 회피), LoadUnlockState unlockedRegions Split RemoveEmptyEntries (옛은 빈 항목 누적). P2 Time.time vs realtimeSinceStartup은 일반 게임 timeScale 영향 적어 수용.
- [x] RegionTerrainBuilder + RegionMapUI (UI:2, 2026-05-21) — 사용자 명시 요청. BuildBoundaries 신규 (60-segment fence + 자동 인접 검출 gateway gap + 노란 마커, 환경별 fence shape). RegionData.connections 선택 필드 추가. RegionMapUI: 사각 테두리 → 원형 ring(32 sample) + 현재 리전 펄스 alpha + gateway 노란 점.
- [x] ItemEffectManager (clean, 2026-05-21) — Explore P0:1+P1:2+P2:1 모두 거짓양성/자체 모순. ActivateItem 옛 item 덮어쓰기는 의도된 동작, 게임 종료 시 메모리 해제, ActiveItemChanged 구독자가 OnDisable에서 해제.
- [x] WorldStateProvider (clean, 2026-05-21) — Explore P0:2+P1:1+P2:1 모두 거짓양성. WorldState는 struct(stack 할당, GC 없음), pull 모델이라 이벤트 구독 디자인상 불필요, null 가드 fallback(DayPhase.Day/hour 12/Clear) 정상.
- [x] WorldChannelManager (P1:1, 2026-05-21) — Update 코루틴 중복 실행 차단(isRefreshingWorld/isUpdatingPresence 플래그). 네트워크 지연으로 30초 timeout 초과 시 매 프레임 새 코루틴 시작 → CurrentWorld 동시 접근/PATCH race. Explore P0 AuthManager null 거짓양성(모든 public API에 IsFirebaseReady 가드), WorldLobbyUI OnDestroy 거짓양성(Unity OnDestroy 전 OnDisable 자동), BestEffortLeaveWorld 중복 거짓양성(RemoveAll idempotent).
- [x] BattleTeamManager (P1:2, 2026-05-21) — SetSlot no-op 가드(같은 값 재설정 시 TeamChanged 발화 안 함), MigrateLegacySlots TeamChanged 발화 제거(시스템 내부 정리는 사용자 액션 아님). 옛은 TutorialQuestManager가 TeamChanged 구독해 q_team 진행도 자동/중복 가산 위험. Explore P0:2+P1:1+P2:1 모두 거짓양성(슬롯 부족 자동 채움, saveData fallback, 사본 반환, single execution).
- [x] BattleTeamUI (P1:16+22, 2026-05-21) — 16개 GUIStyle 캐시 필드 + InitTeamStyles + 21개 static readonly Color. DrawTeamPanel(3) + DrawSlot(7) + DrawInsectPicker(2) + DrawPickerItem(4) 모두 캐시 사용. railCol/pickerNameCache.textColor 동적 갱신. OnGUI 본문 잔존 new GUIStyle/Color = 0. 매 프레임 owned.Count × 5+ 객체 → 0.
- [x] PlayerInsectCollection (P1:3, 2026-05-21) — (a) Awake에서 instanceId 중복 시 새 GUID 발급 + dirty mark — 옛은 dictionary가 1개만 보존되어 BattleTeam.GetByInstanceId가 나머지 인스턴스 못 찾는 회귀(옛 세이브 마이그레이션 시). (b) GainXp(data, insect, amount)에 insect null 가드 — 형제 오버로드(line 169)와 비대칭으로 line 185 NRE 위험. 호출자 0건 확인했으나 방어 코드 유지. (c) GetAllOwned null 항목 sanitize — 손상 세이브 시 호출자 6곳(BattleTeamUI/CollectionUI/DexScreenUI/TrainingUI/PlayerStatusHUD/PlayerInsectSelectionUIController) NRE 차단. 슬롯 인덱스 race는 Unity 단일 스레드 + BattleTeamManager.SetSlot 중복 가드(line 59-63)로 거짓양성.
- [x] PlayerProgressController (P1:3, 2026-05-21) — (a) Awake sanitize: 손상 세이브 시 level 1~maxLevel 클램프 + currentXp ≥ 0 클램프 + dirty 시 즉시 Save. 옛은 외부 JSON 편집/손상 파일로 GetXpToNextLevel 음수 진입 위험. (b) 만렙 도달 시 currentXp = 0 클램프 — `Lv 50 / XP 99999` UI 불일치 차단. (c) 만렙 후 GainXp 호출 시 early return — 잉여 XP 누적 + 무의미한 디스크 쓰기/SaveCloud 차단. level/xp 동기 race는 단일 writer(GainXp) + private data로 거짓양성. 디스크 IO 디바운스는 호출 빈도 낮아(배틀/캡처/튜토리얼 시점만, burst 없음) P2 수용. CloudSaveManager.Instance null 가드는 SaveToCloud 내부 3중 가드(IsFirebaseConfigured + IsLoggedIn + IsSaving)로 거짓양성.
- [x] PlayerProgressSaveService (P1:1, 2026-05-21) — Save(null) 가드 추가. static service라 임의 호출자가 진입 가능, 옛은 JsonUtility.ToJson(null)이 빈 문자열 → WriteAllText로 빈 파일 → 다음 Load 데이터 손실. atomic write 부재(P2)는 다른 SaveService(PlayerInsectCollection/BattleTeamManager/CashShopManager 등) 모두 동일 패턴이라 일관성 차원에서 별도 글로벌 라운드로 이관. 단일 writer(PlayerProgressController.GainXp)라 동시 쓰기 race 없음, FromJson 손상 회복은 catch + ?? new로 충분.
- [x] PlayerCandyInventory (P1:2, 2026-05-21) — (a) AddCandy/SpendCandy 내부 data null 가드 추가 — Candies 프로퍼티(line 17)는 null 가드 있으나 두 변경 메서드는 비대칭, Awake 실패/순서 race 시 NRE. (b) Save(null) 가드 — PlayerProgressSaveService와 일관성. 호출자 6곳(InsectBattleController/CaptureController/RaidBattleController/CashShopManager/GachaBoxManager/UI)은 외부 `candyInventory?.` 가드 사용 중. 디스크 IO 디바운스는 호출 빈도 낮음(배틀/캡처/가챠 보상만)으로 P2 수용, atomic write는 글로벌 라운드로 이관.
- [x] PlayerCurrencyWallet (P1:5, 2026-05-21) — (a-d) AddGems/SpendGems/AddCoins/SpendCoins 4개 변경 메서드 data null 가드 추가 — Gems/Coins 프로퍼티(line 18-19)와 비대칭 회귀. (e) Save() 내부 data null 가드 — 빈 파일 쓰기 + CurrencyChanged 구독자에게 null 전파 차단. CashShopManager gems 단일 소스 패턴(이전 audit)과 정합 유지. atomic write는 글로벌 라운드로 이관.
- [x] PlayerItemInventory (P1:3, 2026-05-21) — (a) Awake sanitize: 손상 세이브 null record / 빈 itemId / 중복 itemId 처리. 중복 시 count 합치고 stale record 제거 — 옛은 lookup이 마지막 record만 보존해 AddItem 변경이 stale로 동기화 깨짐. (b) GetSnapshot 얕은 복사 반환 — DexScreenUI:659 등 호출자가 save.items 직접 변경 시 lookup race 차단. (c) Save(null) 가드 — 다른 SaveService와 일관성. UseItem 0 잔량 제거 로직(line 78-82)은 정상, GetCount 음수 클램프(line 96)도 정상.
- [x] PlayerInsectLevelUpUIController (P1:3, 2026-05-21) — (a) Refresh에서 current null 가드 — selectedInstanceId가 stale(곤충 삭제/강화 후 변경)이면 GetByInstanceId null 반환 → line 75 current.insectId NRE. (b) OnEnable/AutoWire 이중 구독 차단 — subscribed 플래그 + Subscribe/Unsubscribe 헬퍼. 옛은 AutoWire 먼저 → 구독 → OnEnable → 또 구독 시나리오에서 HandleInsectUpdated 중복 발화, Refresh 2회 호출. (c) AutoWire가 isActiveAndEnabled 시에만 Subscribe — OnDisable 동안 구독 잔존 차단. 호출자 곤충 변경 시 자동 갱신 정상.
- [x] ShopUIController (P1:1, 2026-05-21) — Start line 65 itemDatabase null 가드 추가 — EnsureDatabase의 Resources.Load("ItemDatabase") 실패 시 itemDatabase는 여전히 null → FindById NRE. UpdatePriceLabels(line 188-205) dead code (호출처 없음, allowCoinPayment [SerializeField]라 런타임 불변)이나 제거 risky로 P2 보류. 토글 무한 루프는 SetCoinPayment/SetGemPayment의 `if (!enabled) return;` first guard로 거짓양성. CashShopManager.Instance race는 단일 스레드로 거짓양성, wallet 동기화는 이전 CashShopManager audit에서 단일 소스 정리 완료.
- [x] GachaBoxUI → CashShopUI 가챠 영역 (P0:11, 2026-05-21) — GachaBoxUI 클래스는 존재하지 않음(가챠 코드가 CashShopUI에 통합). DrawGachaTab/DrawBoxCard/DrawGachaResultScreen 매 OnGUI new GUIStyle 11개 + new Color 5개 회귀. InitGachaStyles + 12개 캐시 필드 + 5개 static readonly Color (BoxBronzeCol/BoxSilverCol/BoxGoldCol/GachaFlashBlue/GachaFlashGold/GachaPriceAffordCol/GachaRateGrayCol/GachaCandyPinkCol). boxPriceStyle/gachaTitleStyle은 textColor만 동적 갱신(BattleScreenUI 패턴). Phase 2의 new Color(alpha 동적)와 new Rect는 struct stack 할당으로 거짓양성. **잔여 작업**: CashShopUI 보석 충전(Tab 0)/아이템 상점(Tab 1) 영역도 동일 회귀 가능성, 별도 라운드 권고.
- [x] ModalUIRegistry (clean, 2026-05-21) — 진짜 회귀 없음. IModalUI 구현체 7개(BattleTeamUI/CaptureChoiceUI/CharacterOutfitUI/CashShopUI/CharacterViewerUI/CollectionUI/DexScreenUI) 모두 OnDisable에서 Unregister 호출 일관. HandleEscape는 PlayerMovement:127 단일 진입점. Register LRU 패턴 정상(Remove + Add), IsAnyOpen/TopModal null/IsOpen 자동 정리, Unregister null 가드 있음. static state scene 잔존은 IsOpen 체크로 회복(P2 수용), race는 Unity 단일 스레드로 불가, TopModal 역순 반복은 stack 변경 안전. Covered 이동만.
- [x] CashShopUI 보석 충전/아이템 상점 영역 (P0:16, 2026-05-21) — Tab 0/Tab 1 + OnGUI 본문 new GUIStyle 16개 + new Color 다수 → InitMainStyles + 16 캐시 필드 + 14 static readonly Color (Backdrop/Panel/CharArea/ResTitle/CoinGold/BonusGreen×2/GemLabel/BuyButton/GemGlow/Border/GradTop/GradBot/Highlight + ItemBuyBlue). resStyle/itemPriceStyle textColor 동적 갱신 패턴(BattleScreenUI 동일). Phase 2 new Color(alpha 동적)와 new Rect(좌표 동적, struct stack)는 거짓양성으로 수용. OnGUI 본문 잔존 new GUIStyle = 0.
- [x] UIHelper (P0:1, 2026-05-21) — GetCachedTex 캐시 가득(256개) 시 texCache.Clear()만 하고 styleCache 미invalidate 회귀. styleCache의 GUIStyle.normal.background이 Destroy된 텍스처를 참조 → 다음 렌더링에서 MissingReferenceException 또는 invisible 버튼. GetButtonStyle(line 189)이 정확히 그 패턴(`style.normal.background = GetCachedTex(bgColor)`). GetCachedTex 캐시 클리어 분기에 styleCache.Clear() 동반 호출 추가. new Color/Rect는 struct stack 거짓양성, Color32 정밀도 손실은 시각적 무해, 동시 호출 race는 Unity 단일 스레드로 거짓양성.
- [x] AudioManager (P1:1, 2026-05-21) — StopBGM이 crossfadeCoroutine만 중단하고 deferredBgmCoroutine 미중단 회귀. PlayBGM(X) → deferred yield 대기 → 즉시 StopBGM 호출 시 fade out 시작 그 후 deferred가 다음 프레임에 CrossfadeBGM(X) 시작 → StopBGM 의도 깨짐 + 페이드 충돌. StopBGM에 deferredBgmCoroutine 중단 추가. FindFirstObjectByType(GameClock) 1초 폴링은 캐시 후 안 호출(P2 수용), ApplyVolumes fade 중 덮어쓰기는 사용자 설정 변경 빈도 낮음(P2 수용), gameClock stale은 Unity == null overload로 자동 재캐시(거짓양성), 다중 PlayOneShot은 AudioSource 특성상 동시 재생(거짓양성).
- [x] ProceduralAudioGenerator (clean, 2026-05-21) — 1957줄 모놀리스이나 구조 단순. public 진입점 3개(GetBGM/GetSFX/GetAmbient) + 단일 Dictionary 캐시 + 나머지는 PCM 생성 알고리즘(audit 범위 외, 캐시 hit 시 호출 안 됨). 모든 진입점이 TryGetValue → switch → cache 저장 패턴 일관, default LogWarning + null 반환, 호출자(AudioManager) null 처리 OK. Unknown key LogWarning spam은 정상 흐름에서 발생 안 함(P2), 캐시 무제한 증가는 총 ~64개로 안정 상태 도달 후 변동 없음(P2), Unity 단일 스레드로 동시 호출 race 거짓양성. Covered 이동만.
- [x] InsectEntity 외부 로직 (P0:1, P1:3, 2026-05-21) — BuildModel은 visual-dev 영역으로 제외. (a) P0: Update의 Camera.main 매 프레임 호출 → static cachedMainCam lazy 캐싱. 최대 20마리×매 프레임 FindGameObjectWithTag 핫패스 회피. (b) P1: Despawn 다중 호출 가드(despawnedThisCycle 플래그) — Battle/Capture 동시 Despawn 시 풀 중복 반환 + onDespawn 두 번 발화 차단. (c) P1: Initialize/BuildForBattle에 cachedNameLabel/cachedShinySparkle/despawnedThisCycle 리셋 추가 — 풀 재사용 시 stale Transform 참조 회피. (d) P1: cachedMainCam과 일괄 처리로 NameLabel 미존재 인스턴스 매 프레임 Find 절감. new Vector3/Color는 struct stack 거짓양성, BuildModel/AnimateWings/AnimateShinySparkle 내부 알고리즘은 visual-dev 영역.
- [x] CameraFollower (P1:2, 2026-05-21) — (a) SetTarget에 baselineValid=false 리셋 추가 — SubArea 좌표 전환 시 옛 baselinePos 기준 Lerp로 첫 프레임 카메라 점프 회귀. (b) EnterBattleMode의 battleTransition=0 무조건 리셋 제거 — fade-out 진행 중 재진입 시 transition 보존으로 시각적 끊김 차단(배틀→이탈→배틀 빠른 전환). new Vector3 다수는 struct stack 거짓양성, Shake 다중 호출 timer 리셋은 의도된 동작.
- [x] InsectDatabase (P1:1, 2026-05-21) — GetWeightedRandom/GetWeightedRandomWithRareBoost 4곳 foreach에 `if (data == null) continue;` 가드 추가. GetCandidates는 null 필터링하나 외부에서 직접 candidates 전달(테스트/디버그 코드) 시 data.spawnWeight NRE 위험. insects 중복 insectId/null/빈 ID 검증은 ScriptableObject Editor 시점 보장(P2), Weight 두 번 계산은 결과에 영향 없음(P2), 부동소수점 누적 오차는 일반 범위에서 무해 + fallback 있음, new List 매 호출은 InsectSpawner 8초 burst 없음.
- [x] OutfitSetData (clean, 2026-05-21) — 진짜 P0/P1 회귀 없음. GetAllSets()는 OutfitBonusProvider.AutoWire에서 1회만 호출되어 allSets 필드에 캐시(매 프레임 호출 거짓양성). 12개 세트 정적 데이터 정합성 시각 review 결과 정상: requiredItemIds 중복/null 없음, partialThreshold ≤ requiredItemIds.Length 모두 만족(set_wizard threshold=2 items=4), partial/full Bonus 모두 정의, setColor 시각용 OK. GetAllSets static readonly 필드화는 변경 risky(P2 보류). Editor 시점 검증 도구는 별도 디자인 작업. Covered 이동만.
- [x] PlayerItemInventoryGridUIController (P1:1, 2026-05-21) — HandleActiveChanged에서 UpdateRemainingTime() 즉시 호출 + remainingTextTimer=0 리셋 추가. 옛은 activeItemText만 갱신하고 남은 시간 표시는 Update의 1초 디바운스 대기 → 만료 시 "남은 시간: 00:00" 표시 최대 1초 지연. 주석(line 56)이 "만료 시점은 ActiveItemChanged 즉시 처리" 명시했으나 실제 호출 누락 회귀. OnEnable/AutoWire 이중 구독은 -=/+= 패턴으로 안전(거짓양성), BuildGrid Destroy+Instantiate 1프레임 중복은 정상 Unity 동작, EnsureDatabase 매 진입은 null 가드로 1회만 Load.

## Round Log

- 2026-05-20: 누적 15개 시스템 처리. P0:5, P1:16, P2:1, 총 ~22건 수정. 회귀 자체 발견 6건(매번 즉시 수정).
- 2026-05-20: RaidBattleUI — P0:2 (EndRaid 타이머/캐시 누수), P2:1 (Find Player 캐싱). 거짓양성 1건 (DrawUniteGaugeBar 게이지 정합성 — CanUniteAttack 내부에 AliveCount 검사 이미 포함).
- 2026-05-20: BattleScreenUI 나머지 — P1:2 (DrawSwapSelect new Color 매 프레임 → SwapHeaderBase static, DisableCanvasBattleUI FindFirstObjectByType 캐싱), P2:1 (CheckGuardianDefeat FindFirstObjectByType<RegionManager> 캐싱). Explore P0 분류는 호출 빈도 1회/배틀이라 P1로 재분류.
- 2026-05-20: RaidBattleController — P1:1 (UseUniteAttack 데미지 표시 정합성). 거짓양성 3건: 라인 157 UniteGauge AOE 가드는 이미 `if (LastDamageToBoss > 0)` 있음, 라인 231 KO 자동전환은 정상, 라인 203 +18 고정값은 의도된 디자인.
- 2026-05-20: 곤충 Despawn 회귀 (사용자 명시 요청) — P0:4건. (a) InsectBattleController 패배 (b) InsectBattleController 도주 성공 (c) CaptureController 캡처 실패 50%→100% (d) RaidBattleController 레이드 패배. 곤충 숫자(maxActiveTotal=20)/레벨(메인 power=3.5, sub=2.0)/위치(60m 디스폰, 8초 재배치) 점검 — 모두 정상.
- 2026-05-20: F11 unstick 안 됨 → F9로 변경 (OS 인터셉트 회피) + frozen 상태 우회 + OnGUI Event 백업. 도구 mesh 회귀 (P1:1) — ApplyToolShape이 scale만 변경하고 mesh는 Cylinder 고정 → 어떤 도구든 막대기. 도구별 PrimitiveType (Cube/Cylinder/Sphere) 교체 + sharedMesh 캐싱.
- 2026-05-20: CaptureFeedbackController — P0/P1 회귀 없음. 이벤트 구독/해제 짝 정상, null 가드 모두 있음, OnGUI 미사용(TextMeshPro). Covered로 이동만.
- 2026-05-20: 튜토리얼 진행 회귀 (사용자 명시 요청) — P0:4건. q_equip/q_item/q_training/q_team이 Notify 호출 누락으로 영구 정지 → 후속 13개 퀘스트 연쇄 정지. (a) BattleTeamManager.TeamChanged 구독 + OnTeamChanged 핸들러 (b) ItemEffectManager.ActivateItem 단일 진입점 NotifyItemUsed (c) TrainingManager.TrainSkill 성공 NotifyTraining (d) TrainingUI 사용자 EquipSkill 후 NotifySkillEquipped. 정상 동작 9개 (Movement/Capture/BattleWon/GuardianDefeated/DexOpened/CollectionOpened/LevelUp/RaidCompleted/Region) 확인.
- 2026-05-20: BattleArenaController — P1:1 (OnDisable ↔ CleanupArena 중복 → 위임으로 단일화). 거짓양성 2건 (Rect struct 매 프레임 new Rect는 stack 할당이라 GC 없음, SetupNormalBattle 첫 줄에서 CleanupArena 호출되므로 rendererCache 이미 Clear됨). P2:1 (effectText shadow new Color 매 프레임 — struct로 영향 작음).
- 2026-05-20: DexController — P0/P1 회귀 없음. Explore 4건 모두 거짓양성 또는 범위 외. SaveAndNotify는 RegisterEncounter/Capture 끝에 무조건 호출. encounter/capture 별개 dictionary로 race 없음. Covered 이동만. DexListUIController는 별도 audit 대상으로 Uncovered 추가.
- 2026-05-20: DexDetailUIController — clean. Canvas 기반 UI라 OnGUI new GUIStyle 회귀 없음, 이벤트 구독 없음, null 가드 견고. Explore가 부수적으로 DexScreenUI의 OnGUI new GUIStyle 다수 P0 발견 → Uncovered 1순위로 분리.
- 2026-05-20: DexScreenUI — P0:31 (매 OnGUI new GUIStyle 31개 + Color 다수). InitDexStyles + 캐시 필드 31개 + 정적 Color 38개. OnGUI 본문 잔존 new GUIStyle 0. 매 프레임 GC 객체 105~150 → 0. 단일 파일 819 → ~1080줄로 증가하나 모놀리스 아님.
- 2026-05-20: SubArea UI + 도구 미리보기 정합성 (사용자 명시 요청). (a) SubAreaWorldBuilder — 진입/퇴장 토스트 알림 (3초 페이드, NotifyEnter/Exit 색), F2 수동 Exit 단축키 (Input + OnGUI Event 백업), 우측 하단 안내 + RegionManager.ForceExitSubArea(). (b) CharacterPortraitRenderer — DrawCharacterTool 9종 분기 신규 (옛은 magnify만), 필드/카드/캐릭터 미리보기 3곳 분기 순서 1:1 정합.
- 2026-05-20: SubArea 바닥 빠짐 P0:8 (사용자 명시 요청). 9개 환경 boundary wall halfSize를 floor 안쪽으로 조정 (Cave 16→14, DeepForest 18→16, Underwater 16→14, FogSwamp 16→14, MountainPeak 14→11, Temple 14→13, Reeds 14→11, GenericArea 12→9), BuildGreenhouse CreateGlassWall NoCollider 제거(유리 벽 collider 보존), Y 안전망(Y<-3 → 자동 RequestExit + 알림 + cachedPlayerTransform lazy 캐싱). 첫 위치 끼임 재검증: FindSafeSpawnPosition spiral radius 7m가 새 벽(halfSize 9~16) 안에 안전하게 위치 — 회귀 없음.
- 2026-05-20: DexListUIController — clean. Explore P0/P1 거짓양성 (SortList null 가드는 BuildList early return으로 보장, AutoWire 이벤트 짝 정상). Covered 이동만.
- 2026-05-20: 인증/로그인 흐름 (사용자 명시 요청) — P1:2. (a) AuthManager.ValidateCredentials 헬퍼 — 빈 이메일/짧은 비밀번호 Firebase API 호출 전 차단, AuthFailed + LoginCompleted/RegisterCompleted false 이중 보고. (b) LoginUI OnGUI 17개 GUIStyle 변형 → base.fontSize 동적 갱신, OnGUI 본문 new GUIStyle 0. 인증 흐름 전반 검증 (TryAutoLogin race 없음, idTokenAcquiredAt 모든 경로, AuthFailed→Login 복귀, 회원가입→CharacterCreate) 정상 확인.
- 2026-05-20: InsectSpawner — P1:2 자체 발견 (Explore P0:2 + P1:1 모두 거짓양성). OnGUI debugStyleCache lazy 캐싱(매 프레임 new GUIStyle), GetPlayerTransform lazy 캐싱(매 호출 FindWithTag+Find). CleanupDeadEntities 풀 회수 누락 거짓양성은 직전 라운드와 동일 (외부 SetActive(false) 직접 호출 없음 grep 검증).
- 2026-05-20: RegionManager — P1:3. IsRegionAccessible 마스터 우회 (AuthManager.IsMasterAccount race 차단), DefeatGuardian 중복 가드 + SaveUnlockState 비용 차단, LoadUnlockState unlockedRegions Split RemoveEmptyEntries (defeatedGuardians만 있던 옵션을 일관 적용). P2 Time.time vs realtimeSinceStartup은 수용.
- 2026-05-21: PlayerInsectCollection — P1:3 자체 발견. (a) Awake instanceId 중복 가드: 옛 세이브에 같은 GUID 인스턴스 2개 이상 있을 시 옛은 dictionary가 1개만 보존 → BattleTeam.GetByInstanceId가 나머지를 못 찾고 슬롯 표시 빈 칸. 충돌 감지 시 새 GUID 재발급 + needsSave 트리거. (b) GainXp(data, insect, amount) insect null 가드: 형제 GainXp(InsectData, int) 오버로드는 line 169에 null 가드 있으나 line 185는 insect.levelCurve 직접 접근 — NRE. 현재 호출자 0건이나 방어 코드 유지. (c) GetAllOwned null sanitize: saveData.insects에 null 항목 있을 시 호출자 6곳 NRE 위험 — foreach + null skip. 슬롯 인덱스 race는 Unity 단일 스레드 + BattleTeamManager.SetSlot 중복 가드(line 59-63) + ResolveLegacyOrInstanceId 정규화로 거짓양성. 가챠 10연 디바운스 저장은 의도된 디자인(OnApplicationQuit/Pause flush 있음).
- 2026-05-21: PlayerProgressController — P1:3. (a) Awake sanitize (level 1~maxLevel, currentXp ≥ 0 클램프 + dirty Save) — 손상 세이브 음수 level/xp 진입 차단. (b) 만렙 도달 시 currentXp = 0 클램프 — UI Lv 50 / XP N>0 불일치 차단. (c) 만렙 후 GainXp early return — 잉여 누적 + 무의미한 Save/SaveCloud 차단. 디스크 디바운스는 호출 빈도 낮음(배틀/캡처/튜토리얼만 burst 없음)으로 P2 수용. CloudSaveManager 가드는 SaveToCloud 내부 3중 가드로 거짓양성.
- 2026-05-21: PlayerProgressSaveService — P1:1. Save(null) 가드 추가. static service라 임의 호출자가 진입 가능, JsonUtility.ToJson(null)이 빈 문자열 → 빈 파일 → 다음 Load 데이터 손실. atomic write 부재는 다른 SaveService 모두 동일 패턴이라 글로벌 라운드로 이관 (P2 보류).
- 2026-05-21: PlayerCandyInventory — P1:2. (a) AddCandy/SpendCandy data null 가드 (Candies 프로퍼티는 가드 있으나 변경 메서드 비대칭). (b) Save(null) 가드 — PlayerProgressSaveService와 일관성. 호출자 6곳 외부 가드 OK.
- 2026-05-21: PlayerCurrencyWallet — P1:5. AddGems/SpendGems/AddCoins/SpendCoins 4곳 + Save() data null 가드. Gems/Coins 프로퍼티(line 18-19)와 비대칭 회귀 복구. Save 안의 CurrencyChanged 발화도 null 전파 차단. CashShopManager 단일 소스 패턴 정합 유지.
- 2026-05-21: PlayerItemInventory — P1:3. (a) Awake sanitize: null record / 빈 itemId / 중복 itemId 합치기 — 옛은 중복 시 lookup이 마지막만 보존해 AddItem 변경이 stale로 동기화 깨짐 (드물지만 손상 세이브/옛 마이그레이션 시 가능). (b) GetSnapshot 얕은 복사 — DexScreenUI:659 외부 호출자 race 차단. (c) Save(null) 가드 — 다른 SaveService와 일관성.
- 2026-05-21: PlayerInsectLevelUpUIController — P1:3. (a) Refresh current null 가드 — selectedInstanceId stale 시 NRE. (b) OnEnable/AutoWire 이중 구독 차단 (subscribed 플래그 + Subscribe/Unsubscribe 헬퍼). (c) AutoWire isActiveAndEnabled 가드 — OnDisable 동안 구독 잔존 차단.
- 2026-05-21: ShopUIController — P1:1. Start line 65 itemDatabase null 가드 — Resources.Load 실패 시 FindById NRE. UpdatePriceLabels dead code(allowCoinPayment 런타임 불변)는 P2 보류. 토글 무한 루프, 인덱스 OOB, CashShop race 모두 거짓양성.
- 2026-05-21: GachaBoxUI → CashShopUI 가챠 영역 — P0:11. GachaBoxUI 클래스 자체 미존재(CashShopUI 통합). DrawGachaTab+DrawBoxCard+DrawGachaResultScreen new GUIStyle 11개+new Color 5개 → InitGachaStyles + 12 캐시 + 5 static readonly Color. boxPriceStyle/gachaTitleStyle textColor 동적 갱신. Phase 2 new Color/Rect는 struct stack로 거짓양성. CashShopUI Tab 0/Tab 1 영역은 별도 Uncovered 추가.
- 2026-05-21: ModalUIRegistry — clean. 진짜 회귀 없음. IModalUI 구현체 7개 OnDisable Unregister 일관, HandleEscape PlayerMovement:127 단일 진입점, Register LRU/IsAnyOpen 자동 정리/Unregister null 가드 모두 정상. Covered 이동만.
- 2026-05-21: CashShopUI 보석 충전/아이템 상점 영역 — P0:16. Tab 0/Tab 1 + OnGUI 본문 new GUIStyle 16개 + new Color 다수. InitMainStyles + 16 캐시 필드 + 14 static readonly Color. resStyle/itemPriceStyle textColor 동적 갱신. Phase 2 new Color(alpha 동적)와 new Rect(좌표 동적, struct stack)는 거짓양성. OnGUI 본문 잔존 new GUIStyle = 0. 같은 라운드에 가챠 영역(P0:11) + Tab 0/Tab 1(P0:16) 총 P0:27 처리.
- 2026-05-21: UIHelper — P0:1. GetCachedTex 캐시 256개 도달 시 texCache.Clear() 후 styleCache.Clear() 동반 호출 추가. 옛은 GUIStyle.normal.background이 Destroy된 텍스처 stale 참조 → MissingReferenceException 또는 invisible 버튼. GetButtonStyle이 정확히 그 패턴. new Color/Rect struct는 stack로 거짓양성.
- 2026-05-21: AudioManager — P1:1. StopBGM에 deferredBgmCoroutine 중단 추가. 옛은 PlayBGM → 즉시 StopBGM 시 deferred coroutine이 다음 프레임에 CrossfadeBGM 시작 → StopBGM 의도 깨짐 + 페이드 충돌. FindFirstObjectByType 1초 폴링 / ApplyVolumes fade 덮어쓰기 / gameClock stale / 다중 PlayOneShot 모두 거짓양성 또는 P2 수용.
- 2026-05-21: ProceduralAudioGenerator — clean. 1957줄 모놀리스이나 GetBGM/SFX/Ambient + 단일 Dictionary 캐시 패턴 일관, generator 본문은 캐시 hit 시 호출 안 되어 audit 범위 외. Covered 이동만.
- 2026-05-21: InsectEntity 외부 로직 — P0:1 + P1:3. (a) Camera.main 매 프레임 → static cachedMainCam (최대 20마리 핫패스). (b) Despawn 다중 호출 가드 (Battle/Capture 동시 호출 시 풀 중복 반환 + onDespawn 이중 발화 차단). (c) Initialize/BuildForBattle cache 리셋 (풀 재사용 stale Transform 회피). (d) cachedMainCam과 NameLabel Find 일괄 처리. BuildModel/AnimateWings/AnimateShinySparkle 내부는 visual-dev 영역.
- 2026-05-21: CameraFollower — P1:2. (a) SetTarget baselineValid=false 리셋 — SubArea 전환 시 첫 프레임 점프 차단. (b) EnterBattleMode 무조건 transition=0 리셋 제거 — fade-out 중 재진입 시각적 끊김 차단.
- 2026-05-21: InsectDatabase — P1:1. GetWeightedRandom/GetWeightedRandomWithRareBoost 4곳 foreach data null 가드. 외부 직접 candidates 전달 시 spawnWeight NRE 차단. 중복 ID 검증/weight 재계산/부동소수점 오차 모두 거짓양성 또는 P2 수용.
- 2026-05-21: OutfitSetData — clean. GetAllSets는 AutoWire 1회 호출 후 캐시. 12 세트 정적 데이터 정합성 정상. Covered 이동만.
- 2026-05-21: PlayerItemInventoryGridUIController — P1:1. HandleActiveChanged에 UpdateRemainingTime + remainingTextTimer 리셋. 옛은 만료/시작 시점 "남은 시간: 00:00" 표시 1초 지연. 주석이 즉시 처리 명시했으나 실제 호출 누락 회귀.
- 2026-05-21: SaveService 글로벌 atomic write — P1:8 + 신규 38줄. AtomicFileWriter 헬퍼(.tmp 쓰기 + File.Replace OS atomic) 신설 + 8곳 File.WriteAllText 일괄 교체(PlayerProgress/Candy/Currency/Item/InsectCollection/BattleTeam/Dex Save + CloudSaveManager.SaveLocalFile). 게임 종료 직전 크래시 시 부분 쓰기 손상 차단. CashShopManager는 PlayerPrefs라 제외. 누적 SaveService 일관성 100%.
- 2026-05-21: CollectionUI 핫스팟 — P0:1 + P1:3. DrawInsectItem 5 GUIStyle + 4 Color 캐시(InitItemStyles + 6 static readonly), GetAllOwned 캐싱(cachedOwned + InsectUpdated invalidate), OnEnable/OnDisable/AutoWire 이벤트 짝 + AutoWire isActiveAndEnabled 가드, DrawCenteredLabel new Color 2곳 static. 잔여 DrawPanel/Detail/Stats/LevelUp/StatBar는 별도 라운드(Uncovered 추가).
- 2026-05-21: TrainingUI 핫스팟 — P0:1 + P1:3. DrawInsectSelect loop 4 GUIStyle + 4 Color 캐시(InitInsectSelectStyles + 4 static readonly), GetAllOwned 캐싱(CollectionUI 동일 패턴), OnEnable/OnDisable/AutoWire 이벤트 짝. 잔여 DrawPanel/MethodSelect/SkillLearn/SkillEquip/SkillReplace는 별도 라운드(Uncovered 추가).
- 2026-05-21: CollectionUI 잔여 영역 — P1:27. DrawPanel/DrawDetailPanel/DrawStats/DrawLevelUpSection/DrawStatBar/DrawCenteredLabel 27 new GUIStyle 캐시화. InitDetailStyles + 25 캐시 필드 + 25 static readonly Color. DrawStats GetCachedOwned 추가. OnGUI 본문 잔존 new GUIStyle = 0. CollectionUI 핫스팟+잔여 합산: P0:1 + P1:30 (전체 처리 완료).
- 2026-05-21: TrainingUI 잔여 영역 — P1:34. DrawPanel/Back/MethodSelect/SkillLearn/SkillEquip/SkillReplace/SkillCard/Feedback 34 new GUIStyle 캐시화. InitTrainDetailStyles + 33 캐시 필드 + 38 static readonly Color. 8 메서드 textColor 동적 갱신. OnGUI 본문 잔존 new GUIStyle = 0. TrainingUI 핫스팟+잔여 합산: P0:1 + P1:37 (전체 처리 완료).
- 2026-05-21: PlayerStatusHUD — P0:1 + P1:2. OnGUI 본문 new Color 16곳 → 16 static readonly Color. GetAllOwned 캐싱(cachedOwnedCount + InsectUpdated invalidate), OnEnable/OnDisable에 InsectUpdated 짝 추가(subscribedInsects 플래그). GUIStyle 캐시는 이미 InitStyles 패턴으로 기존 구현됨. shine/regionCol scaled/accent scaled/alert alpha 동적 new Color는 struct stack 거짓양성.
- 2026-05-21: TrainingManager — P1:1. TrainSkill의 SpendCandy를 LearnSkill/ReplaceSkill 성공 후로 이동 + 실패 early return. 옛은 ReplaceSkill 실패 시 candy 손실 위험. LearnSkill은 사전 가드로 실패 불가이나 일관성 차원에서 동일 패턴 적용.
- 2026-05-22: CashShopManager 잔여 — P1:3. PurchaseWithGems의 잔액 검사(line 119)와 환불 기준값(line 137) 모두 stale `gems` 캐시 → `Gems` 프로퍼티로 교체(wallet 단일 소스 일관성). AddGems(0) early return 추가로 무의미한 GemsChanged 발화 차단.
- 2026-05-27: PlayerMovement 모달 차단 + InputAction 흐름 — clean. Explore 8 책임 매트릭스 점검 결과 진짜 P0/P1 회귀 0건. 모달 차단(HandleEscape+IsAnyOpen 이중 방어), InputAction(Input.GetKey+OnGUI Event 백업 이중 경로), OutfitChanged 동기(2026-05-20 완료), CameraFollower 점프(2026-05-21 완료) 모두 견고. FindFirstObjectByType 0건, transform.Find 1회 캐싱 정상, outfitBonus null 가드 있음. Covered 이동만. **누적 Uncovered 0개 도달.**
- 2026-06-06: **모델 심화 잔여 + 치비 폴리시 2차**. **모델**: Butterfly(몸통 0.15,0.4→0.18,0.34 짧고통통·머리 0.25→0.3 확대·WingTip 0.12,0.18→0.18,0.24 둥글게·눈 z 0.4→0.45), Bee(MakeTopGloss 광택). 날개는 ApplyColor 반투명 경로의 _Glossiness 0.2로 자동 무광. **치비 폴리시(감사 통합목록 P1)**: ① 필드 HairTop 정수리 덮기(ShortHair y0.15→0.22 scale 0.52,0.3,0.5→0.62,0.34,0.60 / UpHair y0.2→0.26 scale→0.55,0.30,0.52) 두피 노출 해소 ② 포트레이트 모자 본체 높이 8s→headH*0.22·오프셋 headH*0.20(두 곳, 큰 머리 정수리 덮음) ③ 포트레이트 헤어 윗덮개 10~12s→headH*0.32~0.36·시작 headH*0.10(얇은 띠→볼륨) ④ 포트레이트 도구 손위치 handY 58s→36s(짧아진 팔 armH 34와 정합, 옛 손보다 24s 아래 허공) ⑤ 카메라 lookTarget 1.0→0.85(치비 가슴~목 중앙, 머리 위 여백↓; 시선벡터 (0,-0.8,0.6) 불변→도구 디스크 정합 유지). **잔여(저우선/위험)**: 필드 도구 스케일(머리높이라 비긴급), 포트레이트 안경렌즈/옆가닥/toolX/bag_science, 필드 헤어 옆·뒷가닥 y, 콜라이더/OverlapSphere 높이(자기가드 회귀 위험). 8항목 PASS.
- 2026-06-06: **곤충 3D 모델 심화** (설계서 `_dex_modelPolish.txt` 적용). **공용 헬퍼(전 34종 동시)**: ① dark 음영색 단순절반→HSV(채도 유지·명도만 ↓)로 색감 살림 ② MakeAntennae 직선 캡슐1개→2분절 굴절 S곡선 + feathered 깃털(나방/모기) ③ MakeLegs 발끝(tarsus) 6개 추가 + 앞·중·뒷다리 각도 변주(upAng 26+4i/loAng 8+3i)·z 부채꼴(기계적 동일각 해소) + 무릎 0.045→0.05 ④ MakeEyes xSpread 파라미터화(넓은 머리 종 대응, 기본 0.12 무변경) ⑤ MakeTopGloss 헬퍼 신설(등껍질 상단 반투명 광택). **대표 종**: GenericBeetle(가슴마디 prothorax 추가+머리 확대 0.4→0.46+직접눈→MakeEyes+TopGloss), HornBeetle(직접눈→MakeEyes xSpread 0.2), RhinocerosBeetle(뿔 3분절 곡선 HornCurve+Y자 분기 HornFork L/R+직접눈→MakeEyes 0.22). 직전 ApplyColor PBR·MakeEyes 하이라이트/글린트와 합쳐 무광 점토→광택 키틴+촉촉한 큰 눈. 신규 노드명(TopGloss/Foot/AntBase·Mid/HornCurve·Fork/Prothorax) WingL/R·NameLabel 예약어 비충돌 확인. MakePart/풀/forBattle 보존, 수치공식無→테스트 영향 없음. 8항목 PASS. **참고**: ApplyColor가 파트마다 new Material(기존 구조, 본 변경으로 종당 +~10파트) — 머티리얼 캐싱은 별도 리팩터(보고만). **잔여**: Butterfly/Bee 대표종 심화(공용헬퍼+MakeEyes 자동 수혜라 우선도 낮음). 실측 Unity 권장.
- 2026-06-06: **도감에 진짜 곤충 3D 표시 완료** (직전 미완 ③ 해결 — dex-shape 핵심). 신규 `InsectModelPreviewRenderer`(Dex): 플레이 영역 밖(Y-5000) 전용 레이어 30에 직교 카메라+전용 광원(cullingMask 30, 월드 이중조명 방지) 리그. `InsectEntity.BuildForBattle`로 34종 3D 모델 재사용 생성→RenderTexture 렌더→insectId별 캐싱. 렌더는 OnGUI 아닌 Update에서 프레임당 1개 lazy(GUI 중 Camera.Render 회피), 모델은 enabled=false로 정적 프레임, 렌더 후 Destroy, OnDestroy에서 RT Release. DexScreenUI.DrawDetail: 포획 곤충은 박스 자리에 3D 프리뷰(ScaleToFit, 알파합성), 미준비/미포획은 기존 DrawTinyInsect 폴백(미포획 실루엣 reveal 로직 보존). Bootstrap EnsureComponent+AutoWire 등록. 직전 PBR 광택+촉촉한 눈 개선이 프리뷰에도 반영. 레이어 30 미사용 확인. 8항목 검증 PASS(new Rect는 struct 거짓양성). **실측은 Unity 에디터 권장**(orthoSize 1.1/회전 150°/조명 미세조정 가능).
- 2026-06-06: **곤충 도감 품질 개선 (동적 워크플로 동시 진행)** — 사용자 "도감 곤충 모양·내용 퀄리티 저하". 진단: (1)도감(DexScreenUI)이 곤충 자리에 단색 박스(whiteTexture)만 그림, 스프라이트 0개, 정작 InsectEntity 34종 3D 빌더는 도감 미사용. (2)InsectLore.json 64종 설명이 건조한 1줄. **완료**: ① **콘텐츠 64/64 리라이트**(InsectLore.json) — 1줄→2~3문장 매력·정확·재미요소 한글(워크플로 병렬 53종 + 직접 11종, 원본ID 머지로 중복제거·누락방지). ② **3D 모델 셰이딩 개선**(InsectEntity): ApplyColor에 PBR(_Glossiness 0.55/_Metallic 0.15, 셰이더명 가드, 날개는 0.2 무광) → 전 34종 무광 점토→키틴 광택 동시 개선(최고 ROI). MakeEyes 하이라이트 0.2→0.28·동공 0.6→0.64·서브 글린트 2개 추가(치비 촉촉한 눈). **세션 한도로 미완**: ③ 도감에 진짜 곤충 표시(RenderTexture 프리뷰) 에이전트 실패 — **도감은 아직 색 박스, dex-shape 핵심 미해결**(다음 단계, 설계 `.claude/_dex_modelPolish.txt` 참조). ④ 모델 심화(다리 발끝/더듬이 곡선/뿔 굴절/대표종) 설계만.
- 2026-06-06: **치비 부작용 감사 적용 (1차)** — 4-에이전트 감사로 치비 리디자인 부작용 23건 도출(포트레이트 디테일 고정크기, 액세서리 허공, 도구 과대, 헤어). **적용(P0)**: 포트레이트 얼굴 부품(눈/동공/하이라이트/코/입/귀/눈썹/볼터치) 전부 headW/headH 비례 전환(옛 7×8 고정 콩알눈 → headW×0.30 큰 치비눈, 필드와 정합). 필드 액세서리 Y(안경 2.20→1.20·목걸이 1.88→1.00·배지 1.65→0.85) 허공 부유 수정+안경 확대. **잔여**: 필드 도구 과대 스케일, 헤어 정수리/옆가닥, 포트레이트 모자/헤어/도구 비례, 카메라 lookTarget, 콜라이더 등 ~20건(P1/P2, 통합목록 워크플로 산출).
- 2026-06-06: **필드 캐릭터 귀여운 치비 리디자인** (사용자 명시 "필드 캐릭터 너무 못생기고 매력없음 개선"). 방향: 사용자가 ~3.5등신 치비 선택. 원인 분석: 사실적 ~6.8등신을 단순 프리미티브로 표현 → 작은 머리+긴 박스 몸통+막대 팔다리+달걀형 머리+작은 눈 = 뻣뻣·비매력. 변경(PlayerVisualBuilder.BuildAll+BuildFace): (a) headPivotScale 0.42→0.60(머리 확대, 얼굴부품 자식이라 함께 스케일=배치 자동유지), (b) 머리구체 달걀형(−0.10,+0.12,−0.04)→둥글게(−0.02,−0.04,−0.04) **Z유지로 얼굴 z재배치 회피**, (c) 눈 0.11→0.15/0.17·동공/하이라이트 확대(큰 눈), 코 축소·입 하향·볼터치 확대, (d) 몸통 Y0.78→0.46 짧고통통, 팔다리 단축(arm 0.50→0.23, leg 0.62→0.20)+좁은스탠스, 목 최소화, head Y2.20→1.22 → ~3.5등신(총높이 ~2.38→~1.43). 연동: 손 Y0.95→0.52 → ApplyToolShape hx/hy + 도구 기본좌표 동기, 카메라 (0,12,-8)→(0,9,-6) 줌인(**Y:Z=1.5 비율유지로 시선벡터·도구 디스크 회전 -20° 보존**)+lookTarget 1.4→1.0, 포트레이트 CalculateProportions도 치비(head42/body40/leg40, ~3.3등신)로 창=필드 정합, OutfitUI 미리보기 2.4→2.9. 노드명 전부 보존(ApplyPartColor/AnimateWalk 무영향). **잔여**: CashShopUI 미리보기 scale(박스폭 미확인으로 미변경), 헤어 정수리 미세 노출 가능, 콜라이더 height 2 유지(치비보다 큼, 자기가드 회귀 회피). 실측은 Unity 에디터 권장.
- 2026-06-06: **도구 필드 vs 캐릭터창 불일치 + "막대기 뒤에 보임"** (P1:6). 사용자 보고 "캐릭터창과 필드 도구가 매칭 안 됨, 도구 끼면 막대기가 뒤에". 3-에이전트 적대검증(high). 원인: 필드 잠자리채 망(NetRing) 디스크가 rot(0,0,90)이라 법선이 ±X(옆) → 부감 카메라(시선 0,-0.8,0.6)에서 edge-on(테두리만)으로 collapse → 망 소멸, 손잡이 Cylinder만 막대기로 잔존. 창(DrawCharacterTool 2D)은 정면 사각 망을 또렷이 그려 불일치. lasso(rot 0,0,70)·magnify(rot 60→투영12%)도 동일 결함. 추가: 손 기준상수 hx/hy(0.34,0.91)가 실제 HandR(0.29,0.95)와 어긋나 도구 분리, PlayerVisualBuilder 기본 NetHandle/NetRing 좌표가 머리높이(2.28)·뒤(Z<0) stale 레거시. 수정(CharacterOutfitManager.ApplyToolShape + PlayerVisualBuilder.BuildAll): (a) hx/hy → (0.29,0.95) 손 정합, (b) net/lasso/magnify 디스크 rot → (-20,0,0)로 법선을 위-카메라쪽 정렬(워크플로 제안 rot(37) 부호오류를 기하 재검증으로 -20 보정 — 플레이어 전 방향에서 57~95% 가시, swing ±25° 더해도 edge-on 없음), (c) 빌더 기본 NetHandle/NetRing을 net 분기 최종값과 일치(레이스 잔재 제거+안전망). 필드를 source of truth로 고쳐 창과 자동 정합. **잔여(경미)**: gun/blaster/tranq 필드 정면뷰 vs 2D 측면 권총, shuriken Y축45° 수평, web_shooter 발사구 방향 — 별도 라운드 후보. net/golden/diamond 등급이 형태 미구별(색만 차이)도 디자인 개선 여지.
- 2026-06-06: **숲/유적/산 캐릭터 차폐 — 진짜 근본원인 최종 확정·수정** (P0:1). 사용자 재신고 "고대유적과 숲에서 캐릭터 위 레이어가 점령해 안 보임". 3-에이전트 적대검증(반증 실패, confidence high) 결과: 차폐 주체는 장식물이 아니라 **상승된 리전 지면 평면 그 자체**. WorldTerrainBuilder.ApplyElevation이 Region_{id} 평면을 forest Y=4.08 / ruins Y=8.08 / mountain Y=12.08로 끌어올리는데, PlayerMovement는 아래방향 2유닛 raycast로만 지면을 따라가 4~8유닛 단차를 못 올라 Y=0에 갇힘 → 불투명 collider 평면이 카메라(0,12,-8) 하향 시선과 플레이어 사이를 가림. CameraFollower.ResolveObstruction은 Region_/Ground/Floor를 차폐검사에서 제외해 보정 불가. **이 고도는 리전판정(ContainsPoint=XZ만)·스폰(player.y 추종)에 영향 0인 순수 장식**이라 확인 → 2026-05-27 이중합산/장식축소/카메라오프셋 등 6+ 라운드 시도가 모두 증상만 건드려 실패한 진짜 원인. 수정: WorldTerrainBuilder.GetRegionElevation의 forest/ruins/mountain을 0f로 평탄화(pond -3/swamp -2 보존), BuildElevationSlopes의 Slope_Meadow_Forest(0→0)·Slope_Mountain_Ruins(0→0) 허공 램프 잔재 정리. 평면·장식·플레이어·곤충 모두 Y=0 수렴. **남은 권고**: GetRegionElevation 회귀 테스트(private+MonoBehaviour라 InternalsVisibleTo 또는 ApplyElevation 통합 테스트 필요 — architect 상담), PlayerMovement.cs:253 Mathf.Max 상향전용 스냅+scenery collider 핀고정 2차 증상(경미, 별도 라운드).
