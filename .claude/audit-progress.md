# Audit 진척

`/audit` 스킬과 훅 2개(`audit_flow_inject`, `audit_reminder`)가 읽고 갱신합니다.
**구조 유지 필수** — 특히 `## Uncovered`의 `- [ ]` 개수가 자동 플로우의 트리거입니다.

- **Covered**: 처리 완료 영역 인덱스. 서술 원문은 `.claude/audit-archive/covered-detail.md`
- **Uncovered**: 다음 audit 후보 큐. 위에서 아래로 우선순위.
  **비면 `python -X utf8 .claude/scripts/audit_candidates.py --emit-md`로 재생성**
- **Round Log**: **최근 5건만** 둔다(이 줄이 개수의 단일 출처). 넘치면
  `.claude/audit-archive/round-log-2026H2.md`로 이관 — 아카이브는 날짜순 정렬

## Covered (P0/P1 처리 완료)

- [x] CharacterPortraitRenderer (P0:2, P1:2, 2026-05-20)
- [x] PlayerVisualBuilder (P1:5, 2026-05-20)
- [x] CloudSaveManager (P1:2, 2026-05-20)
- [x] PlaySceneBootstrap (P1:1, 2026-05-20)
- [x] CharacterOutfitUI (P1:2, 2026-05-20)
- [x] CharacterOutfitManager (P1:2, 2026-05-20)
- [x] PlayerMovement (P1:1, 2026-05-20)
- [x] CashShopManager (P1:1, 2026-05-20)
- [x] CaptureMinigameController (P0:1, P1:1, 2026-05-20)
- [x] GachaBoxManager (P1:1, 2026-05-20)
- [x] TutorialQuestManager (P1:1, P0:4, 2026-05-20)
- [x] BattleScreenUI (P2:1 DrawCombo + P1:2 P2:1 추가, 2026-05-20)
- [x] SubAreaWorldBuilder (P0:8 + UI:1, 2026-05-20)
- [x] AuthManager (P1:2, 2026-05-20)
- [x] LoginUI (P1:2, 2026-05-20)
- [x] RaidBattleUI (P0:2, P2:1, 2026-05-20)
- [x] RaidBattleController (P1:1, P0:1, 2026-05-20)
- [x] InsectBattleController (P0:2, 2026-05-20)
- [x] CaptureController (P0:1, 2026-05-20)
- [x] CaptureFeedbackController (clean, 2026-05-20)
- [x] BattleArenaController (P1:1, 2026-05-20)
- [x] DexController (clean, 2026-05-20)
- [x] DexDetailUIController (clean, 2026-05-20)
- [x] DexScreenUI (P0:31, 2026-05-20)
- [x] DexListUIController (clean, 2026-05-20)
- [x] SaveService 글로벌 atomic write 라운드 (P1:8, 2026-05-21)
- [x] CollectionUI 핫스팟 (P0:1, P1:3, 2026-05-21)
- [x] TrainingUI 핫스팟 (P0:1, P1:3, 2026-05-21)
- [x] CollectionUI 잔여 영역 (P1:27, 2026-05-21)
- [x] TrainingUI 잔여 영역 (P1:34, 2026-05-21)
- [x] PlayerStatusHUD (P0:1, P1:2, 2026-05-21)
- [x] TrainingManager (P1:1, 2026-05-21)
- [x] CashShopManager 잔여 영역 (P1:3, 2026-05-22)
- [x] InsectSpawner Spawn/Despawn 다중 호출 검증 (clean, 2026-05-22)
- [x] RegionMapUI 깊은 점검 (clean P0, P1 보류, 2026-05-27)
- [x] TutorialQuestUI DrawQuestPanel 캐시 (P0:1, 2026-05-27)
- [x] WorldChannelManager 깊은 점검 (clean P0/P1, 2026-05-27)
- [x] **RegionTerrainBuilder c.y 이중 합산 버그
- [x] Forest/Ruins 환경 추가 축소
- [x] 메인 월드 Forest/Ruins region 환경 높이 (P1:2, 2026-05-27)
- [x] SubArea 덮개 가시성 (P1:2, 2026-05-27)
- [x] 어깨 추가 + SubArea 부유/환경 높이 후속 (P1:4, 2026-05-27)
- [x] 캐릭터 시각 버그 3종 (P1:3+구조 변경, 2026-05-27)
- [x] TutorialQuestUI 잔존 캐시 (P1:8, 2026-05-27)
- [x] CapturePopupUI 캐시 (P1:12, 2026-05-27)
- [x] RegionMapUI 캐시 + OnDisable 정리 (P1:28+1, 2026-05-27)
- [x] WorldLobbyUI MakeTex 누수 (P0:7, 2026-05-27)
- [x] CharacterOutfitUI infoStyle 캐시 + OnDisable 정리 (P0:1+P1:1, 2026-05-27)
- [x] CapturePopupUI 점검 (clean P0, P1:1 보류, 2026-05-27)
- [x] CharacterPortraitRenderer 깊은 정합성 (clean, 2026-05-27)
- [x] PlaySceneBootstrap 깊은 점검 (clean, 2026-05-27)
- [x] SubAreaWorldBuilder 깊은 점검 (clean, 2026-05-27)
- [x] TutorialQuestManager 깊은 점검 (clean, 2026-05-27)
- [x] BattleScreenUI 깊은 로직 (clean, 2026-05-27)
- [x] RaidBattleUI 깊은 로직 (clean, 2026-05-27)
- [x] BattleArenaController 이펙트 (clean, 2026-05-27)
- [x] PlayerMovement 모달 차단 + InputAction 흐름 (clean, 2026-05-27)
- [x] InsectSpawner (P1:2, 2026-05-20)
- [x] RegionManager (P1:3, 2026-05-20)
- [x] RegionTerrainBuilder + RegionMapUI (UI:2, 2026-05-21)
- [x] ItemEffectManager (clean, 2026-05-21)
- [x] WorldStateProvider (clean, 2026-05-21)
- [x] WorldChannelManager (P1:1, 2026-05-21)
- [x] BattleTeamManager (P1:2, 2026-05-21)
- [x] BattleTeamUI (P1:16+22, 2026-05-21)
- [x] PlayerInsectCollection (P1:3, 2026-05-21)
- [x] PlayerProgressController (P1:3, 2026-05-21)
- [x] PlayerProgressSaveService (P1:1, 2026-05-21)
- [x] PlayerCandyInventory (P1:2, 2026-05-21)
- [x] PlayerCurrencyWallet (P1:5, 2026-05-21)
- [x] PlayerItemInventory (P1:3, 2026-05-21)
- [x] PlayerInsectLevelUpUIController (P1:3, 2026-05-21)
- [x] ShopUIController (P1:1, 2026-05-21)
- [x] GachaBoxUI → CashShopUI 가챠 영역 (P0:11, 2026-05-21)
- [x] ModalUIRegistry (clean, 2026-05-21)
- [x] CashShopUI 보석 충전/아이템 상점 영역 (P0:16, 2026-05-21)
- [x] UIHelper (P0:1, 2026-05-21)
- [x] AudioManager (P1:1, 2026-05-21)
- [x] ProceduralAudioGenerator (clean, 2026-05-21)
- [x] InsectEntity 외부 로직 (P0:1, P1:3, 2026-05-21)
- [x] CameraFollower (P1:2, 2026-05-21)
- [x] InsectDatabase (P1:1, 2026-05-21)
- [x] OutfitSetData (clean, 2026-05-21)
- [x] PlayerItemInventoryGridUIController (P1:1, 2026-05-21)
- [x] WorldFieldMultiplayerUI (P0:1, P1:1, P2:2, 2026-07-17)
- [x] UIHelper 텍스처 캐시 오버플로 (P1:1, 2026-07-17)
- [x] AccountLinkUI (P1:3, P2:2, 2026-07-17)
- [x] SubAreaEnvironment (P1:1, 2026-07-17)
- [x] AccountSettingsUI (P0:1, P1:2, 2026-07-17)
- [x] SaveConflictUI (P0:1, P1 보류, 2026-07-17)
- [x] KeyGuideHUD (P1:2, 2026-07-17)
- [x] QuickAccessBarUI (P0:1, 2026-07-17)
- [x] SocialPvpUI (P0:1, P1:2, 2026-07-17)
- [x] SceneAutoWire (clean — dead code 확인 후 삭제, 2026-07-17)
- [x] VillageBuilder (P1:2, P2:1, 2026-07-17)
- [x] NpcManager (P1:1 + 튜닝 프로필 동반 수정, 2026-07-17)
- [x] NpcDialogueUI (P1:1, 2026-07-17)
- [x] CatcherKidNpc (P1:1, 2026-07-17)
- [x] VillagerNpc + NpcWalkAnimator (clean, 2026-07-17)
- [x] CaptureItemPickup + CaptureItemSpawner + CaptureProximityTrigger (clean — dead code 확인, 2026-07-17)
- [x] HospitalUI (clean P0/P1, P2:1 처리, 2026-07-19) — score 36(프레임할당9/싱글턴9) 둘 다 거짓양성 확인; 모달 ESC·결제 원자성·stale 아이템 등 회귀 7종 전부 회피(TrainingUI 패턴). P2(OnEnable 재구독) 후속 수정 완료
- [x] GuidedTutorialController (clean P0/P1, 2026-07-19) — score 15(프레임할당5) 거짓양성(Rect/Color struct + GUIStyle 1회 캐시); Start↔OnDestroy 페어링이라 SetActive 소실버그 무관, Dictionary 기반 진행이라 정지·범위초과 없음
- [x] StoryDirector (P1:1, P2:1 처리, 2026-07-19) — CaptureInsect가 InsectUpdated(포획+XP+치료+진화)를 오발화(치료경제가 악화) → 포획 전용 InsectCaptured 이벤트 신설(1884971). 싱글턴2 거짓양성(CloudSaveManager 가드 내). P2(렌더러 미배선 시 인트로 무음소모): deferredBeat 보류/구독 시 flush로 수정(c6f7fc4)
- [x] GameConstants (P0:0, P1:2, 2026-07-22) — 6스킬 상수의 stale 테스트를 6/7번째 기준으로 정정하고 story_progress.json을 SaveScope v4 마이그레이션·삭제 대상에 등록
- [x] PlayerProgressUIController (P0:0, P1:1, 2026-07-22) — 런타임 텍스트 참조 바인딩 후 현재 레벨·XP·사탕 값을 즉시 다시 그리도록 Refresh 경로 추가
- [x] InsectElement (clean P0/P1, 2026-07-23) — 전체 enum 상성 매핑, 이중 타입·중복 타입 처리, 미등록 enum 중립 폴백 및 1v1·레이드 호출부의 None 폴백 확인
- [x] InsectExpansionDefinitions (clean P0/P1, 2026-07-24) — 신규 ID 64개의 유일성·기존 ID 비중복·리전/서브에리어 풀 배정과 insectId 저장 호환성 확인
- [x] InsectLoreService (clean P0/P1, 2026-07-27) — DB/Lore ID 128/128 일치, 중복·누락 없음. AutoWire 순서와 정적 캐시 초기화·조회 경로 확인
- [x] NpcDialogueDatabase (clean P0/P1, 2026-07-31) — RegionLines 키 7개가 RegionManager 진행 체인의 리전 ID 7개와 완전 일치. FNV-1a 해시·음수 Mod·null 폴백 안전, GetLines/GetVillagerName은 대화 시작·NPC 생성 시 1회 호출(프레임 할당 아님)
- [x] CharacterOutfitUI 재감사 (clean P0/P1, 2026-08-17) — 좌우 배치 증분은 `Mathf.Max` 가드와 고정 가상 캔버스로 안전. 구독 0개라 OnEnable 불필요(OnDisable은 모달 상태만 정리), `new GUIStyle` 9건은 전부 `UIHelper.CachedStyle` 팩토리(키당 1회). **이번 세션의 `CharacterPortraitRenderer` 주변색 수정이 이 화면에서 온전히 동작함을 확인** — GUI.color 리셋이 OnGUI 맨 끝 위생 복구뿐이라 페이드가 초상 뒤에도 유지된다
- [x] DexScreenUI 재감사 (P1:5 처리 + 저장소 전수 스윕, 2026-08-17) — 증분은 clean. 직전 커밋이 남긴 단서("리터럴 문구는 text_fit_lint 대상이 아니다")를 따라가 **잘린 리터럴 라벨을 전 저장소에서 세는 스캐너**를 만들었다 — 82건(10%↑ 부족 36건, 13파일). DexScreenUI에서 이웃 레이아웃을 검산할 수 있는 5곳을 `LineH()`로 고쳐 77건(31건)으로 줄였다. 나머지는 전용 정리 라운드로 위임
- [x] PlayerMovement 재감사 (P1:1 처리, P1:1·P2:1 위임, 2026-08-17) — **처리**: frozen 안내 문구가 대화·컷신·연출까지 전부 frozen인 탓에 스토리를 읽는 내내 화면 아래에 남고, 그때 ESC는 모달을 닫는 키라 "이동 잠금 해제"가 틀린 안내였다 → `ModalUIRegistry.IsAnyOpen()` 가드. **위임**: 같은 문구와 리전 차단 문구가 `UIScale` 밖 픽셀 좌표라 스케일≠1 기기에서 크기가 어긋나고 제스처바 아래로 들어간다(고치려면 UI 계층으로 옮겨야 — Core→UI 금지). `hasReceivedInput`은 제거된 배너의 잔재로 CS0414가 매 빌드 발생. `ui_layout_lint`가 Rect 인라인 `Screen.height - N`을 놓치는 것도 확인(실제 3건)
- [x] CollectionUI 재감사 (clean P0/P1, P2:1 보고, 2026-08-17) — 정렬 증분(+112줄)의 핵심 불변식(정렬 순서 ↔ 행 문자열 캐시가 인덱스로 결합)이 무효화 2지점·재구축 1지점에서 모두 지켜짐. TeamChanged 구독으로 팀 변경 시 낡음 방지, 비교자는 instanceId 타이브레이크로 전순서 보장. 거짓양성 2건 철회. P2: BuildRowTextCache의 null 분기가 발화 불가(Sort가 상류에서 거름)
- [x] UIHelper 재감사 (P1:1, 2026-08-17) — `LabelFit`의 폰트 축소 캐시 키(`FitKey`)가 (텍스트·폭·높이·기준폰트)만 담아 **서로 다른 스타일이 답을 공유**했다. 측정은 `CalcHeight`/`CalcSize`라 폰트·볼드·wordWrap에 좌우되고, 특히 `checkWidth = !wordWrap`이라 가로 검사를 안 한 값이 가로로 넘치는 라벨에 쓰일 수 있었다(조용한 글자 잘림 — LabelFit이 막으려던 결함이 캐시로 되살아남). 참조 동일성을 키에 추가 + 회귀 테스트 5건. `AnimatePanelOpen`의 호출 계약(wasOpen 고착으로 두 번째 열림부터 페이드 소실)도 문서화
- [x] OpeningSceneController 재감사 (clean P0/P1, 2026-08-17) — 스타일 1회 가드, `Resources.UnloadAsset`(Destroy 아님) 정확, 구독 짝 3지점 정합, 일시정지 복귀 시 재생 클럭 리셋, 스킵/결과 보고 멱등(`resultReported`), 타임라인 상수 12개가 이미지 3장·내레이션 3줄에 빈 구간 없이 배분됨. score 66은 `EnsureStyles` 단일 가드 안의 할당이라 거짓양성
- [x] StoryDirector 재감사 (P1:1, 2026-08-17) — 발화가 `AllBeats()`(Dictionary.Values, 순서 비결정)의 **첫 일치**를 집어, 동시에 자격을 갖는 비트가 있을 때 실행마다 다른 비트가 뜨고 HUD 목표와 갈렸다. 실측 5쌍(어르신 대화에서 `ch1_intro`(1막 개막+프롤로그 컷신) vs `talk_elder`(잡담) 포함). 순위 규칙을 `StoryObjectiveResolver.CompareBeatPriority` 하나로 모아 목표 도출·발화가 같은 답을 내게 함
- [x] BattleScreenUI 재감사 (P1:1, 2026-08-17) — 직전 라운드의 P0 수정(`ConcludeDefeatWithoutSwap` 호출)이 `EnterResult` **재진입**을 만들었다: 종료 확정 → `BattleEnded` 발화 → `OnBattleEnded`가 다시 `EnterResult` → 결과 진입이 통째로 2회, 패배 SFX·BGM 겹침. 상태(`phase=Result`)를 Conclude **앞**으로 옮기고 `phase == Result` 조기 반환 추가. 명암비·LabelFit 증분과 신규 `BattleEffectTextOverlay`는 clean
- [x] StoryBeat (P1:1, P2:1 처리, 2026-08-17) — 데이터 모델은 clean(JsonUtility 관례·null 가드 전부 정상). 결함은 소비자 쪽 enum↔switch 드리프트: `StoryJournalUI.HintFor`의 문자열 리터럴 사본이 `GuardianDefeat`·`DexProgress`를 놓쳐 82비트 중 9건이 저널에서 "조건 미상"만 표시 → `StoryDirector.Trigger*` const로 교체 + 2케이스 추가. **P2 처리**: 읽는 곳 0이던 `oneShot` 필드 + JSON 키 82개 삭제(반복 구현은 앰비언트 비트도 캔디 5개를 줘서 무한 파밍이 되므로 배제), 재발 방지로 story_lint 검사 14(JSON 키 ↔ StoryBeat 필드 실재성) 신설 — 주입 4건 정확 검출
- [x] ItemRarityPalette (clean P0/P1, P2:1 처리, 2026-07-31) — ItemRarity 5값 ↔ switch 6개 정합. `.asset` 인스턴스 부재로 `Resources.Load`가 항상 null이었으나 소비자 3곳 전부 null 가드 + 하드코딩 폴백이라 무해했음. **P2 처리**: `ItemRarityPaletteBuilder`(에디터)로 `Resources/ItemRarityPalette.asset` 생성 + 그리드 아이템 프리팹에 팔레트 주입
- [x] PlayUIRefs (P1:1, 2026-07-31) — 90필드 ↔ 생성기 77대입 ↔ WireFromRefs 88읽기 대조. 미채움 13개 중 12개는 Text/TMP 이중화(의도된 설계)이나 `itemRarityPalette`만 짝 없이 비어 프리팹 경로에서 팔레트가 null → PlayUIPrefabGenerator에 로드 1줄 추가
- [x] NpcVisualBuilder (clean P0/P1, 2026-07-31) — NpcWalkAnimator가 Find하는 노드명 8개 전부 일치(직계 자식), 머티리얼 생명주기 3스폰 경로 모두 OnDestroy에서 CleanupMaterials 커버. NpcManager가 NPC를 파괴하지 않고 SetActive로만 토글하므로 OnDisable이 아닌 OnDestroy에 건 것이 정확
- [x] OpeningSceneController (P1:2, P2:1 처리, 2026-08-03) — score 48 "프레임 할당 16" 중 진짜는 `new GUIContent` 3개뿐(나머지 Rect/Color는 struct). 타이틀 구간 CalcSize 3회/패스를 화면크기 키로 메모이즈, OnDestroy가 재생 중인 AudioClip을 언로드하던 순서 수정, 매 프레임 PlayerPrefs 조회 제거
- [x] UISurface (P1:1, **P2:3 전부 처리**, 2026-08-03) — 스타일 헬퍼 6종이 `CachedStyle("리터럴"+fontSize, 캡처 람다)` 형태라 호출마다 문자열+클로저+델리게이트 3개 할당(적중해도). int 키 캐시 하나로 통합 + 폰트 크기 clamp. **P2 후속 완료**: ①`EvictAll`이 `Object.Destroy`로 텍스처를 파괴하고 `GetRoundedStyle`을 private으로 닫음(UISurface.cs:101-111) ②`Rounded`가 앰비언트 알파를 보존 ③얇은 것 전용 `Flat` 신설, `HospitalUI:155,241`이 사용

- [x] RaidRoundResolver (clean P0/P1, P2:1 보고, 2026-08-03) — 순수 정적 리졸버라 checklist 7항목 전부 해당 없음. 1v1과의 계약 대조에서 **버프·디버프가 레이드에선 영구 누적**(effectDurationTurns 미사용, RecalculateBonuses 부재)임을 확인. **P2 종결(2026-08-03 사용자 결정: 현행 유지)** — 이후 도입된 `MaxBuffStacks=3`이 방향별 상한을 줘 최악(보스 공격 0.3배 고정)이 유한해졌고, 만료를 넣으려면 장비 보너스와 전투 버프가 한 필드에 섞인 구조부터 분리해야 해 레이드 난이도가 실측 없이 오른다. 의도된 divergence로 코드 주석에 못박음

- [x] RaidRoundModels (P1:1, 2026-08-03) — 호출부 0인 `SetBossDamage`가 슬롯 기록 + TotalDamageToTeam 가산을 같이 해, 컨트롤러의 기존 합산과 겹치면 팀 피해가 두 배가 되는 함정이었다. 제거 + 재발 방지 주석

- [x] CaptureChanceCalculator (P1:1, 2026-08-03) — 레벨 차 상한(±5)이 전투 경로에만 있고 미니게임 경로엔 없어, 메인 필드가 항상 Lv.1부터 스폰하는 탓에 고레벨 플레이어의 저레벨 전설 포획이 사실상 보장됐다. 두 경로를 같은 MaximumLevelDelta로 통일

- [x] QuestRewardFormatter (P1:2, 2026-08-03) — GrantRewards가 적용하는 rewardInsectLevel이 표시에서 누락(q_approach의 Lv.6 장수풍뎅이가 종만 표시)되던 것 + HasAny가 포함 조건을 복사해 두 곳에 있던 것을 공유 술어로 통합

- [x] DexBrowseLayout (P1:1, **P2:1 처리**, 2026-08-03) — WrapIndex는 정확했으나 호출부가 항상 ±1이라, 좌측 1열 리스트→전체 폭 그리드 개편 뒤 ↑↓가 아래 행이 아니라 옆 칸으로 갔다. 행 이동을 lastListColumns로 수정. **P2 후속 완료**: 호출부 0인 `FormatElementLabel` 제거(테스트도 함께, 대신 생산 코드가 쓰는 `ShouldShowSecondary`를 직접 고정) + 보유 탭의 `floor(panelW/410f)` 자기 공식을 `GetGridColumns`로 통일(폭 300~1400 중 1229 한 곳만 2→3열로 1px 빨라짐)

- [x] OpeningReplayCoordinator (P0:1, P1:2, 2026-08-03) — UI 루트 SetActive 토글이 BattleScreenUI/RaidBattleUI/RegionMapUI의 OnDisable 해지를 되살리지 않아, 오프닝 다시보기 한 번에 배틀·레이드 화면이 영구 먹통. OnEnable 재구독 + subscription_lint 신설로 CI 고정

- [x] OpeningSequenceState (clean P0/P1, 2026-08-03) — 타임라인 상수 8개 ↔ 이미지 인덱스/블렌드 전 구간 정합, Complete 이중 발화 가드, 스킵이 현재 페이드를 이어받는 처리, 시계 clamp·역행 방어, 게이트 armed 전이 전부 확인. 파일 수정 0건

- [x] UIDirectScroll (P1:1, 2026-08-03) — 클래스엔 z-order 개념이 없는데 도감 개편으로 상세 모달이 그리드 위에 겹치면서, 먼저 그려지는 그리드가 모달 위의 휠·드래그를 Use()로 가로채 상세가 스크롤 불가(터치는 두 인스턴스가 같은 손가락을 잡아 동시 스크롤). Handle에 interactive 매개변수 추가 + 겹친 배경은 입력 포기

- [x] UITween (P1:1, 2026-08-03) — `Evaluate`가 호출마다 `elapsed`를 더했는데 유일한 호출부가 `OnGUI`(프레임당 Layout+Repaint+입력마다 1패스)라 0.2초 페이드가 2배 이상 빨랐고 마우스를 움직이면 더 빨라졌다. `TweenHandle.lastFrame`으로 프레임당 1회만 전진. 이징 5종의 0→0/1→1 계약을 테스트로 고정

- [x] UISafeLayout (clean P0/P1, **P2:1 처리**, 2026-08-03) — 좌표계 계약(Px ↔ UIScale.Begin 미사용 / 가상 ↔ Begin 사용) 34개 파일 전수 일치, Begin/End 전 경로 균형 확인(불일치로 보인 6건은 주석 2 + 조기 return 4). **P2 후속 완료(문서화로 종결)**: 미사용 4멤버를 `rules/ui-layout.md` API 표에 올리고, `UISafeLayout.ContentWidth`(MarginX 24 고정판)와 `UIScale.ContentWidth(margin)`(일반판)이 중복이 아님을 명시 — 삭제하면 ContentTop/Bottom/Height만 남아 비대칭이라 누가 다시 넣는다. `Overflows`는 원래 표에 있었다

- [x] InsectBattleStats (clean P0/P1, 2026-08-03) — 부호 있는 스택 카운터(±MaxBuffStacks)가 반대 방향 전환을 막지 않는지, `protected set`이 `RaidBossStats` 상속 때문에 필요한지 확인. **핵심은 보너스 이중 적용 여부** — `DefenseBonus`는 `ApplyDamage` 안에서만, `AttackBonus`는 호출부 3곳에서만 곱해져 겹치지 않는다
- [x] InsectSizeCalculator (clean P0/P1, 2026-08-03) — FNV-1a 음수 방어(`Abs(int.MinValue)` 회피), `-1` 센티넬 → instanceId 해시 폴백, 무게가 길이 배율의 세제곱인지, 라벨 포맷 경계(10g/1000g, 100mm) 전부 확인
- [x] WeeklyContestSchedule (clean P0/P1, 2026-08-03) — 주차 경계(604800 전후)·음수 시각·빈 풀 폴백, insectId 사전순 정렬로 기기 간 같은 대상 종 보장, 티어 임계 3단 정합 확인
- [x] WeeklyContestManager (clean P0/P1, 2026-08-03) — 포획 전용 `InsectCaptured` 구독(치료·레벨업 오발화 회피), World 루트라 SetActive 토글 무관(Start↔OnDestroy 페어링), PlayerPrefs 키가 `int.TryParse`만 써 culture 무관, 주차 캐시로 128종 정렬 반복 회피 확인
- [x] NpcDuelController (P1:1, 2026-08-03) — 도주 시 `DuelEnded`가 발화하지 않아 90초 재도전 쿨다운이 통째로 우회됐다(주석은 "결과와 무관하게"라고 적혀 있는데 한 결과가 빠져 있었음). `TryEscape`에 발화 추가 + 풀 인덱스의 `Abs(int.MinValue)` 음수 방어를 순수 함수로 분리해 테스트 고정

- [x] InsectBattleController 재감사 (P1:1, 2026-08-03) — `RecalculateBonuses()`가 전투 **시작** 경로에 없어 의상·아이템 ATK/DEF 보너스가 첫 턴에 적용되지 않았다. 하필 `SeedPersistentState`가 감염 곤충일 때만 `AddEffect`→Recalculate를 태워서 "독에 걸려 있어야 의상 보너스가 켜지는" 상태. `BeginBattleCommon` 끝에 호출 추가

- [x] PlaySceneBootstrap 재감사 (clean P0/P1, 2026-08-03) — 감사 이후 2107줄 변경분 대상. EnsureComponent 경로 87개·AutoWire 오버로드·리플렉션 주입 154건·Resources.Load·부팅 canary·신규 등록 3종 전수 대조. 파일 수정 0건

- [x] RegionMapUI 재감사 (P1:1, 2026-08-04) — 도감 브라우저가 항목마다 매 OnGUI 패스에 `$"등급 | CP n"`(보간+enum 박싱+CP 재계산)과 `new string('?', len)`을 새로 만들었다. 둘 다 종 데이터 파생이라 불변 — insectId·이름 길이로 캐시

- [x] DexScreenUI 재감사 (P0:1, P1:2, **P2:3 후속 처리**, 2026-08-06) — 3D 썸네일 도입 접점의 결함. 그리드 2곳이 뷰포트 컬링 없이 전 항목을 돌아 24칸 LRU가 영구 스래싱(P0) + `GetInsectData`의 캡처 람다 O(N)(P1) + `OwnsShiny`가 매 패스 List 할당(P1)

- [x] RaidBattleUI 재감사 (P1:3, **P2:2 + 배너 복원 후속 처리**, 2026-08-06) — 결함 3건 모두 5f0776f의 라운드 파이프라인 교체가 남긴 것. 유나이트 종료 상한이 2.5s→1.15s로 바뀌었는데 2D 오버레이 타임라인은 2.5s 그대로라 5인 팀의 착탄 3건·최종 폭발·TOTAL이 전부 미렌더 + `wantMouseClick` 탭 래치 + TurnAnnounce 배너 기계장치 사장

- [x] BattleScreenUI 재감사 (P1:2, P2:1 보고, 2026-08-06) — 인트로/결과 탭이 **지난 전투의 `escapeRect`로** 소비돼 턴을 보기도 전에 도주가 실행되던 탭 래치(RaidBattleUI와 같은 계열) + `GetSkillColor`가 `SkillEffectType` 7종 중 3종만 처리해 신규 4종이 전부 회색

- [x] SubAreaWorldBuilder 재감사 (P1:1, 2026-08-06) — 서브에리어 진입마다 런타임 머티리얼 41개가 새고 있었다(`Destroy(subAreaRoot)`는 GameObject만 지운다). 25m 이탈·[E] 재진입으로 세션 내내 반복되는 경로라 누적된다

- [x] CharacterModelPreviewRenderer (P1:3, 2026-08-06) — 카드마다·패스마다 썸네일 키 문자열 생성(구조체 키로 교체) + 렌더 프레임마다 PlayerPrefs 4회(InvalidatePreview 배선) + 경계 계산의 배열 반환판(리스트 채우기판으로)

- [x] OutfitShapeLibrary (P1:2, 2026-08-06) — 슬롯 컨테이너 이름의 enum→문자열 할당을 미리 굽고, **프리뷰가 각도만 바뀐 프레임에도 옷을 통째로 다시 입히던 것**을 함께 차단(교차 수정)

- [x] RaidSupportPlanner (P1:2, 2026-08-06) — Stage 4가 버프를 팀 전체로 바꿨는데 플래너는 시전자 스택만 보고 있었고(쓸 수 있는 스킬을 버림), 보스 연속기절 면역도 몰라 저항당할 시도를 반복했다

- [x] CharacterOutfitData (P1:2, 2026-08-06) — `GetPrimaryBonusText`가 카드마다·패스마다 문자열을 만들고(바로 윗줄 GUIStyle 회귀는 막아뒀던 자리), 세트 별 표시가 덧붙이기 루프였다. 둘 다 캐시로 교체

- [x] UIShapes (P1:1, 2026-08-06) — 중간 roundness 혼합 수식의 **방향이 뒤집혀** 있었다(큐브가 타원, 원통이 사각형). 2D 의상 카드 폴백이 실제로 그 값을 넘기고 있었음

- [x] TutorialQuestUI 재감사 (P1:1, 2026-08-06) — 상세 패널이 열려 있는 동안 패스마다 List 2개를 만들고 퀘스트 31개를 다시 갈랐다. 분류가 불변이라 원본 배열 참조를 키로 캐시

- [x] TrainingUI 재감사 (**P0:1**, P1:1, 2026-08-06) — 보유 목록이 컬링 없이 전 개체에 3D 썸네일을 요청해 24칸 LRU가 영구 스래싱(도감 P0와 같은 구조) + 개체마다 정보 문자열 2개를 매 패스 생성

- [x] 3D 썸네일 목록 컬링 일괄 스윕 (**P0:4**, 2026-08-06) — `InsectVisual.Draw` 호출부 11곳 전수. 팀편성·보유곤충·병원·지역맵 4곳이 컬링 없이 전 항목을 돌아 24칸 LRU가 영구 스래싱. 단일 진입점에 규칙 명문화

- [x] BattleArenaController 재감사 (P1:1, 2026-08-06) — 스킬 연출마다 만든 런타임 머티리얼 31경로가 아무도 안 지워 전투가 길수록 누적. CleanupArena에서 일괄 파기

- [x] RaidBattleController 재감사 (P1:1 + 방어 1, 2026-08-06) — 오늘 레이드 5단계로 924줄 바꾼 파일 자체 감사. **회복 수치가 시전자 슬롯에 뜨던 것**(Stage 4에서 대상이 최저 HP 아군으로 갈렸는데 표시가 안 따라감)을 받은 슬롯으로 이전

- [x] LoginUI 재감사 (clean P0/P1, 2026-08-06) — score 183은 InitStyles 가드 안의 GUIStyle 40여 줄이라 거짓양성. 텍스처 3종 생성기 전부 정리 목록에 등록, 구독 짝·싱글턴 가드 정상. 파일 수정 0건

- [x] TutorialQuestManager 재감사 (clean P0/P1, 2026-08-07) — quest_lint 7 PASS. 세이브 5키 × 클라우드 4지점 완전, 구독/해지 6/6 대칭(**q_team 영구정지를 냈던 `battleTeamManager.TeamChanged` 라인 존재 확인**), Update는 조기이탈 3중 + Find 캐시. 파일 수정 0건

- [x] ProceduralAudioGenerator 재감사 (clean P0/P1, 2026-08-07) — static 생성기라 검사 항목 1~7이 대부분 해당 없음. 요청 문자열 ↔ 생성기 case를 **양방향 전수 대조**해 누락 0·사장 0 확인(계산 문자열 경로 2종 포함). `CreateClip`의 in-place 게인은 배열 공유가 없어 이중 적용 불가. 파일 수정 0건

- [x] CashShopUI 재감사 (clean P0/P1, **P2:1 처리**, 2026-08-07) — score 203은 `mainStylesReady`/`gachaStylesReady` 가드 안 GUIStyle 29개라 거짓양성. 탭 3곳 전부 매니저 null 가드, 결제 2단계 검증·환불, 캐시 3종 정상. **P2 처리**: 카드 라벨을 `cardTextCache`로 품목당 1회만 굽되 **실결제 가격은 캐시 제외**(IAP 준비 시 스토어 현지화가로 바뀜) + 레이아웃 전환 시 무효화

- [x] InsectEntity 재감사 (P1:2, 2026-08-07) — `AnimateWings`가 매 프레임 `transform.Find` 2회 + `insectId.Contains` 최대 10회를 다시 했다(날개 없는 종은 그 Find가 영원히 실패 — 실패하는 Find가 가장 비싸다). `Update`의 NameLabel 조회도 배틀 모델에선 영구 실패. 둘 다 같은 파일의 캐시 7개와 같은 형태로 1회 확정

- [x] WorldChannelManager 재감사 (P1:1, 2026-08-07) — 온라인 필드에 올리는 내 레벨을 옛 저장소 `PlayerPrefs("player_level")`에서 읽어 **신규·로컬 플레이가 영원히 Lv.1**로 보였다(복원 계정은 복원 시점 값으로 고착). CloudSaveManager가 같은 이유로 이미 진행도 컨트롤러를 우선하는데 이 클래스만 그 정정에서 빠져 있었다

- [x] CollectionUI 재감사 (P1:1 + 사장 코드 2, 2026-08-07) — 보유 목록이 행마다·패스마다 문자열 4개를 새로 만들었다(enum 박싱 + SizeLabel + 보간 2). 전부 (종·개체·레벨) 파생이라 불변이고 무효화 배선(`ownedCacheDirty`)은 이미 있어서 `cachedOwned`와 같은 순간에 함께 굽게 했다. 빈 `Update()`·빈 `DrawToggleButton()` 제거

- [x] RaidBattleUI 재감사 (clean P0/P1, 2026-08-07) — "2961줄 변경"은 실체가 `Draw.cs` 분리(이동)라 신규 로직은 116줄뿐. 사장 필드 0건(과거 결함이 전부 이 계열이었다), 입력 표면 4종의 페이즈 간 잔존을 전부 추적해 안전 확인. 파일 수정 0건

- [x] RegionTerrainBuilder 재감사 (P1:1, 2026-08-07) — 배치 난수 87회가 시드 없이 돌아 **월드 지형지물이 실행마다 다른 자리에 섰다**. collider를 남기는 9종(나무·통나무·바위·죽은나무·생울타리·아치기둥·폐허 벽/기둥)이라 장식이 아니라 지나갈 수 있는 길 자체가 바뀐다. 고정 시드로 가두고 전역 난수 상태는 finally로 원복(스폰·IV·포획은 그대로 무작위)

- [x] CharacterPortraitRenderer 재감사 (P1:2, 2026-08-07) — `DrawWithOutfit`가 `GUI.color`를 복원하지 않고 끝나 **호출부의 다음 라벨이 장착 악세서리 색으로 물들었다**(상점 "보유 재화/💎/🪙"). + `DrawOutfitAccessories`가 OutfitCache를 우회해 매 OnGUI 패스에 PlayerPrefs + 스코프 키 문자열을 새로 만들고 있었다

- [x] PlayerVisualBuilder 재감사 (P1:1 + 트랩 1, 2026-08-07) — `OnDestroy`가 슬롯 필드 11개만 손으로 나열해, 얼굴·머리 장식이 **지역 변수로** 만든 6~9개가 정리 목록에 오르지 못했다. 마네킹은 파괴가 정상 수명이라 다시 지을 때마다 샜다. 생성 지점(`MakeMaterial`) 하나에 등록을 걸어 호출 19곳 무변경으로 전부 덮음

- [x] CharacterOutfitManager 재감사 (P1:1, P2:1 보고, 2026-08-07) — `GetItemsForSlot`이 `allOutfits.Where(...).ToArray()`라 **의상 패널을 열어 둔 내내 프레임당 2회 이상 95벌을 훑고 배열을 새로 할당**했다(OnGUI는 Layout+Repaint로 최소 2패스). 카탈로그는 `BuildCatalog` 이후 불변이라 `Initialize`에서 슬롯별로 한 번만 갈라 캐시하고, 유일한 LINQ였던 터라 `using System.Linq`도 함께 뗐다. **P2로 넘긴 것**: `unlockCondition`을 **평가하는 코드가 어디에도 없다** — 그리는 곳(`CharacterOutfitUI:722`)만 있고, 부여 API `UnlockItem`은 호출부가 0건이다. 그래서 조건부 4벌(`hat_flower` region_garden / `shoe_waders` region_pond / `top_lab` level_15 / `acc_badge` quest_q_complete)이 `price=0`·`gemPrice=0`·`unlockedByDefault=false`라 **구매도 해금도 불가능한 영구 잠금 상태**다. 고치려면 해금 시점을 어디에 걸지(리전 해금 이벤트/레벨업/퀘스트 완료) 정해야 해서 게임 디자인 결정이 섞인다 — 자동 처리 범위 밖으로 두고 보고만 했다

- [x] CloudSaveManager 재감사 (P1:1, 2026-08-07) — **같은 세션에서 방금 만든 결함을 잡았다.** Phase 5의 명부회 간부 보스전이 격파 기록을 `InsectGame.DefeatedLedgerBosses`(PlayerPrefs CSV)에 쓰면서 **클라우드 동기 5지점을 배선하지 않았다.** `save-system.md`가 "퀘스트 세이브 필드를 늘리면 CloudSaveManager 4곳을 함께 고쳐야 한다"고 못박은 바로 그 함정이고, 증상은 **기기를 바꿀 때마다 같은 간부와 다시 싸우고 승리 보상을 다시 받는 것**이다. `defeatedGuardians`와 완전히 동형이라 그 5지점(수집·적용·직렬화·파싱·DTO)에 나란히 붙였다. 추가로 `NpcDuelController`를 `ICloudReloadable`로 만들고 Bootstrap에 `RegisterReloadable`했다 — PlayerPrefs만 갈아끼우면 인메모리 `defeatedBosses` 캐시가 낡아 그 세션 동안은 여전히 미격파로 보인다(RegionManager 해금 상태와 같은 이유). **통과 확인**: `storyProgress`(이번 2막에서 늘어난 세이브)는 이미 5지점이 전부 배선돼 있었다. `ApplyCloudFile`의 "빈 값이면 로컬 보존, forceReplace면 삭제"는 부트 로드(보존)와 명시적 '클라우드 사용'(치환)을 가르는 **의도된 설계**라 결함이 아니다(주석이 근거를 적어 뒀다). **거짓양성으로 제외 1건**: `CollectSaveData`의 `AuthManager.Instance.DisplayName`이 무가드로 보이나, 진입점 4곳이 전부 `Instance == null || !IsLoggedIn` 조기이탈을 통과하고 `CollectSaveData`는 첫 `yield` 이전(동기 구간)에 실행된다

- [x] CapturePopupUI 재감사 (P1:1 ×5파일, 2026-08-07) — **`"beetle"`이 `"bee"`를 품는데 `id.Contains("bee")` 분기가 `stag`/`rhinoceros`/`hercules`/`longhorn`보다 앞에 있었다.** 그래서 **딱정벌레 31종(사슴벌레·장수풍뎅이·헤라클레스·하늘소·물방개·비단벌레 포함)이 벌 그림으로 그려졌다.** 포획 팝업은 방금 잡은 곤충을 보여주는 자리라 증상이 가장 잘 보이는 곳이다. 같은 사슬이 저장소에 **5곳** 복제돼 있고 그중 `InsectEntity.BuildModel`만 `&& !id.Contains("beetle")` 가드를 갖고 있었다 — 나머지 4곳(`CapturePopupUI`·`DexScreenUI`·`BattleScreenUI`·`RaidBattleUI.Draw` ×2)에 같은 가드를 붙였다. 규칙이 5곳에 복제된 이상 또 어긋날 것이므로 `InsectPortraitRoutingTests`(4검사)를 새로 만들어 **소스에서 조건식을 읽어** 가드 누락을 고정했다. **거짓양성으로 제외 1건**: `InsectEntity.ResolveWings:510`의 `bee || dragonfly`는 모델이 아니라 **날갯짓 속도·진폭**을 정하는 분기라 딱정벌레가 걸려도 종이 바뀌지 않는다. **통과 확인**: 2026-05-27 라운드가 고친 GUIStyle 12개 캐시(`popupStylesReady` 가드)가 살아 있고, `stars` 배열은 포획 시 1회 할당(프레임 경로 아님), `OnEnable`↔`OnDisable`의 `CaptureResolved` 짝과 `AutoWire`의 `-=` 뒤 `+=`가 모두 성립한다

- [x] AuthManager 재감사 (인증 코어 clean, 드리프트 1건 처리, 2026-08-07) — **인증의 급소는 전부 정상이었다**: 토큰·비밀번호를 찍는 `Debug.Log`가 0건이고, `ClearAuth`가 인메모리 6필드 + PlayerPrefs 4키를 모두 지우며, `Logout`이 `ClearAuth`(토큰 무효화) **전에** 마지막 클라우드 플러시를 쏘고(첫 yield 전 헤더 설정 + `DontDestroyOnLoad`라 씬 리로드 후에도 완료), `DeleteAccount`가 `SetDeletionInProgress(true)`로 자동저장의 Firestore 문서 재생성(PII 부활)을 먼저 차단한다. `ScopedKey`는 uid가 없으면 전역 키로 떨어지는 **의도된 폴백**(비로그인 진행 보존)이다. **처리한 것 — 하드코딩 리전 목록 드리프트**: `ApplyMasterPrivileges`가 `"meadow,pond,forest,swamp,mountain,garden,ruins"`를 문자열로 박아 둬, 2막 6지역 + ruins 수문장 신설로 **해금 6건·격파 7건이 누락**됐다. 마스터는 `RegionManager.IsRegionAccessible`의 우회로 이동 자체는 되지만(그래서 증상이 낮다), `IsGuardianDefeated`엔 우회가 없어 **지도에 수문장이 미격파로 남고 필드에 스폰된다** — 진행을 건너뛰라고 있는 계정인데 2막에서만 안 건너뛴다. `RegionDefinitions.CreateAll()`에서 파생하도록 바꾸고, 다시 하드코딩되면 잡도록 `RegionProgressionTests.MasterPrivileges_DeriveRegionLists_NotHardcoded`를 추가했다. **증상 자체는 P2급(이동 가능·표시만 어긋남)인데 처리한 이유**: 고치는 방법이 새 동작 추가가 아니라 **단일 출처 파생**이라 위험이 없고, 두면 리전을 추가할 때마다 같은 자리가 또 낡는다

- [x] CharacterOutfitUI 재감사 (P1:1, 2026-08-07) — **잠긴 의상 카드에 원문 토큰이 그대로 그려졌다.** `GUI.Label(hintRect, item.unlockCondition, ...)`이라 한국어 게임 화면에 `region_garden`·`level_15`·`region_pond`·`quest_q_complete`가 노출된다(조건부 의상 4벌 전부). `DescribeUnlockCondition`으로 문장화했고 — 지역명은 **`RegionDefinitions`에서 파생**한다(이름을 박으면 리전을 고칠 때 낡는다, 같은 세션의 AuthManager 드리프트와 같은 형태) — 미지의 형식은 토큰을 그대로 돌려줘 새 조건을 추가해도 칸이 비지 않는다. 그리는 쪽은 `GUI.Label` → `UIHelper.LabelFit`로 바꿨다: 문구 길이가 데이터에서 오는데 상자가 고정 26px이고 `wordWrap = true`라, 한국어로 길어지면 아랫줄이 통째로 잘리는 자리였다(`rules/ui-layout.md`). `OutfitUnlockConditionTextTests` 5검사로 고정. **이 라운드는 P2와 겹치지만 다른 것을 고쳤다** — 직전 `CharacterOutfitManager` 라운드가 보고한 "조건부 의상 4벌이 영구 잠금"은 **해금 판정**이 없다는 것이고 게임 디자인 결정이 섞여 그대로 남아 있다. 여기서 고친 건 **표시**뿐이다. **거짓양성으로 제외 2건**: ①score 155의 실체인 `new GUIStyle` 12개는 전부 `stylesInitialized` 가드 뒤라 1회만 돈다(이번 큐에서만 다섯 번째로 나온 같은 형태다). ②호버 툴팁이 매 패스 문자열 7개를 만들지만 **한 번에 카드 하나**라, 목록 규모를 P1로 봤던 CollectionUI 라운드가 명시적으로 제외한 것과 같은 크기다. **통과 확인**: `OnDisable`이 `isOpen = false` + `Unregister`를 함께 해 stale 모달을 남기지 않고(옛 회귀의 방어 흔적이 주석에 있다), 툴팁·프리미엄·힌트 스타일이 전부 `UIHelper.CachedStyle`을 탄다

- [x] InsectSpawner 재감사 (P1:1, 2026-08-07) — **스포너 본체는 clean이었고, 결함은 그것을 먹이는 부트스트랩에 있었다.** `PlaySceneBootstrap.GetRegionLevelRange`가 ver1 7지역만 아는 switch에 `default: return 5`라, **2막 6지역이 전부 default로 떨어져 필드 스폰 레벨 상한이 47/51/55/59/63/67에 묶여 있었다**(의도는 48/52/56/60/64/70). 에러도 안 나고 화면상 티도 안 나서 오래 남을 결함이다. 6개 case를 `Docs/StoryBible.md`의 리전 표와 같은 대역으로 넣고, **default를 조용히 넘기지 않고 `LogWarning`을 남기게** 했다 — 다음에 리전을 추가하는 사람이 알아채야 한다. `RegionProgressionTests.EveryRegion_HasExplicitSpawnLevelRange`로 고정. **거짓양성으로 제외 3건 — 전부 내가 세운 가설이었고 코드를 읽어 철회했다**: ①`GetUnderpopulatedRegionId`가 플레이어 위치를 안 봐서 먼 리전에 스폰을 낭비할 것 같았으나, `GetAvailableSpawnPoint`에 **55m 거리 게이트**가 이미 있어(주석이 "60m 디스폰 밖 스폰은 5초 내 정리되는 낭비"라고 이유까지 적어 뒀다) 먼 리전이 선호되어도 실제 스폰은 플레이어 근처로 떨어진다. ②리전이 7→13이 되며 `spawnPointCount/regionCount`가 반토막 날 것 같았으나, `pointsPerRegion = Max(base, radius/11)`이라 **반경 비례가 지배**해 리전 수와 무관하다(6~8개 유지). ③`Update`의 리전 필터가 매 프레임 List를 할당하는 것처럼 보였으나 `spawnIntervalSeconds`(5초) 스로틀 뒤에만 돈다. **통과 확인**: `DespawnEntity`가 `activeSelf` 가드 뒤에 풀 반환하고, `DespawnAllActiveInsects`가 스냅샷 후 리스트를 먼저 비워 콜백 재진입이 no-op이며, `SubAreaChanged` 구독이 `AutoWire`(`-=` 뒤 `+=`)와 `OnDisable`에서 대칭이다

- [x] PlayerMovement 재감사 (P1:1, 2026-08-07) — **걷는 내내 매 프레임 `Collider[]`를 2개씩 할당했다.** `IsBlockedPosition`이 `Physics.OverlapSphere`(할당형)를 쓰는데, Update가 이동 중 **두 번** 부른다 — 다음 위치 차단 판정(`nextPos`)과 끼임 감지(`transform.position`). 초당 120개 배열이 GC로 가는 셈이라 모바일에서 특히 나쁘다. `OverlapSphereNonAlloc` + 고정 버퍼(16)로 바꿨고, 같은 형태였던 `IsClearAt`(안전 위치 탐색이라 한 번의 복구에서 여러 번 돈다)도 같은 버퍼를 쓰게 했다 — **둘은 호출이 겹치지 않는다**(끼임 판정이 끝난 뒤에야 `RecoverToSafePosition`이 불린다). 버퍼가 차도 오판정이 없다: 가득 찼다는 건 이미 막을 것을 찾았다는 뜻이라 결과가 바뀌지 않는다. **거짓양성으로 제외 2건**: ①이번 세션에 월드 경계를 320→520으로 넓혀 이동 제한이 낡았을까 봤으나, PlayerMovement엔 좌표 클램프가 **없다** — 경계는 `WorldTerrainBuilder`가 세운 물리 벽이 담당하므로 자동으로 따라간다. ②`IsBlockedPosition`의 `foreach (regionManager.Regions)`가 리전 7→13으로 두 배가 됐지만 `ContainsPoint`는 곱셈 두 번이라 프레임당 13회는 무시할 수준이다. **통과 확인**: 리전 잠금이 이동 단계에서 실제로 강제되고(`IsRegionAccessible`, 서브에리어 안에서는 좌표계가 달라 의도적으로 건너뛴다), OnGUI 스타일 3종이 캐시 필드이며, 클릭 이동 raycast가 `try-finally`로 player layer를 복원한다(본인 raycast 통과 회귀 방어 흔적)

- [x] BattleTeamUI 재감사 (P1:2, 2026-08-07) — **①화면 전체가 한국어인데 이 화면만 영어로 남아 있었다**: 제목 `"BATTLE TEAM"`, 피커 제목 `"Select Insect for Slot {n}"`, 뒤로 버튼 `"< Back"`. 퀵바(`QuickAccessBarUI`)와 HUD(`PlayerStatusHUD`)가 이 화면을 **"배틀팀"**으로 부르고 있어 같은 말로 맞췄다. 화살표는 ASCII `<`를 유지했다 — 그 라벨에서 이미 렌더되던 글자라 폰트 아틀라스가 확실하다(전각 `＜`로 바꿨다가 되돌렸다). **②빈 `Update() { }`** — 본문이 없어도 Unity는 매 프레임 managed→native 호출을 한다. `CollectionUI` 재감사(같은 날)가 지운 것과 같은 것이라 지우고 되살리지 말라는 주석을 남겼다. **거짓양성으로 제외 3건**: ①score 102의 실체인 `new GUIStyle` 11개는 전부 `teamStylesInit` 가드 뒤다 — **이번 큐에서만 여섯 번째로 나온 같은 형태**(LoginUI·TrainingUI·CollectionUI·CashShopUI·CharacterOutfitUI에 이어). ②피커 목록이 개체마다 `$"Lv.{n} | CP {n}"`을 매 패스 만들지만 `DexBrowseLayout.GetVisibleRowRange`로 **이미 컬링돼** 보이는 행에만 돈다(CollectionUI 라운드가 넣은 방어가 여기선 처음부터 있었다). ③슬롯 5칸의 `$"Lv.{lv} | {data.rarity} | CP {cp}"`는 enum 박싱을 포함하나 **5개 고정**이라, 목록 규모를 P1로 봤던 라운드가 "한 번에 개체 하나~다섯 행"을 명시적으로 제외한 것과 같은 크기다. **통과 확인**: `CloseModal`과 `OnDisable`이 `isOpen`·`selectingSlot`·`directScroll`·레지스트리를 동일하게 정리해 stale 모달이 남지 않는다

- [x] StoryJournalUI (P1:1, 2026-08-08) — **탭 한 줄이 프레임마다 스토리 전체를 훑고 있었다.** `DrawChapterTabs`가 챕터 15개마다 `SeenIn()`으로 그 챕터 비트를 세는데, 안쪽 `HasSeen`이 `progress.seenBeatIds.Contains`(**`List<string>` O(진행도)**)라 패스당 **비트 72개 × 진행도만큼의 문자열 비교**가 든다. 다 본 세이브면 패스당 5천 회가 넘고, OnGUI는 프레임당 Layout+Repaint 최소 2패스에 마우스를 움직이면 더 돈다. 여기에 챕터마다 `$"{라벨}  {done}/{total}"` 보간 문자열 15개와 `ChapterLabel`의 15칸 선형 탐색이 얹혔다. **열람 상태는 저널을 열어 둔 동안 절대 바뀌지 않는다** — 다시 읽기(`ShowStoryReplay`)가 seen을 마킹하지 않는 게 이 화면의 설계이고 클래스 주석이 그 이유를 적어 뒀다. 라벨과 `done == 0` 판정을 `SeenCount`를 키로 캐시했다(저널 밖에서 비트를 하나 더 열람하면 키가 달라져 자동으로 다시 굽는다). **자체 회귀 1건을 즉시 되돌림**: 처음엔 배경색 판정을 캐시된 라벨에서 문자열 파싱(`EndsWith`+`Substring`)으로 되읽게 썼는데, 그건 매 패스 문자열 연산을 도로 넣는 것이라 `bool[]`을 함께 캐시하는 형태로 고쳤다. **거짓양성으로 제외 3건**: ①score 41의 "프레임 할당 10"은 대부분 `new Rect`/`new Color`(struct, 스택) + `EnsureStyles`의 `new GUIStyle` 7개(`stylesReady` 가드 뒤 1회) — **이번 큐에서만 일곱 번째로 나온 같은 형태**. ②"싱글턴 참조 11"의 실체인 `UITheme.Instance`는 정적 필드에 캐시돼 첫 호출만 `Resources.Load`를 탄다. ③`DrawBeatList`가 컬링 없이 `list.Count`를 전부 도는 게 도감·보유목록 라운드의 P0와 같은 형태로 보이나, **3D 썸네일 요청이 없어 LRU 스래싱이 없고** 한 챕터 최대 8행(ch7)이라 뷰포트(824px)에 거의 다 들어와 스크롤조차 잘 생기지 않는다 — 규모로 보면 기존 라운드들이 명시적으로 제외한 "한 번에 다섯 행" 급이다. **통과 확인**: `IModalUI` 3짝(`Toggle`/`CloseModal`/`OnDisable`)이 `isOpen`·`directScroll`·레지스트리를 동일하게 정리하고(주석이 "isOpen을 남기면 ESC가 건너뛴다"는 옛 회귀를 적어 뒀다), `ReplayBeat`가 `HasSeen`으로 미열람 비트를 한 번 더 걸러 대사 소비/보상 누출을 막으며, `EnsureIndex`의 미등록 챕터 정렬이 Dictionary 순회의 비결정성을 없앤다. Bootstrap 배선(`EnsureComponent` → `AutoWire(storyDirector, npcDialogue)` → `quickBar.AutoWire`)도 완전하다

- [x] InsectExpansion2Definitions (P1:1, 2026-08-08) — **데이터 자체는 clean이었고, 결함은 그것을 소비하는 쪽과의 "짝"을 아무도 검사하지 않는다는 것이었다.** 54종의 ID 유일성·1막 64종과의 비충돌·레어도 밴드(weight/difficulty 5등급 × 2범위) 위반 0건·등급 분포(C18/U12/R12/E10/L2)·풀 배정·`BuildModel` 라우팅 54종 전수를 확인했고 파일 수정은 없다. **처리한 것**: 시드 파일 주석이 "habitat 태그는 `InferPrimaryType`의 zone 폴백과 **짝**이다 — 한쪽만 고치면 오타가 조용히 Bug 속성으로 떨어진다"고 경고하는데, 기존 `NewSeeds_HabitatTags_AreKnown`은 **시드 쪽만 자기 하드코딩 HashSet과 맞춰 봤다.** 짝의 반대편(부트스트랩)에서 분기가 사라지거나 오타가 나면 그대로 통과하고, 그 리전 종 6~12마리의 속성이 전부 Bug가 된다 — 속성은 상성 계산의 입력이라 전투가 통째로 어긋나는데 에러도 경고도 없다. 같은 형태가 하나 더 있었다: Epic/Legendary 12종의 **전용기 switch**도 "누락하면 default 『궁극 생태 해방』으로 조용히 떨어져 전용기가 전부 같은 이름이 된다"는 주석만 있고 검사가 없었다. 둘 다 소스를 읽어 대조하는 테스트로 고정했다(`InsectPortraitRoutingTests`가 같은 이유로 만든 선례를 따랐다 — 분기가 private 인스턴스 메서드라 실행할 수 없다). **검출력 실측**: `zone == "Canopy"`에 오타를 주입하니 Canopy를 잡았고, `case "mantis_canopy":`를 지우니 그 종을 잡았다. **거짓양성으로 철회 2건 — 둘 다 내가 세운 가설이었고 코드를 읽어 접었다**: ①"2막 6지역이 zone 폴백에 하나도 없다"고 봤으나 `grep -A 45`의 범위가 짧아 못 본 것이었다(3139~3151에 6개가 전부 있다). ②`IsAntInsectId`가 `mantis`의 'ant'를 잡아 사마귀가 전부 Earth로 떨어질 것 같았으나, 구현이 `StartsWith("ant_") || Contains("_ant_") || Contains("antlion")`이라 애초에 안 걸린다. 이번 큐에서 세 번째로 반복되는 교훈이다 — **의심스러운 구조를 발견하면 방어 코드를 먼저 찾을 것.**

- [x] NpcBossDuels (P1:1, 2026-08-08) — **표 자체는 clean이었고, 결함은 주석이 약속한 검증이 실제로는 없다는 것이었다.** 파일 주석이 "곤충 ID·아이템 ID는 각각 InsectExpansion2Definitions / ItemDatabase에 실재해야 하며 `NpcBossDuelTests`가 그 정합을 고정한다"고 적었는데, 그 테스트 8건을 열어 보니 곤충은 `EveryBoss_InsectExistsInExpansion`이 실재성을 보지만 **아이템은 `EveryBoss_HasRewardAndDisplayName`이 `string.IsNullOrEmpty`만 본다.** 오타를 물어도 런타임엔 조용히 실패해(퀘스트 보상과 같은 함정 — `LogWarning`만) **간부를 이겼는데 보상만 안 들어온다.** 아이템 ID가 캡처아이템·상점 진열·상점 지급·ItemDatabase **네 소스의 합집합**이라 C#에서 다시 모으면 사본이 생겨 어긋나므로, 그 레지스트리를 이미 완비한 `quest_lint.py`에 검사를 붙였다(`extract_boss_duel_rewards` + 검사 4b). 추출이 0건이면 `ExtractorBroken`을 던지게 해 파서가 낡았을 때 "위반 0건"으로 조용히 죽지 않게 했다 — 이 파일의 관통 원칙이다. **검출력 실측**: `net_gold` → `net_goldd` 오타를 주입하니 `ledger_grip:net_goldd`를 잡았고, 현재 3건은 전부 실재한다(`net_gold`·`full_restore` ×2, 아이템 31종 대조). 주석도 "고정하는 곳이 둘로 나뉜다"로 정확히 고쳤다 — 약속과 실제가 어긋난 게 이 라운드의 본질이라 문서를 그대로 두면 다음 사람이 또 믿는다. **통과 확인**: 상대 곤충 3종(`centipede_sand`/`mantis_icicle`/`moth_effaced`)이 2막 정의에 실재하고, `storyNpcId` 3개가 story.json의 `NpcTalk` 트리거·화자와 일치하며, `ledger_ink`가 표에 없다(잿불 골짜기에서 이탈해 아군이 되므로 — 테스트가 명시적으로 고정한다). `All()`이 struct 배열을 복사해 돌려주고 `TryGet`은 3항목 선형 탐색이라 성능 표면이 없다. 호출부 3곳(`NpcDuelController` 진입·표시·보상)도 배선돼 있다

- [x] OutfitShapeLibrary 재감사 (P1:1, 2026-08-08) — **직전 라운드가 고친 머티리얼 누수의 "나머지 절반"이었다.** 2026-08-07 `PlayerVisualBuilder` 라운드가 `OnDestroy`의 손나열을 `MakeMaterial` 생성 지점 등록으로 바꿔 17~20개를 덮었는데, **spawn 의상 파츠의 머티리얼은 생성 지점이 `OutfitShapeLibrary.CreatePartMaterial`로 따로 있어 그 목록에 들어가지 않는다.** 이쪽 정리는 `TrimContainer`뿐이고 그건 갈아입을 때 **남는** 파츠만 지우므로, **마지막까지 입고 있던 파츠**는 루트가 통째로 파괴될 때 아무도 안 지운다. 마네킹은 파괴가 정상 수명이다 — `CharacterModelPreviewRenderer`가 외형 해시(성별·피부·머리·얼굴)가 바뀔 때마다 `Destroy(mannequin)` 후 다시 짓고, 그건 캐릭터 커스터마이즈 화면에서 토글 한 번마다 일어난다. 동시 착용 기준 **최대 22개**(hat 7 / acc 6 / outer 5 / bag 4)가 매번 샜다. `DestroySpawnedMaterials(root)`를 신설해 `PlayerVisualBuilder.OnDestroy`가 자기 목록을 비운 직후 부르게 했다. **bind 노드는 건드리지 않는다** — 그쪽 소유자는 PlayerVisualBuilder라 이중 파기하면 캐릭터가 분홍색이 된다(그 파일 주석이 적어 둔 검정/마젠타 회귀와 같은 자리). 가르는 기준이 노드 이름의 `OP_` 접두 하나뿐이라 `BindNames_NeverStartWithSpawnPrefix`로 고정했다. **거짓양성으로 철회 2건 — 둘 다 내 가설이었고 코드를 읽어 접었다**: ①`hideNodes`(HideCap/HideBackpack)를 끄는 코드는 있는데 되살리는 코드가 없어 "모자를 바꾸면 기본 캡이 영영 안 돌아온다"고 봤으나, 복원은 `ApplyPartColor`가 하고(알파가 있으면 `SetActive(true)`) `ApplyToCharacter`가 **ApplyPartColor → ApplyShapeRecipe 순서**로 부른다 — 켜고 나서 필요할 때만 다시 끄는 구조다. `OutfitShapeLibrary` 주석 270행이 "SetActive(true)도 하지 않는다 — 판정은 ApplyPartColor 한 곳에만 둔다"고 그 계약을 명시해 뒀다. ②`TryGet`이 `id.Contains` 사슬이라 부분문자열 오매칭이 있을 줄 알았으나, Tool만 그렇고(주석이 순서 의존을 명시 + `OutfitRecipeProjectionTests`가 분기를 고정) 나머지 슬롯은 `ExactRecipes.TryGetValue` **정확 매칭**이다. **통과 확인**: 갈아입기 경로의 누수는 이미 막혀 있다(`TrimContainer`가 머티리얼을 함께 파기, 벗을 때도 `TrimContainer(container, 0)`로 도달) / `ApplySpawned`가 파츠 재사용 시 `sharedMaterial.color`만 바꿔 `renderer.material` 게터의 인스턴스 증식을 피한다 / `GetPrimMesh`가 PrimitiveType별 built-in 메시를 1회만 추출 / 기존 테스트 22건이 카탈로그 정합·bind 노드 실재·도구 분기 도달성을 이미 덮는다

- [x] CharacterModelPreviewRenderer 재감사 (P1:2, 2026-08-08) — **`RenderTexture.Release()`는 GPU 리소스만 반환하고 객체는 남긴다**(다시 `Create()`하면 되살아나는 게 그 설계다). 캐시에서 버리는 텍스처를 `Release()`만 하고 놓아 두면 참조가 끊긴 껍데기가 쌓여 씬 전환(`UnloadUnusedAssets`) 전까지 남는다. 이 파일의 세 정리 지점(24장 LRU 축출 · 외형 변경 시 전체 무효화 · `OnDestroy`)이 전부 그랬다. 외형 토글 한 번마다 최대 24장이 버려지므로 커스터마이즈 화면에서 반복 누적된다. `DisposeRT` 헬퍼로 `Release()` + `Destroy()`를 묶었다. **같은 결함이 형제 파일에도 있었다** — 도감 3D 썸네일 `InsectModelPreviewRenderer`의 세 지점(축출 · `OnDestroy` ×2)이 같은 형태라 같은 헬퍼로 고쳤다(이번 세션에서 네 번째로 반복된 "형제 파일 복제" 결함이다: 상점↔PvP, PlayerVisualBuilder↔OutfitShapeLibrary에 이어). **두 번째 P1 — 마네킹이 고아로 남았다**: `mannequin`은 씬 루트에 서 있어(부모가 없다) 이 컴포넌트가 죽어도 따라 사라지지 않는데, `Destroy(mannequin)`이 외형 변경 경로에만 있고 수명 종료 경로엔 없었다. `OnDestroy`에 추가했고, 그러면 그쪽 `PlayerVisualBuilder.OnDestroy`가 머티리얼과 spawn 의상 파츠까지 연쇄로 정리한다(직전 라운드가 붙인 `DestroySpawnedMaterials`가 여기서 값을 한다). **통과 확인**: 직전 감사(2026-08-06)가 넣은 구조체 썸네일 키·`InvalidatePreview` 배선·경계 계산의 리스트 채우기판이 그대로 살아 있고, `EnsureRig`가 1회 가드를 갖는다

- [x] PlayerStatusHUD 재감사 (**P0:1**, P1:2, 2026-08-08) — **P0: `AutoWire`가 `insectCollection` 구독을 안 걸어 COLLECTION의 "보유" 숫자가 세션 내내 고정됐다.** Bootstrap이 `EnsureComponent` → `AutoWire` 순서라 `AddComponent`가 동기로 부르는 `OnEnable` 시점엔 `insectCollection`이 아직 null이고, 그쪽 `insectCollection != null` 가드가 거짓이라 구독이 통째로 건너뛰어진다. 그러면 `ownedCountCacheDirty`를 되살릴 경로가 `OnEnable` 하나뿐이라 첫 프레임 이후 영구 stale — 곤충을 잡아도 방출해도 숫자가 안 변한다. **바로 아래 `regionManager`가 같은 이유로 이미 지연 구독 형태를 쓰고 있는데 여기만 빠져 있었다**(형제 UI `CollectionUI`·`HospitalUI`도 전부 AutoWire에서 재구독한다). `rules/ui-layout.md`가 못박은 구독 소실 계열인데 `subscription_lint`는 `OnDisable` 해지↔`OnEnable` 재구독 짝만 봐서 이 형태(애초에 구독된 적 없음)를 못 잡는다. **P1-1: 펼침 패널이 좌측 스택을 통째로 덮는데 그 아래 버튼이 여전히 클릭을 받았다.** 패널은 x[safeL+20, safeL+500] × y[ContentTop, +540]이고 미니맵(ContentTop+150~370)·퀘스트 칩·목표 행(ContentTop+380~)이 완전히 그 안이다. IMGUI는 겹침으로 입력을 막지 않으므로 **안 보이는 목표 행 버튼이 눌린다**(오늘 Phase A에서 그 자리에 버튼을 새로 놓아 실입력 결함이 됐다). `expanded` 기본값이 true라 **데스크톱은 첫 프레임부터** 해당된다. `IsExpanded` 공개 + `MinimapUI.LeftStackOccluded`로 좌측 스택이 스스로 빠지게 했다(칩은 모바일에서만 그 자리라 `IsMobileLayout &&` 조건이 붙는다 — 데스크톱 칩은 좌하단이라 무관). **P1-2: 서브에리어 진입 배너만 세이프에어리어 하네스를 안 썼다.** 같은 파일 패널은 `UISafeLayout.ContentTop`으로 전환됐는데 이 배너만 y가 70/74/110으로 박혀 있어, 노치 기기(`VirtualSafeTop ≈ 130`)에서 배너 전체가 상태바 뒤로 들어간다 — 하네스를 도입한 바로 그 플랫폼에서 실패한다. `ContentTop + 70`으로 옮겨 상단 중앙 관례를 지켰다(ContentTop 토스트 → +30 퀘스트 토스트 → **여기 +70~142** → +150 가이드 코치). `ui_layout_lint`는 금지 관용구 목록 기반이라 맨 정수 리터럴은 안 잡는다. **거짓양성으로 제외**: `OnGUI`의 `new Rect`/`new Color`(struct — 파일 주석도 같은 결론), `stylesInitialized` 가드 뒤 GUIStyle(이번 큐에서만 여덟 번째), `Update`/`OnGUI` 내 Find류 없음, `SubAreaChanged`는 `subscribedSubArea` 플래그로 이중구독·누수 모두 방어됨, 세이브 데이터 없음, 508줄 + `DrawXxxSection` 분리가 이미 돼 있어 모놀리스 분해 실익 없음

- [x] GachaBoxManager 재감사 (P1:1, P2:1 처리 · P2:1 보고, 2026-08-08) — **확률 공시의 마지막 구멍은 표기가 아니라 풀 배치였다.** `data_lint`가 이미 지키는 것은 ①`gachaExclusives` ID ↔ displayName 매핑 ②등급 임계값 분포(합 100%·단조증가) ③UI 표기가 코드에서 파생되는가 셋인데, **`normalPool`의 티어 배치는 아무도 안 봤다.** 이 파일 주석이 "각 티어의 ID는 InsectDatabase 실제 rarity와 일치해야 함(저가 박스가 상위 곤충을 누출하지 않도록)"이라 못박아 뒀는데도 그렇다. `GetDbRarity`는 결과 **표시**만 보정하고 **풀 선택은 바꾸지 않으므로**, 배치가 어긋나면 브론즈 상자가 55% 확률로 상위 곤충을 흘린다 — 공시한 확률과 실제 분포가 갈리는 것이고 이건 스토어·법 이슈다. 기존 검사 9번이 지키는 건 "표기가 코드에서 파생되는가"이지 "그 코드가 옳은가"가 아니다. `data_lint`에 검사 10번(`extract_gacha_normal_pool` + `extract_insect_rarities`)을 붙였다 — 등급 출처 셋(1막·2막 시드 + 부트스트랩 `CreateStableInsect`)을 합쳐 대조하고, 추출 실패는 `ExtractorBroken`으로 죽어 조용한 PASS를 막는다. **실측: 현재 풀 64종 전부 정합**(불일치 0, DB 미존재 0, 등급 194종 대조)이라 이번 라운드는 회귀 방지가 목적이다. **검출력 실측**: 한 종의 등급을 바꿔 주입하니 `stag_beetle(풀=Rare 실제=Legendary)`를, DB에서 빼니 `Uncommon:mantis_green`을 잡았다. **P2 처리**: 도감 등록이 상자를 열 때마다 `FindFirstObjectByType<DexController>()`로 씬을 훑었다 — 바로 위 `database`만 캐시가 있고 여기는 빠져 있어서, 같은 형태의 lazy 캐시로 맞췄다. **P2 보고(자동 처리 범위 밖)**: 가챠 전용 곤충 확률(gold 50% / silver 30% / bronze 20%)이 `GetRateText`에 **표기되지 않는다** — 등급 확률만 공시된다. 한국 게임법의 확률형 아이템 구성 비율 공시 의무에 걸릴 수 있으나 법적 판단 + 표기 문안이 섞여 결정이 필요하다. **통과 확인**: `isOpening` 가드가 rapid-click 이중 지급을 막고, 세 보상(곤충·캔디·도감)이 각자 try-catch라 하나가 실패해도 나머지가 지급되며, `ValidateInsectId`가 data drift를 `beetle_basic`으로 폴백하고, 임계 배열 3종이 전부 4칸이라 `rates[t.Length]`가 Legendary에 정확히 떨어진다

- [x] CameraFollower 재감사 (**P0:1**, P1:2, 2026-08-08) — **P0는 같은 세션에서 두 시간 전에 내가 만든 것이었다.** Phase D의 `CutsceneDirector.Play`에 재진입 가드가 없었다. 재생 중에 `Play`가 다시 불리면 "되돌릴 상태 기록"(`restoreBattleMode`/`restoreFrozen`)이 **컷신 1번이 스스로 만든 값**(`battleMode=true`, `frozen=true`)을 읽고, 그러면 `Stop()`이 복구를 **둘 다 건너뛴다** — 조작이 영영 안 돌아오고 카메라도 배틀 모드에 갇힌다. 클래스 주석(18행)이 "못 되돌리면 영구히 움직일 수 없는 상태가 된다"고 스스로 경고해 둔 바로 그 상태다. `if (playing) Stop();` 한 줄로 막았고 `ModalUIRegistry` 중복 등록도 함께 사라졌다. **도달 경로는 지금 데이터로는 안 닿는다** — Explore가 제시한 연쇄(`GrantReward` → `ProgressChanged` → `LevelReach` 비트 발화 → 그 모달을 닫으면 재생 중 `Play`)는 실재하지만, 컷신이 붙은 2비트가 둘 다 `SubAreaEnter`라 그 연쇄에 걸리지 않는다. 그래도 P0로 처리한 이유는 **위험이 비대칭**이라서다: 방어가 한 줄이고 증상은 영구 먹통이며, `StoryBible`이 "컷신을 더 붙일 때"의 절차를 이미 적어 뒀다. **P1-1: 배틀 구도 클로버.** `restoreBattleMode`가 true면 `ExitBattleMode`를 건너뛰는데(그건 맞다), `battlePos`/`battleRot`는 컷신 마지막 컷 값으로 남아 **전투 화면 카메라가 그 구도로 굳는다**(`LateUpdate`가 매 프레임 적용한다). `EnterBattleModeFramed`가 덮어쓰기만 하고 되읽을 방법이 없던 게 원인이라 `GetBattleFraming`을 신설해 빌려 가기 전에 기억하고 끝나면 되돌린다. `BattleWin` 트리거 비트가 `BattleEnded`에 발화하고 `BattleScreenUI`의 `ExitBattleMode`는 결과 화면 정리에서만 도므로 그 사이가 창이다. **P1-2: 쉐이크 래치가 역대 최댓값을 되감았다 — 이건 컷신과 무관한 기존 결함이다.** `Max(shakeIntensity, intensity)`로 최댓값을 붙들고 `shakeTimer = shakeDuration`으로 그 값을 통째로 되감아서, `Shake(0.55, 0.55)` 0.2초 뒤 `Shake(0.1, 0.3)`을 부르면 **0.55/0.55가 처음부터 다시 재생**됐다. 래치는 타이머가 완전히 만료될 때만 풀리므로, 레이드처럼 타격이 연달아 오면 흔들림이 영영 잦아들지 않는다. 비교 대상을 "지금 **남아 있는** 세기"(`curIntensity × curTimer/curDuration`)로 바꿨다 — 새 호출이 더 강하면 갈아타고, 약하면 세기는 두고 지속만 늘린다(여진이 본 흔들림을 끊지 않게). 순수 함수 `ResolveShake`로 분리해 `CameraShakeTests` 7건으로 고정했다(이 저장소 관례 — `CutsceneTimeline`·`StoryObjectiveResolver`와 같은 형태). **Explore가 검증해 준 거짓양성 1건 — 내가 위임 전에 세운 가설이었다**: `EnterBattleModeFramed`를 컷신이 매 프레임 다시 부르면 `normalPos`/`normalRot`(복귀 지점)가 배틀 카메라 위치로 덮여 복귀 지점을 잃을 줄 알았으나, 그 두 필드는 **저장소 어디서도 읽히지 않는 죽은 필드**다(grep 확인). 복귀는 `battleTransition` 감쇠로 살아 있는 팔로우 위치를 향해 이뤄진다. **통과 확인**: `battleTransition`을 `Enter*`가 리셋하지 않는 것은 주석이 적어 둔 의도라 매 프레임 재진입에도 전환이 정상 완료되고, `Shake`는 배틀 모드에서도 적용되며(그 블록이 `battleMode` 분기 밖) `Time.unscaledDeltaTime`을 써 컷신의 무배율 시계와 맞는다. 기존 소비자 4곳은 `Enter*`를 각 1회만 부르고, `FocusOn`은 `battleMode`에서 자기 차단, `ReleaseFocus`는 `focusTimer <= 0`이면 no-op이라 컷신과 다투지 않는다

- [x] WorldLobbyUI 재감사 (**P0:1**, P1:2, 2026-08-08) — 2026-05-27 라운드는 **텍스처 누수(P0:7)만** 처리했고 그 뒤 267줄이 바뀌었다. **P0: 월드 선택 화면 뒤에서 캐릭터가 걸어 다녔다.** 이 클래스는 `IModalUI`를 구현하지 않고 `ShowLobby()`의 정상 경로가 `SetFrozen(true)`를 걸지 않아, 전체화면 오버레이 뒤에서 WASD 이동(`frozen`만 게이트)과 클릭 이동·포획(`ModalUIRegistry.IsAnyOpen()`만 게이트)이 전부 살아 있었다. **계약 불일치가 증거다** — 같은 `WorldSelect` phase로 들어가는 다른 경로 `OnWorldLeft`는 "로비로 돌아오면 이동 잠금" 주석과 함께 이미 잠그고 있었다. 최초 진입만 빠진 것이다. **Bootstrap 주석이 그 자리를 뺀 이유를 적어 뒀다**("월드 선택 미완료 시 frozen이 풀리지 않는 버그 회피 / 이전: SetFrozen(true) 호출") — 그래서 되돌리기 전에 퇴출 경로를 전수 확인했다: `WorldSelect`를 빠져나가는 길은 셋뿐이고(입장 성공 `OnWorldJoined` / 오프라인 진입 / `skipLobby`) 전부 `SetFrozen(false)`를 부른다. 유일한 예외 `HideLobby()`는 **호출부가 0인 죽은 public 메서드**였고, 나중에 누가 부르는 순간 그 옛 버그가 되살아나므로 거기에도 해제를 넣었다. `IModalUI` 구현은 `IsOpen => phase != Hidden`이고 `CloseModal`은 **InWorld 오버레이만** 닫는다 — 월드 선택은 필수 단계라 ESC 하나로 온라인을 포기시키면 안 된다(Explore는 오프라인 라우팅을 권했으나 채택하지 않았다). phase 대입이 파일 곳곳에 10군데 흩어져 있어 등록/해제를 따로 붙이면 반드시 하나를 빠뜨리므로 `SetPhase()` 단일 통로로 모았다. `OnEnable`/`OnDisable`에도 등록·해제를 넣었다(오프닝 다시보기가 UI 루트를 껐다 켜는 계열). **P1-1: 입장 클릭이 조용히 삼켜지고 "접속 중"에 갇혔다.** 두 호출부가 phase를 **먼저** Joining으로 바꾸고 매니저를 부르는데, `CanStartRequest`는 `IsBusy`면 **이벤트조차 쏘지 않고** 반환한다. 로비에 있는 동안 목록 새로고침이 5초마다 자동으로 돌아 `IsBusy`가 상시 켜졌다 꺼지므로 하필 그 구간의 클릭이 증발한다 — 화면만 "접속 중"으로 남는다. 매니저가 이미 노출한 `IsBusy`를 눌린 순간 확인하는 `BeginJoinRequest()`로 통일하고, 그래도 이벤트가 끝내 안 오는 경우(다른 요청이 `ActionCompleted`만 쏘며 `IsBusy`를 잡고 있었던 경우 — 이 UI는 그 이벤트를 구독하지 않는다)를 대비해 15초 타임아웃 폴백을 뒀다(매니저 자체 타임아웃 12초보다 길게 잡아 정상 실패가 먼저 오게 한다). **P1-2: InWorld 패널에 에러 표시가 없었다.** `OnError`가 `errorMsg`를 채우는데 그리는 코드가 월드 선택 패널에만 있어서, 나가기가 실패하면 phase는 InWorld 그대로라 **화면에 아무 변화도 없고** 버튼만 계속 눌렸다("눌러도 아무 일 없는 버튼"). 에러 라벨 + 요청 중 `GUI.enabled = false` + "나가는 중..." 라벨을 넣었다. **통과 확인**: `InitStyles`는 `stylesInitialized` 1회 가드(이번 큐에서만 아홉 번째 같은 거짓양성), 이벤트 4쌍 `+=`/`-=` 대칭, Bootstrap이 매니저를 먼저 생성해 `OnEnable`의 `Instance`가 non-null, 플레이어 목록은 `MaxPlayersPerWorld = 5`라 행 문자열은 규모 제외 대상

- [x] AudioManager 재감사 (P1:2, P2:1 보고, 2026-08-08) — **두 결함 다 "오늘 내가 넣은 기능"과 부트스트랩 사이에서 났다.** **P1-1: 게임을 시작하면 리전 곡이 안 나왔다.** `DelayedAudioStart`가 2프레임 뒤 무조건 `PlayBGM(Explore)`를 부르는데, 그 사이 frame 0의 `RegionManager.Update`가 이미 `RegionChanged(meadow)`를 쏴 `ExploreMeadow`가 걸려 있다. `PlayBGM`의 조기 반환 가드는 "같은 곡이면 무시"라 **다른 곡인 이 호출은 그대로 통과해 리전 곡을 덮었다** — 리전 곡 13개를 만들어 놓고 시작할 땐 늘 범용 곡이 흐르고, 리전을 옮기거나 첫 전투가 끝나야 제 곡이 나오는 역전이었다. 공교롭게 오늘 넣은 `RestoreExploreBGM()`이 부트스트랩보다 정확해서(마지막 리전 곡을 알고, 없으면 Explore로 떨어진다) 그걸로 교체했다. **P1-2: 보스 BGM에 긴장 램프가 안 걸렸다.** Update의 피치 램프 조건이 `Battle || RaidBattle`을 직접 적어 둔 형태라 보스 2종이 else로 떨어져 피치가 능동적으로 1.0으로 되돌려졌다 — 벽으로 설계된 간부·최종전이 유일하게 긴장 램프를 잃었다. **enum 주석이 등록 지점을 "3지점"이라 못박아 둔 것이 화근이었다** — 이 Update 분기가 목록에 없는 4번째 지점이라 내가 그대로 빠뜨렸다. 판정을 `IsCombatBgm`으로 모으고 주석을 4지점으로 고쳤으며, `BossBgmRegistrationTests`에 검사를 붙여 **누락을 주입해 검출력을 실측**했다(BossLedger·BossFinal 제거 시 정확히 그 둘을 지목). **Explore가 clean 판정한 것 2건 — 내가 위임 때 의심으로 지목했던 것들이다**: ①보스전 복귀 경로는 승/패/도주/fallback이 전부 `BattleEnded`로 수렴해 `RestoreExploreBGM`에 도달한다(주석의 주장이 실제로 성립). ②서브에리어는 BGM을 건드리지 않고 ambient만 바꾸며 `subAreaSticky`가 위치 판정을 스킵해 `RegionChanged`가 울리지 않으므로 `lastExploreBgm`이 리전 기준으로 정확히 유지된다. **P2 보고**: `SetBGMVolume`/`SetMasterVolume`/`SetSFXVolume`의 유일한 호출부 `SettingsPanel`이 어느 씬에도 없고 부트스트랩도 생성하지 않아 **인게임에서 볼륨을 바꿀 수단이 없다**(`bgmVolume`은 0.6f 하드코딩 + 저장 없음). 오늘 diff 밖의 선재 문제이고 UI 신설이 필요해 자동 처리 범위 밖으로 뒀다

- [x] PlayerInsectCollection 재감사 (P1:1, **P2:2 처리**, 2026-08-08) — 세이브의 심장부라 되돌리기가 가장 어려운 영역이었다. **P1: `GetAllOwned()`가 호출마다 List를 새로 만드는데 캐시 없이 매 OnGUI 패스에서 부르는 호출부가 2곳 남아 있었다**(`DexScreenUI` 보유 탭, `BattleTeamUI` 피커). 나머지 5곳은 전부 `cachedOwned + InsectUpdated invalidate`로 캐시하고 있었고, 같은 파일의 `OwnsShiny`는 **이미 같은 함정을 캐시로 고쳐 놓은 상태**였다 — 두 곳만 남은 것이다. 공유 캐시로 바꾸지 않고 **재사용 버퍼 `OwnedView`(IReadOnlyList)를 따로 뒀다**: 캐시로 만들면 무효화 지점을 하나만 빠뜨려도 stale 목록이 뜨는데 그건 "안 보이는 곤충"이라 할당보다 훨씬 나쁜 결함이고, 기존 호출부 다섯은 반환 리스트를 **보관**하므로(`cachedOwned = GetAllOwned()`) 공유 버퍼를 주면 그쪽이 깨진다. `GetAllOwned`는 그대로 두고 매 프레임 경로 2곳만 옮겼다. **P2-1 처리 — 문서가 보증한다고 적은 동작이 실제로는 죽어 있었다.** `save-system.md`는 "로드 루프의 `EnsureHp`가 -1이면 실제 MaxHp로 채워 구세이브 곤충이 0 HP 기절로 뜨는 것을 방지한다"고 못박았는데, **부트에서 한 번도 실행되지 않는다** — Bootstrap이 `AddComponent`로 만들어 `Awake`→`LoadAndIndex`가 `AutoWire`보다 먼저 돌고, 그 시점엔 `database`가 null이라 `GetInsectData`가 전 개체에 null을 준다("insect null이면 다음 로드에 미룸" 분기로 전부 스킵되고 그 "다음 로드"도 순서가 같아 영영 오지 않는다). **지금 증상이 없는 이유까지 확인했다**: `GetEffectiveHp`가 음수를 풀피로 보고 `IsFainted`가 `==0`이라 -1이 기절로 읽히지 않으며 `HospitalUI`도 자체 `currentHp < 0` 가드를 갖는다. 즉 P2가 맞다 — 다만 가드 없는 소비자가 하나만 생겨도 그대로 터지고, 고치는 비용이 `AutoWire`의 기존 루프에 세 줄이라 처리했다(`EnsureLevelSkills`가 같은 이유로 이미 거기서 재실행되고 있었다 — `EnsureHp`만 빠졌던 것). **P2-2 처리 — 주석의 전제가 낡았다.** `ReloadFromDisk`가 아무 이벤트도 쏘지 않으면서 "컬렉션 UI는 매 프레임 읽어 자동 반영하므로 발화 불필요"라고 적어 뒀는데, 지금은 그 UI 넷이 전부 목록을 캐시하고 `InsectUpdated`로만 무효화한다. 클라우드 복원은 `saveData`/`lookup`을 통째로 새 객체로 바꾸므로 알리지 않으면 열려 있던 UI가 **고아 `PlayerInsectData`**를 들고 남고, 거기서 레벨업·치료를 하면 **재화만 빠지고 변경은 사라진다**. `InsectUpdated?.Invoke(null)` 한 줄 + 주석 갱신(구독 4곳이 전부 인자를 무시해 null 안전). 잠복인 이유도 확인했다 — 복원 트리거가 로그인 한정이고 그 시점엔 해당 UI가 닫혀 있다. **결함 아님으로 확인**: 센티넬 로드 경로는 `Awake`/`ReloadFromDisk` 둘 다 `LoadAndIndex`를 공유해 우회가 없고 `EnsureSize`는 중복 GUID 재발급 **뒤**에 호출돼 결정적이다(FNV-1a라 실행마다 안 바뀐다) / `instanceId`는 로드 시 중복 검출→재발급+저장 / `SetAfterBattle`의 `Clamp(_,0,Max)`가 "0=기절" 계약을 지키고 `HealInsect`는 `currentHp<0`을 max로 승격한 뒤 회복해 -1 오염이 없다 / 상태 변경 public 메서드 전부 `MarkDirty`·`SaveNow` 보유, 외부 직접 변형 2곳은 `ForceSave` 호출. **테스트를 붙이지 않았다** — 이 클래스는 `Awake`가 실제 세이브를 읽고 `needsSave`면 덮어쓰므로, 하네스를 만들면 사용자 세이브를 건드릴 위험이 이득보다 크다

- [x] RaidSupportPlanner 재감사 (P1:2, 2026-08-08) — **직전 감사(2026-08-06)가 고친 2건은 재발하지 않았다** (팀 전체 버프 인식은 `BuffRoom`이 `RaidRoundResolver.ApplyTeamBuff`와 부호까지 일치, 보스 연속기절 면역 가드도 살아 있다). **문제는 그 다음이었다 — 이 파일은 순차 턴 커밋에서 한 줄도 안 바뀌었다.** 컨트롤러 391줄·리졸버 11줄이 바뀌는 동안 플래너만 옛 전제로 남았다. **P1-1: 이번 라운드에 이미 명중한 기절을 모른다 — 2026-08-06 결함이 새 통로로 재발했다.** 기절은 팀 전체에서 **한 번만** 판정되는데(`roundStunLanded` bool 하나를 `FinishTeamPhase`가 소비한다), 플래너는 `bossStunImmune`(=지난 라운드 면역)만 받고 이번 라운드 명중은 못 본다. 팀 턴이 순차가 되면서 슬롯 0이 맞히면 슬롯 1~4의 기절은 전부 확정 무의미인데, Guard 스탠스에서 기절 가중치가 1.4로 최고라 그대로 골라진다 — "전원 자동"이면 라운드당 최대 4턴이 날아간다. 호출부 한 줄(`|| roundStunLanded`)로 막았다(플래너 시그니처 무변경). **P1-2: 전체공격이 꺼지면서 방어 버프가 5명 중 4명에게 영구히 죽었다.** `RaidBossUsesAreaAttack = false`(같은 커밋에서 꺼짐)라 `incomingArea`가 상시 false이고, `if (!incomingArea && !targetedAtMe) return 0f;`가 "보스가 지목한 그 한 슬롯"으로 축소됐다 — 나머지 4명은 방어 버프를 **영원히 후보에 못 올렸다**. 레이드 버프는 만료가 없고(2026-08-03 의도된 divergence) 팀 전체에 걸리므로 지목 여부와 무관하게 값이 있는데, 바로 옆 `BuffAttack`은 `×2f`로 다라운드 가치를 매기는 비대칭이었다. 게이트를 없애되 **밸런스 변화를 최소화**했다 — 지목된 경우는 기존 가치 그대로 두고 미지목은 `×0.5`(후보에 오르되 뒤로 밀린다). **테스트가 초록인 채로 통과하던 이유도 고쳤다**: 두 테스트가 `Intent(true, -1)`(AOE)로만 검증해 **프로덕션이 안 타는 분기**를 고정하고 있었다. 둘 다 단일 대상 예고로 바꿨고, 그 과정에서 **내 기대값이 틀린 것도 하나 잡았다** — 단일 대상에서 기절 가치는 전체공격의 `alive`배가 아니라 `0.4`배라 Assault로는 데미지가 이기는 게 정상이라, Guard 스탠스로 조건을 바꿨다. **검출력 실측**: 옛 게이트를 되돌려 주입하니 `GuardStance_ShiftsPickToDefensive`가 정확히 실패했다. **결함 아님 확인**: 순수 함수이고 컨트롤러 상태를 변형하지 않으며, 회복 대상은 `RaidRoundResolver.LowestHpAlly`를 직접 호출해 리졸버와 같은 대상을 본다. `TeamCooldowns[i] = new int[sc]`로 길이 계약이 코드로 보장돼 짧은 배열 허용 분기는 도달 불가

- [x] CashShopManager 재감사 (P1:1, P2:2 보고, 2026-08-08) — **돈이 오간 직후 클라우드에 확정하지 않았다.** 실결제는 공급자 검증 → `ApplyVerifiedGemBalance`(서버 권위 잔액 반영)가 유일한 릴리스 경로이고, 그 아래 `GrantGemPackage`는 주석이 못박은 대로 **에디터/개발 빌드 전용**이다. 그런데 `SaveToCloud()`를 부르는 쪽은 에디터 전용인 후자뿐이었다 — 결제 직후 앱을 끄면 120초 자동저장을 못 만나고, 다음 실행의 클라우드 복원이 결제 전 잔액을 덮어쓴다. 서버가 권위 잔액을 들고 있어도 그건 재검증 시점 이야기고 사용자에겐 "결제했는데 보석이 없다"로 보인다. 두 경로가 대칭이 되도록 한 줄 추가했다. **통과 확인(결제 코어는 정상)**: 클라이언트가 `rewardCount`를 직접 더하지 않아 재전달·재시작 중복 지급이 없고(`ApplyVerifiedGemBalance`가 서버 계산 잔액을 SetGems), 프로덕션에서 결제 모듈이 없으면 무료 지급 대신 거부하며(Play 정책), `PurchaseWithGems`가 **사전 검증 → 차감 → 실패 시 환불** 3단계로 원자적이고 환불 기준을 `Gems` 프로퍼티(wallet 단일 소스)로 캡처해 stale 캐시를 피한다. `AddGems`는 wallet이 있으면 wallet으로만 쓰고 캐시는 read-back한다. **P2 보고 1 — 보석 *소비*는 즉시 저장하지 않는다**: 아이템 구매 후 앱을 끄면 보석은 그대로인데 아이템만 사라지거나(또는 반대) 할 수 있다. 다만 충전과 달리 실제 화폐가 오간 게 아니고, 매 소비마다 클라우드를 치면 호출량이 는다 — 즉시 저장을 어디까지 넓힐지는 아키텍처 결정이라 넘긴다. **P2 보고 2**: `PurchaseWithGems`가 같은 `FindFirstObjectByType`를 사전 검증과 지급에서 두 번 한다(구매는 버튼 클릭 경로라 프레임 비용은 아니다). **거짓양성으로 철회 1건**: 가챠 분기가 `OpenBox` 호출 직후 `delivered = true`를 세워 `isOpening` 가드에 걸리면 보석만 나가는 것처럼 보였으나, `OpenBox`는 동기 실행이고 재진입 경로가 없어 현재 도달 불가다

- [x] CashShopManager 재감사 (P1:2 처리, **P1:1 보고만**, 2026-08-08) — 재화를 다루는 파일이라 결함이 곧 사용자 손실이다. **P1-1 처리 — 젬 소비가 클라우드에 확정되지 않아 복제가 가능했다.** 서버 IAP 지급은 Firestore의 `gems`를 읽어 `currentGems + rewardCount`로 계산하는데 그 필드는 **클라이언트 PATCH로만** 갱신된다. `PurchaseWithGems`가 wallet 로컬 파일만 쓰고 `SaveToCloud()`를 부르지 않아서, 젬으로 상자를 산 뒤 자동저장(120초) 전에 젬 패키지를 결제하면 서버가 **차감 전 잔액**에 보너스를 더해 확정한다 — 쓴 젬이 돌아오고 아이템은 남는다. 지급 경로 둘(`ApplyVerifiedGemBalance`·`GrantGemPackage`)은 이미 즉시 저장하고 있었고 **소비 경로만 빠져 있던 비대칭**이었다. **P1-2 처리 — 재검증이 로컬 잔액을 뒤집었다.** 서버는 이미 청구된 토큰에 `newlyGranted: false` + **마지막 PATCH 시점 잔액**을 돌려준다(재시작 시 미확정 주문을 다시 검증하는 정상 경로다). `ApplyVerifiedGemBalance`가 그 값을 무조건 절대 세팅해서, 그 사이 로컬에서 번 젬은 증발하고 쓴 젬은 되살아났다. `newlyGranted`는 **DTO에 이미 있었는데 호출부가 안 넘기고 있었다** — 인자로 전달해 지급이 없었으면 잔액을 건드리지 않게 했다. 같은 메서드의 `Mathf.Max(0, verifiedBalance)`도 함께 고쳤다: Firestore에 `gems` 필드가 없는 신규 문서면 0으로 클램프돼 **결제 직후 잔액이 통째로 0이 됐다** — `CloudSaveManager`가 같은 필드에 `-1` 센티넬 + `>= 0` 가드를 두는 것과 정반대였다. 이제 음수는 "미상"으로 보고 스킵한다. **P1-3은 보고만 했다 — 고치려면 구조를 바꿔야 하고 잘못 고치면 더 나빠진다.** `AutoWire`의 레거시 젬 마이그레이션이 부트에서 도는데 그 시점 `AuthManager.UserId`가 null이라 wallet이 **전역** 파일을 가리키고, 병합 직후 PlayerPrefs 키를 지워 원본이 사라진다. 로그인 후 계정별 파일을 재로드하면 그 병합분이 버려질 수 있다(레거시 유저 한정). 그런데 단순히 키 삭제만 막으면 **다음 AutoWire에서 또 병합돼 젬 복제**가 되고, 마이그레이션을 로그인 시점으로 옮기려면 `AutoWire`가 부트에 한 번만 불리는 구조 자체를 바꿔야 한다. 재화가 걸린 자리라 자동 처리하지 않았다. **결함 아님 확인**: 클라우드 5지점 전부 배선(수집/적용/직렬화/파싱/DTO) / 구매 원자성은 사전 deliverable 검증 + try-catch + `gemsBefore - Gems` 차분 환불로 정확하고 `AddGems` 실패 시 refund=0이라 이중 환불이 없다 / 중복 구매는 `GachaBoxManager.isOpening` 재진입 가드와 IAP `verifyingOrders` orderKey 중복 차단이 막는다

- [x] RaidRoundResolver 재감사 (**P1:1**, P2:1 처리, 2026-08-08) — **리졸버 자신은 순차 턴 개편과 정합했고, 결함은 그것을 부르는 컨트롤러의 합체공격 경로에 있었다.** 직전 `RaidSupportPlanner` 라운드가 "주변이 바뀌었는데 이 파일만 안 바뀜"이었다면 이번은 그 반대다. **P1: 합체공격을 라운드 중간에 쓰면 이미 행동한 슬롯이 한 라운드에 두 번 때린다.** 개편 전엔 팀 커맨드가 라운드당 1회라 합체가 팀 턴을 **대체**했는데, 지금은 게이지가 슬롯마다 차올라(`CommitMemberAction`이 `+2.5 + Damage×0.15`) 팀 턴 **도중에** 100을 넘고 `CanUniteAttack`은 `ActiveSlot`을 보지 않는다. 슬롯 0~3으로 스킬을 다 쓴 뒤 슬롯 4 차례에 [F]를 누르면 0~3이 2회 공격한다. **컨트롤러 주석이 반대쪽 절반만 막고 있었다** — "아직 차례가 오지 않았던 팀원도 참여했다"고 적고 전원을 acted로 찍는데, 이미 행동한 쪽은 기여 루프에서 거르지 않았다. `HasActedThisRound(i)` 한 줄로 막았다. **같은 자리의 두 번째 증상도 함께 고쳤다**: `roundInProgress`를 새 결과 객체로 갈아끼우면서 합체 이전에 커밋된 피해가 통째로 버려져 `LastDamageToBoss`와 UI 기여도가 과소 집계됐다 — `TotalDamageToBoss`를 이월한다. **P2 처리: `CreateBossIntent`의 `allowAreaAttack` 기본값이 `true`였다.** 프로덕션은 상수를 넘기지만 기본값이 살아 있으면 **인자를 빠뜨린 새 호출부가 조용히 AOE를 부활**시키고, 그 경로는 `rageMultiplier`를 무시하고 전원을 깎아 곧 전멸이다. 기본값을 없애 필수 인자로 만들었다(컴파일 오류 0 — 모든 호출부가 이미 넘기고 있었다). **검출력 실측**: 두 수정을 모두 되돌리니 신규 테스트 2건이 각자의 결함을 정확히 잡았다. **정합 확인(재점검 불필요)**: 리졸버는 라운드 상태를 전혀 들지 않고 `AttackBonus`·`CurrentHp`를 전부 **호출 시점 라이브 읽기**로 가져와 앞 슬롯의 디버프·피해가 뒤 슬롯에 즉시 반영된다 / `IsArea` 분기는 도달 불가일 뿐 잘못된 값을 내지 않는다 / 기절은 리졸버가 명중만 판정하고 컨트롤러가 `roundStunLanded`로 라운드당 1회 소비해 중복 적용이 없다 / `LowestHpAlly`가 죽은 슬롯을 거르고 HP를 라이브로 읽어 앞 슬롯의 회복이 반영되며 플래너가 같은 함수를 써 대상이 어긋나지 않는다 / `ApplyTeamBuff`가 대상별 `TryStack*`을 타 5명이 연속 시전해도 상한에서 정확히 멈춘다

- [x] RegionManager 재감사 (P1:1, 2026-08-08) — **진행 사슬이 세 곳에 적혀 있는데 둘만 대조하고 있었다.** ①`RegionDefinitions`(데이터) ②`RegionProgressionTests.MainChain`(테스트 사본) ③`RegionManager`의 `GetNextRegionId`/`GetPreviousRegionId` switch(런타임 진행). 기존 12검사는 ①↔②만 봤고, ③은 그 배열 주석이 "switch를 고치면 여기도 고칠 것"이라고 **사람에게 부탁**만 하고 있었다. ③이 빠지면 리전을 추가해도 전부 통과하는데, **Next 누락은 수문장을 잡아도 다음 리전이 안 열려 진행이 영구 차단**되고(해금 경로가 `DefeatGuardian` 하나뿐이다), **Prev 누락은 `GetGuardianPosition`의 fromCenter가 원점이 되어 수문장이 맵 한복판에 선다**(그 자리 주석이 add-region 시나리오 E로 적어 뒀다). 둘 다 런타임엔 에러 없이 조용히 막힌다. 소스에서 메서드 본문을 잘라 사슬을 양방향으로 대조하는 테스트 3건을 추가했다(정방향·역방향·종착지에 전방 간선 없음). **실측: 현재 13리전 사슬이 완전 정합**(Next 11 / Prev 12 + garden 분기, 불일치 0). **자체 발견·즉시 수정 1건**: 처음엔 파일 전체에서 문자열을 찾게 썼는데 두 switch가 서로의 역방향이라 `case "nameless": return "canopy";`(Prev)가 종착지 검사에 걸려 **거짓 실패**했다 — 중괄호 균형으로 메서드 본문만 잘라 쓰도록 고쳤고, Prev 항목이 Next 본문에 없음을 실측으로 확인했다. **검출력 실측**: Next에서 `canopy→nameless`를 지우면 그 간선을, Prev에서 `dunes→hollow`를 지우면 그 간선을, 종착지에 전방 간선을 주입하면 그것을 각각 잡았다. **거짓양성으로 철회 1건**: `UnlockKey`/`GuardianKey`가 전역 PlayerPrefs라 계정 간 해금이 공유될 줄 알았으나 둘 다 `SaveScope.PrefsKey(...)`를 거친다. **통과 확인**: `ICloudReloadable.ReloadFromDisk`가 클라우드 로드 후 인메모리를 갱신하고, `LoadUnlockState`가 `RemoveEmptyEntries`로 ",,meadow,," 잔존을 막으며, 이벤트 4종은 발행 전용이라 구독 해제 짝이 필요 없다

- [x] 컷신 3파일 — CutsceneData / CutsceneLibrary / CutsceneDirector (clean P0/P1, 2026-08-08) — **파일 수정 0건.** 큐 1·2순위를 함께 돌리고 인접한 `CutsceneDirector`(250줄, 큐에 없음)까지 봤다. 이 세션이 다른 영역에서 반복해 지적한 "짝 규칙을 아무도 검사하지 않는다"가 **여기엔 이미 적용돼 있다**. **①타임라인**(`CutsceneData`, 순수 static): 누적 합으로 컷을 찾아 경계에서 건너뛰지 않고(컷별 시작 시각을 따로 들면 duration 수정 시 조용히 어긋난다는 주석이 근거를 적어 뒀다), `TryGetShot`이 음수 elapsed를 0으로 clamp하며, 자막 페이드가 짧은 컷에서 깜빡이지 않게 `duration <= fade*2`면 불투명 고정이다. 테스트 15건이 `EveryInstantIsCoveredExactlyOnce`까지 덮는다. **②라이브러리**: cutsceneId를 `public const`로 두어 리터럴 중복이 없고, `story_lint.py` 검사 9번이 **JSON↔Library 실재성 + switch 미배선**을 양쪽으로 본다(내가 다른 라운드에서 신설한 것과 같은 형태가 이미 있었다). **③디렉터**: `Play`에 **재진입 가드**가 있고(없으면 복원값이 컷신 자신이 만든 상태를 읽어 조작이 영구히 안 돌아온다 — 주석이 그 시나리오를 적어 뒀다), 모든 종료 경로가 `Stop` 하나로 모이며 `OnDisable`도 그걸 부른다. 배틀 카메라를 **구도까지** 기억해 복원하고(안 하면 전투 화면이 컷신 마지막 컷 구도로 굳는다), `Time.unscaledDeltaTime`을 써 전투 슬로모션에 끌려다니지 않으며, `IModalUI`라 ESC로 건너뛸 수 있다. 알 수 없는 cutsceneId는 `LogWarning`을 남긴다("컷신이 안 나오는 건 화면상 티가 안 난다"). **거짓양성으로 철회 2건 — 둘 다 내 가설이었다**: ①`TotalDuration`은 음수를 0으로, `TryGetShot`은 0을 0.0001로 보정해 둘이 다른 총길이를 본다고 봤으나, 실제 duration이 전부 1.4~3.4초 양수이고 종료 판정은 `TryGetShot`만 쓴다(`TotalDuration`의 소비자는 테스트뿐). ②UI 루트 토글(`OpeningReplayCoordinator`)에 `SetFrozen(true)`가 물려 영구 정지할까 봤으나 Bootstrap이 `World/CutsceneDirector`에 붙이므로 그 토글 대상이 아니고, 설령 꺼져도 `OnDisable → Stop`이 복구한다

- [x] RegionManager 재감사 · 후속 (**P0:1**, P1:1, 2026-08-08) — **다른 세션이 같은 라운드를 동시에 돌려 `984e3cb`로 먼저 커밋했다. 겹치지 않고 짝을 이룬다** — 그쪽은 진행 사슬의 세 번째 사본(`RegionManager` switch)을 테스트로 묶었고, 이쪽은 **좌표의 네 번째 사본**을 찾았다. **P0: 실물 수문장이 지도 마커와 다른 자리에 서 있었다.** `PlaySceneBootstrap.GetGuardianWorldPosition`이 `RegionManager.GetGuardianPosition`의 낡은 사본이었다 — 같은 공식(`(이전 리전 중심 + 이 리전 중심)/2`)을 쓰면서 이전 리전 switch만 자체 보유해 **ver1 5개 case에서 멈춰 있었다**. 그래서 `ruins`와 2막 6리전은 `prevId`가 null → 원점 기준 → 실물이 `center/2`에 섰다(최대 105m 오차, `dunes` 수문장은 연못 안, `canopy`는 유적 안). 지도 마커(`RegionMapUI`)와 목표 추적기(`StoryObjectiveTracker`)는 `RegionManager` 쪽을 쓰므로 **가리키는 곳에 아무것도 없다** — `ruins` 수문장은 2막의 유일한 문이라 에러 없이 조용히 진행이 막힌다. 사본을 삭제하고 `CreateGuardians`가 `RegionManager`를 직접 물어보게 했다(단일 출처화). **P1: 2막 서브에리어 4종에 조명 프로필이 없었다 — 이건 내가 Phase C에서 만든 누락이다.** `SubAreaWorldBuilder`에 `vault`/`archive`/`kiln`/`ledger` 지오메트리를 넣으면서 `SubAreaEnvironment.GetProfileForType`을 빠뜨려 전부 `default`(야외 주광)로 떨어졌다 — 밀폐 공간을 지어 놓고 바깥 햇빛을 쬐는 꼴인데 지오메트리만 보면 티가 안 난다. **검출력 실측**: 4종을 제거하니 신규 테스트가 정확히 그 넷을 지목했다. 좌표 사본 재발도 소스 검사로 고정했다(`private Vector3 GetGuardianWorldPosition` 부재 + `regionMgr.GetGuardianPosition(r)` 존재). **다른 세션이 확인한 것(재점검 불필요)**: 진행 체인 13리전 전부 배선, `ruins`→`hollow` 해금, `GuardianDefeated` 구독 짝, PlayerPrefs 직렬화, 서브에리어 sticky의 리전 수 비의존, `ReloadFromDisk`가 이벤트를 안 쏘지만 소비자가 라이브 조회라 무해

- [x] RaidBattleController 재감사 (P1:2 + 문서결함:1, 2026-08-08) — 순차 행동 전환(590줄 변경) 후속. 이 세션이 반복해 만난 **"짝 규칙을 한쪽 입구만 지킨다"**가 여기서 두 번 나왔다. **①합체 게이트가 참가 규칙과 다른 수를 봤다**: `ResolveUniteCommand`는 이미 행동한 슬롯을 참가에서 빼는데(빼지 않으면 한 라운드 2회 공격 — 주석이 근거를 적어 뒀다) `CanUniteAttack`은 `AliveCount() >= 2`로 판정했다. 게이지는 행동마다 차서 보통 슬롯 3~4 차례에 100에 닿으므로, 마지막 슬롯에서 [F]를 누르면 **참가자 1마리**분만 나오고 게이지 100이 통째로 소각된다 — 같은 게이지를 슬롯 0에서 쓰면 5마리분이라 **가치가 5배** 차이나고, 1마리분은 그 곤충의 평범한 스킬보다도 약했다. `RemainingActors >= 2`로 교체. **②`CanUseSkill`에만 `HasActedThisRound` 검사가 없었다**: `RaidMemberActionResolved` 발화 시점에는 `ActiveSlot`이 아직 **방금 행동한 슬롯**이고 `roundStage`도 `Ready`라 `CanSubmitTeamCommand`가 true다. 그 창에서 커맨드를 넣는 구독자가 생기면 한 곤충이 라운드에 두 번 때린다(현재 구독자는 없어 잠재 결함). 형제 입구인 `ResolveUniteCommand`는 막고 있었다. **③`RaidBossHpMultiplier = 8.5`의 산출 근거가 무효화됐다**: 주석은 "리더 1×1.0 + 서포트 4×0.6 = 3.4유닛" 전제로 뽑은 값이라 명시하는데, 지금은 `ResolveTeamCommand`가 **전 슬롯**을 `ResolveLeaderSkill`(배율 1.0)로 태워 **5.0유닛(+47%)**이고 AOE도 꺼졌다. **값은 바꾸지 않았다** — 사용자가 "레이드가 너무 세다"고 해 AOE를 껐으므로 HP를 올리면 그 요청을 되돌리는 셈이다. 주석에 현재 상태와 그 판단을 적고, 조일 때는 이 숫자가 아니라 서포트/리더 배율 구분부터 재설계하라고 남겼다. **Explore가 정상 확인한 경로(재보고 불필요)**: 팀 페이즈 중엔 아군이 죽지 않아 `BeginRound`가 전멸 상태로 열릴 수 없고, `raidEndedRaised`가 `RaidEnded` 중복을 막으며, 종료 후 `IsActive=false`가 커맨드 전체를 닫는다. 쿨다운은 `CompleteRoundPresentation`에서 **라운드당 1회**만 감소(순차 전환으로 5배 빨라지지 않았다), `AudioManager.Instance`는 전부 null 가드, `PersistTeamHp`는 전 슬롯 순회, `OnEnable`/`OnDisable` 구독 짝 6개 모두 정상

- [x] VillageBuilder 재감사 (P1:2, 2026-08-08) — 2026-07-17 이후 479줄 증분(순수 추가). 둘 다 **"주석이 약속한 것을 아무도 검산하지 않는다"** 형태다. **①병원과 집5가 같은 자리에 겹쳐 세워졌다**: 병원 주석은 "빈 각도 315°"라 적어 뒀지만 점유 각도는 25·55·90·145·180·215·270·**320**이었다. 315°/12m와 집5(320°/13m)의 중심 거리는 **1.48m**인데 병원 벽이 6×5, 집 벽이 3.8×3.2 — 집이 병원 안에 박혔다. 둘 다 `keepCollider: true`라 통행까지 막혔고, 같은 각도의 주민 앵커(320°/9.5m "집5 앞")는 병원 정면 벽에 붙어 스폰됐다. 거리만 늘리는 방식(315°/17m)은 4.2m라 여전히 겹친다. **352°**(320°와 25°의 이등분선)로 옮겨 양쪽 여유를 6.96m/7.17m로 맞췄다 — 350°도 겹치지는 않지만 집5 쪽 여유가 0.15m뿐이라 작은 조정에 다시 깨진다. 상호작용 포인트는 `pos`에서 파생되므로 함께 따라간다. **②서릿길 얼음벽이 호가 아니라 방사형 스파이크였다**: 이 파일의 극좌표 배치물은 yaw를 `-(각도 + 90)`으로 접선 정렬한다(울타리·가챠 육각벽 2곳에 명시). `BuildIceShelter`의 얼음 블록 2단(9개)만 **+90이 빠져** 블록의 긴 축(1.1)이 반지름 방향을 향했다 — 바람막이 벽이 통로로 튀어나온 스파이크가 됐고 `keepCollider: true`라 충돌체까지 같이 틀어졌다. **회귀 방지 테스트 신설**: `VillageBuildings_DoNotOverlapEachOther`가 본마을 건물 9개의 실제 벽 크기(외접원 반지름)로 쌍마다 겹침을 검산한다 — 각도 목록을 눈으로 세는 방식이 이미 한 번 틀렸기 때문이다. 도입 즉시 옛 315°를 잡는 것을 확인했다(수식 검산 1.48m < 6.39m). **거짓양성 1건 철회**: `Prim`의 `Destroy(col)`가 EditMode에서 LogError를 쏟아 `OutpostFacilityTests`를 무력화한다는 지적 — 이 저장소의 테스트는 **PlayMode 러너**로 돌고(`rules/testing.md`, EditMode 러너는 0건을 잡는다) 직전 실행에서 해당 픽스처 11건이 모두 통과했다. **정상 확인**: `Build`는 Bootstrap에서 1회만 호출돼 재빌드 누적 없음, `materialCache`가 색상당 1개 공유라 머티리얼 누수 없음, 상호작용 id 9건 전부 유일, `InteractionKind` 4값 ↔ switch 4분기 충족, `BuildOutpost`의 12리전 분기·`OutpostAngles` 12개 모두 등록(default 낙하 없음), `Polar`가 `center.y`를 보존해 RegionTerrainBuilder식 y 이중 합산 없음

- [x] BattleArenaController 재감사 (P1:2, 2026-08-08) — 2026-08-06 이후 56줄 증분(아레나 가시성). 증분 자체는 깨끗했고, 결함은 **3D 연출 컴포넌트가 화면 문구까지 직접 그리던 옛 `OnGUI`**에 있었다. **①전투 문구가 `UIScale` 밖 픽셀 좌표로 그려졌다**: `Screen.width/height`와 `fontSize 40`을 그대로 썼는데 나머지 전투 UI는 전부 가상 캔버스(가로 1920×1080 / 세로 1080×1920) 안이다. 스케일은 `min(w/refW, h/refH)`라 **1920×1080에서만 1.0**이고, 1440×3200 폰이나 2560×1440 데스크톱은 1.333이라 "효과가 굉장했다!"만 다른 라벨보다 **25% 작게** 찍히고 `Screen.height*0.35` 위치도 가상 기준보다 아래로 내려갔다 — Game View를 1080p로 보면 안 보이는 종류다. 그리기를 `BattleEffectTextOverlay`(UI 신설)로 옮겨 `UIScale.Begin()` 안에서 그린다. **아레나가 `UIScale`을 직접 부를 수는 없다** — UI가 이미 Battle을 참조해 순환이 된다(`architecture.md`). `GetActiveEffectTexts()`가 처음부터 공개였던 게 그 이음매였고 호출부가 0이었다. 만료 정리는 `OnGUI`에 있었으므로 목록의 주인(아레나) 쪽으로 함께 옮겼다. **②같은 자리 고정 600×50 상자에 `fontSize 40`**: 한글 세로 폭(약 54)을 못 담아 잘렸고, `$"{skill.displayName}!"`는 길이를 데이터가 정하는데 wordWrap이 꺼져 있어 **가운데 정렬로 앞뒤가 같이** 잘렸다. 58×760 + `UIHelper.LabelFit`으로 교체. `text_fit_lint`가 못 잡은 이유는 Rect 인자의 높이가 리터럴이 아니라 지역변수(`height`)여서 파서가 건너뛰기 때문이다. **정상 확인**: `CleanupArena`가 `StopAllCoroutines` 직후 `playingSkill=false`로 되돌려 코루틴이 종료점 전에 죽어도 플래그가 남지 않고 `OnDisable`도 거기에 위임한다. `playingSkill=true` 이후의 `yield break`는 한 곳뿐이며 거기서 false로 복구한다(나머지 조기 반환은 전부 true 이전). 레이드 카메라 z=-9.5, 1v1 카메라(팔로워 계산) x=-3.6·z=1.7로 둘 다 `ArenaWallSpan`(13) 안. `AudioManager.Instance`는 전부 null 가드, `cachedCameraFollower` 지연 캐싱, `rendererCache`가 `GetComponentsInChildren` 반복을 막는다

- [x] NpcDuelController 재감사 (P0:1, P1:1, 2026-08-08) — **전멸 패배에서 `DuelEnded`가 영영 발화하지 않았다.** `PlayerFainted`는 **구독자가 있기만 하면** `fainted=true`로 보고 종료를 UI에 넘기는데, 유일한 구독자 `BattleScreenUI`는 벤치가 비면 결과 화면으로 가면서 컨트롤러에 되돌려 알리지 않았다(`EndBattle`도 안 알림). 결과: 재도전 쿨다운 90초·120초가 통째로 우회되고, `activeBossId`가 남아 **다음 아이 대결의 승리가 간부 격파로 오기록**됐다 — PlayerPrefs 영구 저장 + 클라우드 동기라 되돌릴 수 없고, 관장을 싸우지도 않고 격파 처리하며 보상까지 나간다. 도주 경로는 같은 누락을 이미 한 번 고쳤는데 전멸 패배만 `fainted` 가드에 가려 남아 있었다. `InsectBattleController.ConcludeDefeatWithoutSwap()`(멱등) 신설 + `EnterResult`가 호출. `TryStartDuel`의 `activeBossId` 초기화도 대칭으로 추가(방어선 2겹)
- [x] SubAreaWorldBuilder 재감사 (P1:3, 2026-08-08) — **신규 4개 방(장부 창고·빙하 서고·잿불 가마·장부의 방)에 경계벽이 하나도 없었다.** 기존 9개 방은 예외 없이 `CreateBoundaryWalls`를 부르는데 2막 신규분만 빠져, 바닥 Plane 가장자리를 넘으면 그대로 낙하 → Y<-3 안전망이 경고와 함께 강제 퇴장시킨다(25m 자동 이탈은 발동조차 못 한다). 최종장 무대인 장부의 방까지 포함. 방마다 크기를 재서 12~14f로 봉했다. **빙하 서고 얼음기둥 8개가 `i*45°`라 i=6이 정확히 (0,0,-8)** — `FindSafeSpawnPosition`의 1차 입구 좌표다. 반지름 1.25 실콜라이더라 이 방만 입구가 막혀 3m 밀려난 데서 등장했다 → 반칸(22.5°) 위상 이동. **장부 판의 yaw가 `i*24f`로 배치각과 무관**해 방 한가운데서 보이는 게 옆날뿐이었다 → 저장소 접선 관례 `-(각도+90)`
- [x] SocialPvpUI 재감사 (P1:1, 2026-08-08) — `OnDisable`이 `ModalUIRegistry.Unregister`만 하고 `isOpen`을 안 지워 **레지스트리엔 없는데 열린 것으로 아는** 상태가 됐다. 오프닝 다시보기가 UI 루트를 껐다 켜면 도달한다 — ESC가 이 모달을 건너뛰어 안 닫히고, 퀵바의 `IsAnyOpen()` 가드가 무력화돼 **친구코드 입력 중 N/T/G/C가 다른 화면을 토글**한다. `CashShopUI`는 같은 커밋에서 이 처리를 주석까지 달아 받았는데 이 파일만 빠졌다
- [x] TrainingManager 재감사 (P1:1, 2026-08-08) — 훈련이 스킬을 갈아 끼우고 `ForceSave`까지 하면서 `PlayerInsectCollection.InsectUpdated`를 발화하지 않았다. 자체 `TrainingCompleted`는 **구독자 0건**이라 아무에게도 안 갔고, 그래서 훈련 직후 캐시를 쓰는 화면(레벨업·선택 UI, 상태 HUD)이 옛 스킬셋으로 남았다. 레벨업 경로(`TryLevelUpWithCandy`)는 `MarkDirty` + `InsectUpdated`를 제대로 부르는 대칭 경로다. 컬렉션에 `NotifyInsectChanged` 공개 API 신설 — `GetByInstanceId`가 실참조를 주므로 외부 수정 경로가 더 생길 자리다
- [x] HospitalUI 재감사 (P1:1, 2026-08-08) — `TryHealAll`만 결제 **전에** `collection == null`을 가드하고 (주석: "재화만 빠지고 아무도 낫지 않는다") `TryHeal`/`TryGemHeal`/`ApplyTreatmentItem`은 결제·아이템 소비 **후에** 무가드 역참조했다 — 이 세션이 반복해 만난 "한쪽 입구만 지킨다" 형태. 셋 다 대칭으로 맞췄다
- [x] BattleTeamUI 재감사 (P1:1, P2:2, 2026-08-08) — 신설한 "회복하러 가기" 버튼 라벨이 잘렸다(31px 글자 6자에 필요 폭 ~190인데 버튼이 172, wordWrap이 없어 가로로 잘린다) → 224로 확장. `hospitalUi.Toggle()`이 말 그대로 토글이라 병원이 이미 열려 있으면 두 창이 다 닫히는 자리를 `IsOpen` 검사로 못박음. 정렬 칩 `Rect[4]`는 대입만 하고 아무도 읽지 않는 죽은 필드 + 길이가 `InsectBrowseSort.Order`와 안 묶여 모드를 늘리면 IndexOutOfRange가 날 자리라 제거(`CollectionUI`의 동일 사본도 함께)
- [x] RaidBattleUI 재감사 (P1:2, 2026-08-08) — 순차 팀 턴 전환의 잔재 2건. **①버프·회복 오라가 엉뚱한 곤충 위에 떴다**: 컨트롤러가 `RaidMemberActionResolved` 직후 `ActiveSlot`을 **다음** 슬롯으로 옮기고 `RaidUpdated`를 쏘므로, `OnRaidUpdated`의 `selectedSlot = ActiveSlot`이 방금 세운 행동 슬롯을 같은 프레임에 덮었다(라운드 마지막이면 -1 → 0번 곤충). 연출은 `lastMemberAction.SourceSlot`을 `RaidSlotLayout.AnchorX`에 넣도록 교체 — 손으로 복사한 앵커식의 1인 팀 중앙정렬 누락도 함께 해소. **②단일 공격에 라운드 누적분이 나갔다**: 투사체 루프가 `activeRound.TeamActions` 전체를 돌아 3번째 곤충 차례에 1·2번 곤충 투사체가 다시 날아가고, 피해 숫자는 한 마리분인데 "TEAM RUSH ×3"이 붙었다
- [x] InsectBrowseSort (P1:1, 2026-08-08) — 신규 파일 표면 점검. `Label()`의 `default`가 "등급"을 돌려줘 모드를 하나 더 늘리면 칩 두 개가 나란히 "등급"으로 뜨고 정렬도 조용히 Rarity로 동작했다 — `Order_CoversEveryMode`는 enum↔Order만 보고 `Label_IsNeverEmpty`는 공허하게 통과한다. 전 case 명시 + `default`는 enum 이름 노출 + **라벨 중복 금지 테스트** 신설. **정상 확인**: 비교자는 반대칭·추이적이라 `List.Sort`의 `InvalidOperationException` 경로 없음, null 3중 방어, 두 호출부 모두 `OwnedView` 버퍼를 즉시 복사해 보관 계약 준수
- [x] CaptureMinigameController 재감사 (P0:0, P1:0, 2026-08-08) — 증분(9줄)은 깨끗했다. 포획률 이중 구현은 이미 `CaptureMinigameProbability` 위임으로 해소됐고, 레벨 차 ±5 clamp도 미니게임·전투 두 경로가 같은 상수를 쓰며 테스트가 그 일치를 단언한다. **거짓양성 1건 철회**: "`panelRoot`에 `SetActive(true)`가 없어 첫 포획 뒤 패널이 영구히 사라진다" → 부트스트랩이 `capturePanel.SetActive(false)`로 시작하고 아무도 켜지 않으므로 이 UGUI 패널은 **처음부터 한 번도 뜨지 않는 죽은 패널**이다(미니게임은 전부 IMGUI로 그린다). 따라서 그 위에 얹힌 "UGUI 버튼 이중 확정" 지적도 도달 불가. 켜는 것이 수정이 아니라 **제거가 정리**이므로 P2로 위임
- [x] TutorialQuestUI 재감사 (P1:0, 2026-08-08) — P1 3건을 받았으나 **자동 처리하지 않고 P2로 위임**한다. 지목된 `DrawActiveQuestDetail`의 고정 높이(34/40)와 `detailHeaderStyleCache` 복원값(37→31) 어긋남은 실재하지만, 이 파일은 실제 한글과 리터럴 `\uXXXX` 이스케이프가 섞여 있어 문자열 매칭 편집이 위험하고 (같은 세션에서 이미 Python 줄범위 치환으로 우회한 이력), 폰트 파생 리팩터는 1229줄 화면의 16곳을 동시에 건드린다 — `audit-flow.md`가 자동 처리 범위를 "표면 회귀만"으로 제한한 자리다
- [x] PlaySceneBootstrap 재감사 (P1:0, 2026-08-08) — 증분(20줄)은 정상. 지적 2건은 **설계 판단이라 위임**: ①마을 try 블록이 커지면서 `VillageBuilder.Build` 실패 시 HospitalUI·NpcDialogueUI·StoryObjectiveTracker·CutsceneDirector가 함께 사라지는 단일 실패점이 됐다(회복 소프트락 + 메인퀘스트 무증상 정지) — 단계 분리는 5005줄 모놀리스의 초기화 순서를 건드리는 일이라 architect 상담 대상. ②배틀팀의 회복 버튼이 위치 게이트 없이 병원 전체를 연다 — 이건 사용자가 요청한 기능 그대로이므로 결함이 아니라 **경제 설계 결정**이다. **정상 확인**: 수문장 좌표 이전은 리전 안이라 이동 차단에 안 걸리고 테스트가 고정 중, 구독 1건 중복 없음, 싱글턴 전부 null 가드, 클라우드 세이브 배선 누락 없음
- [x] NpcDialogueUI 재감사 (P1:0, 2026-08-08) — P1 3건을 받았으나 **전부 데이터·연출 설계라 위임**한다. ①`GetStoryPortrait`에 2막 간부 4인(`ledger_grip/scale/ink/chief`) case가 없어 Story.json 11비트가 초상 없이 뜬다 — 얼굴 디자인은 visual-dev 영역이고 story_lint 검사 추가가 함께 필요하다. ②이름은 줄마다 화자가 바뀌는데 초상은 비트 고정이라 2인 대화에서 얼굴이 안 바뀐다 — `StoryLine`에 `speakerNpcId`를 더하는 데이터 모델 변경이다. ③진행 표시 상자 30f는 경계값(추정). **정상 확인**: 본문은 `LabelFit` 경유, 패널은 전부 `UISafeLayout`, 구독 짝·인덱스 범위·모달 전환 정상

- [x] CashShopUI 재감사 · 2회차 (P1:2, 2026-08-17) — 가챠 결과 초상이 파사드(`InsectVisual.Draw`)가 아니라 **그 아래 2D 폴백**(`CapturePopupUI.DrawTypedInsectPortrait`)을 직접 불러, 다른 9개 화면이 전부 3D 썸네일을 보여주는 동안 **가챠 결과만 홀로 2D**였다 → DB 조회 캐시 + 파사드 경유로 교체(조회 실패 시에만 2D 폴백 유지 — 파사드는 data가 null이면 아무것도 안 그려 빈 사각형 회귀가 난다). Phase 2 플래시가 `850x620` 하드코딩이라 1200x820(모바일 1200x1560) Area의 좌상단만 덮는 **잘린 흰 판**으로 보였다 → 감싸는 Area 크기를 인자로 받게 변경. 부수: 3D 썸네일은 첫 요청이 null이라 결과가 열리는 프레임에 2D→3D로 튀고 그 프레임에 `InsectEntity` 생성/파괴 비용까지 겹쳐, 상자 흔들림 1.5초 구간으로 예열을 옮겼다. **Explore 부분 오류 1건 정정**: "shiny 플래그가 빠진다" → `GachaResult`에 shiny 필드 자체가 없다(지급 시점 `AddCapturedInsect`가 굴린다). **정상 확인**: `DrawChest` 계수는 전부 양수 배수 + 진입 가드, `BeginArea`는 여전히 OnGUI 1회, 구독 짝 대칭, 신규 코드에 프레임 할당 없음

- [x] PlayerItemInventory 재감사 (clean, 2026-08-17) — 증분은 3가지뿐(`ICloudReloadable` 구현 / `ReloadFromDisk` 신설 / 경로를 `SaveScope.FilePath`로 이관)이고 전부 정상. 이 라운드의 급소는 **`RegisterReloadable` 등록 누락**이었다 — 빠지면 인터페이스가 죽은 코드가 되고 계정 전환 시 아이템이 영구 stale이 된다. `PlaySceneBootstrap:225`에 실재하고 순회 지점 2곳(`ApplySaveData` 직후 / `ReloadAllLocalFromDisk`)도 살아 있으며 파일 쓰기→리로드 순서도 맞다. 직렬화 필드 추가가 0건이라 마이그레이션 논점 자체가 성립하지 않고, 소비처 4곳 모두 차감 **앞**에 실패 가드가 있어 아이템 증발/이중 지급 경로가 없다. `Update()` 자체가 없다
- [x] CaptureController 재감사 (P1:2 처리, P1:1 **현행 유지 결정**, 2026-08-17) — ①`CaptureResolved` 이벤트가 `AddCapturedInsect`보다 **먼저** 발화해, 팝업의 `GetLatestOwnedBySpecies`가 방금 잡은 개체 대신 **같은 종의 직전 개체**를 읽었다(그 종의 첫 포획이면 조회가 비어 이전 팝업의 등급·IV가 그대로 남았다 — 초기화 코드가 없었다) → 지급 뒤로 이동 + `CapturePopupUI`에 방어적 초기화. ②`NotifyCapture`가 연출 컴포넌트(`CaptureFeedbackController`)에만 있었다. 멀티캐스트는 앞선 구독자가 던지면 뒤를 부르지 않으므로 `CapturePopupUI` 예외 하나로 포획 퀘스트가 영구 유실될 수 있었다 → 컨트롤러 성공 분기로 이동(전투 경로가 이미 같은 이유로 직접 부른다). ③**밸런스 되돌림**: 07-17에 `CaptureController`+`GameplayTuningProfile` 두 파일을 같은 값으로 내린 너프(base .6→.35 / 난이도 .4→.45 / 퍼펙트 .15→.1)를 08-03 리팩터가 통째로 원복했고, 같은 커밋이 그 잘못된 쌍을 고정하는 테스트까지 심었다 — 사용자 판단으로 **현행(.60/.40/.15) 유지**. 되돌리면 floor가 전 등급을 지배해 더 나빠지기 때문이다. 경위를 `CaptureChanceTuning` 상단 주석 · `GetRarityFloor` 요약 · `rules/balance.md` 신설 절 · 해당 테스트 주석 4곳에 남겼다
- [x] AccountSettingsUI 재감사 (clean, 2026-08-17) — 증분은 오프닝 다시보기 버튼 추가 + 좌표계 하네스 이관 둘. 다시보기가 `playUiRoot`를 통째로 껐다 켜는 그 기능이라 **구독 소실 P0 계열의 정면**인데, `AccountDeleted`는 `OnEnable`에서 복구되고 `openingReplayService`는 이벤트가 아닌 필드라 무관하다. `ReplayOpening`이 `TryReplay` **전에** `SetOpen(false)`를 불러 `OnDisable` 시점엔 이미 모달 해제 상태(실패 시 복원). 버튼 4개 누적 y가 448 박스 안이고 `UIScale.Begin/End`가 `finally` 짝이라 조기 return 3곳에서도 matrix가 새지 않는다
- [x] DexController 재감사 (**P0:1**, P1:1 처리 + 검사기 1개 신설, 2026-08-17) — 본문은 clean인데 마지막 증분이 `CapturedSpeciesCount`를 **스토리 진행 판정값으로 승격**시키면서 기존 도감 등록 누락이 표시 결함에서 진행 결함으로 격상됐다. **P0**: 튜토리얼 보상 곤충(첫 파트너)이 소유·출전까지 하면서 도감엔 영원히 미발견 — 100% 완주 불가 + 전 플레이어의 DexProgress 비트가 한 종 늦게 열림. `dexController`가 선언·대입만 되고 한 번도 읽히지 않는 죽은 필드였다. **P1**: `StoryDirector` 보상도 같은 형태(현재는 전 비트 `rewardInsectId:""`라 휴면). 둘 다 수정 후 `dex_grant_lint.py` 신설
- [x] DexBrowseLayout 재감사 (clean, 2026-08-17) — 증분은 `GetVisibleRowRange`/`GetVisibleItemRange`/`NumberLabel` 신설이고 기존 함수는 한 줄도 안 바뀌었다. 컬링이 **덜 돌려주지 않음**을 양쪽 경계에서 확인: 상단은 잘리는 행이 전부 화면 위, 하단은 `floor(x) ≥ ceil(x)-1`이라 항상 그 이상 + overscan 1행. 호출부 7곳 전부 stride가 실제 그리기와 일치하고 전부 실제로 소비하며, `scroll.y`를 `BeginScrollView`가 클램프한 **반환값**에서 읽어 오버스크롤에도 안 어긋난다. 같은 커밋에 테스트 119줄이 동반 신설됐다

- [x] PlayerHintOverlay (첫 감사, P1:2, 2026-08-17) — 차단 문구가 **모달 위에 영구 고착**했다: `PlayerMovement`의 frozen 분기가 `return`해 `blockedMsgTimer`가 안 줄고(바로 옆 `catchSwingTimer`는 이미 같은 이유로 줄이고 있었다) 오버레이엔 모달 가드도 없었다. 리전에서 튕긴 직후 2초 안에 대화가 열리면 붉은 굵은 글씨가 모달 닫을 때까지 박힌다 — 스토리 NPC가 스스로 걸어와 말을 거는 지금 구조가 그 창을 자주 연다. 둘 다 수정. 하단 잠금 안내는 퀵액세스 바 **안쪽에 통째로** 들어가 있었다(겹침 100%) → `QuickAccessBarUI.BarReservedHeight` 신설 후 그만큼 올림 + x 기준을 `ContentLeft`로 통일(폭만 마진 제외분이라 24px 쏠려 있었다)
- [x] StoryStageDirector (첫 감사, P1:2, 2026-08-17) — **콜백 계약은 clean**: 종료 11경로(NPC 부재·길 막힘·이동 타임아웃·시퀀스 하드 타임아웃·ESC·OnDisable·새 비트·BeginTalk 절단·파괴 등)에서 `onPreludeDone`이 정확히 1회. invoke 전 null 대입 + `mode==Idle` 조기 반환이 2회를 막는다. **P1 2건**: ①복귀 래치가 앵커 도착에서만 풀리는데 복귀 이동은 8초(=25.6m) 타임아웃으로 중도 종료된다 → 스토리 NPC가 들판에 영구 유기(`wanderRadius=0`이라 배회 복귀도 없다). 진입 데드존을 `engaged`로 고쳤던 것의 거울상 → `!approachReturning || !approachNpc.IsScripted`. ②`SnapToFinalPose`가 `stepIndex`부터만 훑어 **정상 완주 시 한 번도 안 돌았다** — 걷다 만 `ReturnToAnchor`가 보정되지 않아 라온이 들판에 정착하고 그 개체가 목표 후보로 남아 HUD 쐐기·자동 주행을 엉뚱한 곳으로 보냈다(역설적으로 ESC로 건너뛰면 결과가 더 나았다) → 지나간 스텝 중 `ReturnToAnchor`만 소급 보정(앵커는 절대 좌표라 낡지 않는다). **P2 위임**: 마지막 `ReturnToAnchor`가 최대 9초 조작을 잠근다 — 기다리지 않게 바꾸면 `SnapToFinalPose`가 즉시 워프시켜 걷는 연출 자체가 사라져 더 나쁘다. 시퀀스 진행↔조작 잠금 분리나 건너뛰기 버튼이 필요한 연출 설계 판단
- [x] 스토리 하네스 순수부 4종 (첫 감사, clean, 2026-08-17) — NpcGesture / StoryNpcApproach / StoryStageDirection / StoryStageLibrary. NaN 위험 전수 확인: 분수 지수 `Pow`는 저장소 통틀어 Recoil 한 곳이고 `Max(0,·)` 가드가 있으며, 나머지 곡선은 sin의 곱뿐이라 sin(π)의 -8.7e-8이 그대로 흘러도 무해. `Trapezoid`는 ramp를 `[0.01,0.5]`로 clamp해 0 나누기가 없고, `gestureTimer/gestureDuration`은 `PlayGesture`가 `duration>0`일 때만 타이머를 켜 0/0이 불가능. `Time`/`Screen`/`Random` 유입 0건. `StoryStageLibrary` 상수 3 ↔ case 3 ↔ Story.json 참조 3 일치, `story_lint`가 실제로 잡는 것도 확인
- [x] VillagerNpc 재감사 (**P0:1**, 2026-08-17) — `WarpTo`가 목적지 +5m 레이의 **첫 히트를 무조건 groundY로 채택**했다. 자기 주석이 "`SampleGround`는 지붕 오인을 막으려 ±0.75m를 거부한다"고 적어 놓고 정작 그 가드를 대체 없이 버린 것이다. 집·상점·병원·회관 몸통은 콜라이더를 남기므로(지붕만 제거) 목적지가 건물 발자국 안이면 상판 y=2.8~3.8m가 잡히고, 이후 `SampleGround` 클램프가 **지상 복귀를 영구 거부**해 자가 회복이 불가능하다. `StoryStageLibrary`의 라온 등장 워프 좌표가 실제로 그 입력을 만든다 → `hit.point.y <= worldPosition.y + MaxGroundStep` 추가. `CompleteScripted` 6경로 정확히 1회는 clean
- [x] NpcWalkAnimator 재감사 (clean, 2026-08-17) — base 회전 드리프트가 구조적으로 불가능함을 확인: `netHandleBaseRot`/`netRingBaseRot`가 `readonly`(대입은 생성자 1곳)이고, 생성자가 `NpcVisualBuilder.Build` 바로 다음 줄에 돌아 애니메이션 전 pristine 값을 읽으며, 같은 오브젝트에 `Initialize`가 두 번 불리는 경로가 없다. 관절 4종은 절대 `Quaternion.Euler` 대입이라 누적 자체가 표현 불가 — `PlayerMovement`에서 났던 툴 base 재캐싱 결함과 형태가 다르다. `PlaySwing` 하위 호환도 상수 일치 확인
- [x] NpcManager 재감사 (P1:0, 2026-08-17) — 3분기 라운드로빈이 전원을 덮는 것을 수식으로 확인(증분 1, gcd(1,N)=1이라 커서가 ℤ/Nℤ의 생성원 → N스텝 안에 전수 1회 방문). 분기 사상이 [0,N) 분할이라 누락·중복 0, `total==0`은 조기 반환이라 mod 0 도달 불가. 리스트에 `Remove`가 전 코드베이스에 없어 순회 중 변경도 불가. **P1 후보 1건은 위임**: `AiTicksPerFrame=3` 고정인데 N이 30→49로 늘어 한 바퀴 0.272s가 `AiInterval` 0.3s와 앨리어싱해 주민 AI 유효 주기가 0.545s로 배증한다(60fps에서만). 프레임 예산 튜닝이라 game-designer 판단
- [x] BattleArenaController 재감사 (clean, 2026-08-17) — 증분은 `OnGUI` 전체 삭제 + 그리기를 `BattleEffectTextOverlay`(UI 레이어)로 이관한 것. 호출부 2곳 모두 `UIScale.Begin/End` 사이 마지막 순서이고, `EndRaid`가 `CleanupArena` → `phase=None` 순이라 연출 손실 창이 없다. Bootstrap이 4개 소비자에 **같은 인스턴스**를 주입해 "아레나는 활성인데 UI 참조만 null"인 경로가 없다. 오히려 인스턴스 `GUIStyle` 2개가 사라져 보유가 줄었다
- [x] GameConstants 재감사 (P1:2, 2026-08-17) — ①`WeeklyContestClaimed`가 클라우드에 안 올라갔다. 대결 **기록**은 곤충 블롭에서 파생돼 따라오는데 수령 상태만 안 따라와서, 기기를 바꾸면 큰 개체는 돌아오고 `ClaimedTier`는 None → 그 주에 대상 종을 한 번 더 잡으면 **보상 재수령**. 바로 위 `defeatedBosses`가 같은 형태를 고친 선례다 → DTO·수집·직렬화·파싱·복원 5점 배선. ②`SaveScope.ScopedStringPrefsKeys`에 5키 누락(`QuestSideProgress`/`QuestSideRepeat`/`QuestUnseen`/`TutorialHidden`/`WeeklyContestClaimed`) — `ScopedKey`를 실제로 거치면서 목록엔 없어 `ClearCurrentAccountLocal`이 못 지웠다. 같은 provider 재로그인은 uid가 같으므로 **삭제한 계정의 서브 퀘스트 진행이 부활**하고 낡은 "이미 수령"이 정당한 보상을 막았다 → 5키 추가 + `MigrationVersion` 4→5
- [x] OutfitSetData 재감사 (P1:0, 2026-08-17) — ID 정합은 **기계 대조로 음성 확인**: 11세트 `requiredItemIds` 55개 전부가 `BuildCatalog()`의 95엔트리에 실재(누락 0), 두 세트 겹치는 itemId 0, 슬롯 1:1, 세트는 직렬화 경로 밖이라 세이브 무관. **P1 후보 2건 위임**(둘 다 표시·노출 설계): 신규 7세트 전원이 `captureChanceBonus`를 full에 얹어 `GetPrimaryBonusText`의 고정 우선순위가 **최소 수치를 광고**한다(ninja의 이속 +10%가 끝내 안 보인다), 0개 착용 세트는 목록에서 빠져 젬 전용 프리미엄 세트가 구매 전엔 존재조차 노출되지 않는다. **수치는 정상, 표시만 틀렸다**
- [x] PlayerInsectLevelUpUIController 재감사 (P1:2, 2026-08-17) — 표시와 차감이 갈려 있었다. UI는 `insect.levelCurve`를 직접 보고 null이면 `cost=0`으로 뒀는데, 이 프로젝트엔 `InsectLevelCurve` **에셋이 하나도 없고** `InsectData`는 런타임 생성이라 `levelCurve`에 대입하는 코드도 0곳 — 즉 항상 null이라 화면은 늘 "사탕 0"인데 결제는 폴백 곡선(4+(Lv-1)×2)으로 **실제로 캔디를 뺐다**. 만렙 가드도 없어 눌렀을 때의 false를 무조건 "사탕 부족"으로 표시했다(캔디가 충분한 사람이 부족한 줄 알고 더 사게 된다) → 둘 다 `collection.GetCandyCostForLevel`/`GetMaxLevel` 정본 API로 교체(`CollectionUI`가 이미 쓰던 것). 차감 원자성은 clean(가드 5개가 전부 `SpendCandy` 앞)
- [x] QuickAccessBarUI 재감사 (**P0:1**, P1:1, 2026-08-17) — **P0**: `FieldHudInput.RegisterBlockingRect` 호출이 파일 전체에 0건이었다. `PlayerMovement`는 클릭-이동을 Update에서 따로 폴링하는데 탭한 프레임엔 아직 모달이 안 열려 `IsAnyOpen()`이 false이고 IMGUI라 `pointerOverUI`도 false다 → 버튼 아래 월드 지점이 목표로 잡혀 **모달을 닫는 순간 캐릭터가 걸어간다. 메뉴를 열 때마다 매번.** 모바일은 키 입력 기반 자동 해제도 없어 더 나쁘다 → 데스크톱 바·모바일 컬럼 두 rect 등록(`CaptureInputController` 선례). **P1**: `GUI.Button`→`OnClick`이 모달 가드를 안 거쳐(핫키만 거쳤다) 도감 켠 채 상점을 누르면 모달이 중첩됐다 → `TryHotkey` 경유(열린 화면 자신의 버튼은 통과하므로 토글 닫기는 유지)
- [x] PlayerProgressController 재감사 (clean, 2026-08-17) — 증분은 `maxLevel 50→80` 실질 1줄. 다중 레벨업은 `if`가 아니라 `while`이고 요구치를 레벨마다 재계산해 한 번에 N레벨분도 소화, 상한 초과분은 0으로 클램프. 이벤트는 xp/level을 **다 갱신한 뒤** 발화해 구독자가 옛 값을 보는 구간이 없다(같은 세션에 `CaptureController`에서 고친 것과 반대 순서). 다중 레벨업 시 이벤트가 총 1회지만 소비처가 전부 pull 방식이라 견디고, 스토리 판정이 `cur >= need`라 건너뛴 레벨의 비트도 발화한다. 80 승격도 씬 직렬화 오버라이드 없음·최대 리전 요구 62 이내 확인

- [x] TrainingUI 재감사 (P1:1, 2026-08-17) — 08-07 증분이 넣은 `ownedInfoCache`가 **이 화면 자신의 장착·해제로는 안 비워졌다.** 무효화가 `InsectUpdated` 하나뿐인데 `DrawSkillEquip`은 `ForceSave()`만 부르고 그건 디스크에만 쓴다(`NotifyInsectChanged`만 이벤트를 쏜다). 캐시 이전엔 매 패스 새로 만들어 항상 최신이었으므로 증분이 만든 회귀다. 목록의 "장착: c/d"가 얼어붙고 닫았다 열어도 유지된다 → 두 호출부에 `NotifyInsectChanged(pid)` 추가. 컬링 stride·stale 개체·구독 짝은 clean
- [x] InsectEntity 재감사 (P1:1 처리, P1:1·P2:1 위임, 2026-08-17) — 풀링은 clean(`erased`가 `Initialize`/`BuildForBattle` 양쪽에서 무조건 대입돼 상속 경로 없음). **「지워진 개체」 연출이 3D 모델 밖으로 전파되지 않았다** — 월드는 검은 실루엣 + `"??? Lv.N"`인데 포획 선택창과 미니게임 HUD가 본명을 그대로 띄워, `CaptureFeedbackController`의 "이름을 되찾아주었다" 회수 문구가 이미 아는 이름을 반복하는 말이 됐다 → `InsectEntity.DisplayNameForPlayer` 단일 출처 신설 후 두 곳 경유. **위임**: `BuildForBattle`의 `erasedOverride` 파라미터를 넘기는 호출부가 **하나도 없어** 전투 진입 시 원색·본명으로 복귀한다(플러밍이 battle 경로 다지점) + 포획 선택창의 곤충 **그림**은 여전히 원색(실루엣 모드가 `InsectVisual`에 없다). P2: 서브에리어 스폰이 `erasedChance`를 안 넘겨 2막 실내 4곳만 0%
- [x] SubAreaEnvironment 재감사 (clean, 2026-08-17) — 증분은 `GetProfileForType` switch에 2막 4종 case 추가뿐이고 기존 좌표·높이는 무변경. 이탈 경로 8개(F2·OnGUI 백업·나가기 버튼·추락 안전망·25m 자동복귀 + 오프닝 다시보기·씬 리로드·프리픽스 숨김)가 전부 `ForceExitSubArea` 단일 깔때기로 수렴하고 진입/이탈 쓰기 8개가 짝을 이룬다. 다시보기는 `playUiRoot`만 끄는데 코디네이터 자신이 `World/`에 있고 `IsCoordinatorUnderUiRoot`를 거부 조건으로 쓰므로 **`playUiRoot`가 `World`일 수 없음이 구조적으로 증명**된다. 중첩 진입은 이중 게이트 + `initialized` 래치로 원본 스냅샷이 덮이지 않는다
- [x] WorldFieldMultiplayerUI 재감사 (**P0:1** 처리, P1:1 위임, 2026-08-17) — **P0**: `FieldHudInput.RegisterBlockingRect` 호출이 파일 전체에 0건이라, 모달이 아닌 상태로 그려지는 버튼 6개(필드 초대·대화·3:3 대전·차단·초대 수락/거절) 위의 탭이 월드 클릭-이동으로 샜다. "3:3 대전"을 누르면 도전과 동시에 캐릭터가 상대 뒤로 걸어간다 → `BlockFieldClicks` 헬퍼 신설 후 상호작용 패널 5곳 등록. **이 화면은 픽셀 좌표계라 `UIScale.Scale`로 나눠 등록해야 한다**(등록은 가상 좌표를 받는다). **위임**: 친구 패널과 초대 팝업이 폭·x가 같고 y만 달라 겹치는데, IMGUI는 **먼저 그린 쪽이 MouseDown을 Use**하므로 "거절"을 누르려다 뒤에 깔린 "초대"가 눌린다 — 상호 배타 처리는 화면 흐름 설계
- [x] NpcVisualBuilder 재감사 (clean, 2026-08-17) — 증분은 `StoryNpcAppearance` switch 81줄 **순수 추가**이고 `Build`/`BuildLeg`/`MakePart`/`CleanupMaterials` 본문은 무변경. **애니메이터가 찾는 노드 8개를 생성 지점과 1:1 대조**해 전부 일치 확인(`ArmL/ArmR/LegLPivot/LegRPivot/Body/HeadPivot/NetHandle/NetRing`, 모두 root 직계라 1-depth `Find`로 잡힌다). 뜰채 base 회전 리터럴도 무변경이고 애니메이터가 곱셈으로 쓰므로 결합 없음. 짝 switch(`StoryNpcDisplayName`)와 VillageBuilder 배치 id 9개까지 3자 대조 — default로 조용히 떨어지는 id 없음
- [x] CharacterOutfitData 재감사 (clean, 2026-08-17) — 증분은 `OutfitLoadout` 클래스 50줄 순수 추가. 의도적으로 `[System.Serializable]`이 아니고 직렬화 필드도 안 늘어 세이브 무관. 신규 멤버 5개 전부 소비처 있음. 세트 `requiredItemIds` 고유 51건을 `MakeItem`/`MakePremiumItem` **두** 등록 경로에 대조해 누락 0(한쪽만 보면 45건이 없어 보이는 거짓양성 주의). 보너스 상한도 실재(`Clamp01`/`Clamp(0.5,2)`)
- [x] NpcDialogueUI 재감사 (P1:2, 2026-08-17) — 둘 다 **캠페인 영구 정지** 계열이다. ①프렐류드 게이트에 예외 봉쇄가 없었다 — `StoryDirector.FireBeat`는 이벤트를 쏘기 **전에** `pendingBeatId`를 커밋하고 `EvaluateTriggers`가 그걸 영구 스킵하므로, 연출에서 예외가 튀면 그 세션에 재발화 경로가 없다. 연출의 자체 타임아웃도 못 구한다(재생 상태를 세우기 전에 던지면 타임아웃이 무장되지 않는다) → try/catch로 감싸 대사로 낙하. ②`stagePrelude != null`은 필드가 **인터페이스 타입**이라 C# 참조 비교로 컴파일된다 — `UnityEngine.Object`의 파괴 검사 `==`가 적용되지 않는다. 제공자는 `World/`, 대화창은 `UI/`라 루트가 달라 한쪽만 죽을 수 있고, 그때 모달 등록 + `SetFrozen(true)`만 남고 `Update`가 안 돌아 하드 타임아웃이 영영 발화하지 않는다 → `TryPlayPrelude` 첫머리에 `isActiveAndEnabled` 가드. **대사를 띄우지 못하는 경로가 없음을 14개로 확인**
- [x] PlayerCurrencyWallet 재감사 (P1:1, 2026-08-17) — 지갑 내부(차감 원자성·음수 가드·젬 이중 관리·이벤트 순서)는 전부 clean인데, **젬 싱크 4곳 중 3곳이 클라우드 확정을 빠뜨렸다.** 로컬 파일만 쓰고 120초 자동저장을 기다리면 그 사이 젬 패키지 결제 시 서버가 **차감 전 잔액**에 보너스를 더해 확정한다 — 쓴 젬이 되돌아오고 아이템·의상·치료는 남는 복제다. `CashShopManager.PurchaseWithGems`가 이 시나리오를 주석으로 적고 자기 경로에만 넣어 뒀고 병원·상점·의상이 빠져 있었다 → 차감 초크포인트인 `SpendGems` 성공 분기 한 곳에 `SaveToCloud()`를 두어 네 경로를 덮었다(연속 호출은 `pendingSave`가 합친다)
- [x] CatcherKidNpc 재감사 (clean, 2026-08-17) — 증분은 듀얼 API + 스냅샷 5줄 **순수 추가**, 기존 상태머신 무변경. 스냅샷이 `SetEngaged(true)`·`Despawn()` **앞**에 있어 순서 역전 없고, `InsectData`가 ScriptableObject 공유 핸들이라 풀 반환 후에도 원래 종을 가리킨다. 예약 해제 3경로(성공·`EnterGiveUp`·`OnDisable`) 전부 확인 + `NpcManager`의 5초 sweep이 2차 방어선. `TickAI`가 거리 무관 라운드로빈이라 쿨다운도 항상 만료된다
- [x] PlayerItemInventoryGridUIController 재감사 (**P0:1**, P1:1 처리, P1:1 위임, 2026-08-17) — **P0**: `BuildGrid`가 `contentRoot`의 **모든** 자식을 파괴하는데 부트스트랩이 `itemPrefab`을 바로 그 `contentRoot`의 자식으로 만든다 → 첫 갱신에서 템플릿이 파괴 예약되고 두 번째부터 `itemPrefab == null` 가드에 걸려 조기 반환, **그리드가 옛 수량으로 영구 동결**(아이템을 써도 개수가 그대로다) → 파괴 루프에서 템플릿 스킵. **P1**: 템플릿이 `SetActive(false)`이고 `Instantiate`가 그 상태를 복사하는데 켜 주지 않아 셀이 보이지 않고 `GridLayoutGroup` 배치에서도 빠졌다 → `SetActive(true)`. **위임**: 대상지정 아이템이 `hospital` 미배선 시 무음 소멸(부트스트랩 배선이 `buildWorld` try 안이라 실패 시 미배선인데 치료 아이템은 무조건 판매된다)
- [x] OpeningSequenceState 재감사 (clean, 2026-08-17) — 증분 43줄은 내레이션 블록 한 덩어리이고 **인스턴스 필드를 하나도 추가하지 않았다**(전부 `const` + `static` 순수 함수). 재진입은 다시보기마다 씬을 로드·언로드하고 `BeginPlayback`이 `new OpeningSequenceState()`로 갈아 끼워 6개 필드가 전부 기본값에서 시작한다 — `Reset()` 부재는 결함이 아니라 "인스턴스 교체" 계약이다. 전진이 전부 `realtimeSinceStartupAsDouble`이라 timeScale 0 차단과도 일치. **위임(테스트 보강)**: 가장 짧은 내레이션 창(1.4s)의 알파가 어디서도 고정돼 있지 않아 창이 `2×NarrationFade` 아래로 줄면 깜빡이는데 테스트가 안 잡는다
- [x] NpcDialogueDatabase 재감사 (clean, 2026-08-17) — 증분은 2막 리전 6개 항목 40줄 순수 추가. **리전 키 13/13을 `RegionDefinitions`와 기계 대조**해 오타·누락·잉여 0 확인. 가장 위험했던 건 신규 항목이 사문화되는 경우였는데(`SyncSpawns`가 앞에서부터 잘라 캡이 옛값이면 전초기지 주민 0명), `NpcManager`·`GameplayTuningProfile` 양쪽 다 앵커 수에 맞게 갱신돼 있고 직렬화 오버라이드도 없어 13항목 전부 도달 가능

- [x] 스토리 진행 게이트 (StoryDirector/StoryObjective/Story.json) (P0:1, P1:1, 2026-08-23) — 여운 비트가 "같은 NPC의 직전 여운"만 prereq로 물어 **진행과 무관하게 뒷 챕터가 샜다**(초원에서 라온 3회 → 12장, 숲에서 세라 4회 → 엔딩 에필로그 전문 + 캔디120/XP250). `requiredBeatId` 신설로 체인(순서)과 진행(단계)을 AND로 분리 — 발화와 목표 도출 **양쪽**에 같은 게이트. **P0**: 앰비언트 `talk_*` 3종이 무게이트라 튜토리얼 중 HUD 목표가 잡담을 가리키고 첫 목표 자동 주행까지 태웠다(도착해 말 걸면 개막이 아니라 잡담이 소비됨) → `ch1_intro` 게이트. 검사 15·16·17·18 신설
- [x] StoryObjectiveTracker 목표 문구 (P1:1, 2026-08-23) — 82비트 중 34개(41%)가 위치 없는 목표로 떨어져 HUD가 "모험을 이어가세요"만 띄웠고 **1막 전체가 그 구간**이었다. 28개는 `requiredRegionId`를 이미 갖고 있었다 → `ActInRegion` 종류 신설(리전 중심을 타깃으로) + 레벨·도감·퀘스트는 현재값 표시. 문구 조립은 순수 함수로 빼 테스트 고정. 지금 그 문구는 **0건**
- [x] StoryStageDirector 재감사 (P1:3, 2026-08-23) — 서브에리어 대치 연출 6곳이 배우를 워프해 등장시키는데 퇴장이 없으면 **서브에리어 좌표에 영구 정착**해 목표 트래커가 그 개체를 집는다(라온이 겪은 회귀의 재발). `GoHome`은 대사 후 최악 8초 조작 잠금을 6배로 늘리므로, **시야 밖(>27m)에서 조용히 워프 복귀**로 처리. 내가 넣은 지연 복귀 자체가 새 나가는 경로 둘(새 입장 연출이 목록을 통째로 비움 / 비활성화 시 Update 정지)도 함께 막음
- [x] CutsceneDirector 전투 경계 (P1:1, 2026-08-23) — `fin_seal`이 **BattleWin에 컷신을 붙인 첫 사례**다. 결과 화면은 4초 뒤 스스로 닫히는데 대사를 그보다 빨리 넘기면 컷신이 전투 UI 밑에서 재생되고, 더 나쁘게는 `Play`가 `InBattleMode=true`를 "원래 상태"로 기록해 **끝날 때 카메라가 전투 구도에 갇힌다**. BattleScreenUI는 IModalUI가 아니라 레지스트리로는 알 수 없어 **배틀 모드를 신호로** 대기(12초 상한, 넘으면 연출만 버림)
- [x] 전투 종료의 프리즈 소유권 (BattleScreenUI/RaidBattleUI) (**P1:1**, 2026-08-23) — `EndBattle`의 finally가 무조건 `SetFrozen(false)`인데 프리즈는 bool 하나라 주인이 여럿이면 마지막에 쓴 쪽이 이긴다. 전투 승리로 스토리 비트가 뜨면 대화 모달이 프리즈를 걸어 두는데 결과 화면이 4초 뒤 그걸 풀어 **대사를 읽는 동안 캐릭터가 걸어다녔다**(`BattleWin` 비트 12개 전부). 모달 중에는 풀지 않게 가드 — 해제는 모달이 닫힐 때 자기가 하고, 안 풀려도 `AutoUnfreezeTime`(20s)이 받는다
- [x] 보스전 소개 게이트 (WorldInteractionController) (P1:1, 2026-08-23) — 도전 조건이 `!storyFired`뿐이라 **"이미 소개를 봤다"와 "아직 차례가 아니다"를 구분하지 못했다.** 집게·저울·관장의 소개는 서브에리어 대치 비트에 걸려 있어 리전 도착 후 본진에서 말만 걸면 **이름도 모르는 채 보스전**이 시작됐다(최종 보스 포함) → `HasMetNpc`(순수 함수, 저작 데이터에서 판정). 그 대가로 생기는 새 잠금 위험(소개 없는 보스 = 영구 도전 불가)은 검사 19가 막는다
- [x] 스토리 인물 앰비언트 (NpcDialogueDatabase/NpcDialogueUI) (P2:1, 2026-08-23) — 비트가 없을 때 마을 주민 풀로 떨어져 **명부회 간부가 날씨 이야기를 했다**(최종 보스인 관장도). 9인 전용 2줄씩 추가 — 줄거리는 진행시키지 않는다(두 번째 서사 경로를 만들지 않기 위해)
- [x] RegionMapUI 목표 마커·범례 (P1:1, P2:1, 2026-08-23) — 지도에 스토리 목표 마커가 0건이었다(리전·서브·수문장·레이드·플레이어만) → 민트 맥동 마커 + 라벨 + `OpenAt`. **P1**: 범례에 한 행을 더하니 7행이 되어 **세로 화면 정보 패널(334px)에서 안내 문구를 뚫었다** → 남은 높이에서 간격을 내는 방식으로(세로 29.4 / 가로 34). **P2**: 마커 라벨 280px가 지도 밖으로 삐져나갈 수 있어 영역 안으로 클램프(위에 자리 없으면 아래로)

- [x] PlaySceneBootstrap 재감사 (P2:1 처리, P2:1 보고, 2026-08-23) — 2026-08-08 이후 증분 131줄(반투명 머티리얼 / 수문장 좌표 사본 제거 / 스토리 배선 / RestoreExploreBGM / 라벨 스케일 상쇄)을 훑었다. **배선 순서는 clean** — `Initialize`가 `LoadUnlockState`를 부른 뒤에 `CreateGuardians`가 오고(격파 상태 선로드 확인), `StoryDirector`의 AutoWire 3건은 전부 마을 try **밖**이라 빌더 예외에도 `dex` 주입이 살아 DexProgress 트리거가 안 죽는다. 라벨 스케일 상쇄(1.8× 곤충의 자식 글자)도 월드 스케일 1 / 높이 2.5m로 산술이 맞다. **P2 처리**: `SpawnGuardianInsect`가 `FindFirstObjectByType<RegionManager>()`로 격파 여부를 **다시** 판정했다 — 호출부가 아우라를 그릴지 정하려고 이미 물어본 값이라 같은 판정의 사본이 둘이었다(바로 그 커밋이 `GetGuardianWorldPosition`이라는 같은 형태의 사본을 걷어낸 참이다) → 호출부에서 한 번만 판정. **P2 보고**: 수문장 엔티티는 부팅 1회 생성이라 **클라우드 로드가 나중에 진행을 내려도 반영되지 않는다** — 기기 교체 후 첫 접속에서 이미 격파한 수문장이 아우라와 함께 서 있고 다시 싸울 수 있다(`DefeatGuardian`이 idempotent라 보상 중복은 없고 재부팅하면 사라진다). 고치려면 엔티티 생명주기를 `ICloudReloadable`에 물려야 해 부트스트랩 구조 변경이다

- [x] TutorialQuestManager 재감사 (**P0:1** 처리, P2:1 보고, 검사기 1개 신설, 2026-08-23) — 2026-08-07 이후 증분 52줄(q_move 선두화 / q_talk_elder 삽입 / NotifyTalkToElder / 도감 등록). **P0**: 첫 파트너 지급이 퀘스트 보상에서 `ch1_intro` 비트로 옮겨가며 `CompleteQuest` 안의 1번 슬롯 자동 배치가 **죽은 코드**가 됐다 — 튜토리얼 퀘스트에 `rewardInsectId`가 **0건**으로 남았고 `SetSlot` 자동 배치 호출부는 저장소 전체에서 그 한 곳뿐이었다. 그래서 **배틀팀이 영원히 빈다**. `CaptureChoiceUI`의 [B] 배틀은 `HasAnyInsect()`만 보고 버튼과 키 입력을 함께 막으므로(`FindPlayerLeader`와 달리 컬렉션 폴백이 없다) `q_battle`(#9)에서 튜토리얼이 정지하고, 팀 편성을 가르치는 `q_team`은 **세 단계 뒤**(#12)라 안내조차 없다. 스토리도 함께 멈춘다(`ch1_first_battle`·`ch1_guardian_call`이 BattleWin) → 규칙을 `BattleTeamManager`의 `InsectCaptured` 구독 **한 곳**으로 옮겨 지급 6경로를 한 번에 덮음(도감 등록이 6곳으로 흩어져 `dex_grant_lint`가 필요해진 것과 반대 방향). `quest_lint` 검사 8 신설 — 구독 제거·`SetSlot` 제거 두 형태 모두 FAIL 지목, 복구 시 PASS 실측. **정상 확인**: `TalkToElder`가 5지점(enum·배열·Notify·게임플레이 호출부·lint) 전부 등록, 구독/해제 5쌍 대칭, `q_talk_elder` 활성 구간에 `ch1_intro`가 목표로 잡혀 조우 접근이 작동(힌트 "먼저 다가와 인사합니다"가 실제와 일치). **P2 보고**: 배열 중간에 퀘스트를 삽입해 **기존 세이브의 튜토리얼 순서가 어긋난다** — 이미 진행한 유저가 나중에 "마을 어르신을 만나다"로 되돌아간다(정지는 아니고 어르신에게 가면 풀린다)

- [x] 튜토리얼 배열 삽입의 기존 세이브 순서 (P1:1, 검사기 1개 신설, 2026-08-23) — `q_talk_elder`를 3번 자리에 끼우자 **이미 진행한 세이브가 뒤로 되돌아갔다**(`ActivateNextQuest`가 배열을 앞에서부터 훑어 첫 미완료를 고른다). 완주한 유저에게 "마을 어르신을 만나다"가 부활해 완주 상태가 깨지고 튜토리얼 칩이 다시 떴다 → `BackfillSkippedStoryQuests`로 소급 완료. **보상은 주지 않는다**(안 한 일에 대한 지급이고 곤충 보상이면 복제다). 판정 경계는 "가장 뒤에 완료된 것" — 그보다 앞만 채우므로 **아직 할 차례인 퀘스트는 삼키지 않는다**(q_move만 깬 세이브에서 q_talk_elder는 그대로 다음 차례). 순수부(`TutorialQuestOrder.CollectBackfillTargets`)로 빼고 테스트 7건으로 경계 고정. `quest_lint` 검사 9 신설 — 전제인 "완료 순서 = 배열 순서"(모든 prereq가 배열 앞을 가리킴)가 깨지면 소급이 아직 할 차례인 퀘스트를 건너뛴다. **검사기를 만들다 껍데기를 하나 만들 뻔했다**: `game_facts`가 주는 키는 `prereq`인데 `prerequisiteQuestId`로 읽어 항상 None이었고, 첫 실측에서 주입한 위반을 못 잡아 발각됐다(`literal_fit_lint`의 정규식 누락과 같은 계열 — 검출력 실측 없이 올리면 아무것도 안 보는 검사기가 남는다)

- [x] CutsceneLibrary 저작 재감사 (P1:2, 2026-08-23) — `cs_story_prologue`가 **재생 시점을 거꾸로 알고** 쓰여 있었다. 컷신은 `StoryBeatCompleted`(대사가 끝난 뒤)에 도는데 자막은 도입부로 쓰여, 셋째 컷이 어르신 대사 2번째 줄을 거의 그대로 되풀이하고 마지막 컷이 "마을 어르신이 그 이야기를 알고 있다"로 **방금 만나고 나온 사람을 찾아가라**고 안내했다 — 게임의 첫 1분이다. 카메라 워크는 그대로 두고 자막 3줄만 나가는 방향으로 고쳤다. **P1**: 테스트의 컷신 목록이 손 관리라(`AllCutscenes` 배열 + `[TestCase]` 5개 이중) 6번째 컷신을 추가하면 길이·복귀 검사가 조용히 그걸 안 본다 → 리플렉션 열거 + `TestCaseSource`, 그리고 "목록이 비면 전부 통과"를 막는 검사를 하나 더. 컷 연결(camFrom==직전 camTo)·시선 퇴화·플레이어 관통도 함께 고정(+11 케이스)
- [x] RegionMapUI 전면 재감사 (P1:2, P2:2, 2026-08-23) — **P1**: 도감 브라우저 헤더가 `{리전} 도감`인데 제목을 먼저 그리고 `< 뒤로` 버튼을 나중에 그려 **리전 이름이 통째로 버튼 밑에 깔렸다**(어느 지역 도감인지가 그 제목에만 있다) → `titleInset`. **P1**: 세로 정보 패널(334px)에서 정보 4줄 + 도감 진행바가 하단 [이동]/[도감] 버튼을 10px 파고들었다 — 지난 라운드에 **같은 패널의 범례 분기만 고치고 이 분기를 빠뜨렸다** → 버튼 자리를 먼저 잡고 남는 높이에서 행 간격을 낸다. **P2**: 목표 마커가 지도 경계 밖(서브에리어·NPC는 리전 원 밖일 수 있다)이면 정보 패널 위에 떴다 → 테두리 클램프. **P2**: 레이드 마커가 스폰 좌표에 굳어 배회한 보스를 못 따라갔다 → 현재 위치
- [x] 수문장 봉인의 생명주기 (PlaySceneBootstrap) (P1:2, 2026-08-23) — 아우라·곤충이 **부팅 때 한 번** 지어질 뿐이라 두 자리에서 어긋났다. ①**인게임 격파**: 이겨도 아우라를 걷는 사람이 없어(`GuardianDefeated` 구독자는 StoryDirector 하나뿐) 지름 5m 붉은 구체가 다음 부팅까지 서 있었다. ②**기기 교체 첫 접속**: 클라우드 로드가 부팅 뒤라 다른 기기에서 깬 수문장이 멀쩡히 서 있었다. 봉인만 딕셔너리로 들고 `GuardianDefeated`와 `ICloudReloadable` 두 신호에 맞춰 걷거나 다시 세운다(**양방향** — 진척이 적은 계정으로 갈아타면 다시 선다). 등록은 `RegionManager` **뒤에** 해야 갱신된 격파 집합을 읽는다
- [x] 전투 결과 화면 ↔ 스토리 대사 겹침 (StoryDirector/BattleScreenUI) (P1:1, 2026-08-23) — `BattleEnded`는 KO 순간에 울리는데 결과 화면은 4초(연출 페이즈 끼면 6초 가까이) 더 떠 있다. 그 위로 대화 모달이 열려 **획득 EXP·캔디 보상 패널을 통째로 덮었다**(`BattleWin` 비트 12개 전부). 결과 화면이 스스로 닫힐 때까지 미룬다 — 통지는 UI→Story 방향으로만 흐른다(그 반대는 의존 방향 위반). **연출인 컷신과 달리 이야기 진행이라 버리지 않는다**: 12초 안에 통지가 없으면 겹치더라도 그냥 쏜다

- [x] StoryDirector 재감사 (P1:2, 2026-08-23) — **읽고 있는 대사가 다른 비트에 밀려 사라졌다.** `EvaluateTriggers`는 같은 비트의 중복 발화만 막았을 뿐(`beat.beatId == pendingBeatId`) **다른** 비트가 끼어드는 건 막지 못했는데, `NpcDialogueUI.ShowStory`는 열린 모달을 `CloseModal`로 정리하고 그 경로가 `CompleteBeat`를 부른다 — **읽지 않은 대사가 보상까지 받고 열람 처리된다**(다시 볼 곳은 저널뿐). 실제 경로가 있다: 유적 이후 6개 리전은 수문장을 잡으면 `gd_X`(GuardianDefeat)와 `chN_clash`(BattleWin)가 **함께** 자격을 얻는다. **P1**: `DefeatGuardian`은 `CheckGuardianDefeat`에서 불려 **결과 화면이 뜨기도 전**이라 `gd_*`가 전투 UI와 겹쳐 열렸다(지난 라운드에 BattleWin만 미루고 이쪽을 빠뜨렸다 — 1v1·레이드 양쪽에서 수문장을 잡는다). 둘을 트리거 큐로 묶어 화면이 닫힌 뒤 **순서대로** 흘린다. 검사 6은 지연 발화를 배선으로 인정하되 **배출부가 살아 있을 때만** 센다 — 그 판정이 처음엔 `EvaluateTriggers` **선언부**의 `string`에 매칭돼 배출부를 통째로 지워도 PASS였다(검출력 실측으로 잡음)
- [x] TutorialQuestUI 재감사 (**P0:1**, P1:1, P2:1, 2026-08-23) — **P0**: 필드 위에 겹쳐 그리는 버튼(퀘스트 칩·✕·"퀘스트 보기"·목표 행)이 `FieldHudInput.RegisterBlockingRect`에 **한 번도 등록되지 않았다** → 그 탭이 월드 클릭-이동으로 새어 캐릭터가 버튼 아래로 걸어갔다. 목표 행은 더 나쁘다 — 누르면 자동 주행이 시작되는데 같은 탭이 클릭-이동으로도 발화해 서로 싸운다. 2026-08-17에 `QuickAccessBarUI`·`WorldFieldMultiplayerUI`에서 P0으로 처리한 것과 **같은 결함이 세 번째 파일에** 있었다. **P1**: `DrawQuestSectionHeader`가 패널 헤더 스타일의 fontSize를 21로 바꿔 그리고 **상수 31**로 되돌리는데, 헤더가 37로 커진 뒤에도 그 31이 남아 **패널 제목이 첫 프레임 이후 영구히 작아졌다**(이 파일의 `RowH` 주석이 경고하는 "숫자를 박아두면 어긋난다"의 실례가 자기 안에 있었다) → 전용 스타일로 분리. **P2**: 상세 팝업의 진행 카운트 상자가 62px 그대로라(칩은 84px로 넓혔다) 두 자리 수에서 가로로 잘렸다 → `LabelFit`
- [x] BattleTeamManager 재감사 (P1:1, 2026-08-23) — 지난 라운드의 `InsectCaptured` 자동 편성은 **앞으로의 획득**만 덮는다. 그 결함을 이미 겪은 세이브(곤충은 있는데 `battle_team.json`이 빈 상태)는 업데이트해도 그대로 막혀 `CaptureChoiceUI` [B]가 잠긴 채 `q_battle`에서 멈춘다 → `SeatFirstOwnedInsect`를 `AutoWire`·`ReloadFromDisk`에 걸어 부팅·클라우드 로드 시점에 구제한다. `TeamChanged`는 발화하지 않는다(사용자 액션이 아니라 시스템 정리 — `MigrateLegacySlots`와 같은 취급)
- [x] NpcDialogueDatabase 재감사 (검사기 1건, 2026-08-23) — 데이터 자체는 clean(9인 전원 키 정확, 오타 0). 다만 **아무도 검사하지 않고 있었다** — 인물을 새로 배치하고 `StoryNpcLines`를 빠뜨리면 주민 잡담으로 조용히 떨어진다(지난 라운드에 실제로 그랬다) → story_lint 검사 20(양방향 대조) 신설. 그 과정에서 `game_facts.story_npc_ids()`가 **VillageBuilder 주석의 예시 문자열(`storyNpcId = "리터럴"`)을 실제 NPC로 세고 있었다**(파이썬 `\w`가 한글을 먹는다 — 추출기가 자기 사용설명서를 데이터로 읽었다) → 줄 주석 제거. `strip_cs`는 못 쓴다, 그쪽은 문자열 리터럴까지 지워 읽을 값이 사라진다
- [x] TutorialQuestData 표면 점검 (clean, 2026-08-23) — `QuestType` 19종·`TutorialQuest` 필드 기본값·신설 `TutorialQuestOrder` 순수부. 퀘스트 정의는 직렬화 대상이 아니라(코드 배열) 세이브 호환 문제가 없고, 사용 중인 QuestType은 quest_lint 검사 5가 전부 도달 가능으로 확인한다. 결함 없음

- [x] NpcDialogueUI 재감사 (P1:2, P2:2, 2026-08-23) — **P1**: 진행 표시("n/총")가 이름과 같은 `nameStyle`을 쓰는데, 스토리 모드에서 그 폰트가 26 → 38로 커진 뒤에도 상자는 **30px 그대로**라 세로로 깎였다(이름 쪽 `nameH`는 52로 함께 키웠는데 여기만 남았다). 폰트에서 파생 + `LabelFit` + 문자열 캐시(OnGUI는 프레임당 여러 번 돈다). **P1**: 저널 다시보기(`ShowStoryReplay`)가 **읽고 있는 진짜 비트를 밀어낼 수 있었다** — `ShowStory`의 `CloseModal`이 `CompleteBeat`를 부르므로 읽지 않은 대사가 보상까지 받고 열람 처리된다. `StoryDirector.FireBeat`에 건 가드는 발화 경로만 덮고 이 경로는 안 지난다. **P2**: 1×1 흰 텍스처를 직접 만들어 들고 있었다(파괴 없음) → `Texture2D.whiteTexture`. **P2**: 2막 간부 4인에게 포트레이트가 없어 8~12장 대치와 **최종 보스 관장이 얼굴 없이** 말했다(1막 하수 2인에게는 있다) → 하수의 검은 상의 계열을 이어받아 4인 추가
- [x] FieldHudInput 등록 누락 전수 (**P0:1**, 2026-08-23) — 버튼을 그리는 25개 파일을 (버튼 수 / 등록 수 / 프리즈 / IModalUI) 표로 뽑아 훑었다. **`SubAreaWorldBuilder`의 진입·퇴장 버튼이 빠져 있었고, 그게 이 게임에서 가장 큰 필드 버튼이다**(620×100 하단 중앙) — 누르면 서브에리어로 들어가면서 동시에 **옛 월드 좌표를 향한 클릭-이동**이 걸려 새 월드의 엉뚱한 지점으로 걸어간다. 픽셀 좌표계라 `UIScale.Scale`로 나눠 등록(`WorldFieldMultiplayerUI.BlockFieldClicks`와 같은 형태). **네 번째 재발**이라 `rules/ui-layout.md`에 이력·전수 방법(표를 뽑는 셸 한 줄)·현재 등록 파일 목록을 남겼다. 검사기는 여전히 두지 않는다 — "IModalUI가 아닌데 버튼이 있고 등록이 없다"로 짜 보면 네 건 중 하나만 잡고(나머지 셋은 IModalUI를 함께 구현한다) 로그인·미니게임·오프닝이 거짓양성이 된다
- [x] StoryObjectiveTracker 재감사 (P2:1, 2026-08-23) — 해석·자동 주행·지도 요청 경로는 clean. 다만 **리전 밖에 서 있으면 목표 행이 항상 지도를 열었다** — `RegionManager`는 어느 리전 원에도 안 들면 `CurrentRegion`을 null로 두는데(리전 사이 빈 땅을 지나는 동안), 그걸 "다른 리전"으로 읽었다. 모르는 것이지 다른 것이 아니다 → 그때는 목표 리전의 **해금 여부**로 가른다(잠긴 곳은 어차피 걸어 들어갈 수 없다)
- [x] MinimapUI 재감사 (P2:1, 2026-08-23) — 좌측 스택 기하(`LeftX`/`StackBelowY`/`LeftStackOccluded`)는 단일 출처가 지켜지고 있고 쐐기·점·캐시 모두 clean. 버튼은 없지만 **불투명 패널이라 탭이 뒤 월드로 새어** 캐릭터가 좌상단 방향으로 걸어갔다 → 퀵액세스 바가 자기 배경까지 등록하는 것과 같은 이유로 함께 막았다. `FindObjectsByType<InsectEntity>`가 0.3초마다 배열을 새로 만드는 것은 남겨 뒀다(프레임당이 아니고, 스포너 쪽에 활성 목록 API를 여는 별건이다)

- [x] PlayerStatusHUD 재감사 (**P0:1**, P1:1, 2026-08-23) — **직전 라운드의 전수 점검이 이 파일을 놓쳤다.** 검색을 `GUI.Button`/`UISurface.Button` 문자열로 했는데 이 화면은 `Event.current` + `Rect.Contains`로 직접 히트테스트한다 — 그래서 **화면에서 가장 큰 HUD**(480×540 패널 + 닫힘 탭)가 `RegisterBlockingRect` 없이 남아 탭이 월드 클릭-이동으로 샜다. `evt.Use()`는 방어가 아니다: `PlayerMovement`는 IMGUI 밖에서 `Input.GetMouseButtonDown(0)`을 폴링한다. **P1**: 모달 검사가 없어 도감·상점·배틀 위에 패널이 얹히고, 더 나쁘게는 **모달의 좌상단 컨트롤을 누른 탭을 이쪽이 먼저 먹었다**(IMGUI는 z-order로 히트테스트를 가르지 않는다. 모바일 기본이 닫힘이라 그 자리에 탭이 서 있다) → 형제 HUD와 같은 `IsAnyOpen()` 가드 + 서브에리어 배너 수명도 모달 중에는 멈춘다. 규칙 문서의 전수 명령을 `EventType.MouseDown`까지 세도록 고쳤다
- [x] StoryStageLibrary 저작 재감사 (**P0:1**, P1:2, P2:1, 검사기 1건, 2026-08-23) — 스테이지 14종의 좌표를 **방 기하와 대조**했다. 서브에리어는 벽으로 봉해진 축정렬 정사각 방이고 진입하면 플레이어가 남쪽 입구(로컬 z=-8)에 **고정 배치**되므로, 워프 지점의 방 로컬 좌표는 저작 시점에 계산된다 — 그렇게 재 보니 6개 대치 중 **셋이 방 밖이거나 벽에 껴 있었다**. `ch12_confront`(**P0**)는 관장이 남벽에서 6m 밖, 바닥 평면 밖에 떨어져 벽에 막힌 채 8초를 밀다 타임아웃으로 "완료"되고 **최종장 첫 대면이 화자 없는 대사만**으로 진행됐다(`SnapToFinalPose`는 지나간 이동 스텝을 소급하지 않는다). `ch7`은 남벽 밖 1m, `ch11`은 벽 안쪽 면에 캡슐이 끼면서 **카메라와 플레이어 사이**에서 불쑥 생겨났다. 원인은 하나다 — 1막 `Ch1RivalEnter`의 "뒤쪽 9m"는 **벽 없는 메인 필드**에서 나온 값인데 그 관용구를 방에 그대로 옮겼다. 셋 다 옆에서 들이도록 고쳤다. **P1**: `ch5_blocked`의 `MoveTo`가 목적지를 `플레이어 + 월드축 북쪽 1.6m`로 잡아, 플레이어가 북쪽에서 말을 걸었으면 그 점이 **플레이어 너머**가 되고 배우가 플레이어 콜라이더에 막혀 **8초 제자리걸음**(조작 잠긴 채) 뒤에야 대사가 떴다 → 목적지를 배우–플레이어 직선 위에서 잡는 `StageAction.ApproachPlayer`를 추가. **P1**: 테스트의 스테이지 목록이 손 유지라 새 시퀀스가 조용히 검사 밖에 남을 수 있었다 → `CutsceneTimelineTests`와 같은 리플렉션 수집으로 교체. `story_lint` 검사 21이 방별 벽 크기(`CreateBoundaryWalls`)와 입구 좌표(`FindSafeSpawnPosition`)를 읽어 이 조건을 강제한다 — 옛 좌표 3개를 되돌려 넣어 3건 FAIL을 확인하고 복원했다
- [x] QuickAccessBarUI 재감사 (P1:2, P2:3, 2026-08-23) — 라이브 P0은 없다(AutoWire 6개 오버로드 전부 Bootstrap에서 불리고, 인덱스 표 4벌이 지금은 서로 맞다). 대신 **그 4벌이 인덱스로만 이어져 있는 것**이 결함이었다: `buttons[]` / `Update` 키 switch / `OnGUI` 키 switch / `IsActive`·`OnClick` case. 항목을 중간에 하나 끼우면 전부 한 칸씩 어긋나 **C가 훈련을 열고 배지가 지도에 붙는다** — 예외도 경고도 없다. 실제로 '캐릭터'[V] 항목을 뺀 적이 있다. → 키 표 2벌을 `buttons[].key` 파싱으로 **파생**시키고(파싱 실패는 경고), 배지 인덱스 상수를 `questBadge` 플래그로 옮기고, `OnClick`의 `default:`에 경고를 넣어 "버튼만 늘리고 배선 누락"이 조용히 지나가지 않게 했다. **P1**: `IModalUI` 구현이 죽어 있었다 — `mobileMenuOpen`을 true로 만드는 곳이 없고 `Register`도 안 부르므로 `IsOpen`은 영원히 false인데, 시그니처만 보면 이 바가 모달 스택에 참여하는 것처럼 읽혔다(모바일 "메뉴→팝업" 2탭 설계의 잔재). 제거. **P2 3건 보고**: ① 데스크톱 바는 모달 위에도 계속 그려지는데 퀵바 GameObject가 모달들보다 **나중에** 생성돼 하단 76px 띠의 클릭을 먼저 먹는다(`PlayerHintOverlay`가 같은 겹침으로 이미 한 번 당했다) ② `KeyGuideHUD`가 키 목록의 6번째 사본이고 Q/P/F4/F6/J가 빠져 있다(낡은 줄번호 참조는 이번에 심볼 참조로 교체) ③ `BarReservedHeight`는 하단 바 기준인데 모바일에서는 바가 우측 세로라 76px이 헛돈다
- [x] WorldInteractionController 재감사 (P1:3, 자체발견 4, 2026-08-23) — 상호작용 우선순위(건물/주민/아이 최근접 단일 대상 + 곤충 거리 비교)와 조우 카메라(`FocusOn`↔`StoryBeatCompleted`의 `ReleaseFocus`)는 clean. **P1**: 원형 버튼 텍스처 2장(`MakeCircle`, 128×128 RGBA32)이 파기되지 않는다 — 런타임 `Texture2D`는 애셋이 아니라 씬이 내려가도 안 사라지고, `AccountSettingsUI`의 로그아웃·계정삭제가 씬을 통째로 재로드한다. **같은 누수를 `CaptureInputController`·`VirtualJoystickUI`에서도 자체 발견**(셋 다 OnDestroy가 없었다) → 셋 다 처리. **P1**: `TutorialQuestManager.Instance?.NotifyTalkToElder()` — null 조건 연산자는 **진짜 참조**만 보므로 파기된 MonoBehaviour가 static에 남아 있으면 그대로 호출로 들어간다(`UnityEngine.Object`의 오버로드된 `==`를 우회). 저장소에 `Instance?.`가 19곳인데 **싱글턴 10개 중 `Instance`를 비우는 것은 `WorldChannelManager` 하나뿐**이었다 → 호출부를 고치는 대신 뿌리에서 `TutorialQuestManager`·`SocialPvpManager`가 OnDestroy에서 static을 비우게 했다(기존 관례 적용). **P1**: `[E] 대화: {DisplayName}` 프롬프트가 길이를 데이터가 정하는데 640 고정 상자에 wordWrap 없이 그려져 가로로 잘릴 수 있었다 → `UIHelper.LabelFit`. `text_fit_lint`는 라벨 인자가 캐시 문자열이라 이 자리를 못 본다. **자체 발견 — 거짓 주석 2건**: `AuthManager.Logout`이 "CloudSaveManager는 DontDestroyOnLoad라 씬 리로드 후에도 in-flight 요청이 완료된다"고 적어 두었는데, DDOL은 `if (transform.parent == null)` 가드 뒤에 있고 부트스트랩이 `World/CloudSaveManager`로 만들어 **가드가 통과하지 않는다**. 유일한 호출부가 바로 다음 줄에서 씬을 재로드하므로 그 플러시는 실제로 끊긴다 → 주석을 사실로 고치고 수명 문제는 큐에 별건으로 올렸다
## Uncovered (우선순위순)

- [ ] 싱글턴 수명 · DontDestroyOnLoad 가드 (Core/AuthManager·CloudSaveManager·AudioManager + PlaySceneBootstrap) — 전부 `World/` 아래 **자식**으로 생성돼 `if (transform.parent == null)` DDOL 가드가 통과하지 않는다. 그래서 로그아웃 직후의 씬 재로드가 in-flight 클라우드 플러시를 끊는데, 주석은 반대로 적혀 있었다(2026-08-23에 주석만 정정). 루트로 올리면 재로드 시 `EnsureObject`가 중복을 만들고 그 중복이 자기를 Destroy해 **부트스트랩이 죽는 컴포넌트를 배선**한다 — 수명 설계 판단이라 architect 상담 대상

## Round Log

보존 개수는 **파일 상단 안내(9줄)가 단일 출처**다 — 여기에 사본을 적지 않는다.
넘치는 항목은 `.claude/audit-archive/round-log-2026H2.md`로 옮긴다(그 이전은 `-2026H1`).

> 이 로그는 **쓰기 전용**이다 — audit 스킬 Step 4가 추가하지만 어느 단계도 읽지 않는다.
> 그런데 Step 1이 이 파일을 통째로 Read하므로 쌓아두면 매 라운드 컨텍스트를 먹는다.
- 2026-08-23: WorldInteractionController 재감사 — **P1:3 처리, 자체 발견 4건.** 우선순위 판정과 조우 카메라는 clean이었고, 결함은 전부 그 주변에서 나왔다. ① 원형 버튼 텍스처가 파기되지 않는다 — 런타임 `Texture2D`는 씬이 내려가도 안 사라지고 로그아웃이 씬을 재로드한다. **같은 누수를 형제 둘(`CaptureInputController`·`VirtualJoystickUI`)에서도 발견**해 함께 고쳤다. ② `Instance?.`는 Unity의 파괴 검사를 우회한다 — 저장소에 19곳인데 `Instance`를 비우는 싱글턴은 하나뿐이었다. 호출부가 아니라 뿌리(싱글턴 OnDestroy)를 고쳤다. ③ 데이터가 길이를 정하는 프롬프트가 고정 상자였다(`LabelFit`). 그리고 **주석이 거짓말을 하고 있었다**: "CloudSaveManager는 DontDestroyOnLoad"라는데 `World/` 아래 자식이라 그 가드가 통과하지 않는다 — 로그아웃의 마지막 클라우드 플러시가 실제로 끊긴다. 주석을 사실로 고치고 수명 자체는 큐에 올렸다. 검증: 컴파일 error CS 0 / warning CS 0, ci_check 통과, PlayMode 672/672.
- 2026-08-23: QuickAccessBarUI 재감사 — **P1:2, P2:3 처리/보고.** 라이브 P0은 없었다 — 배선도 인덱스 표도 지금은 맞다. 문제는 **맞다는 것을 아무도 강제하지 않는 것**이었다: 같은 표가 네 벌(배열·Update 키 switch·OnGUI 키 switch·IsActive/OnClick case)인데 서로 인덱스로만 이어져 있어, 버튼 하나를 중간에 끼우면 전부 한 칸씩 밀려 C가 훈련을 열고 배지가 지도에 붙는다. 키 표 2벌을 `buttons[].key`에서 파생시키고(파싱 실패는 경고), 배지는 플래그로, `OnClick` default에는 경고를 넣었다. 덤으로 죽은 `IModalUI` 구현을 걷어냈다 — `Register`를 부르는 곳이 없어 `IsOpen`이 영원히 false인데 시그니처만 모달이었다. 검증: 컴파일 error CS 0 / warning CS 0, ci_check 통과, PlayMode 672/672.
- 2026-08-23: StoryStageLibrary 저작 재감사 — **P0:1, P1:2, P2:1 처리 + story_lint 검사 21.** 컷신에서 통했던 구분("재생기는 clean인데 저작이 틀렸다")이 여기서도 값을 했다. 좌표를 **방 기하와 대조**하니 서브에리어 대치 6개 중 셋이 벽 밖이었다 — 그중 `ch12`는 **최종장 관장의 첫 등장**인데 남벽 6m 바깥·바닥 밖에 떨어져 8초를 벽에 밀린 뒤 화자 없이 대사만 떴다. 원인은 1막의 "뒤쪽 9m" 관용구를 벽 있는 방에 그대로 옮긴 것. 곁들여 `ch5`는 목적지가 플레이어 너머일 수 있어 8초 제자리걸음이 났고(새 `ApproachPlayer` 액션으로 교체), 테스트의 스테이지 목록이 손 유지였다(리플렉션으로 교체). 검사 21은 벽 크기와 입구 좌표를 코드에서 읽으므로 사본이 없다 — 옛 좌표 주입 → 3 FAIL → 복원 → PASS로 검출력을 실측했다. 검증: 컴파일 error CS 0 / warning CS 0, ci_check 통과, PlayMode 672/672.
- 2026-08-23: PlayerStatusHUD 재감사 (audit 자동 플로우) — **P0:1, P1:1 처리.** **직전 라운드에 "전수 점검했다"고 적은 그 점검이 이 파일을 놓쳤다** — 버튼 위젯 문자열로 훑었는데 이 화면은 `Event.current`로 직접 히트테스트한다. 그래서 화면에서 가장 큰 HUD(480×540 패널 + 닫힘 탭)가 등록 없이 남아 있었다. 전수의 결론이 아니라 **전수의 방법**이 틀렸던 것이라, 규칙 문서의 명령을 `EventType.MouseDown`까지 세도록 고치고 다시 돌려 남은 미등록이 없음을 확인했다(나머지는 전부 IModalUI·프리즈·월드 없음). 곁들여 이 HUD만 모달 검사가 없어 도감·상점 위에 얹히고 **모달 좌상단의 탭을 먼저 먹고 있었다** — `evt.Use()`가 방어가 아닌 이유(PlayerMovement는 IMGUI 밖에서 폴링한다)를 문서에 함께 적었다. 검증: 컴파일 error CS 0 / warning CS 0, ci_check 통과, PlayMode 669/669.
- 2026-08-23: **audit 큐 3건 전수 — 큐 소진**(사용자 요청 "남은 큐 3건도 다 개선해") — **P0:1, P2:2 처리.** 전수 점검이 값을 했다: 버튼을 그리는 25개 파일을 표로 뽑으니 **가장 큰 필드 버튼이 등록에서 빠져 있었다** — `SubAreaWorldBuilder`의 620×100 진입/퇴장 버튼. 누르면 서브에리어로 들어가면서 동시에 옛 월드 좌표를 향한 클릭-이동이 걸린다. **이 결함의 네 번째 재발**이라 규칙 문서에 이력과 전수 방법(표를 뽑는 셸 한 줄)을 남겼고, 검사기를 왜 안 두는지도 **실측으로** 적었다(네 건 중 하나만 잡고 셋이 거짓양성). 나머지 둘은 "모른다를 아니다로 읽은" 것들이다: 리전 사이 빈 땅에서 `CurrentRegion`이 null인데 그걸 "다른 리전"으로 읽어 바로 앞 목표를 눌러도 지도가 열렸고, 미니맵은 불투명 패널인데 버튼이 아니라는 이유로 탭이 뒤 월드로 샜다. 검증: 컴파일 error CS 0 / warning CS 0, ci_check 통과, PlayMode 669/669.
> 영역별 처리 이력은 위 Covered 인덱스가 이미 갖고 있다.
>
> 개수를 두 곳에 적었다가 실제로 어긋났다 — 상단은 "최근 10건", 여기는 "최근 3건만 둔다"라고
> 서로 다른 말을 하는 동안 23건이 쌓여 47KB가 됐다(2026-08-03 정리).

